# amber に頼まれたこと

Taketan が頼んだことが、いまも満たされているか。`python3 scripts/requests.py`
が機械で確かめる。

**これは lint ではなく契約。** 落ちたときの答えは「正規表現を直す」ではなく
「頼まれたものを消してしまった ── 消す前に訊く」。実際に一度、頼まれて入れた
ものを後の設計パスで自分の美意識で消し、同じ指摘を二度させたことがある。

作り直して構わない。**ただし検査を通したまま**、あるいは検査ごと変えるなら
**先に訊いてから**。

**2026-09-05 に cian から分かれた。** 分けた日に、検査が amber のファイルを
指していた 77 行がこちらへ来た。それ以前の残りは
`~/workspace/cian/gui/REQUESTS.ja.md` にある。

**2026-09-06、cian の窓から amber モードが消えた。** そのモードに入っていた
19 件は [REQUESTS.moved.ja.md](REQUESTS.moved.ja.md) にある ── **こちらの表とは
別扱い**で、`scripts/requests.py` は読まない。amber の窓は書き直しなので、
まだ果たしていないものが混じっているから。果たしたら、検査の欄を書き換えて
この表へ移す。

| # | 日付 | 頼まれたこと | なぜ | 検査 |
|---|------|------------|------|------|
| 26 | 2026-09-02 | AIの会話枠はいらない。コマンド提案・ディレクトリ検索・ゴミ検索の品質と精度を1段2段あげたい | モデルに渡す事実を増やした ── **種類・サイズ（ディレクトリは配下の合計）・最終更新からの日数・パス**の4列。ゴミ検索は木を4階層下まで見る（前は1階層） | `crates/amber-core/src/survey.rs ~ pub fn survey` |
| 28 | 2026-09-02 | （同上）見た範囲を偽らない | 打ち切ったら**モデルにも人にも**言う。「N件入らなかった」ではなく「何階層目までは全部見た」 | `crates/amber-core/src/survey.rs ~ pub fn whole_to` |
| 43 | 2026-09-02 | 共有フォルダで複数人が同じものを書きたい（Inkdrop 相談の前提） | **保存前に、開いたときと同じファイルかを見る。**違えば書かずに訊く（差分を見る／別名／それでも上書き）。端末版は `:w` が断り `:w!` で通す。判定は `cian_core::stamp` の1か所 | `crates/amber-core/src/stamp.rs ~ pub fn changed\(` |
| 46 | 2026-09-02 | 指定したディレクトリでマークダウンのノートを管理したい（Inkdrop 相談。名前は **cian モード**） | **DB は作らない。**題は front matter の `title`、無ければ最初の見出し、無ければファイル名 ── その判断は `cian_core::note` の1か所にあり、窓版もいずれの iOS も同じ答えを見る（Electron は iPhone で動かないので、判断を窓に置くと二度書くことになる）。二行目に本文の頭。**ノート以外のファイルも並ぶ** ── ペインの一覧だから、リンク先の画像を隠すのは嘘になる | `crates/amber-core/src/note.rs ~ pub fn read\(` |
| 49 | 2026-09-02 | 自分のノートを書き足せるようにしたい | `:newnote` ── 題を訊いて、前置き（`title`／`created`／`tags`）だけのノートを作って開く。名前と体裁の判断は `cian_core::note`（iPhone へ運べる側）。ファイル名は**スラグにしない** ── 題は日本語のことが多く、ASCII に削ると `.md` だけが残る。消すのは OS が拒む文字だけ。`CON` は拡張子を付けても `CON`、末尾の点と空白は Explorer が黙って食う | `crates/amber-core/src/note.rs ~ pub fn new_note\(` |
| 50 | 2026-09-02 | タグでノートを絞れるようにしたい | 一覧は名前で絞る ── それはファイルには正しく、ノートには間違い。**タグはファイル名のどこにも無い**し、取り込んだページは `page-0012.md` の中に題を持っている。ペインが項目ごとに「探せる一行」を持ち（`Pane::search_text`）、その中身は `note::haystack`（題＋`#タグ`＋本文の頭）。**iPhone 側も同じ判断を使う**ので窓には置かない。到着時に marks・filter と一緒に捨てる ── 前のフォルダの言葉で次のフォルダを絞ることになる | `crates/amber-core/src/note.rs ~ pub fn haystack\(` |
| 51 | 2026-09-03 | iPhone でも使えるようにしたい（Mac と iPhone の連携が本命） | ノートの半分を C ABI の向こうへ。**扉は1つ**（`amber_call(method, params)` で JSON を渡して JSON が返る ── 窓がエンジンと交わしているのと同じ会話）。関数を増やすと Xcode の橋渡しヘッダを手で合わせ直すことになり、そこは何も検査できない。**判断は置かない** ── 題も抜粋もタグも命名も `cian_core::note` のまま。`trash` は任意機能にした（iPhone にゴミ箱の API は無い）。stamp は**秒に丸めない** ── 丸めた最初の版は保存のたびに「誰でもない相手」と競合していた | `crates/amber-ffi/src/lib.rs ~ pub unsafe extern "C" fn amber_call\(` |
| 52 | 2026-09-03 | iPhone のアプリを作り始めたい（Mac と同じノートを両方から） | SwiftUI ＋ `CianFFI.xcframework`。**Swift には何も判断を書かない** ── 題も抜粋もタグも `amber_call("notes")` の答えをそのまま使い、絞り込みも `search`（`note::haystack`）で当てるので、窓と同じ `#タグ` が同じノートを引く。置き場所は `UIDocumentPicker` で選んで security-scoped bookmark で覚える ── **iCloud も Google Drive も Dropbox も iOS では「ファイル」のプロバイダ**なので、これ1つで三者に届き、同期のコードを1行も書かない。`startAccessingSecurityScopedResource()` を忘れると、選べるのに読めない | `ios/Cian/Bridge.swift ~ static func call\(` |
| 53 | 2026-09-04 | iPhone でも写真をノートに入れたい（Mac の貼り付けと同じこと） | 置き場所と名前は `cian_core::note::attach` ── **窓が貼るスクリーンショットと電話が付ける写真が、同じ `attachments/` に同じ形の名前で落ちる。**別々に書けば、両方から書いたノートフォルダは「たまたま重なった2つのフォルダ」になる。拡張子は**バイトから見る**（撮影は PNG、写真は大抵 HEIC。取り違えると何でも開けないファイルが残る）。画像は**先にディスクへ、あとから本文へ** ── 逆順だと、届かないかもしれないファイルへのリンクが本文に残り、あとから「無い」のか「消された」のか区別できない | `crates/amber-core/src/note.rs ~ pub fn attach\(` |
| 54 | 2026-09-04 | （同上）ノートを消せるようにしたい | スワイプして「削除」の二段。**電話にゴミ箱は無い**ので `cian_core::DESKTOP` は false、`DeleteMode::Trash` は断る。消すのはノートだけで、`attachments/` の画像は残す ── 別のノートが同じ画像を指していることがある | `crates/amber-core/src/api.rs ~ "delete" =>` |
| 55 | 2026-09-04 | Inkdrop のようにノートが「読める形」で出てほしい | 見出し・強調・箇条書き・引用・コード・区切り線・**画像**を組んで出す。**何が見出しかは `cian_core::note::blocks`、どう見えるかは前端** ── 逆に切ると Markdown の解釈が電話の上に載り、テストが一行も届かなくなる。`#仕事` は見出しではない（空白の有無で決まる。誤ると タグ付きのノートが全部 32pt で開く）。閉じていない ``` は残り全部を飲む。前置きは出さない（題もタグも既に画面にある） | `crates/amber-core/src/note.rs ~ pub fn blocks\(` |
| 56 | 2026-09-04 | （Inkdrop 化）ノートの本文まで探せるようにしたい | 一覧が持っている一行（題＋タグ＋冒頭100字）は打つそばから絞れるが、**思い出すのはたいてい奥の一文**。`cian_core::search`（窓の `:grep` と同じ）で歩き、行そのものを返して抜粋の代わりに見せる。打鍵ごとに歩かない ── 250ms 待って、前の探索は取り消す（5文字で5回歩けば、捨てる4回のぶん電池が減る）。1ノート1行（同じノートが20行出るのは答えとして悪い） | `crates/amber-core/src/api.rs ~ "find" =>` |
| 57 | 2026-09-04 | ノートブックで絞りたい／並び順を変えたい（更新順・題順） | 絞りと並びは1つのメニュー（増えた一覧に対して訊くことは「どこの」と「どの順で」の2つだけ。ボタンを2つ置くと、ノートを作る側の場所が無くなる）。**絞っている間は題名がノートブック名**になる ── 絞ったのにフォルダ名のままだと「ノートが消えた一覧」に見える。題順は `localizedStandardCompare`（素の比較だと「あ」「い」も note-2/note-10 も並びを間違える） | `ios/Cian/NotesStore.swift ~ enum Order` |
| 58 | 2026-09-04 | タグを付けられるようにしたい | 前置きの `tags:` を書き換えて**本文として返す** ── 保存は普通の保存なので、**タグ付けも競合チェックを通る**。自分でファイルを書く実装にすると「ノートを書く道」が2本になり、2本目が誰かの段落を消す。フォルダにあるタグを候補に出す（「仕事」と「 仕事」は別のタグに見えないのに別物）。`#` は剥がす（人は見た目どおり `#` を打つ）。リスト形式の `tags:` は `- ` の行ごと差し替える | `crates/amber-core/src/note.rs ~ pub fn set_tags\(` |
| 59 | 2026-09-04 | ノートをピン留めしたい | 前置きの `pinned: true`。**外すときは行ごと消す** ── `pinned:` と値なしで残すと、次に読むものが「留まっている」と解釈する。`true`/`yes`/`1` の三通りを受ける（どれだったか誰も覚えていない）。並びは**安定** ── 留めたものの中では選んだ並びが保たれる（`pinned` だけで並べ替えると中でばらける） | `crates/amber-core/src/note.rs ~ pub fn set_field\(` |
| 60 | 2026-09-04 | ノートを別のノートブックへ移したい | **画像も連れて行く。**リンクは相対（`attachments/…`）なので、ノートだけ移すと全部切れる ── しかも切れたことは後で開くまで分からず、開いても「消したのか届かなかったのか」が区別できない。どれが「そのノートの」画像かは命名（`<題>-<時刻>.ext`）で分かる。**同名があれば移さない**（上書きしない）。画像を先に、ノートを最後に動かす ── 途中で失敗しても、ノートは元の場所で元の画像を指したまま | `crates/amber-core/src/note.rs ~ pub fn move_to\(` |
| 61 | 2026-09-04 | 読む／書くの切替は同じ絵柄で（開いた目・閉じた目） | 目とえんぴつは**2つの別のボタン**に見える。開閉する1つの目なら、それが1つのスイッチだと分かる | `ios/Cian/Desk.swift ~ "eye.slash" : "eye"` |
| 62 | 2026-09-04 | 新規作成は 題 → タグ → 作成、そのあと書き込みモードで開く（ボタンは「作成」） | 題を打った瞬間に出来てしまうと、**何のノートか言う前に世に出る**。あとからタグを付けるには、出たばかりのノートに戻ることになる。シートにしたのは、アラートの入力欄は問いを1つしか持てないから。`navigationDestination` は **`NavigationStack` の中**に置く（外側に付けると、ノートは出来るのに何も開かない） | `ios/Cian/Making.swift ~ struct Making` |
| 63 | 2026-09-04 | 電話から打つとき、マークダウンの定型を選びたい | 入力欄の上の帯。**書式ツールバーではない** ── ファイルはマークダウンのままで、押すと「自分で打ったはずの文字」が入る。行頭の印（`#` `- ` `- [ ] ` `> `）は**押し直すと外れる**（間違えて押した瞬間に欲しくなる）。素の `TextEditor` はカーソル位置を渡さないので、対象は**最終行** ── 正しく直すには UIKit が要り、この帯に見合わない | `ios/Cian/Writing.swift ~ private var marks` |
| 64 | 2026-09-04 | フォルダのボタンが何をするか分からない。保存場所の選択・変更と、取り込み・書き出しに使いたい | いきなりファイル選択が開くのは**訊かれていない問いに答えている** ── 「ファイルを探す」に見えて、実は「ノートはここに住んでいる」の話。まず今どこかを示し、選び直す・iPhone の中に戻す・取り込む・書き出すを並べる。取り込みは**コピー**（元は残る。まだ cian に住まわせると決めていない人が最初に知りたいこと）。同名は連番にして**上書きしない** | `ios/Cian/Where.swift ~ struct Where` |
| 65 | 2026-09-04 | フォルダを作れるように。平らに溜めるのも、PC のように整理するのも、どちらも使い方 | 一覧の上にフォルダ、下にノート（ファイルマネージャが最初からやっている並び）。入る・上へ戻る。**両方の使い方を残す** ── メニューの「全部まとめて見る」で平らに戻る。フォルダの一覧は**エンジンの歩いたディレクトリから**取る、ノートから逆算しない ── 作ったばかりのフォルダは空で、ノートから作った一覧には出ず、**それは「作れていない」のと見分けがつかない**。新しいノートは**いま開いているフォルダの中**にできる（そうでないと整理は毎回あとからの作業になる）。名前はシートで訊く ── テキスト欄のあるアラートは自動検査から操作できず、**見たことのない画面を出すことになる** | `ios/Cian/Booking.swift ~ struct Booking` |
| 66 | 2026-09-04 | フォルダ作成が並べ替えメニューの中で見つけられない | 上のバーへ。**作ることは、並べ替えの設定ではない** | `ios/Cian/ContentView.swift ~ "folder.badge.plus"` |
| 67 | 2026-09-04 | 置き場所の画面に、どこにあるか（パス）を出してほしい | パンくずで出す。**「cian」だけでは「何という名前か」の答えで、訊かれているのは「どこにあるか」** ── 電話では同じ名前のフォルダが3つのクラウドに在りうる | `ios/Cian/NotesStore.swift ~ var trail` |
| 68 | 2026-09-04 | 「保存場所を選ぶ」を押しても何も起きない | 原因は2つ。**同じ View に `.fileImporter` が2つ**あった（片方しか効かず、負けた方のボタンは無反応）。そして**シートの中からファイル選択は開けない** ── 提示の上に提示を重ねることになり、黙って何も起きない。シートを先に閉じ、下の画面から開く | `ios/Cian/Where.swift ~ let choose:` |
| 69 | 2026-09-04 | 「出し入れ」ではなく「バックアップ」「インポート」に | 名前を変え、いまあるノートを上書きしないことを画面に書いた（同名は番号を付ける） | `ios/Cian/Where.swift ~ バックアップとインポート` |
| 70 | 2026-09-04 | ピン留めすると何が起こるのか分からない | **「上に固定」という見出しの節に入れる。**黙って上へ跳ぶノートは、理由の見えない移動でしかない。**最上階では、どのフォルダに入っていても固定分を出す** ── ピンの意味は「探しに行かなくても手が届く」なので | `ios/Cian/NotesStore.swift ~ func pinnedHere\(` |
| 71 | 2026-09-04 | ノートを複数同時に開きたい。タブで分かる・切り替える・タブごとに閉じる・左右スワイプ | **本文は「机」が持つ、画面が持たない。**`TabView` はページを作っては捨てるので、ページに本文を置くと**保存前の段落がスワイプで消える**。開いているタブは `Desk` の状態で、エディタはそこへの窓。選択は**索引ではなくパス** ── 閉じると後ろの索引がずれ、選択が隣のノートを指し始める。閉じたら**左隣**へ（来た方向。遠くへ飛ぶと居場所を失う）。未保存は点で出す（閉じる判断をする場所に出す） | `ios/Cian/Desk.swift ~ final class Desk` |
| 72 | 2026-09-04 | ノートをドラッグ＆ドロップでフォルダへ。**中から外へ出す**のもやりたい | フォルダの行が受け口。外へ出す口は **`..` の行** ── cian のペインには最初から `..` があり、ずっと両方の意味だった（上へ行く／これを上へ置く）。掴んで運ばれるのは**パス**（どの操作もパスを取るし、ノートは2つのフォルダの中間には居られない）。落とした先の処理は長押しメニューと**同じ `store.move`** ＝ `note::move_to`（画像も連れて行く／同名は移さない）。指の部分だけは自動検査で駆動できていない | `ios/Cian/ContentView.swift ~ private func drop\(` |
| 73 | 2026-09-04 | 通知がほしい。`mm/dd hh:dd` で一度きり。毎週の定型 TODO（水曜 9 時に「週報」など）も | **判断は `cian-core` に、鳴らすのは OS に。**書式は本文の front matter（`remind:` と `repeat:`）＝**ノートがただのファイルであることを壊さない**ので、Mac の窓版も同じ文字列を読める。繰り返しは `due_since` が**留守の間に過ぎた分を数える**（月末 31 日は、その月の最終日へ寄せる。上限 32 個）。作られるのは**テンプレートの複製**で、テンプレート自身は残る（`carry_out`）。同じ日の分が既にあれば**作らずに既存を返す** ── 2 台が同時に追いついても 2 つにならない。iPhone は勝手にアプリを動かさないので、**時刻に届くのは通知だけ、その日のノートは次に開いたとき**。そう書いてある | `crates/amber-core/src/note.rs ~ pub fn due_since\(` |
| 74 | 2026-09-04 | バックアップとインポート。範囲を選べるように（すべて／フォルダ／タグ／ノート1つ） | 範囲が要るのは、バックアップが**何かの前**にやることだから ── 入れ直す前・人に渡す前・片付ける前で、要る量が違う。zip を作るのは `cian-ffi` の1つの口（`backup`）で、**窓版もいずれ同じ口を使える**。名前は `<対象>-<日付>.zip`。空のタグは**エラーにする**（黙って全部入りの zip を渡さない）。共有シートは `ShareLink` ではなく `UIActivityViewController` ── **zip は押した瞬間に作る**ので、画面を組み立てる時点では渡すものがまだ無い | `crates/amber-core/src/api.rs ~ "backup" =>` |
| 75 | 2026-09-04 | cian だからシアンっぽい色を基調にした見た目に | **色は1つ、置き場所も1つ**（`Assets.xcassets/AccentColor`）。画面ごとに違う色を出すと、触れるものの見分けが画面ごとに変わる。明暗それぞれに別の値を持つ（暗い地の上では、同じシアンは沈む）。アプリのアイコンから採った色で、**アプリの外（ホーム画面）と中で同じ色** | `ios/Cian/CianApp.swift ~ \.tint\(Color\("AccentColor"\)\)` |
| 76 | 2026-09-05 | 読む画面で TODO のチェックを押したり外したりしたい | **行番号で裏返す**（何番目のチェックか、ではない）。画面は一瞬前の `blocks()` から描かれていて、上にタスクが1つ増えていたら数え方はずれる。行が違っていた・そこが箇条書きだった、のときは**何もしない** ── 画面とファイルが食い違っているのに書き込むのが一番悪い。押した瞬間にディスクへ（チェックを押してから保存ボタンを探す人はいない）。`- [ ]` は箇条書きでもあるが、**押せる方を返す** | `crates/amber-core/src/note.rs ~ pub fn set_check\(` |
| 77 | 2026-09-05 | 通知を押したら、そのノートを開いてほしい | 通知に**ノートのパスを積む**（`userInfo`）。受け口は `Ring` という小さな置き場 ── ロック画面から押されたときは、**渡す相手の画面がまだ無い**うちに答えが来るので、誰かが取りに来るまで持っておく係が要る。`Ring.listen()` は `init()` で（あとから delegate を差しても、その通知はもう誰にも届かない）。ノートが読み込まれる前に押された場合に備えて、**ring と notes の両方を見て、後に来た方が開く** | `ios/Cian/Bell.swift ~ final class Ring: NSObject` |
| 78 | 2026-09-05 | 一覧を日付でまとめたい。作成日と更新日の2種類 | 見出しは**並び順に従う** ── タイトル順に並べながら日付で区切ると、3月のノートの上に「今日」が乗る。だからタイトル順のときは**頭文字**が見出しになる。作成日は front matter の `created:`、無ければファイルの誕生時刻、それも無ければ更新時刻（日付の無いノートは日付の見出しから落ちて、消えたように見える） | `ios/Cian/NotesStore.swift ~ func bands\(` |
| 79 | 2026-09-05 | 「題で並べる」ではなく「タイトルで並べる」 | 語を1つに揃えた。**窓版の「ノートの題」も同時に直した** ── 片方だけ直すと、同じものを2つの名前で呼ぶことになる（それを機械で見ているのが `parity.py`） | `ios/Cian/NotesStore.swift ~ タイトル順` |
| 80 | 2026-09-05 | タグで絞るのに検索窓へ打ちたくない。既存のタグを押して選びたい | 帯に出るのは**いま見えているノートが実際に持っているタグ**（多い順）。フォルダ全部のタグを出すと誰も読まない帯になる。複数選ぶと **かつ**（両方持つノート）── **または**にすると、押すほど一覧が増えることになり、絞る操作としては逆。タグは検索と同じで**どこにあるノートでも探す**（フォルダの中にいても、そのフォルダの分だけ出すのは誰も訊いていない答え） | `ios/Cian/NotesStore.swift ~ var tagsHere: \[String\]` |
| 81 | 2026-09-05 | 書く道具をキーボードの上に。よく使うものは今ので良い。見出しは複数回押すと `#` `##` `###`。使わないものは畳んでおく（表・リンク・ソースコード・取消線・水平線…） | 上のバーは **戻る・タイトル・保存・読む書く・⋯** の5つだけにして、道具は**キーボードが出ているときだけ**下に出す（キーボードが引っ込んでいるときの帯は「キーボードの上」ではなく、ただの画面の底）。見出しは**押すたびに深くなる** ── ボタンを3つ置くと、1つの考えに3つの名前が付く。タグ・通知・写真は上の ⋯ に残す（読んでいる間はキーボードが無く、通知を仕掛けるのは読み終えた直後だから）。**文字色は入れていない** ── Markdown に文字色は無く、書き方を決めると両前端がそれを覚えることになるので、先に相談したい | `ios/Cian/Editor.swift ~ static func deepen\(` |
| 82 | 2026-09-05 | ノートを複数開いてスワイプすると、上のアイコンが一瞬増えて消える。ちらつきを消したい | `TabView` は**隣のページも生かしておく**ので、SwiftUI に2枚分のツールバーが渡り、入りきらない分が 「⋯」 に押し出される ── 増えて減っていたのは、その ⋯。**手前のページだけがツールバーを出す**ようにした。**これでは足りず（89）**、ツールバーごと机に上げた ── ページが作るものは、ページが作り直されるたびに作り直される | `ios/Cian/Desk.swift ~ private var chrome: some ToolbarContent` |
| 83 | 2026-09-05 | ノートの行は、題の字を押さないと反応しない。囲みのどこでも反応してほしい | ラベルは**いちばん長い行の幅しか無い**ので、題が短いノートは行の右半分が指に対して死んでいた。`frame(maxWidth:)` と `contentShape` で行全体を的にした。同じ行が場所によって答えたり答えなかったりするのは、バグとして読まれる | `ios/Cian/ContentView.swift ~ .contentShape\(Rectangle\(\)\)` |
| 84 | 2026-09-05 | ダークモードとライトモードの2つを選べるようにしたい | 選択肢は**3つ**（iPhone に合わせる／ライト／ダーク）── 2つしか無い切り替えは、一度倒すと元に戻せない切り替えになる。既定は「合わせる」。色は `AccentColor` が明暗それぞれの値を持っているので、切り替えても**シアンが沈まない** | `ios/Cian/CianApp.swift ~ enum Look: String` |
| 85 | 2026-09-05 | ピン留めではなく**お気に入り**にしたい。本当のフォルダにも居るし、お気に入りからも見える。お気に入り自身にも階層を作りたい。⭐️ で普通のフォルダと違うと分かるように | お気に入りは**2つ目の居場所**であって移動ではない ── ノートは書いた場所に残り、front matter の `star:` が「どの棚にも並ぶ」と言う。だから**ノートに書く**（移動・改名・同期を越えて付いていく。一覧をどこかに持つと、その一覧だけが古くなる）。棚は⭐️の四角で描く ── フォルダの絵にすると「動かせばファイルも動く」と約束することになる。**空の棚だけ**は書ける場所が無いので `.cian/settings.json` が憶える（その1行を失っても、失うのは空の棚だけ。ノートの居場所は失われない）。古い `pinned: true` も読む。外すと `pinned` も一緒に消す ── 残しておくと、外したのにお気に入りのままになる | `crates/amber-core/src/note.rs ~ pub star: Option<String>` |
| 86 | 2026-09-05 | フォルダの色は自分で決めたい。シアン系にこだわらない | 色でフォルダを見分けるのが目的なので、**全部を同じ色相に収める配色は目的に反する**。11色（シアンからはいいろまで）と「色をなくす」。書き場所は `.cian/settings.json` ── フォルダには色を書き込むノートが無い。壊れた設定ファイルは既定に戻すだけで、**ノートを見せない理由にはしない** | `crates/amber-core/src/notebook.rs ~ pub fn set_color\(` |
| 87 | 2026-09-05 | 書く道具がカーソル位置に効いてほしい | `TextEditor` はカーソルを渡さないので、道具は**最後の行**に効いていた（見出しを押すと、いちばん下に見出しができる）。`UITextView` に置き換えて選択範囲を報告させた。**IME が字を組んでいる間（`markedTextRange`）は、どちらの向きにも触らない** ── 確定前の字に書き込むと、組みかけの語が飛ぶ。編集は `Marks` に (text, 選択) → (text, 選択) の関数として置いた（画面の外に出したので、そのまま試験にかけられる形になる）。写真も、末尾ではなくカーソルの段落に入る | `ios/Cian/Editor.swift ~ enum Marks` |
| 88 | 2026-09-05 | 文字色（Markdown で使えるものが欲しい） | 書き方は `<span style="color:#rrggbb">`。**GitHub は `style` を落とす**ので色は付かないが、**字は読める** ── `$\color{red}{…}$` の抜け道は GitHub でだけ描かれ、他所では読めない記号の塊になる。ノートは**文へ壊れる**べきで、記法へ壊れてはいけない。読み取りは `note::spans` の1か所で、**3つの前端すべてが同じ答えを見る**（iPhone は run ごとに塗り、窓は `markdown::Inline::Colored`、端末は 24bit 色）。窓の HTML は「ファイルから来たものは全部エスケープする」という約束があるので、**通すのは検査済みの6桁だけ** ── `onclick` も `class` も、これまで通り字になる。色数はフォルダと同じ11色に固定（色でしか区別できないノートは、grep に映らず、読み上げにも伝わらない）。一覧の二行目と検索は `note::plain` で色の記法を外す | `crates/amber-core/src/note.rs ~ pub fn spans\(` |
| 89 | 2026-09-05 | 目のボタンで読む／書くを切り替えるときも、上のアイコンがちらつく（⋯ が一瞬増えて消える） | 前回の「手前のページだけが出す」では足りなかった ── **ページが作るツールバーは、ページが作り直されるたびに作り直される**。`TabView` は隣のページを生かしたまま、状態が変わるたびに作り直すので、その一瞬だけ入りきらず ⋯ に押し出される。ツールバーを**机（`DeskView`）に上げた** ── 1つしか無く、ページが何をしても作り直されない。読み書き・保存・写真・タグ・通知も机の持ち物にした | `ios/Cian/Desk.swift ~ private var chrome: some ToolbarContent` |
| 90 | 2026-09-05 | ノートの長押しメニューは ブックマークに登録する → フォルダへ移す → エクスポート の順に | 手が伸びる順。ブックマークは片手間、移すのは整理、書き出すのは **amber の外へ出る**もので、外へ出るものは最後。**依頼 249 で札を「お気に入り」から改名したとき、ここが鳴った** ── 約束は順番のほうなので、検査は札ではなく並びに張り直した（札だけ変わって約束が生きていた三度目） | `ios/Cian/ContentView.swift ~ ブックマークに登録する[\s\S]{0,900}?フォルダへ移す[\s\S]{0,900}?エクスポート` |
| 91 | 2026-09-05 | メモしたら自動保存してほしい。わざわざ保存しない | 打つのをやめて 0.9 秒で書く（一打ごとに書くと、一文で40回ファイルを書き直すことになり、同期フォルダでは向こうの端末に40回の変化として届く）。**画面を離れるときと、アプリが前面でなくなるときにも書く** ── 電話は断りなくアプリを止めるので、時計だけに任せると最後の一文が消える。保存ボタンは**状態表示に変えた**（●＝書いている／✓＝書いた）── 書かれたかどうか何も言わないノートは、置いて立ち去れない | `ios/Cian/Desk.swift ~ private func later\(` |
| 92 | 2026-09-05 | ミスったときのために Undo / Redo が欲しい | **電話自身の undo を使う**（振って戻す・3本指スワイプと同じ山）── Swift で書き直せば、電話と食い違う2つ目の undo ができる。そのために道具は**文字列を丸ごと入れ替えるのをやめ**、`replace(_:withText:)` で1回の編集として入れるようにした（丸ごと入れ替えは undo の山に載らないので、見出しを押した分だけ戻せなかった） | `ios/Cian/Editor.swift ~ final class Pen` |
| 93 | 2026-09-05 | iPhone のキーボードに矢印が無いのが本気で使いにくい。左下上右（vim の順）で道具に入れて | 長い文をスマホで書くのが辛い理由のほとんどがこれ（虫めがねを引きずってカーソルを置く）。上下は `.layout` 方向 ── 折り返された行に**描かれている通り**に動く。並びは h j k l | `ios/Cian/Editor.swift ~ func step\(` |
| 94 | 2026-09-05 | 表が 2×2 固定で使いにくい。もっといい UI/UX を | **骨組みを置くのが間違いだった** ── 打ち込む表は縦棒を数え続ける表で、揃え方の行（`:---` `:---:` `---:`）は誰も形を覚えていない。列の数・名前・揃えを選ぶ画面にして、縦棒は cian の仕事にした。**入るのはただの Markdown**（cian の表ではなく、どの道具でも読める表）。空欄は全角空白 ── 中身が空の升は描く側によっては消えてしまい、消えた表は「作れなかった」に見える | `ios/Cian/Tabling.swift ~ struct Tabling` |
| 95 | 2026-09-05 | 最初に出てくる「cian」のところをもう少しクールにしたい | ナビゲーションバーの大見出しは**他人の組版**。cian には名前が最初に目に入る画面が1つあるので、そこだけはアイコンと同じ組み方にした ── 描いた印（ホーム画面のアイコンと同じ2つの括弧）、丸ゴシックの太い字、そして中身を一行。**フォルダの中に降りたらバーに戻す** ── そこでの問いは「いまどこ」で、ロゴはそれに答えない。検索欄は `navigationBarDrawer(.always)` で出しっぱなしにする（バーを `.inline` にすると、既定では引き下げないと出てこない） | `ios/Cian/Writing.swift ~ struct Wordmark` |
| 96 | 2026-09-05 | フォルダを移動したときにアニメーションが欲しい。パッと切り替わるので押せたか分からない | **一瞬で変わる一覧は、変わらなかった一覧に見える** ── 指が着いた時にはもう中身が違っていて、目が追うものが無いので、もう一度押して確かめることになる。一覧ごと差し替えて、来た方向から滑らせる。**どちらへ行ったかで滑る向きを変える**（両方向で同じに滑るなら、滑らない方がまし）。復元や戻りで場所が変わったときは滑らせない ── 滑りは「あなたがした移動」のためのもの | `ios/Cian/ContentView.swift ~ private func go\(` |
| 97 | 2026-09-05 | 左→右のスワイプで上の階層へ、右→左でそれを戻す（cian → hoge → cian → hoge） | 上がった先を憶えておく（`forward`）── **戻せない操作は、そのうち誰も使わなくなる**。フォルダを手で選んだときは、その先は無かったことにする（新しい方向へ進んだので）。**画面端からだけ**受ける ── 画面いっぱいの横スワイプは行そのものの操作（お気に入り・削除）で、出口は電話が既に置いている場所に置く。`simultaneousGesture` なので**縦スクロールが勝つ**し、指が離れるまで何も決めない | `ios/Cian/NotesStore.swift ~ func back\(` |
| 98 | 2026-09-05 | フォルダ移動のスワイプ、指の移動が長すぎる。もっと短くても反応してほしい | 距離だけで見ないで、**離した瞬間まだ動いている指**を数に入れる（`predictedEndTranslation`）── 指を弾いた人は「もっと先まで行く」つもりで離している。48pt の意図的なドラッグを求めていたのは、スワイプではなくドラッグ | `ios/Cian/ContentView.swift ~ predictedEndTranslation` |
| 99 | 2026-09-05 | 表の画面、列と行の増やす・減らすの見た目を揃えて。文言は「増やす」「減らす」 | 同じことを2つの数にする道具が2つの別の考えに見えないように、1つの形にして両方で使う | `ios/Cian/Tabling.swift ~ private func more\(` |
| 100 | 2026-09-05 | 書く帯は、1段目＝見出し・箇条書き・チェック・太字・画像・⋯、2段目＝undo/redo …（空けて）… 矢印 で右詰め | 1段目は**ノートが何でできているか**、2段目は**動き回るための道具**。分けたのは、片方のつもりの指がもう片方を押さないようにするため。右詰めなのは親指の側だから。写真は上の ⋯ から降ろした（書いている最中に使うものなので） | `ios/Cian/Writing.swift ~ mark\("画像", "photo"` |
| 101 | 2026-09-05 | 新規作成すると title / created / tags が見えるノートができる。プログラムが見ているだけなら書く画面に出さないでほしい | 前書きは**ノートが自分を説明する言葉**で、ノートが言うことではない ── 自分で打っていないものを越えて自分の1行目に辿り着くのは変。書く画面は本文だけを持ち、保存のときに繋ぎ直す。どこで前書きが終わるかは `note::front` の1つの答え（`split`）なので、電話・窓・端末が同じ場所でノートを始める。**チェックは丸ごとに対して**行う ── 行番号はファイルの行番号だから | `crates/amber-core/src/api.rs ~ "split" =>` |
| 102 | 2026-09-05 | 自動保存を既定に。設定で切って手動保存も選べるように | 切ると上のバーに「保存」が出る。**切っていても画面を離れるときに一度だけ訊く** ── 自分で決めたい人でも、消えていいとは言っていない | `ios/Cian/Desk.swift ~ private var autosave = true` |
| 104 | 2026-09-05 | 題を入れずに作ったときの既定のタイトルに、時分秒まで入れてほしい | 日付だけだと午後に3本作ると「-2」「-3」が付くだけで、**どれがどれか分からない**。`created:` は日付のまま ── あれは機械が読む欄で、タイトルは人が読むもの。ファイル名からは `:` が落ちる（Windows で作れない） | `crates/amber-core/src/note.rs ~ pub fn now_stamp\(` |
| 105 | 2026-09-05 | 設定の「保存場所を選ぶ…」を押しても何も起きない | **同じ画面に `fileImporter` が2つあると1つしか効かない**（SwiftUI は後勝ち、負けた方のボタンは何もしない）。2026-09-04 に一度直したときに書いた注意書きの2行下で、同じことを2度目にやっていた ── インポートに負けて、押しても配線されていないボタンに見えていた。**1つにして、何を選びに行くかで受け取るものを変える** | `ios/Cian/ContentView.swift ~ enum Fetching` |
| 106 | 2026-09-05 | フォルダを長押しで、色だけでなく名前の変更と削除もしたい。削除は「中のノートごと消していい？」と訊いてから | 削除は**何本が道連れになるかを数で言ってから**（「中のノート 3 本も一緒に消えます」）。**電話にゴミ箱は無い**ので、これは本物の削除で、取り消せない。`..` を渡してノートの外を消せないよう、パスを絶対化してから根の下にあることを確かめる。名前を変えたフォルダの中に立っていたら、上へ退く ── 消えた場所を見せ続けない | `crates/amber-core/src/api.rs ~ ノートの外は触れません` |
| 107 | 2026-09-05 | フォルダのツリー構造が分かる表示がほしい | 一覧は**一階層ずつ**で、それはフォルダを**使う**ための正しい形。ツリーは別の問い ──「ここに何があって、どこまで深いのか」。字下げは `allBooks` のパスの深さそのもので、組み立てるものは何も無い。数字は下の階層も数える。いま居る場所には印を出す | `ios/Cian/Writing.swift ~ struct Tree` |
| 108 | 2026-09-05 | フォルダに入るとフォルダ名だけになる。パンくずリストでどこにいるか分かるように | **フォルダ自身の名前は「どこ」の答えではない** ── 別々の場所にある「2026」は一覧の上では見分けが付かない。各段は、その階層への戻り道。いま居る段だけは字の色にして押せなくする（同じ色にすると、押しても何も起きないものが押せるものに見える） | `ios/Cian/Writing.swift ~ struct Crumbs` |
| 109 | 2026-09-05 | 「（いちばん上）」という言い方をやめて、素人にも分かる言葉に（デフォルトなど） | 彼の案どおり「デフォルト」に。ノートを移す先と、お気に入りの棚の両方 | `ios/Cian/ContentView.swift ~ Button\("デフォルト"\)` |
| 110 | 2026-09-05 | お気に入りに階層を作りたい（棚が「いちばん上」しか無い） | **棚は作れていたが、作りに行く道が隠れていた** ── お気に入り画面からしか作れず、その画面は「お気に入りが1つ以上ある」ときにしか出てこない。だから最初の棚を、要るその場で作れなかった。棚を選ぶ画面に「新しい棚…」を置いた（作って、そのまま入れる。`棚/中の棚` と書けば階層） | `ios/Cian/Stars.swift ~ 新しい棚…` |
| 111 | 2026-09-05 | 通知は ⋯ の中ではなく最上段にベルで分かるように | **鳴るかどうかは状態**で、状態はメニューの奥にあると誰も知らない。仕掛かっているときは橙の塗りつぶし | `ios/Cian/Desk.swift ~ ToolbarItem\(id: "bell"` |
| 113 | 2026-09-05 | 窓版でも cian モードを（チェックを押せるように） | HTML の升に**元の行番号を積む**（`data-line`）── `note::set_check` は行番号を取るので、窓に要るのはそれだけ。何番目の升かを数えると、前書きのあるノートでずれる。押すとエディタに戻して**ふつうに保存する**ので、打ち込みと同じ衝突検査を通る | `crates/amber-core/src/markdown.rs ~ data-line=` |
| 123 | 2026-09-05 | iPhone: 「フォルダの構成」の見え方を既定に。中のノートも出す。エクスプローラっぽく。お気に入りフォルダを一番上に | 一階層ずつの一覧はフォルダを**使う**ための形で、ツリーは**知る**ための形 ── ノートアプリでやっていることは、たいてい後者。お気に入りは**フォルダとしては描かない**（⭐️の四角）── 2つ目の居場所であって、ディレクトリのふりをさせると「動かせばファイルも動く」と約束することになる。**探しているときはツリーにしない**（検索結果の木は誰も読まない）。再帰する view は `AnyView` で1か所だけ型を消す ── 自分を含む view は自分を含む型になり、Swift は推論しない | `ios/Cian/Nest.swift ~ struct Nest` |
| 124 | 2026-09-05 | iPhone: 検索を OR / AND に対応。タグは入力欄を押すと選べるように。既定のタグバーは消す | 空白は AND、`OR`（`or` と縦棒も）は OR で、**OR のほうが弱い綴じ** ── `仕事 週報 or 家` は (仕事 AND 週報) OR (家)。みんなそう書いて、誰も説明しない。**問いの意味は `cian-core`**（`note::terms`）── 3つの前端が別々に決めると、2語打つまでは一致する検索欄が3つできる。タグは**打っている場所に出す**（`searchSuggestions`）── 帯は画面の1行を永久に取って、たまに押されるだけだった | `crates/amber-core/src/note.rs ~ pub fn terms\(` |
| 125 | 2026-09-05 | iPhone: 「あたらしい」→「新しい」 |  | `ios/Cian/Making.swift ~ 新しいノート` |
| 128 | 2026-09-05 | （105 の続き）保存場所を選んでも、選んだ後に何も起きていなかった | **開くところしか見ていなかった。** 105 で「押しても何も起きない」を直したとき、確かめたのは**選択画面が開くところまで**で、フォルダを選んだ後は一度も動いていなかった。原因は SwiftUI の作法: 閉じると `isPresented` が false になり、その setter が「何を選びに行ったか」を消し、**そのあとで** completion が走ってそれを読む ── nil なので何もしない枝に落ちる。何を頼んだかは**別に持って**、答えを受け取ってから捨てる。**presentation が自分のコールバックに要るものを消すのはこれで3度目**（`.alert(presenting:)`、`.sheet(item:)`、これ） | `ios/Cian/ContentView.swift ~ private var asked: Fetching\?` |
| 129 | 2026-09-05 | iPhone: Google Drive / Dropbox / iCloud がどこにあるか分からない。探せなくて困る | **一度目の探し方を、画面に書いた** ── 「ブラウズ」から辿る、Drive と Dropbox は「場所」の下、出てこないときは「…」→「サイドバーを編集」。これは amber が代わりにできない設定（「ファイル」アプリのもの）なので、**せめてどこにあるかを言う**。灰色で選べないものがあることも書く（アプリ側の作りなので、こちらでは変えられない）。**「選んだ場所を覚えて一覧にする」という後半は、2026-09-06 に本人が取り下げた**（依頼 253 ── 窓と同じく置き場所は一つに）。取り下げなので、張り直しではなく**約束そのものが変わっている** | `ios/Cian/Where.swift ~ サイドバーを編集` |
| 130 | 2026-09-05 | 保存場所を変えるとき「いままでのノートを丸ごと移しますか」と訊いて、OK なら構成ごと移す | 新しいフォルダは**空のフォルダ**で、それを予期していなかった人は書いたもの全部を見失う。だから訊く ── 何件かも数で言う。移すのは**コピーして、確かめて、それから消す**（プロバイダ間ではこれは rename ではない。向こうに全部書けてから、こちら側に手を付ける。途中で失われたノートは、このアプリがやり得る最悪のこと）。**同じ名前が向こうにあったら1つも動かさない** ── 2つのノート束を混ぜるのは判断で、ここで人の代わりに決めることではない。画像（`attachments/`）と `.cian` も連れて行く（置いていくと、絵のあるノートが全部壊れる） | `crates/amber-core/src/api.rs ~ "migrate" =>` |
| 131 | 2026-09-05 | 「この iPhone の中に戻す」が分からない。どこなのかを言ってほしい | ボタンは「どこなのか」も「なぜ戻るのか」も言っていなかった ── 道を**名前の連なり**で出す（「この iPhone › amber」）。「amber」だけでは *何という名前か* に答えていて、訊かれているのは *どこにあるか* だから。**「開いてきた場所の履歴にする」という後半は、2026-09-06 に本人が取り下げた**（依頼 250）── 窓は置き場所を一つしか持たないので、電話も一つにした。取り下げなので、張り直しではなく**約束そのものが変わっている** | `ios/Cian/NotesStore.swift ~ var trail: \[String\]` |
| 132 | 2026-09-05 | バックアップの zip を選べないので戻せない | 作れて使えない zip は、バックアップではなくただのファイル。選べるものに `.zip` を足して、**フォルダの構成ごと戻す**。**いまあるノートは上書きしない**（先週の写しで今日の仕事を失う戻し方はしない）── 残した件数も言う。展開は一度別の場所へ（半分展開された書庫がノートの中に立たないように）。zip の中は `ノート/…` のように**そのフォルダ自身の名前**で始まるので、1つの名前の下に全部あるときだけ剥がす ── 剥がさないと、戻すたびに中に1階層増える | `crates/amber-core/src/api.rs ~ "restore" =>` |
| 133 | 2026-09-05 | 電話に入れたものを、窓版にも持っていける作りで | **判断は `cian-core` に移した。** 「丸ごと移す」と「バックアップから戻す」は `cian_core::notebook::migrate` / `restore` の1つずつで、電話も窓も同じものを呼ぶ ── 2つ書けば、2つのうち片方だけが「上書きしない」を守る日が来る。窓では移す元を**開いてきた場所の履歴**から選ぶ（電話と同じ理由で覚えている）。戻すのは反対のペインの zip ── 2画面ファイラの「向こう側」がそのまま行き先／出どころ | `crates/amber-core/src/notebook.rs ~ pub fn migrate\(` |
| 148 | 2026-09-05 | iPhone のアプリ名も amber に | 「ファイル」アプリに出るフォルダ名は表示名に従うので、**表示名と説明文は必ず一緒に変える** ── 片方だけ変えると「ファイル → この iPhone 内 → cian」が嘘になる。中の名前（`cian.look` などの設定キー・バンドル ID）は変えない。変えると彼の設定が消える | `ios/Info.plist ~ <string>amber</string>` |
| 150 | 2026-09-05 | cian と amber を完全に分ける。amber は `~/workspace/amber` で独立 | 「大木に育つ前に別アプリにすべき」。分ける前、**iPhone は 18,128 行の cian-core を丸ごと積んでいた** ── git も svn も SharePoint も差分もアーカイバも。`desktop` 機能は `trash` の依存を切っているだけで、モジュールは一つも切っていなかった。**扉も amber のものにした**（`amber_call` / `amber_free`）── バンドル ID だけは変えない | `crates/amber-ffi/src/lib.rs ~ pub unsafe extern "C" fn amber_call` |
| 151 | 2026-09-06 | amber の窓版を作る（Mac / Windows）。cian の amber モードを外す前に | **`gui/` は持ってこない。** cian の renderer は 11,468 行あるが、それは2画面ファイラの土台（ペイン・シェル・アーカイブ・git・差分・SFTP・PTY）で、amber が使うのはその上の 151 行。持っていくと大木が二本になる。三列（行き先・ノート・中身）専用に書き直すほうが小さい ── 要るのは Monaco と `amber-server` の口だけ。**判断は書かない** ── 絞り込みの意味は `note::terms` に訊く（iPhone と同じ形。三つの前端がそれぞれ「二語」の意味を決めると、二語を打つまで一致して見える検索窓が三つできる） | `gui/renderer.js ~ amber-core` |
| 152 | 2026-09-06 | Gemini に描いてもらったアイコンを amber に取り込む（案2「琥珀の中の Markdown」） | **この絵は式で描けない** ── ガラスは透けて、中に気泡があり、縁に光が回っている。`amber.py` の走査線は角丸と丸端の線しか塗れないので、**画素そのものを原本として持つ**。輪郭はどの物差しでも切れなかった（赤−青では下端が影に 35px 溶け、彩度では上端の光った縁が欠ける）ので、**角丸四角の形を測って当てはめ、それを型にする** ── 四辺が同じ理屈で決まり、どの大きさでも縁が解析的に滑らかになる。絵は 788x846 と正方形ではなかったので、**縦を詰めて**正方形にする（伸ばすと無い画素を作る） | `packaging/amber_icon.py ~ def radius` |
| 153 | 2026-09-06 | 窓でも Ctrl+E でプレビューと書く面を行き来したい（Inkdrop の `view:toggle-preview`） | 組み方は `markdown::to_html`、見た目は前端 ── **`blocks`（iPhone）と同じ行単位の読み方を通る**ので、同じノートが二つの見た目を持たない。逃がし（`javascript:` を落とす・`onclick` も `class` も字にする・色は検査済み6桁だけ）は `to_html` の中の1か所。**重ねずに横へ並べる** ── 重ねて片方を消すと Monaco が幅ゼロのまま測る | `gui/renderer.js ~ function toggleRead` |
| 154 | 2026-09-06 | F12 でノートだけを最大化、F12 か Esc で戻る（Inkdrop の distraction free） | 畳むのは**左の二列だけ**。上の帯を消すと、いま何を開いているのか分からない画面になる。**どこを打っていても効く** ── 書いている最中に大きくしたくなるのだから、エディタの中でこそ効かないと意味がない | `gui/renderer.js ~ function setZen` |
| 155 | 2026-09-06 | 窓にも Markdown の簡易入力を（iPhone の「書く道具」と同じ顔ぶれ） | **書式ツールバーではない** ── ファイルは Markdown のままで、押した跡は打った跡と見分けがつかない。**意味は `markdown::marks`（core）** に置いた ── 窓が自分で `#` を数えはじめると iPhone と押し心地が分かれる。位置は渡さない（JS は UTF-16 の桁、Rust は文字で数えるので絵文字一つで境目がずれる）── **選んだ字そのもの**を渡す。行頭の印は押し直すと外れ、見出しは押すたびに深くなる（`#` → `##` → `###` → 無し） | `crates/amber-core/src/markdown.rs ~ pub mod marks` |
| 156 | 2026-09-06 | vim モードが欲しい。ただし**素のメモ帳が基本** | 既定は入れない ── 知らずに入っていると `i` を押すまで一文字も打てず、それは壊れているのと同じに見える。中身は `monaco-vim`（CodeMirror の vim の移植）── 自前で書くと `ci"` や `.` のような「本物なら動くのに動かない」に必ず当たる。読めなければ素のまま続ける（**入れられないことで書けなくなる理由は無い**） | `gui/renderer.js ~ function setVim` |
| 157 | 2026-09-06 | 読める形の升を、キーボードと読み上げからも押せるように | `<span>` で出していた頃、押せるのに操作要素として名乗っておらず、読み上げはただの字として読み、Tab では辿り着けなかった ── **押せる升が一つ出せないだけで、ノートの半分が触れなくなる**。`<button>` にして `aria-pressed` を積む | `crates/amber-core/src/markdown.rs ~ aria-pressed=` |
| 158 | 2026-09-06 | 番号つきリストを、読める形でも番号で出す | 書いた `1. 2. 3.` が黒丸で出ると、**書いたものと読めるものが違う**ので Markdown が壊れて見える。始まりの番号も渡す ── 途中から続ける手順（`3.` から書きはじめた行）が毎回 1 に戻らない。同じ深さで印が変われば別のリストにする | `crates/amber-core/src/markdown.rs ~ <ol start=` |
| 159 | 2026-09-06 | 行そのものが絵の行を、読める形でも絵で出す | それまで `!<a href>` になっていた（`!` が字のまま残る）。**どこからが絵かは `note::lone_image` の1か所** ── 窓が自分で `![` を探しはじめると、iPhone が絵として積む行を窓が字で出し、同じノートが二つの見た目を持つ。出せない先（`javascript:`）は隠さず字で残す | `crates/amber-core/src/markdown.rs ~ crate::note::lone_image` |
| 160 | 2026-09-06 | アプリの中の印も、渡したアイコンをベースにしたい | **描き直さず、アイコンそのものを小さくして出す**（`amber-mark.png` 128px ── 窓の 54 の2倍と iPhone の 38pt の3倍を両方覆う一枚）。前は葉を四か所に写して描き、揃っているかを `amber.py` の `agree()` が見張っていた ── **それでもアイコンを替えた日に、中の印だけが葉のまま残った。** 見張れていたのは「四つの写しが揃っているか」であって「アイコンと同じか」ではなく、揃ったまま全部が古くなれた。写しを持たなければ、ずれようがない。CSS と SwiftUI の角丸は外す（絵が自分で角丸を持っているので二重に削れる） | `gui/renderer.js ~ amber-mark.png` |
| 161 | 2026-09-06 | iPhone のアクセントを ayu-light / ayu-dark のような黄色に | 暗い側は **ayu-dark の金 `#E6B450` をそのまま**（黒の上で 11:1）。明るい側は ayu-light の `#FF9940` を**そのまま使えなかった** ── 白の上で 2.12:1 しかなく、iOS のアクセントは文字ボタンにも乗るので読めない（Apple 既定の青ですら 3.6:1）。**ayu の金の色相（40°）を保ったまま明度だけ落として `#A87000`**（4.21:1）。ロゴは追従させない（台帳 95）── 印は絵になったので、もう色を拾わない | `ios/Cian/Assets.xcassets/AccentColor.colorset/Contents.json ~ "red" : "0.902"` |
| 162 | 2026-09-06 | 窓の一覧にも並び順を（iPhone にはある。Inkdrop も既定は更新順） | 三つしかないものに開いて閉じる幕は要らない ── **押すたびに次へ**。名前順は `localeCompare(…, {numeric: true})` で、素の `<` は「あ」と「い」も `note-2` と `note-10` も両方まちがえる。**core には上げていない** ── iPhone は Foundation の `localizedStandardCompare` を使っており、Rust には土地を知った自然順が標準に無いので、上げると iPhone の並びのほうが悪くなる。同じ規則を、それぞれの土地の言葉で言っている | `gui/renderer.js ~ const ORDERS` |
| 163 | 2026-09-06 | 読める形のコード枠に色を（Inkdrop は色が付く） | **Monaco の色付けをそのまま借りる。** ハイライタを別に持ってこない ── エディタは既に `rust` も `js` も色分けしていて、同じ言語に二つの色分けを持つと、書いている面と読める面で色が違うノートができる。知らない語彙は素のまま（**色が付かないのは読めないことではない**）。組み直しに追い越されたら捨てる | `gui/renderer.js ~ async function paintCode` |
| 164 | 2026-09-06 | いま何字か見えるように | **数えるのは本文だけ** ── 前書きはノートが自分を説明する言葉で、書いた量ではない。語数ではなく**字数**にしたのは、日本語に語の切れ目が無いから（空白で切ると一段落が「1 語」になる）。題の隣ではなく右端に置く ── ずっと動く数字が題のとなりにあると、視線が題に戻るたびに数字を読んでしまう | `gui/renderer.js ~ function drawCount` |
| 165 | 2026-09-06 | 字の大きさを変えたい（⌘+ / ⌘− / ⌘0） | **書く面と読む面を一緒に動かす** ── 片方だけ動くと、並べたときに同じノートが二つの大きさで出る。キーは `Equal` / `Minus`（位置のキー）で当てる ── JIS では `+` に Shift が要り、`e.key` で当てると刻印どおりに打っても効かない | `gui/renderer.js ~ function setFont` |
| 166 | 2026-09-06 | 探す先を名指ししたい（Inkdrop の `tag:` `book:` `title:` と `-` の打ち消し） | 日本語の見出しも通す（`タグ:` `フォルダ:` `題:`）── 日本語で書いている最中に英字へ切り替えるのは高い。二重引用符で空白を含む一語。**`body:` は入れていない** ── 一覧が持っているのは本文の頭 100 字だけなので、受けると「本文を探したのに見つからない」を作る（奥の一文は `find` の仕事）。**知らない見出しは字のまま** ── `http://…` や「10:30」を打っただけで消える語ができると、探せなくなったことに気づけない。`terms` の返す形を字から `{見出し, 語, 打ち消し}` に変えたので、**窓と iPhone の両方を同時に直した** ── 片方だけ直すと、片方の検索窓だけが `tag:定型` という字を本文から探し続ける | `crates/amber-core/src/note.rs ~ pub struct Term` |
| 167 | 2026-09-06 | 窓に足りていなかった機能を全部入れる（iPhone にはあった） | お気に入り・棚・タグ・文字色・フォルダへ移す・新しいフォルダ・通知・書き出し・削除・フォルダの色・バックアップ・置き場所。**窓ができることを、一つの表にした** ── パレットも ⋯ の献立もそこを見る。二か所に書くと、片方にだけ増えた命令ができて「あるはずなのに無い」になる。ノートを書き換えるものは全部 `editNote` の一本道を通る（前書きを戻し、切り直し、いつもの保存）── **押して付けた星と、打って書いた星が別の扱いになる理由は無い**（衝突の検査も一度で済む） | `gui/renderer.js ~ const CMDS` |
| 168 | 2026-09-06 | 命令を名前で探せるように（コマンドパレット ⌘⇧P） | 覚えなくていいのが値打ち。**小窓は一枚**（選ぶ・打つ・はい／いいえ）── 別々に作ると、微妙に違う「Esc で閉じる」が三つできて、そのうち一つだけ閉じない日が来る。Esc は**手前にあるものから**閉じる（献立 → 小窓 → 最大化） | `gui/renderer.js ~ function sheet` |
| 169 | 2026-09-06 | 目次がほしい（⌘⇧O） | **何が見出しかは core が決める**（`note::blocks`）── 窓が `#` を数えはじめると、`#仕事` というタグの行が目次に出る（空白の有無で決まる）。飛び先の行番号も core が持ってくる ── 何番目の見出しかを数えると、前書きのあるノートでずれる（チェックの升と同じ理由）。書く面ならその行へ、読む面ならその見出しへ | `gui/renderer.js ~ function drawToc` |
| 170 | 2026-09-06 | GitHub 風の注記（`> [!NOTE]`） | 受けるのは GitHub が読める五つだけ ── 増やすと**ここでだけ見える記法**になり、同じノートが GitHub で壊れる。**引用の中の一行目でしか始まらない**（本文に `[!NOTE]` と書いただけの行が消えない）。色だけで区別せず、種類の語を必ず出す ── 色が見えない人にも、白黒で刷ったときにも残る | `crates/amber-core/src/markdown.rs ~ fn alert_kind` |
| 171 | 2026-09-06 | mermaid で図を描きたい | **図のあるノートを開くまで読み込まない**（3.4MB あって、ほとんどのノートに図は無い）。Monaco のローダには渡さない ── 束ねの中に `define.amd` を見て自分から名乗り出る部品が入っていて、ローダはそれを mermaid だと受け取る（実際にそうなった）。**`define` を伏せてから素の `<script>` で読む。** 描けない図は書いた字のまま残す（消すと直しようがない）。地の明暗に合わせる ── 図だけ白いと暗い画面で目を焼く | `gui/renderer.js ~ function loadMermaid` |
| 172 | 2026-09-06 | 書き出し（Markdown / HTML / PDF） | HTML は**一枚で完結**させ、外を参照しない。PDF は**窓そのものを刷らない** ── `printToPDF` は三列ごと刷るので、見えない窓をもう一つ建てて読める形だけを入れる。data: の URL ではなく一時ファイルを読ませる（Chromium は data: を最上位の遷移で渡すと黙って空の頁にすることがある） | `gui/main.js ~ amber:savePDF` |
| 173 | 2026-09-06 | 「上に留める」は入れない。お気に入りが既にそれ | 一覧の上に**別枠で**出す（iPhone と同じ形）。並び順に混ぜて上へ寄せるのではなく別の段にする ── 二つの場所に同じノートが出ると、人はそれを二度消そうとする。**「これは大事」と言う道を二つ持たない。** ついでに、チェックのあるノートには済んだ数を出す ── **数えるだけで、新しい欄は作らない**（進み具合は既にノートの中に書いてある） | `gui/renderer.js ~ class="sect"` |
| 174 | 2026-09-06 | 題を入力させるのをやめたい。「牛乳」だけ書きたいのに題とタグを訊かれるのは変。題を飛ばすと日付が出るのも変 | **書いた人が一度も言っていない名前を、前書きに入れない。** 新しいノートは `title:` も `tags: []` も書かない（`created` だけ残す ── 人が読む名前ではなく、並べ替えが読む欄）。題は **`title:` → 最初の見出し → 書き出しの一行 → ファイル名**。行頭の印は外し（`- 牛乳` の題は「牛乳」）、強調も外し、60字で切る。**題になった行は二行目に出さない**（一行のノートが二行に見える）。ファイル名まで落ちるのは**中身がまだ何も無いとき**だけで、そのときは他に呼びようがない。判断は `note::read` の1か所なので、窓も iPhone も端末も同じ題を見る | `crates/amber-core/src/note.rs ~ fn first_line` |
| 175 | 2026-09-06 | 左上の amber のアイコンと名前は要らない（Inkdrop にも無い） | アプリの名前は**窓の外**（Dock・タスクバー・⌘Tab）が既に言っている。中で二度言うと、そのぶんノートが下がる。mac の信号機のぶんだけ列の上を空ける | `gui/index.html ~ #railtop` |
| 176 | 2026-09-06 | いろんなテーマで表示できるようにしたい（育ててきた琥珀も取り込んで） | 琥珀（既定・OS に従う／明／暗）、ayu（明・中間・暗）、紙（白と黒だけ）。**名前で選んだら、選んだとおりに出す** ──「ayu-dark にしたのに昼は明るい」は選んだことにならない。既定だけが OS に従う。Monaco と mermaid にも**同じ明暗を渡す**（別々に訊くと、替えた日にエディタだけ前の明暗で残る）。足すときは色を全部埋める ── 変数は継承するので、抜けは崩れではなく**混ざり**として出る | `gui/renderer.js ~ const THEMES` |
| 177 | 2026-09-06 | マークダウンの描画が芋臭い。画像の周りにも配慮を | 直したのは三つ。**一行の長さ**（34em で止める ── 窓を広げるほど一行が長くなり、折り返すたびに目が左端を探す。読みにくさの半分はここで、字体や色の話ではない）。**見出しの上下**（上を厚く下を薄く ── 見出しは次のかたまりの持ち物）。**絵**（裸で置かない ── 地と同じ色の絵はどこからが絵か分からない。細い枠と丸みと薄い影、`alt` があれば下に添える）。区切り線は端まで引かない ── 全幅の線は「ここで終わり」に見えるが、`---` は「話が変わる」でしかない | `gui/index.html ~ max-width: 34em` |
| 178 | 2026-09-06 | 書く面でも、画像の行だけ実物を小さく出したい | **字は消さない。** ファイルは Markdown のままで行はそこにある ── 消して絵に置き換えると、パスを一文字直したいだけのときに直しようがない。Monaco の `view zone`（行と行のあいだの空き）に置く。読めない絵は黙って空けず、何が読めなかったかを出す（貼り間違いに気づけるように） | `gui/renderer.js ~ function drawZones` |
| 179 | 2026-09-06 | 読む面でも書けるようにしたい。初学者がこちらだけで完結できるように | 押したかたまりがその場で入力欄になり、上の小さな帯（見出し・箇条書き・チェック・太字・引用）で記号を打たずに変えられる。**元の行は `to_html` が `data-line` / `data-span` で持たせる** ── 何番目のかたまりかを数えると前書きのあるノートでずれる。印はいったん制御文字で置き、最後に属性へ変える（かたまりが何行食べたかは**次のかたまりが始まる場所**を見るまで分からない）。閉じ札には差さない ── 箇条書きの二つ目は前の項目を閉じてから始まる。空にしたらそのかたまりを消す（消し方を別に用意すると、探すことになる）。「＋ ここに書く」を末尾に置く ── 空のノートで手が止まる | `crates/amber-core/src/markdown.rs ~ fn stamps` |
| 180 | 2026-09-06 | マークダウンのサポート（書き方）を出す | 押せる形で出し、**選ぶとその場に入る** ── 読むだけの一覧にすると、読んでから自分で打ち直すことになる | `gui/renderer.js ~ async function cmdSyntax` |
| 181 | 2026-09-06 | タグを文章で入力させるのはいただけない。一覧から選び、無いものだけ足す | すでに `#仕事` があるのに打ち直させると、打ち間違いで**似た名前のタグがもう一つ生える**。押すと付け外し、`＋ 新しいタグを作る` だけが入力。やめたら何も変えない。**札から「…」を外したとき（依頼 192）ここが鳴った** ── 約束は守られたまま札だけ変わった例で、検査を新しい札に付け替えた | `gui/renderer.js ~ ＋ 新しいタグを作る` |
| 182 | 2026-09-06 | 絞り込みが IT プロ向けすぎる。初学者向けに | 「探し方…」から選ぶ ── タグ・フォルダは一覧から選ばせ、記法を覚えなくても絞れるようにした。**「選ぶとその書き方が絞り込みの欄に入る」という後半は、2026-09-06 に本人が取り下げた**（依頼 294 ── 「`tag:仕事` とかになってシステムチックすぎる」）。押しただけなのに機械の言葉が現れ、外すには字を消すことになっていた。取り下げなので、張り直しではなく**約束そのものが変わっている** ── 選んだものは選んだものとして持ち、字にしない | `gui/renderer.js ~ \*\*押して選んだものは、字にしない。\*\*` |
| 183 | 2026-09-06 | フォルダ・タグ・お気に入りの作り方が分からない。削除もしたい | **できることは前からあった**（`仕事/2026` と書けば階層）が、それを言う場所が画面に無かった ── 使えないのと、あるのに見えないのは、使う人には同じこと。段の見出しに「＋」を付け、**一つも無くても段を出す**（無いと最初の一つを作る道が消える）。右押しで名前を変える／外す。フォルダの名前替えは**中のノートを一本ずつ移す**（フォルダはただのディレクトリなので） | `gui/renderer.js ~ function railMenu` |
| 184 | 2026-09-06 | 「見た目を変える」がノートの右押しに出るのはおかしい | ノートを右押しした人が訊いているのは「このノートをどうするか」で、アプリの色ではない。**⚙ を題字の帯に置いた** ── 見た目・vim・字の大きさ・書き方・バックアップ・置き場所はそちら。⋯ はノートのことだけ | `gui/index.html ~ id="gear"` |
| 185 | 2026-09-06 | 窓の削除は、消さずにゴミ箱へ | **`core` の `delete` は消してしまう**（`remove_file`）── 電話にはゴミ箱が無いのでそれでよいが、机の上では戻せないのは強すぎる。窓は Electron の `shell.trashItem` を通す。フォルダも同じ道 | `gui/main.js ~ amber:trash` |
| 186 | 2026-09-06 | 読む面は、押して開くのではなく**メモ帳のように置いて打てる**ようにしたい。道具の帯も下に出したまま | 面ぜんぶを `contenteditable` にし、落ち着いたら **DOM を Markdown に戻して**いつもの保存を通す。戻せる札は `to_html` が出すものだけなので、逆に読むのも数十行で済む（貼り付けは字だけにして、よそから来た札を入れない）。**戻せないかたまり（枠・表・図・注記・絵）は触らせない** ── `contenteditable="false"` にして、押したら書く面のその行へ送る。**打てるのに保存されない、がいちばん悪い**。飾りは `execCommand`（`<b>` が `**` に戻る）、字そのものを変えるもの（チェック・リンク・表…）は一度ぜんぶ字に戻してから直す ── 見た目の上で組み立て直すと、そこだけ別の作り方が生える | `gui/renderer.js ~ function readToMd` |
| 187 | 2026-09-06 | 自分でも立ち上げたい（Windows の run.bat と、Mac のコマンド） | `./gui/run.sh` と `gui\run.bat`。**両方に `vendor` の用意を足した** ── `gui/vendor/` は git に入れていないので、無いまま起動すると窓は開くが中身が真っ白で、原因が「落としていない」だと画面のどこにも書いていない。README にも書いた | `gui/run.bat ~ node vendor.js` |
| 188 | 2026-09-06 | 描く側の例外が、どこにも出ない | 読み込みの途中で落ちると窓が真っ白になるだけで、端末には何も出なかった ── 開発者ツールを開くまで何が起きたか分からない（実際に白い窓を前に十分ほど探した）。`console-message` を端末へ流す | `gui/main.js ~ console-message` |
| 189 | 2026-09-06 | 読む面で Enter が改行にならない | `document` のキー処理が読む面の Enter まで拾い、**焦点を書く面へ飛ばして**いた。打っている場所は「絞り込みの欄」「書く面」の二つだと数えていて、読む面を数え忘れていた | `gui/renderer.js ~ const inRead` |
| 190 | 2026-09-06 | 読む面で表・注記・引用も書きたい。特に表は行と列を足したい | 表と注記を**触れるかたまり**にした（字に戻せる形をしているので、触らせない理由が無い ── 触れないままだと「読む面だけで完結できる」が嘘になる）。表は caret が入ると**すぐ上に道具が出る**（行/列を足す・消す、寄せ）── **縦棒を数えさせない**。空の升は全角空白で埋める（中身が空の升は描く側によっては消え、消えた表は「作れなかった」に見える）。注記の種類の札だけは打てなくする ── 「注意」を「ちゅうい」に直せると、それは記法を壊す | `gui/renderer.js ~ function tableDo` |
| 191 | 2026-09-06 | 絞り込みに「タグ（複数）」「フォルダ」「期間」がほしい | タグはいくつでも選べ、**選んでいる途中を出す**（出さずに複数選ばせると、押した数を数えることになる）。期間は `updated:` のような記法を増やさない。日付も打たせない（「先週」を `2026-08-30..` に直すのは人の仕事ではない）。**三つを一度に一つしか選べなかったのは 2026-09-06 に直した**（依頼 294）── 三つが引き出しとして常に並び、いくつでも重なる | `gui/renderer.js ~ ここは三つの引き出しが常に並び、いくつでも重なる` |
| 192 | 2026-09-06 | 「xxx…」で終わる札が多すぎる。3点リーダーを一律で無くす | 「…」は「続きがある」の符牒だが、**何が起きるかは言っていない**。`タグを付ける…` → `ノートのタグ設定`、`フォルダを削除…` → `このフォルダをゴミ箱へ` のように、押すと何が起きるかを札そのものに書いた。**畳んだ道具の帯の「…」だけは残る**（依頼 215）── あれは札ではなく「まだある」という印で、押すと開くことは形が言っている。**依頼 213 で札を付け替えたとき、ここが鳴った** ── 約束は生きたまま札だけ変わった二度目の例 | `gui/renderer.js ~ このフォルダをゴミ箱へ` |
| 193 | 2026-09-06 | ⋯ と ⚙ の違いが分からない。歯車が小さすぎる | **形ではなく言葉で分ける** ── 点三つを「ノート ▾」にした。歯車は絵なので、字と同じ大きさだと小さく見える（19px に）。置き場所を訊く小窓は、**いまどこを見ているかを先に出す** ── 「amber のディレクトリはどう決めるのか」が分からなかったのは、決める場所が無かったからではなく、いまどこかが画面に出ていなかったから | `gui/index.html ~ ノート ▾` |
| 194 | 2026-09-06 | mermaid の図が芋臭い。IT 素人でも図を作れるように | 既定の紫と水色は**ノートの地とも琥珀とも合わない**（図だけ別のアプリから貼ってきたように見える）── `base` にテーマの色を渡し、いま出ている色で描かせる。作る側は**よく使う四つだけ**（流れ図・分かれ道・やりとり・円グラフ）を選ばせ、言葉を訊いて骨組みを入れる ── 込み入った図まで画面に載せると「難しいもの」に見えて誰も押さなくなる | `gui/renderer.js ~ async function cmdDiagram` |
| 195 | 2026-09-06 | Monaco の色分けが本体スレッドで走っていた | CSP に `worker-src` が無く、`script-src` が代わりに使われて `blob:` の worker が弾かれていた。通したあとは、Monaco が組み立てる道が `file:///vendor/…`（ファイルシステムの根から）になって取り込みに失敗 ── **絶対の道で渡す**。どちらも動くので気づかないが、長いノートで窓が固まる。`console-message` を端末へ流して初めて見えた | `gui/renderer.js ~ getWorkerUrl` |
| 196 | 2026-09-06 | amber の外にある `.md` を、**単発で**開きたい。左の一覧は出さず、閉じたら元に戻る | 置き場所は動かさない ── 一本開くたびに一覧が入れ替わると「さっきまでのノートが消えた」に見える。索引にも混ぜない ──「フォルダがそのまま索引」の外にあるものを索引に入れると、索引が索引でなくなる。**異例な開き方だと画面が言う**（左の二列を出さず、上に帯と「閉じて一覧に戻る」）── 言わないと、いつもの一本と見分けが付かないまま別のフォルダへ書く。題は前端で決めず `note::read` に訊く（core に一本用の口 `note` を足した）── ここで自分で決めると、外の一本だけ別の題になる | `gui/renderer.js ~ async function openGuest` |
| 197 | 2026-09-06 | `.md` を窓に落としても同じ開き方にしたい | 置き場所の**中**のものは、いつもの一本として開く ── 同じファイルが一覧と客の両方に居ると、どちらに書いたのか分からなくなる。**Electron 32 から `File.path` は無い**ので `webUtils.getPathForFile` に訊く（描く側に「好きなファイルの道を知る」口は作らず、落とされた `File` を渡して訊くだけ）。ついでに `amber note.md` と Finder の「このアプリケーションで開く」も同じ道に繋いだ ── `open-file` は `whenReady` より先に鳴ることがあるので、来た順に溜める | `gui/main.js ~ open-file` |
| 198 | 2026-09-06 | 表示面で打つとチェックリストが消える | **字に戻す処理が、画面のほうを壊していた。** 升（`.box`）と入れ子の箇条書きを `remove()` してから読み、入れ子だけ付け直して**升は付け直していなかった** ── 一度書き戻すと升が画面から消え、次の書き戻しではただの `- やること` になって、ファイルからチェックが消えた。**読むだけで済むものを、動かして読む理由は無い** ── 一切触らずに読むように直した | `gui/renderer.js ~ 画面には一切触らない` |
| 199 | 2026-09-06 | 面の名前を「表示 / 並べて表示 / コード」に | 「読む・書く」は**こちらの都合の言葉**で、使う人から見れば片方は組んだ姿、もう片方は Markdown そのもの。並び順も、ふだん見る順（表示が先）にした | `gui/index.html ~ 並べて表示` |
| 200 | 2026-09-06 | フィルタは打たせない。「言葉で探す」「題」「含まない」は要らない | 言葉で探すのは上の欄がやる ── 同じことを二か所で頼めると、どちらが効いているか分からなくなる。タグもフォルダも**そのものを指す**（名前を打たせない）── 打たせると打ち間違いが「見つかりません」として返り、間違いに見えない。2026-09-06 に小窓ごと引き出しへ移したが、**押して選ぶ・打たせない**はそのまま | `gui/renderer.js ~ 押して付け外し（全部付いたものだけ）` |
| 201 | 2026-09-06 | ノートの既定は「表示」に | 開いてまず見たいのは組んだ姿で、記号の並びではない ── 書きたくなったらその場で打てる | `gui/renderer.js ~ let view = 'read'` |
| 202 | 2026-09-06 | 太字にしたあと、続けて打った字まで太字になる | 選んだところに `<strong>` を掛けると caret はその中に残る ── **飾りのすぐ後ろへ出す**。選んでいなければ「ここから太字」の意味なので、そのまま | `gui/renderer.js ~ 飾りの外へ caret を出す` |
| 203 | 2026-09-06 | 水平線や表でノートが終わると、その下に書けない | 表の外側は表の一部ではないので、矢印でも出られない ── **末尾にいつも降りられる一行を置く**。空のままなら字に戻すときに落ちるので、増えも減りもしない | `gui/renderer.js ~ function tailStop` |
| 204 | 2026-09-06 | 図がカーソルの場所ではなくノートの一番下に入る | 帯の釦を押した瞬間、焦点は釦へ移り選択も消える ── そのとき「どこか」を訊いても分からず末尾に落ちる。**動いたときに憶えておく**。釦は焦点を奪わない（`mousedown` を止める）。入れたあとは、入れたものの次に caret を降ろす | `gui/renderer.js ~ let caretAt` |
| 205 | 2026-09-06 | 図を続けて入れると、前の図が消える | 組み直しで `<pre>` を `<div class="mermaid">` に**掛け替えるとき、元の字を持っていかなかった** ── 字に戻すとき中身がどこにも無く、保存のたびに図が消えた（絵も同じ）。掛け替えるところは必ず持っていく。札を配るのは掛け替えより**先**。そして**元の字が取れないときは書き戻さない** ── 空を返すと黙って消え、気づくのは何回か保存したあと | `gui/renderer.js ~ function keepMark` |
| 206 | 2026-09-06 | 太字にしても保存されない | `styleWithCSS` を真にしていたので、太字が `<b>` ではなく `<span style="font-weight">` になり、字に戻すとき飾りが落ちていた（色のために真にしていた）。**飾りは札で、色は `<font color>` で読む** | `gui/renderer.js ~ styleWithCSS` |
| 207 | 2026-09-06 | 円グラフの見た目が悪い | 既定の派手な12色は琥珀の隣で喧嘩する ── **フォルダと文字色に使っている11色と同じ並び**を渡し、アプリのどこを見ても同じ色の家族にする。境目は地の色で抜き、割合は中に置く | `gui/renderer.js ~ pieStrokeColor` |
| 208 | 2026-09-06 | crmaine に同梱するので、Windows 用の `amber-server.exe` が要る（配布先はネットに出られない社内端末） | 依存はすべて純 Rust だった（`zlib-rs` で C の zlib すら要らない）── `chrono` の Windows 経路だけが `dlltool` を要るので mingw が要る。**組むのは CI だけ**（Windows の上で組み、その場で一往復させ、DLL も見る）。mac からのクロスビルド（mingw）も一度作ったが**捨てた** ── 走るところを確かめられず（mac の上で Windows の実行ファイルは動かない）、見る DLL も CI の四つに対して十三で、**確かめられないほうを残す理由が無かった**。道具（mingw-w64、1.4GB）も要らなくなった | `.github/workflows/release.yml ~ amber-server-win-x64.exe` |
| 209 | 2026-09-06 | 落として置くだけで動くこと（DLL を連れていないこと） | 既定の MSVC ビルドは `VCRUNTIME140.dll` を要求していた ── 素の Windows には無い。**`crt-static` で CRT ごと取り込む**。この食い違いは**走らせるだけでは出ない**（CI の runner には再頒布可能パッケージが入っていて、往復試験は通ってしまった）── 捕まえたのは DLL の検査だけで、入れておいてよかったもの | `.github/workflows/release.yml ~ crt-static` |
| 210 | 2026-09-06 | 変換の Enter で、小窓まで確定してしまう | 「さかな」→「魚」を Enter で確定した瞬間に小窓が閉じ、「魚の目」と打つつもりが「魚」というフォルダが出来る。**変換中の Enter は変換の確定であって返事ではない** ── `isComposing`（見ない環境では `keyCode 229`）で見分ける | `gui/renderer.js ~ e.isComposing` |
| 211 | 2026-09-06 | サブフォルダを「/」で一気に作らせたくない。右押しから作りたい | 「仕事/2026 と書けば階層になります」は**書き方を知っている人にしか通じない**。名前に「/」が来たら全角に落とし、階層は**親を右押しして作る**（フォルダもブックマークの置き場所も同じ形） | `gui/renderer.js ~ この中にフォルダを作る` |
| 212 | 2026-09-06 | 「お気に入り」を「ブックマーク」に。登録時に置き場所を選べて、その場で作れるように | 前は「入れる／外す」と「置き場所を選ぶ」が別の命令で、入れたあとにもう一度探して選ぶ形だった ── **フォルダへ移動と同じ一手**で済む話。「棚」という言い方もやめた（何のことか画面が説明していなかった） | `gui/renderer.js ~ どこに登録しますか` |
| 213 | 2026-09-06 | 献立の名前を、することの名前に揃える | ノートの側は **ブックマークに登録 / ノートのタグ設定 / フォルダへ移動 / 通知設定 / エクスポート / ゴミ箱へ入れる**。amber の側は **テーマ / vimモード / 一括バックアップ / amber保存ディレクトリ変更 / コマンド一覧**。テーマと vim と保存先は、**いまどうなっているかを押す前に見せる** | `gui/renderer.js ~ amber保存ディレクトリ変更` |
| 214 | 2026-09-06 | vim は歯車の中だけでいい。選んだら憶えていてほしい | 使うのはたいてい一人で、毎日見る帯に居座る値打ちは無い ── 道具の帯から外した。入切は憶える（前から憶えていたが、献立に**いまどちらか**を出すようにした） | `gui/renderer.js ~ async function cmdVim\(` |
| 215 | 2026-09-06 | 道具の帯を整える。コードとコード枠は要らない。フローは画像の隣に | 図は「使う人は使う」もので、**二列目に畳んでおくとあることに気づかれない**。チェックリスト・番号リストは名前のとおりに呼び、番号はチェックの隣へ。畳んでいるときの札は「…」 | `gui/renderer.js ~ ['フロー', '', cmdDiagram]` |
| 216 | 2026-09-06 | 図の種類を増やしたい（キャッチーなので） | 流れ図・分かれ道・**マインドマップ**・**年表**・**予定表**・**四象限**・やりとり・円グラフ の八つ。**書き方は覚えなくていい** ── 言葉を訊いて骨組みを入れる。年表と四象限は日記や「やることの整理」と相性がよく、mermaid を知らない人がいちばん喜ぶところ | `gui/renderer.js ~ quadrantChart` |
| 217 | 2026-09-06 | 文字色は要らない | Markdown に文字色は無く、`<span style>` は GitHub で落ちる ── 「読める形は残る」とはいえ、**記法を増やしてまで残す機能ではなかった**。フォルダの色は残す（あれはフォルダの見分けで、文の飾りではない） | `gui/renderer.js !~ cmdPaint` |
| 218 | 2026-09-06 | mermaid の図は、ふつうの人には更新がクソ難しい。右押しで、図を見ながら直せるように | 作るところ（`cmdDiagram`）は選ぶだけで済むのに、**直すところが字だけ**だった ── `flowchart LR` も `A --＞｜はい｜ B` も覚えていない人にとって、一度作った図は「作り直すしか手が無いもの」で、それは作れると言えない。押すと工房が開き、**左に表・右に図**。表を直すとその場で描き直るので、当たっているかを**保存する前に目で確かめられる**。線の行き先は箱の名前から選ばせる（`A`、`B` のような合言葉を人に打たせない ── 打たせると、消した箱を指したままの線が残る）。押しても右押しでも開く ── どちらだったかを覚えている人はいない | `gui/renderer.js ~ async function studioOpen\(` |
| 219 | 2026-09-06 | 手で書いた凝った図でも、工房を開けないことが無いように | 枝の枝や囲みのある図は表にできない ── そこで閉め出すと「amber で作った図しか直せない」になる。**読めない形は表にせず、字の面を出す**（右の絵は同じように見えながら直せる）。読めなかったことは画面が言う（黙って半分だけ読んだ表を出すと、直して保存した瞬間に読めなかった行が消える ── 気づくのは何日も後） | `gui/renderer.js ~ function studioRaw\(` |
| 220 | 2026-09-06 | 図を直しても、触っていないところが変わらないこと | **表で直せる、とは「字に戻せる」ということ。** 組み直した字が一字でも違えば、その差は保存の瞬間にノートへ入る。`cmdDiagram` の出す八つ全部について「読んで、組み直して、元と同じ」を機械で見る。**二度目も同じか**まで見る（工房は開いて閉じてまた開かれる）── 表にしない行（`dateFormat`、`x-axis`…）は元の字のまま取っておいて戻す | `scripts/diagram-test.js ~ 作った図が、そのまま戻るか` |
| 221 | 2026-09-06 | 幕の上に出した釦が、押しても効かない | `#top` は窓ごと引きずるための帯（`-webkit-app-region: drag`）で、**上に重ねたものより先に押しを取る ── 重ね順は効かない**。工房は上から 4vh に出るので帯とちょうど重なり、「この形にする」も「やめる」も押せなかった。幕の側で切る | `gui/index.html ~ 重ね順は効かない` |
| 222 | 2026-09-06 | マインドマップと予定表の見た目が悪い | マインドマップだけは `themeVariables` を見ず、灰と藤色で描かれていた ── 琥珀のノートの上で**そこだけ別のアプリ**に見える。枝は円グラフと同じ十一色にし、まん中は琥珀そのものに（色の並びは一か所 `FAMILY` に置いた ── 図ごとに別の並びを持つと、同じノートの中で色の意味が変わる）。予定表は既定の幅だと目盛りが重なって日付が読めない（`09/1009/12…`）── **広く描いてから、入るところまで縮める** | `gui/renderer.js ~ const FAMILY` |
| 223 | 2026-09-06 | 「新しいノート ＋」が絶妙にダサい | 全角の「＋」は字なので、書体によって太さも位置も変わる ── 線で描いた印に替えた。琥珀に薄い階調と、琥珀色の影を一枚。押すと 1px 沈む（押した手応えが無いと、効いたかどうかを画面のどこかで探すことになる） | `gui/index.html ~ #new:active` |
| 224 | 2026-09-06 | ルーラー・行番号を歯車から入切したい | **行番号は入れた**（長いノートで「何行目」と言い合うときに要る）。憶えるので、次に開いても同じ。**ルーラーは入れていない** ── 桁で折り返す決まりがノートには無く、表示面はもともと 34em で measure を止めている。要る日が来たら言ってほしい | `gui/renderer.js ~ async function cmdLineNo\(` |
| 225 | 2026-09-06 | 「新しいノート ＋」が絶妙にダサい。案を見せてから作ってほしい | 四案を実寸・実色・三テーマで並べて見てもらい、**案一**が選ばれた。古く見えていたのは色ではなく**塊であること**で、階調と琥珀色に光る影は、その塊を立体に見せるための飾りだった。塊をやめ、琥珀は 22px の丸だけに残す ── 面積が小さいぶん色は強く効き、字も高さも列の他の行と揃うので、左の列が一本の並びとして読める。押せることは丸の形が言うので、地も枠も要らない | `gui/index.html ~ #new .ring` |
| 226 | 2026-09-06 | 歯車の「いま：」は要らない。右詰めに。vim はオン／オフで | 献立に出ている値は、これから選ぶ値ではなく**いまの値**しかありえない ── 一行ごとに同じ二文字を読ませる意味が無い。右端に揃えるのは、名前のすぐ後ろだと命令ごとに違う位置で始まって目が毎回探しに行くから。行番号も「出している」ではなく**オン／オフ**に揃えた ── 同じ形の設定が二つの語彙を持つほうが分かりにくい | `gui/renderer.js ~ vimOn \? 'オン' : 'オフ'` |
| 227 | 2026-09-06 | 初回起動時に、既定のノートを同梱して表示してほしい（Inkdrop のように） | 何も無い一覧を前にすると、Markdown を知らない人は「何ができるのか」をどこからも知れない ── 説明を読ませるより、**読めて・押せて・書き換えられるノートが最初から入っている**ほうが早い。三枚（案内・覚え書き・設計）とフォルダ二つ。窓と iPhone は**同じ一組**（`packaging/welcome`）を使う ── 電話用に写しを置くと、片方だけ直った日に二つの amber が別のことを言う。置くのは一度きりで、**消したら二度と戻さない**（邪魔だから消したものが起動のたびに生えるのがいちばん嫌われる）。既にノートがある人には置かない | `gui/main.js ~ function seedWelcome` |
| 228 | 2026-09-06 | 見本の中身は『覚悟の磨き方』から。図はデザインパターン（ストラテジー） | 超訳の文そのものは池田貴将さんの著作なので写さず、**松陰本人の言葉**（『士規七則』『留魂録』、下田の歌 ── どれも公有）を引き、本は出会った場所として挙げた。設計のほうは分岐と差し替えを図で見比べ、**「二つしか無いなら `if` でいい」まで書く** ── パターンを知っていることと、そこで使うことは別のこと | `packaging/welcome/覚悟の磨き方.md ~ 士規七則` |
| 229 | 2026-09-06 | iPhone でも表と注記が読めること | 電話は表の行を空白で繋いで「縦棒つきの一文」として描き、`[!TIP]` を字として出していた ── **同じノートが二つの amber で別のものに見えていた**。`Block` に表と注記を足した（升に切るのは核の仕事で、どう描くかが電話の仕事）。表は狭めずに**横へ流す** ── 402pt に四列を押し込むと、升が二文字ずつの縦の列になる | `crates/amber-core/src/note.rs ~ Block::Table` |
| 230 | 2026-09-06 | 一覧の二行目に `**` が生で出る | 題は `inline` を通して飾りを落としていた（依頼 174）のに、二行目だけ取り残されていた ── `**大事**` は四つの星印を出したうえ、**そこは探すときに当たる文でもある**ので 大事 で見つからなくなる。引用と箇条書きの印も落とす（文であることは変わらない） | `crates/amber-core/src/note.rs ~ Nor is the notation` |
| 231 | 2026-09-06 | 図と枠の両方があるノートで、どちらかが描けない | 図の読み込みは `define` を伏せる一瞬を持っていて（AMD ローダに横取りされるため）、Monaco は枠に色を付けるとき言語を後から読みに行く ── 重なると `define is not a function` か「無名の define は一つまで」で落ちる。**旗（`define.amd`）だけ下ろすのでは足りない**（中の部品は関数の有無しか見ていない）ので、**枠の色付けを待ってから図を読む** | `gui/renderer.js ~ paintCode\(\).then\(drawDiagrams\)` |
| 232 | 2026-09-06 | 題が「a」一文字になる | 帯には面の札も字数もタグも並ぶのに、縮む床が題にしか無かった ── そこが全部のしわ寄せを引き受けて「amber へようこそ」が「a」になった。題に床を敷き、縮むのは右端の meta のほうに | `gui/index.html ~ min-width: 9ch` |
| 233 | 2026-09-06 | 円グラフ・年表・マインドマップの色が芋臭い | フォルダの色（`PALETTE`）をそのまま図に流用していたのが原因 ── **7px の点と、円の三割を占める面は、同じ色でうまくいくものではない**。どれも明るさも鮮やかさも同じくらいで、並べると平らな帯になっていた（九〇年代の業務資料の色）。図の側は琥珀を一つめに置いて**明るさをわざと散らす** ── 深い青 → 明るい緑 → 沈んだ紫 → 明るい珊瑚と交互にしたので、隣り合う二つが必ず明暗で分かれ、色の見分けにくい人にも切れ端が数えられる。年表は `cScale` を渡していなくて**灰色の帯が並ぶだけ**だった。**この日の色そのものは半日で捨てた**（依頼 237）── 明暗を散らすために深い色を混ぜたら、今度は「濃くて沈む」になった。守られているのは色の並びではなく「**図とフォルダは別の家族**」と「年表に色を渡す」の二つなので、検査もそちらに張り直した | `gui/renderer.js ~ 'cScale' \+ n` |
| 234 | 2026-09-06 | マインドマップのまん中の色が野暮ったい | ただの丸い橙だった。濃い琥珀で塗り、一段明るい琥珀の輪を掛け、影を一枚 ── **枝より手前にあるものとして見えないと、そこが中心だと形が言っていない**。枝の字の色もこちらで決めた（渡さないと mermaid が塗りから作り、明るい琥珀の枝では「仕事」が薄い字で消えていた） | `gui/renderer.js ~ .mindmap-node.section--1 circle.basic` |
| 235 | 2026-09-06 | 枝葉にさらに枝葉を書けるように。枝の間にも字を置きたい | 字下げを深さの番号に直して読む ── **空白が二つでも四つでも「一段下」は一段下**なので、出てきた字下げを浅い順に並べて何番目かを取る（人が書いた図の空白の数を当てにしない）。工房では「→」で深く「←」で浅く、深さは字下げそのもので見せる（「2」と書くより、ずれている形のほうが枝に見える）。**親のいない孫は作らせない** ── 一段目の次にいきなり三段目を置くと mermaid はその枝を捨て、「足したのに図に出ない」という分かりにくい壊れ方になる。枝と枝のあいだに字は置けない（mermaid に線の札が無い）ので、そう画面に書いた | `gui/renderer.js ~ function settle` |
| 236 | 2026-09-06 | 分かれ道の既定を「左利きですか？」に | 「足りている？→出す／足す」は**何の話か分からない例**だった。ハサミなら、はい・いいえの先が具体的に違うものになるので、骨組みを見た瞬間に「自分の場合はこう」と置き換えられる | `gui/renderer.js ~ 左利きのハサミを使う` |
| 237 | 2026-09-06 | 円グラフの色が濃くて沈む。案を見せてほしい | 三案を同じ数字・同じ大きさで並べて見てもらい、**案一（やわらか）**が選ばれた。明るさを上げ鮮やかさを落とした淡彩 ── 濃い色を並べると円の面積の大半が暗い塊になり、**紙より図のほうが重くなる**。淡いので字は白ではなく濃い墨を載せる（明るい地に白は乗らない）。この十一色は年表の札とマインドマップの枝でも同じものを使う | `gui/renderer.js ~ '#F7BD5C', '#8FC8E8'` |
| 238 | 2026-09-06 | 年表の字が小さくて読めない | SVG の中では 14px だったが、`#read .mermaid` を**文の幅**（34em）に押し込んでいたので `useMaxWidth` が全体を 0.6 倍に縮め、画面上は 8px になっていた。**図は文の幅に従わない** ── 34em は目が一行を追える長さの話で、図はそもそも行ではない。図は図の幅で描かせ、はみ出すぶんだけ横に流す | `gui/index.html ~ 図は、文の幅に従わない` |
| 239 | 2026-09-06 | 四象限の題と軸の言葉を変えたい | 「やることの置きどころ」は何をする図か言っていなかった ── **やることの優先順**。軸も「大きい／小さい」ではなく **影響大／影響小**、「いま／いつでも」ではなく **期限近／期限遠**。四つの区画（すぐやる・段取りする・あとで・誰かに頼む）は新しい軸でも位置の意味が合う | `gui/renderer.js ~ x-axis 期限遠 --> 期限近` |
| 240 | 2026-09-06 | iPhone でも図を出してほしい | 電話は `flowchart LR` と四行の字を灰色の枠で出していた ── 窓が絵を出しているところなので、**同じノートが開く端末によって別のものに読めていた**。描くのは mermaid 自身（`WKWebView`）で、Swift で描き直さない ── 八種類ぶんの実装をもう一つ持てば答えがもう一組でき、食い違うのはマインドマップの配置や予定表の目盛りという**いちばん確かめにくいところ**になる。色も設定も窓と同じものを渡す。図のあるノートを開くまで 3.4MB は読まない | `ios/Cian/Drawing.swift ~ struct Drawing` |
| 241 | 2026-09-06 | 図が描けないときに、何も出さないこと | 束ねに mermaid が無い（`node gui/vendor.js` を通していない木から建てた）ときも、書けない図を書いたときも、**書いた字をそのまま出す** ── 空白を出すと、そのノートがこの端末でだけ中身を失ったように見える。描けない理由も一緒に出す | `ios/Cian/Drawing.swift ~ Say the source, rather than nothing` |
| 242 | 2026-09-06 | crmaine が握っている amber 側の約束を、黙って変えないこと | 四つ ── 描く側に見せている口の名前、エンジンを「画面の隣」に探すこと、印を `../packaging/amber-mark.png` と指すこと、画面の組み立て手順。**黙って変わると「札は出るのに何も起きない」という形で出る**（いちばん辿りにくく、しかも壊したこちらには何も起きない）。名前を変えるなという話ではなく、**変えるときに鳴る**ようにした ── 鳴ったら一声かけてから直す。口は crmaine が使う九つではなく**出している十六を丸ごと**約束にする（使われていないと思った一つを消した日に、それが向こうの九つだった、が起こりうる）。増えたぶんは落とさず「表に足すか決めてください」と言う | `scripts/contract.js ~ 同梱する側との約束` |
| 243 | 2026-09-06 | Release の mac が arm64 だけで、Intel の Mac で動かない | runner が Apple シリコンなので、素直に組むと arm64 の一枚しか出ない ── **落とした人の手元でだけ分かる壊れ方**。`lipo` で二つを束ねて universal に。束ねたと言い張らず、`lipo -info` に両方が居ることを CI で見る | `.github/workflows/release.yml ~ amber-server-mac-universal` |
| 244 | 2026-09-06 | Release に画面の zip がほしい（同梱する側が Monaco を自分で積まなくて済むように） | 同梱する側はいま「タグのソースを落として `npm install` → `node gui/vendor.js`」をしていて、**こちらがエディタの積み方を変えるたびに追随が要る**。積み終えた形で配れば、その追随が要らなくなる。`packaging/amber-mark.png` も同じ zip に入れる（画面は `../packaging/` を指しているので `gui/` だけでは印の出ない画面になる）。`node_modules` は入れない（Electron 本体 200MB 超）。**実行ファイルも入れない** ── 機械ごとに別で、置く場所は同梱する側が決めている | `.github/workflows/release.yml ~ amber-gui.zip` |
| 245 | 2026-09-06 | 不具合が人づてに回ってくる（社内 Windows 端末に配られるので） | 戻ってくる報せはたいてい「amber が変です」だけで、どの版のどのエンジンかは書いていない ── 歯車に **amber について**（版・エンジン・置き場所）を置いた。**画面とエンジンを別々に出す** ── 同梱するときは実行ファイルだけ差し替えられるので二つはずれうる（この機能を作った画面で、実際に 2.4.0 と 2.3.0 がずれていた） | `gui/renderer.js ~ async function cmdAbout` |
| 246 | 2026-09-06 | iPhone の画面が窓と全然違う。最上段は歯車だけ、そこに小さな印 | 前は最上段にフォルダ＋・ノート＋・並べ替えの三つが並び、その下に大きなアイコンと「amber」の見出しがあった ── **どちらも窓には無い形**。作るのは一覧の頭の釦、並べ替えとフィルタはその下、最上段は歯車と小さな印だけ。アプリの名前は窓の外（Dock・ホーム画面）が既に言っているので、中で二度言うぶんだけノートが下がる | `ios/Cian/ContentView.swift ~ 最上段は歯車だけ` |
| 247 | 2026-09-06 | iPhone も「すべてのノート・ブックマーク・フォルダ・タグ」の順に | 窓の左の列と同じ順に並べた ── **二つの amber で同じものを同じ場所に探せる**。タグは窓にあって電話に無く、探す欄の提案としてしか出ていなかった（「どんなタグがあるか」を見るのに一度打ちはじめる必要があった ── タグは思い出すものではなく、見て選ぶもの）。木（フォルダごとの表示）は既定を切りに ── 木はフォルダの段とノートの段で同じものを二度描くので、窓と同じ形にはならない。要る人は「フィルタ」から入れる | `ios/Cian/ContentView.swift ~ 窓の左の列と同じ順` |
| 248 | 2026-09-06 | iPhone に見本のノートが入っていない | 自動で置くのは**アプリ自身のフォルダを使っている初回だけ**にしてあった ── 既に自分のフォルダを選んでいる人（同期先を向けている人）に置くと、見ていないところに三枚落ちて、何週間かあとにどこから来たか分からないものとして見つかる。それでも「入れてくれ」と言える場所が要るので、設定に **見本のノートを入れる** を置いた。同じ名前は飛ばすので二度押しても増えず、**何枚置いたかを言う**（既にあると本当に何も起きない回があり、黙っていると押せていないように見える） | `ios/Cian/NotesStore.swift ~ func addWelcome` |
| 249 | 2026-09-06 | 「お気に入り」は iPhone にも残っていた | 窓では依頼 212 で「ブックマーク」に改名したが、電話は古い名前のままだった ── **同じものが二つの amber で違う名前で呼ばれていた**。「棚を変える」も「置き場所を変える」に揃えた | `ios/Cian/ContentView.swift ~ 依頼 212 で「お気に入り」から` |
| 250 | 2026-09-06 | バックアップは四種きっちり。既存に別のノートがある場合などを調べて | 窓は `scope: 'all'` を決め打ちで渡していて、**四つあることはエンジンしか知らなかった**（電話には四つとも出ていた）。すべて／フォルダ／タグ／ノート一枚を訊くようにし、四つとも往復で確かめた。**戻すときはいまあるものを消さない** ── 同じ名前があれば今のを残し、何枚避けたかを言う（言わないと「戻したつもりで戻っていない」に見える）。確かめて出た不具合が二つ ── ①復元の一時置き場が pid だけで名前を付けていて、**二つ同時に走ると混ざる**（片方がもう片方の展開途中を消しうる）②フォルダの zip を戻すと**フォルダごとではなく中身が根に散っていた**（形からはノート帳ぜんぶの zip と見分けられないため）── zip の中に「何の範囲か」の札を書くようにした | `crates/amber-core/src/api.rs ~ 範囲の四つは_それぞれ違うものを包む` |
| 251 | 2026-09-06 | バックアップから戻す口が窓に無い | バックアップは取れるのに戻せない、という片道になっていた（電話には両方ある）。エンジンの `restore` は前からあり、窓が呼んでいなかっただけ | `gui/renderer.js ~ async function cmdRestore` |
| 252 | 2026-09-06 | 外のファイルが反映されるようにしてほしい | **同じフォルダを二つの端末で触るのがこのアプリの前提**なのに、外から書き換えたものは窓を開き直すまで出てこなかった ── iPhone で書いた一行が Mac に現れず、「同期していない」ように見える（同期はしていて、見ていなかっただけ）。どのファイルがどう変わったかは読み解かない ── **「何か動いた」だけを伝えて、数え直すのは描く側**。打っている途中のノートは触らない（いま書いているものを向こうの版で黙って置き換えるのが一番悪い） | `gui/main.js ~ function watch` |
| 253 | 2026-09-06 | iPhone の置き場所も窓と同じく一つに | 電話には「開いてきた場所」が八つ並んでいて、**二つの amber で「いまどこに書いているか」の答えが違う形**をしていた。探すのが大変なのは変わらないが、それは選ぶ画面の話で、選んだあとに八つ並べておく話ではない。戻る道（この iPhone の中に戻す）だけ、いまの場所の隣に残した | `ios/Cian/Where.swift ~ 置き場所は一つ` |
| 254 | 2026-09-06 | ノートに取り消し／やり直しを。上の帯に印を | **Monaco の積み木には任せられない。** 表を入れる・図を入れる・升を押す、はどれも `editor.setValue()` で組み直しており、`setValue` は Monaco の積み木を**まるごと捨てる** ── 押した直後に ⌘Z を押しても、そこには何も積まれていない。ノートの字そのものを積んで一本化した。積むのは**保存のたび**（打鍵の 0.9 秒後）で、一続きに打っているあいだは一つにまとまる ── 一文字ずつ積むと、消した一段落を戻すのに何十回押すことになる。**捕捉の段で受ける**（泡の段では、Monaco が自分の textarea で止めることがある）。別のノートを開いたら捨てる ── 別のノートの姿を戻せると、一度の押し間違いで二本まとめて壊れる | `gui/renderer.js ~ async function stepBack` |
| 255 | 2026-09-06 | フォルダの左に、ノートやタグと違うと分かる印を | 色を付けたフォルダにだけ小さな四角が出ていて、**色の無いフォルダとブックマークの棚は字の形しか違わなかった** ── 列は上から下へ読むので、段の見出しを覚えていないと「いま何の一覧を見ているか」が分からない。四つとも形を持たせた（受け皿・星・フォルダ・井桁）。**色は形の上に載せる** ── 色だけで分けると、色の見分けにくい人には何も分からない。名前から「★」「#」は外した（形のほうが言っている） | `gui/renderer.js ~ const RAIL_MARKS` |
| 256 | 2026-09-06 | ノートの履歴。何月何日の何時の姿に戻したい | **自動保存のアプリに履歴が要る理由は、自動保存だから** ── 消したことに気づいたときには、消えたほうが保存されている。取り消し（依頼 254）は窓を閉じれば消えるので、日をまたぐ後悔には届かない。**一世代は「書いていた一区切り」**（最後の打鍵から五分）── 保存のたびに残すと十分書けば数十世代になり、五十世代が一回の執筆で埋まる。置き場所は `.amber/history/`、ノートの隣のただのファイルなので**同期先にもそのまま運ばれる**（Inkdrop は最新の版しか同期しないと言っている）。消し方も**数えて言い切る** ── 新しい五十、または三十日以内（大きいほう）。ノートでもフォルダでも右押しから見られ、フォルダは中のノートの姿をまとめて時系列で出す。**戻す前にいまの姿を一世代残す** ── これが無いと「戻す」は取り返しのつかない操作になり、押すのが怖くなる | `crates/amber-core/src/history.rs ~ pub fn keep` |
| 257 | 2026-09-06 | 自動保存だと世代がすぐ埋まる。⌘S のような明示保存は要るか | **保存としては要らない。** 保存していないノートは窓の中にしか無いノートで、そこで iPhone を持ち上げると書いたものが無い ── 二つの端末で同じフォルダを触る、というこのアプリの前提が崩れる。升を押せばその場で書くなど、他のどこにも保存の概念が無い。**それでも人は反射で ⌘S を押す**ので、意味のある返事を用意した ── ⌘S は「ここを残す」。印の付いた姿は五十世代・三十日の勘定から外れ、古くなっても消えない。自動保存では作れない、人にしか分からない区切り | `gui/renderer.js ~ async function cmdKeepNow` |
| 258 | 2026-09-06 | iPhone にも図の工房を | **窓と同じ解析器を使う。** 八種類の形をもう一組 Swift で書けば答えがもう一組でき、食い違うのはマインドマップの深さや予定表の日付という**いちばん確かめにくいところ**になる。`gui/renderer.js` の読み書きを切り出して `JavaScriptCore` で走らせた ── `scripts/diagram-test.js` が同じ切り出し方で往復を見ているので、電話が使うのは**試験の通ったものそのもの**。WebView ではなく `JSContext` なのは、これがただの関数だから。窓は左に表・右に図だが 402pt にそれは入らないので、**上に図・下に表**。長押しで開く（電話に右押しは無い） | `ios/Cian/Studio.swift ~ enum Mmd` |
| 259 | 2026-09-06 | iPhone でも履歴を見て、戻したい | ノートの長押しから。フォルダの長押しなら中のノートの姿がまとめて出る。**決まりは core が持っている** ── いつ一世代にするか、何世代残すかが窓と違うと、片方の決まりで消したものをもう片方が残っていると思う。電話は並べて、見せて、戻すだけ。**見てから決める**（名前と日付だけで戻すかを決めさせない）。戻す前にいまの姿を一世代残す | `ios/Cian/Past.swift ~ struct Past` |
| 260 | 2026-09-06 | 題なしで作ると、上に日付の入ったノートが起動する | 依頼 174 で前書きから追い出した「書いた人が一度も言っていない名前」が、**ファイル名から戻ってきていた** ── 題が無いときの最後の受け皿がファイル名で、そのファイル名は amber が付けた `2026-09-06 13-07-22` だった。amber が付けた符号だけを見分けて題にしない（人が名づけた `買うもの.md` は、中身が空でも題でいい）。画面は **（タイトルなし）**、一行書けばそれが題 | `crates/amber-core/src/note.rs ~ pub fn made_up_name` |
| 261 | 2026-09-06 | iPhone の初期表示を窓と同じ「表示」に。表示のままでは何もできない | 作ったノートも「表示」で開くようにした。**「電話に `contenteditable` は無いので、一叩きで書く面に入る」で代えた ── その日のうちに間違いと分かった。** 無いのではなく、`WKWebView` の中でならある。逃げ道を作る前に、本当に無いのかを確かめるべきだった（依頼 266 で、窓の面そのものを電話で動かすようにした）。この行が残しているのは「作ったノートも表示で開く」の側 | `ios/Cian/ContentView.swift ~ 作ったノートも「表示」で開く` |
| 262 | 2026-09-06 | iPhone にブックマークとタグの段が初期表示で無い。フォルダ＋を釦一つで | **一つも無くても段は出す**（窓と同じ）── 無いときに段ごと消えると、最初の一つを作る道がどこにも無くなり、「実装されていないのか、まだ無いだけなのか」が使う人には見分けられない。何をすれば増えるかも、その場に書いた。フォルダを作るのは献立の中に畳んでいたので、押しはじめる前に「どこにあるか」を探すことになっていた ── 釦一つに | `ios/Cian/ContentView.swift ~ 一つも無くても段は出す` |
| 263 | 2026-09-06 | 窓のフォルダの色が線にしか付かず、分かりづらい | 15px の輪郭の色を見分けることになって、「色を付けた」と気づけなかった（電話は前から塗っている）。**塗る** ── 離れて見ても色で拾える | `gui/renderer.js ~ 色を付けたフォルダは、塗る` |
| 264 | 2026-09-06 | 窓にもベルマークを。通知は画面上部から | 通知は「このノートに」するものなのに、献立の奥にあった ── **仕掛けたことも、仕掛かっていることも見えない**。歯車の左に置き、仕掛けてあるノートでは鐘が琥珀になる（開くたびに献立を開いて確かめなくていい） | `gui/renderer.js ~ const BELL_ICON` |
| 265 | 2026-09-06 | 見本のノートを、いまの Mac と iPhone にも入れたい | 自動で置くのは**まだ一本も無いときの一度きり**（既にノートがある人のフォルダに三枚落とすのは、ただの散らかし）。それでも「入れてくれ」と言える場所が要るので、窓は歯車に、電話は設定に置いた。同じ名前は飛ばすので、二度押しても増えない | `gui/renderer.js ~ async function cmdWelcome` |
| 266 | 2026-09-06 | iPhone でも「表示」で打ちたい | **窓の面そのものを電話で動かす。** 窓の「表示」は `<div contenteditable>` で、そこに直接打てる ── 電話だけ読むだけだと、**同じ名前の面が二つの amber で別のもの**になり、打てると思って叩いた人は何も起きない画面を前に「壊れている」と思う。SwiftUI で書き直さない: 組む側は core の `to_html`、書き戻す側は `gui/renderer.js` から切り出した一組（`richBlock` … `inlineToMd`）── **書き戻しをもう一組 Swift で書けば、同じノートが端末によって別の字に保存される**。失うのはたいてい表と升と図で、気づくのは何回か保存したあと。そのために書き戻しを箱と字だけ見る形に直し、`scripts/paper-test.js` が往復を見ている。絵と図は `amber://` で Swift が給仕する（`WKWebView` は文字列から作った頁に隣のファイルを読ませない）── ついでに**ノートの外は配らない**ことがそこで保証できる | `ios/Cian/Paper.swift ~ struct Paper` |
| 267 | 2026-09-06 | 「表示」の面の書き戻しを、二つ持たない | 窓と電話が同じ一組を使うので、**片方だけ直った日に同じノートが別の字で保存される**ことが起きない。切り出せるように、`armPaper` と `paperToMd` は箱と字だけを受け取る形にした（画面のどこにも触らない）。往復は `jsdom` の上で見る ── 升を読んでも壊さないこと、図の元の字を持っていないときは**書き戻さない**ことまで | `scripts/paper-test.js ~ 打っても字を失わないか` |
| 268 | 2026-09-06 | フォルダの色をカタカナ十一色に。グリーンはくすんでいる | 名前を**シアン・ターコイズ・ブルー・バイオレット・パープル・ベルガモット・カーマイン・アンバー・マスタード・グリーン・グレー**に（「みどり青」と「青むらさき」は、二つ並べたときにどちらがどちらか言えなかった）。グリーンは `#5E8C42` のオリーブ寄りで、緑というよりくすんだ黄土に見えていた ── `#3FA05C` に。**並びは core が持つ** ── 窓と電話に同じ表を書いていて、両方のコメントに「同じ並び」と書いてあったのに**十一色のうち六色がずれていた**（電話で付けた青が Mac では少し違う青で出ていた）。写しを持てば、いつかずれる | `crates/amber-core/src/notebook.rs ~ pub const PALETTE` |
| 269 | 2026-09-06 | iPhone のフォルダ＋と「フォルダ」が同じ意味。統合して、既存から選ばせて | 印と札が並んでいて、同じことを二度置いていた。押すと**フォルダの構成**が出て、そこで選ぶ／作る ── 名前をいきなり打たせない（たいていは既にあるものへ行きたい）。「フィルタ」からは新しいフォルダとフォルダの構成を外した（フィルタは絞る話で、作る話ではない） | `ios/Cian/ContentView.swift ~ フォルダのことは、この印一つに` |
| 270 | 2026-09-06 | フィルタの右の余白を触ると、フォルダ作成の小窓が出る | 一覧の段は、**中に釦があっても段ごと押せる** ── 右の余白を触ると最初の釦が鳴っていた。段そのものに当たり判定を敷いて、押しをそこで止める | `ios/Cian/ContentView.swift ~ 右の余白は、何でもない場所` |
| 271 | 2026-09-06 | 一覧から右→左のフリックで、開いていたノートへ | 一覧から左へ払うのはノートを開くときと同じ向きの手ぶりなので、**待っているノートのほうを先に見る**（フォルダを一つ潜っただけの「進む」より） | `ios/Cian/ContentView.swift ~ 右から左は「開いていたノートへ戻る」` |
| 272 | 2026-09-06 | 見本のノートに絵の例を。フォルダもタグも作らないで | 入れた瞬間にタグの一覧とフォルダの一覧が汚れるのが嫌だった ── **前書きごと外して、直下に三枚**。絵は amber の印そのもの（`attachments/amber.png`）── 「隣の `attachments/` に置かれ、本文にはリンクだけが入る」を、実物で見せる。電話の設定に見本の入口が**二つ**出ていたのも消した | `packaging/welcome/amber へようこそ.md ~ attachments/amber.png` |
| 273 | 2026-09-06 | 「前の姿（履歴）」は日本語として変。過去バージョンに | 「姿」はこちらの言い方で、使う人の言葉ではなかった ── **過去バージョン**、**現状バージョン保存**。「ノートのタグ設定」も、ノートの献立に出ているのだからノートは自明 ── **タグ設定** | `gui/renderer.js ~ name: '過去バージョン'` |
| 274 | 2026-09-06 | はい／いいえの小窓に、謎の入力欄が出る | 小窓は一枚しか無く（依頼どおり）、字を打つ欄が常に出ていた ── 二択の返事に欄があると「何か打つものがある」に見え、タグを外すだけの返事で人が止まる。**欄は残して、見せない** ── 上下と Enter を受けているのはそこなので、消すと鍵盤で選べなくなる | `gui/index.html ~ #sheet.bare input` |
| 275 | 2026-09-06 | 窓の鐘・戻す・やり直す・歯車が小さくて見にくい | 15px の線画を 27px の枠に置いていて、鐘か矢印かを見分けるのに目を寄せることになっていた ── 19px に、枠も歯車と同じ背丈に | `gui/index.html ~ 小さすぎた` |
| 276 | 2026-09-06 | 歯車から「フィルタ」は要らない | 絞るのは一覧の頭がやること ── **歯車は amber の設定で、「いま何を見るか」は設定ではない**。2026-09-06 に引き出しへ移したので、いまは命令の表からも外した（三つが一覧の頭に常に出ているものを、命令からも呼べると道が二つになる）── こよみだけは命令として残す | `gui/renderer.js ~ \*\*絞り込みは、命令ではなくなった。\*\*` |
| 277 | 2026-09-06 | 窓のチェックリストを押すと、改行されて表示される | 箇条書きは DOM を直に触るので caret が残り、チェックリストは字を組み直すので**次の行へ降りて**いた ── 同じ帯の隣り合う二つが違う振る舞いをしていた。「この行をそうする」道具は、その行の終わりに残る | `gui/renderer.js ~ landAfter\(at, stay\)` |
| 278 | 2026-09-06 | はい／いいえの小窓は一律おかしい（ゴミ箱・見本・タグ外し） | 小窓は一枚しか無く、字を打つ欄が常に出ていた。**四つまでのときは見せない** ── 四つを絞り込む人はいないし、二択に欄があると「何か打つものがある」に見えて人が止まる | `gui/renderer.js ~ 四つまでのときは、字を打つ欄を見せない` |
| 279 | 2026-09-06 | iPhone のノート長押しに「削除する」が無い | 消し方は横払いしか無く、**長押しの献立に無い＝消せない**と読める（実際にそう読まれた）。窓の献立にも入っているもの。取り返しがつかないので、必ず訊く | `ios/Cian/ContentView.swift ~ 長押しからも消せる` |
| 280 | 2026-09-06 | iPhone の表示モードで、升の行で改行すると箇条書きになる | `contenteditable` の既定は「同じ種類の次の行」だが、**升は付いてこない**ので `- ` だけの行が生まれる ── 押した人は升を足したつもりで、出てきたのは点。まず「何も無い行に降りる」にした。**その「何も無い行」は 2026-09-06 に本人が取り下げた**（依頼 285）── 点も番号も「次も同じ、空なら降りる」なのに升だけ違い、**やることを続けて三つ書けなかった**。取り下げなので、張り直しではなく**約束そのものが変わっている**。判断は窓と電話で一つ（`checkEnter`） | `gui/renderer.js ~ 升の行で改行したら、次も升` |
| 281 | 2026-09-06 | iPhone の表示モードにも入力サポートの帯を | 打てる面なのに記号の入れ方が無いと、`#` や `- [ ]` を覚えている人にしか使えない ── 電話の鍵盤にその記号は出ていない。**書く面より少なく出す** ── `execCommand` で確かにできるものだけ（押しても何も起きない釦は、あることより悪い）。升だけは `execCommand` に作れないので、箇条書きにしてから升を自分で置く | `ios/Cian/Writing.swift ~ 道具の帯は、表示の面にも要る` |
| 282 | 2026-09-06 | 表の見本に、項目の列にも中身を入れて | 空の升は「これで合っているのか」が分からず、打つ前に一度立ち止まる ── 見本の字（`項目`）が入っていれば、上から順に置き換えるだけになる | `gui/renderer.js ~ 升を空で出さない` |
| 283 | 2026-09-06 | 太字・斜体などのショートカットを Inkdrop から | **⌘K リンク・⌘' 引用・⌘1〜4 見出し**（Inkdrop が持っているもの）に揃え、表に ⌘⇧T を足した。書いていて分かったのは、**鍵盤からの一打が読む面で効いていなかった**こと ── 一打の表が帯とは別にもう一組あり、そちらは書く面用しか持っていなかった（⌘B が帯からは効いて一打からは効かない、という見分けの付かない差）。**表は一つ（`MARKS`）**にして、帯に書いた鍵をそのまま引く。見出しの深さは鍵盤からは一打で、帯の釦は押すたびに深くなるまま（Inkdrop も `toggle-heading-1` … `-4` を別々に持っている） | `gui/renderer.js ~ 表は一つ（\`MARKS\`）` |
| 284 | 2026-09-06 | 表の中で Tab を押したら次の升へ | 表を打つのは「升から升へ」で、行を打つのではない ── 既定の Tab は焦点を面ごと出してしまい、打っている途中で表から追い出される。最後の升で押したら**行を一つ足す**（足し方を探しに行かせない）。caret の居場所は字の節のことも升そのもののこともあるので、両方見る | `gui/renderer.js ~ 表の中の Tab は、次の升へ` |
| 285 | 2026-09-06 | 窓もiPhoneも、升の行で改行したら次も升に（空でもう一度押したら何も無い行） | 点も番号も `contenteditable` の既定がそうしている ── 押せば次の行が同じ形で出て、空でもう一度押すと降りる。**升だけが違っていた**。揃えるのは形ではなく**押し心地**。判断は切り出し（`checkEnter`）に置き、窓も電話も同じ関数を呼ぶ ── ここで別々に書けば、同じノートを窓で足すと升・電話で足すと点、になる | `gui/renderer.js ~ 升の行で改行したら、次も升` |
| 286 | 2026-09-06 | ショートカットはどうやって確認したらいい（マークダウンのぶんも） | 鍵は帯の吹き出しと ⋯ の献立に散らばっていて、**全部を一度に見る場所が無かった**。⌘⇧/ で一枚に出す ── 記号・窓のこと・一覧の中、の三段。選べばその場で走る（読むだけの紙にすると、見ながら手で打ち直すことになる）。ついでに、献立が `⌘⇧/` と書きながら**受け口がどこにも無く**、押すと左の列が畳まれていたのも直した | `gui/renderer.js ~ ショートカットの一覧（⌘⇧/）` |
| 287 | 2026-09-06 | ノートを選んで Delete でゴミ箱の小窓を | 一覧を見ているときの Delete は、机の上ではどのアプリでも「これを消す」── 受けていないと、右押しか ⋯ を開くしかない。**必ず訊いてから**（取り返しがつかない）。打っている場所では上で戻しているので、ここに来るのは一覧を見ているときだけ | `gui/renderer.js ~ 消すのは、必ず訊いてから` |
| 288 | 2026-09-06 | 下の道具箱に絵かアイコンを | 太字と斜体は Office でも同じ形をしていて、絵のほうが先に読める。**名前は消さない** ── 絵だけで当てられるのは太字と斜体くらいで、「注記」や「フロー」は名前が答えになる。絵は入口、名前は答え合わせ | `gui/renderer.js ~ 道具の絵。\*\*名前は消さない。\*\*` |
| 289 | 2026-09-06 | ノートを打っているとき、一覧や左の列は一度押しても移らない（二度押しで移る） | `click` は押し下げと離しが同じ節に当たって初めて出る ── 焦点が外れたことで保存が走り、一覧が組み直され、離す頃には押した行がもう別の節になっていた。**押し下げで開く**（机の上の一覧のふつうでもある）。焦点は移さず、開く前に読む面の打ち跡を先に字へ戻す ── 戻さないと最後の数百ミリ秒ぶんが黙って消える | `gui/renderer.js ~ \*\*押し下げで開く。\*\*` |
| 290 | 2026-09-06 | 上の ⋯ の「通知設定」はベルと重複しているので消して | 同じことを頼む道が二つあると、片方を直した日にもう片方が古いまま残る。ベルは常に上の帯に居て、押せば同じ小窓が出る。命令の表には残す（⌘⇧P から名前で探せる） | `gui/renderer.js ~ \*\*献立には出さない。\*\* 上の帯にベルが居て` |
| 291 | 2026-09-06 | ブックマークの置き場所が削除できない | 置き場所は空でも残るように core が憶えている（`notebook::add_star`）のに、窓は `drop` を一度も呼んでいなかった ── ノートから外すだけでは中身の無い棚が並び続け、**消せないもの**になっていた。消したのは棚であってしおりではないので、中のノートはブックマークの直下へ移す。名前を変えるときも、下の階層ごと付け替えてから忘れる | `gui/renderer.js ~ \*\*棚そのものも忘れる。\*\*` |
| 292 | 2026-09-06 | 左の列のタグの「#」が全角みたいで格好悪い | 16px の枠に四本線を渡すと線が詰まって、字の「＃」が太って見えるだけになる ── 隣のフォルダも星も形で分かるのに、タグだけ記号を読ませていた。**札の形**にする。段の見出しの「＋」も同じ理由で線に直した（全角の記号は行の高さも幅も字に引かれて、一つだけ大きく沈む） | `gui/renderer.js ~ \*\*札の形。\*\*` |
| 293 | 2026-09-06 | フィルタのタグで絞るとき、字を打つ欄は要らない | ここは押して選ぶ一覧で、打つところではない ── 欄があると「何か打つものがある」に見えて選ぶ手が止まる（はい／いいえの小窓で同じことが起きた、依頼 278）。同じ日に小窓ごと引き出しへ移した（依頼 294）ので、**打つところは初めから無い** | `gui/renderer.js ~ \*\*絞れることが、絞る前から見えている。\*\*` |
| 294 | 2026-09-06 | フィルタは案三（一段の帯）で。タグ・フォルダ・期間を重ねられるように | 三案のモックから本人が選んだ。**絞れることが、絞る前から見えている** ── 「フィルタ」という言葉を覚えなくてよい。三つの引き出しが一覧の頭に常に並び、いくつでも重なる（タグは全部・フォルダはどれか ── ノートは一つのフォルダにしか居ないので、フォルダを「全部」にすると二つ選んだ瞬間に必ず 0 件になる。どちらなのかは引き出しに書く）。選んだものは `tag:仕事` という字にせず、選んだものとして持つ。帯の札には**何で絞っているかを出す**（一つなら名前、二つ以上なら数）── 「タグ 1」では開かないと分からない | `gui/renderer.js ~ \*\*絞れることが、絞る前から見えている。\*\*` |
| 295 | 2026-09-06 | 毎回ぜんぶ出ているとボリュームが大きい。「ノートを探す」を押したら入力欄が出る形に | 帯と探す欄を並べて置きっぱなしにすると、一覧の頭が毎回二段ぶん要る ── **言葉で探すのは、絞るより回数が少ない**。畳んでおいて、釦（と ⌘F・`/`）で開く。閉じるときは**必ず空にする** ── 見えない絞り込みが残ると、一覧が減っている理由が画面のどこにも書いていないことになる。空のときの言い方も「見つかりません」から「絞り込みに当たるノートがありません」に分けた（外せば出てくることに気づけるように） | `gui/renderer.js ~ \*\*探す欄は畳んでおく。\*\*` |
| 296 | 2026-09-06 | 日付はカレンダーから、いつからいつまでのレンジで探したい | 「7日以内」のような決め打ちは、**去年の秋**を探せない。こよみを出して、押した日が範囲の端になる ── 次に押した日がどちらに入るかを先に見せ（「いつから」「いつまで」の札）、二度押しで範囲が決まる。片方だけでもよい（「この日から先ぜんぶ」が言えないと範囲は使いものにならない）。前後が入れ替わったら黙って入れ替える（始まりを言い直しているのがふつうで、0 件を返して考えさせる場面ではない）。前月・翌月のはみ出しも押せる ── 月末をまたぐ範囲はよくある。比べるのは秒ではなく `YYYY-MM-DD` の字：秒で比べると「6日まで」が 6日 0時 までになり、その日に書いたものが軒並み落ちる | `gui/renderer.js ~ \*\*いつからいつまでを、押して決める。\*\*` |
| 297 | 2026-09-06 | 新しいノートの題が、一行目を打っても（タイトルなし）のまま | 題は一行目から決まる（`note::title`）ので、一覧の二列目はすぐそう出ていた ── 帯だけが開いたときの値で止まっていて、**同じノートの名前が画面の二か所で食い違って**見えていた。読み直しのたびに書き直す | `gui/renderer.js ~ 開いたときだけでなく、保存のたびに書き直す` |
| 298 | 2026-09-06 | 「保存しました」が出ると、取り消し・やり直しを連続して押せない | 字数と状態が中身の長さぶんの幅を取っていた ── 字が出た瞬間に帯じゅうが左へずれ、いま押した釦がカーソルの下から逃げる。**一覧の行が一度で開けなかったのと同じ「押した先が動く」不具合**（依頼 289）。幅の決まった枠に住まわせれば、中の字が変わっても外は動かない。1180px の既定の窓では帯が一杯で枠が 0 になったので、名無しの ☰（目次）と ⤢（拡大）を「ノート ▾」へ名前付きで移した ── 記号二つより、名前のほうが見つかる | `gui/index.html ~ \*\*字数と状態は、幅の決まった枠に住む。\*\*` |
| 299 | 2026-09-06 | 太字などで飾ったあと、続けて打つ字まで飾られる | `execCommand` は選び目を `<b>` の中に残すので、そこから打てば中に入る ── **飾ったのは選んだ字であって、これから打つ字ではない**。飾りの終わりに立っているときだけ外へ出す（途中なら、そこは中の字）。選び目を動かすだけでは足りない ── ブラウザは「いま打つと何になるか」を別に憶えていて、飾りの隣で打った字をその飾りへ吸い込む。憶えているほうも消す。かな漢字は組み始めに出す（組んでいる最中に動かすと変換が壊れる） | `gui/renderer.js ~ 飾りの終わりに caret があるなら、その外へ出す` |
| 300 | 2026-09-06 | 引用の中で、何も書かずに Enter を押したら引用から抜けたい | 中で改行すると引用が続くのは既定のとおりで正しい ── けれど**出る道が無かった**。点も番号も升も「空でもう一度押したら降りる」のだから、引用と注記だけ抜けられないのは覚え違いに見える。真ん中で押しても後ろの行は失わない（そこで箱を割り、注記なら種類の札を割った先にも付け直す） | `gui/renderer.js ~ 空の行に降りたら\*\*そこから出る\*\*` |
| 301 | 2026-09-06 | 注記が「表示」モードで直せなかった | 実は打てていた ── **打てないと言っていたのは見た目のほう**。注記と表は触れるようになったのに、指の形（`cursor: pointer`）と「押すと書く面へ飛ぶ」枠だけが**触れないかたまりのまま残っていた**。押すと別の面へ行く印が出ているところを、打てると思う人はいない。顔ぶれは `richBlock()` と揃える | `gui/index.html ~ \*\*注記と表は、ここから外した。\*\*` |
| 302 | 2026-09-06 | 電話版は窓版を正として、機能を揃えてほしい（⋯ の献立） | 電話の ⋯ には「タグ」しか無く、ブックマークもフォルダ移動も履歴も削除も**一覧まで戻って長押し**するしかなかった ── 開いているノートに対してすることなのに、開いている画面からは頼めない。窓の「ノート ▾」と同じ顔ぶれ・同じ順に並べる（ブックマーク・タグ設定・フォルダへ移動・エクスポート・目次・過去バージョン・現状バージョン保存・ゴミ箱） | `ios/Cian/Desk.swift ~ \*\*⋯ の顔ぶれは、窓の「ノート ▾」と同じ。\*\*` |
| 303 | 2026-09-06 | 一つ戻す・やり直すを電話にも（置き場所は電話の都合で決める） | UIKit の取り消しは「打った字」の取り消しで、見出しや升のように面を組み直したところで**積み木ごと消える**うえ、「表示」の面（`WKWebView`）には届かない。窓と同じくノートの姿を積む一本にした。**置き場所は窓と変えた** ── 窓の通り上の帯に六つ並べたら iOS が黙って二つ落とし、消えたのは「表示／コード」とベルだった（落ちたことはどこにも出ない）。下の帯なら幅も足り、**電話の親指は下に居る** | `ios/Cian/Desk.swift ~ \*\*一つ戻す／やり直すは、上の帯ではなく下の帯に。\*\*` |
| 304 | 2026-09-06 | 目次を電話にも | 何が見出しかは core が決める（`note::blocks`）── 電話が `#` を数えはじめると `#仕事` というタグの行が目次に出る。窓は右に居座る列だが、**電話は畳む** ── 393pt の幅で常に一列取るとノートそのものが半分になる。飛び先は desk が持ち、飛ぶのは面それぞれ（「表示」は `scrollIntoView`、「コード」は行頭へ caret）。前書きのぶんを引くのを忘れると、前書きのあるノートでだけずれる | `ios/Cian/Touring.swift ~ 電話は畳む` |
| 305 | 2026-09-06 | 現状バージョン保存を電話にも | 自動保存だと世代が打鍵の切れ目で決まる ── 「ここは残しておきたい」を人が言える道が要る。窓の ⌘S と同じ `keep`（間を置かず・印を付けて）で、印の付いた世代は数の勘定から外れるのであとから流れて消えない | `ios/Cian/NotesStore.swift ~ \*\*いまの姿を、一世代として残す。\*\*` |
| 306 | 2026-09-06 | マークダウンの書き方と amber について を電話の設定にも | 窓の歯車にあって電話に無かった二つ。記号を知らない人がいちばん先に困るのがここで、電話には帯の釦しか道が無かった。電話では**入れる**のではなく**見せる**だけにした ── 設定の奥から辿ってくる頃には、打っていた場所を離れている（記号そのものは下の帯から入る） | `ios/Cian/Syntax.swift ~ 電話では\*\*入れる\*\*のではなく\*\*見せる\*\*だけ` |
| 307 | 2026-09-06 | 絞り込み（タグ・フォルダ・期間）を電話にも。窓と同じ一段の帯で | 三案から本人が選んだ形（依頼 294）を電話にも。一覧の頭に三つの引き出しが常に並び、いくつでも重なる ── **絞れることが、絞る前から見えている**。タグは全部・フォルダはどれか、期間はこよみで範囲（窓と同じ判断・同じ言い方）。**タグの段は畳んだ** ── 引き出しと段で同じことを頼む道が二つになるうえ、絞った瞬間に段が消えて一覧じゅうが上へずれ、次に押した指が別の行に当たった（窓の「押した先が動く」と同じ） | `ios/Cian/Sifting.swift ~ \*\*窓と同じ形\*\*` |
| 308 | 2026-09-06 | 「ノートを探す」欄も、窓に合わせて畳む | 帯と並べて置きっぱなしにすると、一覧の頭が毎回二段ぶん要る ── 言葉で探すのは、絞るより回数が少ない。`isPresented` で釦から開く（`displayMode: .always` は開いているかに関わらず場所を取り続けるので畳めない）。閉じたら空にする ── 見えない絞り込みを残さない | `ios/Cian/ContentView.swift ~ \*\*畳んでおく。\*\* 絞り込みの帯と並べて` |
| 309 | 2026-09-06 | 窓にあって電話に要らないものを決めた（並べて表示・拡大・前後のノート・字数） | 「まだ作っていない」と「作らないことにした」は画面の上では同じ顔をするので、**どちらなのかを書いておく**。393pt では二面を並べても両方読めない／電話はもともと全画面／左フリックと開いたノートの帯があるのでたどった道は要らない／帯は四つで既に一杯（六つ並べたら iOS が黙って二つ落とした） | `ios/Cian/Desk.swift ~ \*\*電話に置かないと決めたもの\*\*` |
| 310 | 2026-09-06 | フローの各オブジェクトの色を、右クリックから変えたい | **箱そのものを右押しすると色の献立**（フォルダの色と同じ手ぶり・同じ十一色 ── 覚える色の名前を画面ごとに増やさない）。図の余白を右押しすれば、これまでどおり工房が開く。紙の上では**淡く塗って濃い線で囲む** ── 濃いまま塗ると中の字が沈み、読ませるには「どの色なら白い字か」を人が考えることになる。淡い地なら字はいつも同じ濃い墨でいい。工房の表にも「色」の列を足した（献立の名前だけでは色が分からないので、選び口そのものをその色で塗る） | `gui/renderer.js ~ 箱そのものを右押しして色を変える` |
| 311 | 2026-09-06 | その色が、ノートの字として往復すること | `style A fill:…,stroke:…,color:…` の一行で持つ ── GitHub でも同じ色で出る。**こちらが書いた形だけ読み戻す**（`NODE_STYLE`）── 手で書いた凝った `style`（線の太さや破線）を色だけの表に押し込むと、書き戻したときに残りが消える。読めない形なら表にせず字の面で直す。色は箱に付けて持つので、箱を消せばその色の行も消える（消した箱を指す線と同じ扱い）。読み書きは切り出しの中なので、電話も同じ字を書く | `scripts/diagram-test.js ~ 往復しても同じ字` |
| 312 | 2026-09-06 | 箱の色を、電話の工房からも変えられること | 窓は右押しと工房の両方から変えられる ── 片方だけだと、電話で直したノートから色が変えられないことに気づけない（「電話版は窓版を正として」の続き、依頼 302）。献立の丸は `Colouring.dot` で描く ── `systemImage:` は accent で塗られて**十一個とも同じ色**になる | `ios/Cian/Studio.swift ~ \*\*箱の色も、電話から。\*\*` |

## 増やすとき

依頼を受けて何かを入れたら、**その場でここに1行足す**。あとでまとめてやると、
まとめる前に自分で消す。番号は cian の台帳と続きにしてある ── 同じ日に
両方へ足したとき、どちらの話か番号で分かる。

**検査を足したら必ず変異テストする**（`scripts/mutate.sh FILE FROM TO`）。
壊した版で鳴るのを見てから戻す。「指摘が消えた」と「検査が黙った」は
画面の上では同じ顔をしている。
