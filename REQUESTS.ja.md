# amber に頼まれたこと

Taketan が頼んだことが、いまも満たされているか。`python3 scripts/requests.py`
が機械で確かめる。

**これは lint ではなく契約。** 落ちたときの答えは「正規表現を直す」ではなく
「頼まれたものを消してしまった ── 消す前に訊く」。実際に一度、頼まれて入れた
ものを後の設計パスで自分の美意識で消し、同じ指摘を二度させたことがある。

作り直して構わない。**ただし検査を通したまま**、あるいは検査ごと変えるなら
**先に訊いてから**。

**2026-09-05 に cian から分かれた。** 分けた日に、検査が amber のファイルを
指していた 77 行がこちらへ来た。それ以前の残りは
`~/workspace/cian/gui/REQUESTS.ja.md` にある。

**2026-09-16、OneNote に直接繋いでいた頃の 23 件を外した。**
[REQUESTS.withdrawn.ja.md](REQUESTS.withdrawn.ja.md) にある ── **本人が
「丸ごと外す」と決めた**（繋がらないまま諦めた道で、代わりに `.onepkg` を
直に読む道が通った）。**行は消さない**。あの日に何があったかの記録なので。
こちらの表と同じく `scripts/requests.py` は読まない ── 撤去したものに
当てになる検査は書けない。

**2026-09-06、cian の窓から amber モードが消えた。** そのモードに入っていた
19 件は [REQUESTS.moved.ja.md](REQUESTS.moved.ja.md) にある ── **こちらの表とは
別扱い**で、`scripts/requests.py` は読まない。amber の窓は書き直しなので、
まだ果たしていないものが混じっているから。果たしたら、検査の欄を書き換えて
この表へ移す。

| # | 日付 | 頼まれたこと | なぜ | 検査 |
|---|------|------------|------|------|
| 26 | 2026-09-02 | AIの会話枠はいらない。コマンド提案・ディレクトリ検索・ゴミ検索の品質と精度を1段2段あげたい | モデルに渡す事実を増やした ── **種類・サイズ（ディレクトリは配下の合計）・最終更新からの日数・パス**の4列。ゴミ検索は木を4階層下まで見る（前は1階層） | `crates/amber-core/src/survey.rs ~ pub fn survey` |
| 28 | 2026-09-02 | （同上）見た範囲を偽らない | 打ち切ったら**モデルにも人にも**言う。「N件入らなかった」ではなく「何階層目までは全部見た」 | `crates/amber-core/src/survey.rs ~ pub fn whole_to` |
| 43 | 2026-09-02 | 共有フォルダで複数人が同じものを書きたい（Inkdrop 相談の前提） | **保存前に、開いたときと同じファイルかを見る。**違えば書かずに訊く（差分を見る／別名／それでも上書き）。端末版は `:w` が断り `:w!` で通す。判定は `cian_core::stamp` の1か所 | `crates/amber-core/src/stamp.rs ~ pub fn changed\(` |
| 46 | 2026-09-02 | 指定したディレクトリでマークダウンのノートを管理したい（Inkdrop 相談。名前は **cian モード**） | **DB は作らない。**題は front matter の `title`、無ければ最初の見出し、無ければファイル名 ── その判断は `cian_core::note` の1か所にあり、窓版もいずれの iOS も同じ答えを見る（Electron は iPhone で動かないので、判断を窓に置くと二度書くことになる）。二行目に本文の頭。**ノート以外のファイルも並ぶ** ── ペインの一覧だから、リンク先の画像を隠すのは嘘になる | `crates/amber-core/src/note.rs ~ pub fn read\(` |
| 49 | 2026-09-02 | 自分のノートを書き足せるようにしたい | `:newnote` ── 題を訊いて、前置き（`title`／`created`／`tags`）だけのノートを作って開く。名前と体裁の判断は `cian_core::note`（iPhone へ運べる側）。ファイル名は**スラグにしない** ── 題は日本語のことが多く、ASCII に削ると `.md` だけが残る。消すのは OS が拒む文字だけ。`CON` は拡張子を付けても `CON`、末尾の点と空白は Explorer が黙って食う | `crates/amber-core/src/note.rs ~ pub fn new_note\(` |
| 50 | 2026-09-02 | タグでノートを絞れるようにしたい | 一覧は名前で絞る ── それはファイルには正しく、ノートには間違い。**タグはファイル名のどこにも無い**し、取り込んだページは `page-0012.md` の中に題を持っている。ペインが項目ごとに「探せる一行」を持ち（`Pane::search_text`）、その中身は `note::haystack`（題＋`#タグ`＋本文の頭）。**iPhone 側も同じ判断を使う**ので窓には置かない。到着時に marks・filter と一緒に捨てる ── 前のフォルダの言葉で次のフォルダを絞ることになる | `crates/amber-core/src/note.rs ~ pub fn haystack\(` |
| 51 | 2026-09-03 | iPhone でも使えるようにしたい（Mac と iPhone の連携が本命） | ノートの半分を C ABI の向こうへ。**扉は1つ**（`amber_call(method, params)` で JSON を渡して JSON が返る ── 窓がエンジンと交わしているのと同じ会話）。関数を増やすと Xcode の橋渡しヘッダを手で合わせ直すことになり、そこは何も検査できない。**判断は置かない** ── 題も抜粋もタグも命名も `cian_core::note` のまま。`trash` は任意機能にした（iPhone にゴミ箱の API は無い）。stamp は**秒に丸めない** ── 丸めた最初の版は保存のたびに「誰でもない相手」と競合していた | `crates/amber-ffi/src/lib.rs ~ pub unsafe extern "C" fn amber_call\(` |
| 52 | 2026-09-03 | iPhone のアプリを作り始めたい（Mac と同じノートを両方から） | SwiftUI ＋ `CianFFI.xcframework`。**Swift には何も判断を書かない** ── 題も抜粋もタグも `amber_call("notes")` の答えをそのまま使い、絞り込みも `search`（`note::haystack`）で当てるので、窓と同じ `#タグ` が同じノートを引く。置き場所は `UIDocumentPicker` で選んで security-scoped bookmark で覚える ── **iCloud も Google Drive も Dropbox も iOS では「ファイル」のプロバイダ**なので、これ1つで三者に届き、同期のコードを1行も書かない。`startAccessingSecurityScopedResource()` を忘れると、選べるのに読めない | `ios/Cian/Bridge.swift ~ static func call\(` |
| 53 | 2026-09-04 | iPhone でも写真をノートに入れたい（Mac の貼り付けと同じこと） | 置き場所と名前は `cian_core::note::attach` ── **窓が貼るスクリーンショットと電話が付ける写真が、同じ `attachments/` に同じ形の名前で落ちる。**別々に書けば、両方から書いたノートフォルダは「たまたま重なった2つのフォルダ」になる。拡張子は**バイトから見る**（撮影は PNG、写真は大抵 HEIC。取り違えると何でも開けないファイルが残る）。画像は**先にディスクへ、あとから本文へ** ── 逆順だと、届かないかもしれないファイルへのリンクが本文に残り、あとから「無い」のか「消された」のか区別できない | `crates/amber-core/src/note.rs ~ pub fn attach\(` |
| 54 | 2026-09-04 | （同上）ノートを消せるようにしたい | スワイプして「削除」の二段。**電話にゴミ箱は無い**ので `cian_core::DESKTOP` は false、`DeleteMode::Trash` は断る。消すのはノートだけで、`attachments/` の画像は残す ── 別のノートが同じ画像を指していることがある | `crates/amber-core/src/api.rs ~ "delete" =>` |
| 55 | 2026-09-04 | Inkdrop のようにノートが「読める形」で出てほしい | 見出し・強調・箇条書き・引用・コード・区切り線・**画像**を組んで出す。**何が見出しかは `cian_core::note::blocks`、どう見えるかは前端** ── 逆に切ると Markdown の解釈が電話の上に載り、テストが一行も届かなくなる。`#仕事` は見出しではない（空白の有無で決まる。誤ると タグ付きのノートが全部 32pt で開く）。閉じていない ``` は残り全部を飲む。前置きは出さない（題もタグも既に画面にある） | `crates/amber-core/src/note.rs ~ pub fn blocks\(` |
| 56 | 2026-09-04 | （Inkdrop 化）ノートの本文まで探せるようにしたい | 一覧が持っている一行（題＋タグ＋冒頭100字）は打つそばから絞れるが、**思い出すのはたいてい奥の一文**。`cian_core::search`（窓の `:grep` と同じ）で歩き、行そのものを返して抜粋の代わりに見せる。打鍵ごとに歩かない ── 250ms 待って、前の探索は取り消す（5文字で5回歩けば、捨てる4回のぶん電池が減る）。1ノート1行（同じノートが20行出るのは答えとして悪い） | `crates/amber-core/src/api.rs ~ "find" =>` |
| 57 | 2026-09-04 | ノートブックで絞りたい／並び順を変えたい（更新順・題順） | 絞りと並びは1つのメニュー（増えた一覧に対して訊くことは「どこの」と「どの順で」の2つだけ。ボタンを2つ置くと、ノートを作る側の場所が無くなる）。**絞っている間は題名がノートブック名**になる ── 絞ったのにフォルダ名のままだと「ノートが消えた一覧」に見える。題順は `localizedStandardCompare`（素の比較だと「あ」「い」も note-2/note-10 も並びを間違える） | `ios/Cian/NotesStore.swift ~ enum Order` |
| 58 | 2026-09-04 | タグを付けられるようにしたい | 前置きの `tags:` を書き換えて**本文として返す** ── 保存は普通の保存なので、**タグ付けも競合チェックを通る**。自分でファイルを書く実装にすると「ノートを書く道」が2本になり、2本目が誰かの段落を消す。フォルダにあるタグを候補に出す（「仕事」と「 仕事」は別のタグに見えないのに別物）。`#` は剥がす（人は見た目どおり `#` を打つ）。リスト形式の `tags:` は `- ` の行ごと差し替える | `crates/amber-core/src/note.rs ~ pub fn set_tags\(` |
| 59 | 2026-09-04 | ノートをピン留めしたい | 前置きの `pinned: true`。**外すときは行ごと消す** ── `pinned:` と値なしで残すと、次に読むものが「留まっている」と解釈する。`true`/`yes`/`1` の三通りを受ける（どれだったか誰も覚えていない）。並びは**安定** ── 留めたものの中では選んだ並びが保たれる（`pinned` だけで並べ替えると中でばらける） | `crates/amber-core/src/note.rs ~ pub fn set_field\(` |
| 60 | 2026-09-04 | ノートを別のノートブックへ移したい | **画像も連れて行く。**リンクは相対（`attachments/…`）なので、ノートだけ移すと全部切れる ── しかも切れたことは後で開くまで分からず、開いても「消したのか届かなかったのか」が区別できない。どれが「そのノートの」画像かは命名（`<題>-<時刻>.ext`）で分かる。**同名があれば移さない**（上書きしない）。画像を先に、ノートを最後に動かす ── 途中で失敗しても、ノートは元の場所で元の画像を指したまま | `crates/amber-core/src/note.rs ~ pub fn move_to\(` |
| 61 | 2026-09-04 | 読む／書くの切替は**一つのボタン**で（当時の言葉：「同じ絵柄で。開いた目・閉じた目」） | 目とえんぴつは**2つの別のボタン**に見える。**一つのボタンが絵を替える**なら、それが一つのスイッチだと分かる。**2026-09-08、同じ人が絵のほうを替えた** ── 目と目にバツは「隠す／見せる」に読め、ここで替わるのは組んだ姿と記号そのもので隠す話ではない（「めっちゃわかりにくいね」）。**ボタンが一つであることは変わっていない**ので、この依頼は生きている ── 検査は絵の名前ではなく、そこを見る（依頼 399） | `ios/Cian/Desk.swift ~ Image\(systemName: here\?.reading == true` |
| 62 | 2026-09-04 | 新規作成は 題 → タグ → 作成、そのあと書き込みモードで開く（ボタンは「作成」） | 題を打った瞬間に出来てしまうと、**何のノートか言う前に世に出る**。あとからタグを付けるには、出たばかりのノートに戻ることになる。シートにしたのは、アラートの入力欄は問いを1つしか持てないから。`navigationDestination` は **`NavigationStack` の中**に置く（外側に付けると、ノートは出来るのに何も開かない） | `ios/Cian/Making.swift ~ struct Making` |
| 63 | 2026-09-04 | 電話から打つとき、マークダウンの定型を選びたい | 入力欄の上の帯。**書式ツールバーではない** ── ファイルはマークダウンのままで、押すと「自分で打ったはずの文字」が入る。行頭の印（`#` `- ` `- [ ] ` `> `）は**押し直すと外れる**（間違えて押した瞬間に欲しくなる）。素の `TextEditor` はカーソル位置を渡さないので、対象は**最終行** ── 正しく直すには UIKit が要り、この帯に見合わない | `ios/Cian/Writing.swift ~ private var marks` |
| 64 | 2026-09-04 | フォルダのボタンが何をするか分からない。保存場所の選択・変更と、取り込み・書き出しに使いたい | いきなりファイル選択が開くのは**訊かれていない問いに答えている** ── 「ファイルを探す」に見えて、実は「ノートはここに住んでいる」の話。まず今どこかを示し、選び直す・iPhone の中に戻す・取り込む・書き出すを並べる。取り込みは**コピー**（元は残る。まだ cian に住まわせると決めていない人が最初に知りたいこと）。同名は連番にして**上書きしない** | `ios/Cian/Where.swift ~ struct Where` |
| 65 | 2026-09-04 | フォルダを作れるように。平らに溜めるのも、PC のように整理するのも、どちらも使い方 | 一覧の上にフォルダ、下にノート（ファイルマネージャが最初からやっている並び）。入る・上へ戻る。**両方の使い方を残す** ── メニューの「全部まとめて見る」で平らに戻る。フォルダの一覧は**エンジンの歩いたディレクトリから**取る、ノートから逆算しない ── 作ったばかりのフォルダは空で、ノートから作った一覧には出ず、**それは「作れていない」のと見分けがつかない**。新しいノートは**いま開いているフォルダの中**にできる（そうでないと整理は毎回あとからの作業になる）。名前はシートで訊く ── テキスト欄のあるアラートは自動検査から操作できず、**見たことのない画面を出すことになる** | `ios/Cian/Booking.swift ~ struct Booking` |
| 66 | 2026-09-04 | フォルダ作成が並べ替えメニューの中で見つけられない | 上のバーへ。**作ることは、並べ替えの設定ではない** | `ios/Cian/ContentView.swift ~ "folder.badge.plus"` |
| 67 | 2026-09-04 | 置き場所の画面に、どこにあるか（パス）を出してほしい | パンくずで出す。**「cian」だけでは「何という名前か」の答えで、訊かれているのは「どこにあるか」** ── 電話では同じ名前のフォルダが3つのクラウドに在りうる | `ios/Cian/NotesStore.swift ~ var trail` |
| 68 | 2026-09-04 | 「保存場所を選ぶ」を押しても何も起きない | 原因は2つ。**同じ View に `.fileImporter` が2つ**あった（片方しか効かず、負けた方のボタンは無反応）。そして**シートの中からファイル選択は開けない** ── 提示の上に提示を重ねることになり、黙って何も起きない。シートを先に閉じ、下の画面から開く | `ios/Cian/Where.swift ~ let choose:` |
| 69 | 2026-09-04 | 「出し入れ」ではなく「バックアップ」「インポート」に | 名前を変え、いまあるノートを上書きしないことを画面に書いた（同名は番号を付ける）。**2026-09-12（依頼 503）に「インポート」は窓と同じ「取り込み」に**（2026-09-12 に本人が「エクスポート／インポート」の対に統一）| `ios/Cian/Where.swift ~ バックアップとインポート` |
| 70 | 2026-09-04 | ピン留めすると何が起こるのか分からない | **「上に固定」という見出しの節に入れる。**黙って上へ跳ぶノートは、理由の見えない移動でしかない。**最上階では、どのフォルダに入っていても固定分を出す** ── ピンの意味は「探しに行かなくても手が届く」なので | `ios/Cian/NotesStore.swift ~ func pinnedHere\(` |
| 71 | 2026-09-04 | ノートを複数同時に開きたい。タブで分かる・切り替える・タブごとに閉じる・左右スワイプ | **本文は「机」が持つ、画面が持たない。**`TabView` はページを作っては捨てるので、ページに本文を置くと**保存前の段落がスワイプで消える**。開いているタブは `Desk` の状態で、エディタはそこへの窓。選択は**索引ではなくパス** ── 閉じると後ろの索引がずれ、選択が隣のノートを指し始める。閉じたら**左隣**へ（来た方向。遠くへ飛ぶと居場所を失う）。未保存は点で出す（閉じる判断をする場所に出す） | `ios/Cian/Desk.swift ~ final class Desk` |
| 72 | 2026-09-04 | ノートをドラッグ＆ドロップでフォルダへ。**中から外へ出す**のもやりたい | フォルダの行が受け口。外へ出す口は **`..` の行** ── cian のペインには最初から `..` があり、ずっと両方の意味だった（上へ行く／これを上へ置く）。掴んで運ばれるのは**パス**（どの操作もパスを取るし、ノートは2つのフォルダの中間には居られない）。落とした先の処理は長押しメニューと**同じ `store.move`** ＝ `note::move_to`（画像も連れて行く／同名は移さない）。指の部分だけは自動検査で駆動できていない | `ios/Cian/ContentView.swift ~ private func drop\(` |
| 73 | 2026-09-04 | 通知がほしい。`mm/dd hh:dd` で一度きり。毎週の定型 TODO（水曜 9 時に「週報」など）も | **判断は `cian-core` に、鳴らすのは OS に。**書式は本文の front matter（`remind:` と `repeat:`）＝**ノートがただのファイルであることを壊さない**ので、Mac の窓版も同じ文字列を読める。繰り返しは `due_since` が**留守の間に過ぎた分を数える**（月末 31 日は、その月の最終日へ寄せる。上限 32 個）。作られるのは**テンプレートの複製**で、テンプレート自身は残る（`carry_out`）。同じ日の分が既にあれば**作らずに既存を返す** ── 2 台が同時に追いついても 2 つにならない。iPhone は勝手にアプリを動かさないので、**時刻に届くのは通知だけ、その日のノートは次に開いたとき**。そう書いてある | `crates/amber-core/src/note.rs ~ pub fn due_since\(` |
| 74 | 2026-09-04 | バックアップとインポート。範囲を選べるように（すべて／フォルダ／タグ／ノート1つ） | 範囲が要るのは、バックアップが**何かの前**にやることだから ── 入れ直す前・人に渡す前・片付ける前で、要る量が違う。zip を作るのは `cian-ffi` の1つの口（`backup`）で、**窓版もいずれ同じ口を使える**。名前は `<対象>-<日付>.zip`。空のタグは**エラーにする**（黙って全部入りの zip を渡さない）。共有シートは `ShareLink` ではなく `UIActivityViewController` ── **zip は押した瞬間に作る**ので、画面を組み立てる時点では渡すものがまだ無い | `crates/amber-core/src/api.rs ~ "backup" =>` |
| 75 | 2026-09-04 | cian だからシアンっぽい色を基調にした見た目に | **色は1つ、置き場所も1つ**（`Assets.xcassets/AccentColor`）。画面ごとに違う色を出すと、触れるものの見分けが画面ごとに変わる。明暗それぞれに別の値を持つ（暗い地の上では、同じシアンは沈む）。アプリのアイコンから採った色で、**アプリの外（ホーム画面）と中で同じ色** | `ios/Cian/CianApp.swift ~ \.tint\(Color\("AccentColor"\)\)` |
| 76 | 2026-09-05 | 読む画面で TODO のチェックを押したり外したりしたい | **行番号で裏返す**（何番目のチェックか、ではない）。画面は一瞬前の `blocks()` から描かれていて、上にタスクが1つ増えていたら数え方はずれる。行が違っていた・そこが箇条書きだった、のときは**何もしない** ── 画面とファイルが食い違っているのに書き込むのが一番悪い。押した瞬間にディスクへ（チェックを押してから保存ボタンを探す人はいない）。`- [ ]` は箇条書きでもあるが、**押せる方を返す** | `crates/amber-core/src/note.rs ~ pub fn set_check\(` |
| 77 | 2026-09-05 | 通知を押したら、そのノートを開いてほしい | 通知に**ノートのパスを積む**（`userInfo`）。受け口は `Ring` という小さな置き場 ── ロック画面から押されたときは、**渡す相手の画面がまだ無い**うちに答えが来るので、誰かが取りに来るまで持っておく係が要る。`Ring.listen()` は `init()` で（あとから delegate を差しても、その通知はもう誰にも届かない）。ノートが読み込まれる前に押された場合に備えて、**ring と notes の両方を見て、後に来た方が開く** | `ios/Cian/Bell.swift ~ final class Ring: NSObject` |
| 78 | 2026-09-05 | 一覧を日付でまとめたい。作成日と更新日の2種類 | 見出しは**並び順に従う** ── タイトル順に並べながら日付で区切ると、3月のノートの上に「今日」が乗る。だからタイトル順のときは**頭文字**が見出しになる。作成日は front matter の `created:`、無ければファイルの誕生時刻、それも無ければ更新時刻（日付の無いノートは日付の見出しから落ちて、消えたように見える） | `ios/Cian/NotesStore.swift ~ func bands\(` |
| 79 | 2026-09-05 | 「題で並べる」ではなく「タイトルで並べる」 | 語を1つに揃えた。**窓版の「ノートの題」も同時に直した** ── 片方だけ直すと、同じものを2つの名前で呼ぶことになる（それを機械で見ているのが `parity.py`） | `ios/Cian/NotesStore.swift ~ タイトル順` |
| 80 | 2026-09-05 | タグで絞るのに検索窓へ打ちたくない。既存のタグを押して選びたい | 帯に出るのは**いま見えているノートが実際に持っているタグ**（多い順）。フォルダ全部のタグを出すと誰も読まない帯になる。複数選ぶと **かつ**（両方持つノート）── **または**にすると、押すほど一覧が増えることになり、絞る操作としては逆。タグは検索と同じで**どこにあるノートでも探す**（フォルダの中にいても、そのフォルダの分だけ出すのは誰も訊いていない答え） | `ios/Cian/NotesStore.swift ~ var tagsHere: \[String\]` |
| 81 | 2026-09-05 | 書く道具をキーボードの上に。よく使うものは今ので良い。見出しは複数回押すと `#` `##` `###`。使わないものは畳んでおく（表・リンク・ソースコード・取消線・水平線…） | 上のバーは **戻る・タイトル・保存・読む書く・⋯** の5つだけにして、道具は**キーボードが出ているときだけ**下に出す（キーボードが引っ込んでいるときの帯は「キーボードの上」ではなく、ただの画面の底）。見出しは**押すたびに深くなる** ── ボタンを3つ置くと、1つの考えに3つの名前が付く。タグ・通知・写真は上の ⋯ に残す（読んでいる間はキーボードが無く、通知を仕掛けるのは読み終えた直後だから）。**文字色は入れていない** ── Markdown に文字色は無く、書き方を決めると両前端がそれを覚えることになるので、先に相談したい | `ios/Cian/Editor.swift ~ static func deepen\(` |
| 82 | 2026-09-05 | ノートを複数開いてスワイプすると、上のアイコンが一瞬増えて消える。ちらつきを消したい | `TabView` は**隣のページも生かしておく**ので、SwiftUI に2枚分のツールバーが渡り、入りきらない分が 「⋯」 に押し出される ── 増えて減っていたのは、その ⋯。**手前のページだけがツールバーを出す**ようにした。**これでは足りず（89）**、ツールバーごと机に上げた ── ページが作るものは、ページが作り直されるたびに作り直される | `ios/Cian/Desk.swift ~ private var chrome: some ToolbarContent` |
| 83 | 2026-09-05 | ノートの行は、題の字を押さないと反応しない。囲みのどこでも反応してほしい | ラベルは**いちばん長い行の幅しか無い**ので、題が短いノートは行の右半分が指に対して死んでいた。`frame(maxWidth:)` と `contentShape` で行全体を的にした。同じ行が場所によって答えたり答えなかったりするのは、バグとして読まれる | `ios/Cian/ContentView.swift ~ .contentShape\(Rectangle\(\)\)` |
| 84 | 2026-09-05 | ダークモードとライトモードの2つを選べるようにしたい | 選択肢は**3つ**（iPhone に合わせる／ライト／ダーク）── 2つしか無い切り替えは、一度倒すと元に戻せない切り替えになる。既定は「合わせる」。色は `AccentColor` が明暗それぞれの値を持っているので、切り替えても**シアンが沈まない** | `ios/Cian/CianApp.swift ~ enum Look: String` |
| 85 | 2026-09-05 | ピン留めではなく**お気に入り**にしたい。本当のフォルダにも居るし、お気に入りからも見える。お気に入り自身にも階層を作りたい。⭐️ で普通のフォルダと違うと分かるように | お気に入りは**2つ目の居場所**であって移動ではない ── ノートは書いた場所に残り、front matter の `star:` が「どの棚にも並ぶ」と言う。だから**ノートに書く**（移動・改名・同期を越えて付いていく。一覧をどこかに持つと、その一覧だけが古くなる）。棚は⭐️の四角で描く ── フォルダの絵にすると「動かせばファイルも動く」と約束することになる。**空の棚だけ**は書ける場所が無いので `.cian/settings.json` が憶える（その1行を失っても、失うのは空の棚だけ。ノートの居場所は失われない）。古い `pinned: true` も読む。外すと `pinned` も一緒に消す ── 残しておくと、外したのにお気に入りのままになる | `crates/amber-core/src/note.rs ~ pub star: Option<String>` |
| 86 | 2026-09-05 | フォルダの色は自分で決めたい。シアン系にこだわらない | 色でフォルダを見分けるのが目的なので、**全部を同じ色相に収める配色は目的に反する**。11色（シアンからはいいろまで）と「色をなくす」。書き場所は `.cian/settings.json` ── フォルダには色を書き込むノートが無い。壊れた設定ファイルは既定に戻すだけで、**ノートを見せない理由にはしない** | `crates/amber-core/src/notebook.rs ~ pub fn set_color\(` |
| 87 | 2026-09-05 | 書く道具がカーソル位置に効いてほしい | `TextEditor` はカーソルを渡さないので、道具は**最後の行**に効いていた（見出しを押すと、いちばん下に見出しができる）。`UITextView` に置き換えて選択範囲を報告させた。**IME が字を組んでいる間（`markedTextRange`）は、どちらの向きにも触らない** ── 確定前の字に書き込むと、組みかけの語が飛ぶ。編集は `Marks` に (text, 選択) → (text, 選択) の関数として置いた（画面の外に出したので、そのまま試験にかけられる形になる）。写真も、末尾ではなくカーソルの段落に入る | `ios/Cian/Editor.swift ~ enum Marks` |
| 88 | 2026-09-05 | 文字色（Markdown で使えるものが欲しい） | 書き方は `<span style="color:#rrggbb">`。**GitHub は `style` を落とす**ので色は付かないが、**字は読める** ── `$\color{red}{…}$` の抜け道は GitHub でだけ描かれ、他所では読めない記号の塊になる。ノートは**文へ壊れる**べきで、記法へ壊れてはいけない。読み取りは `note::spans` の1か所で、**3つの前端すべてが同じ答えを見る**（iPhone は run ごとに塗り、窓は `markdown::Inline::Colored`、端末は 24bit 色）。窓の HTML は「ファイルから来たものは全部エスケープする」という約束があるので、**通すのは検査済みの6桁だけ** ── `onclick` も `class` も、これまで通り字になる。色数はフォルダと同じ11色に固定（色でしか区別できないノートは、grep に映らず、読み上げにも伝わらない）。一覧の二行目と検索は `note::plain` で色の記法を外す | `crates/amber-core/src/note.rs ~ pub fn spans\(` |
| 89 | 2026-09-05 | 目のボタンで読む／書くを切り替えるときも、上のアイコンがちらつく（⋯ が一瞬増えて消える） | 前回の「手前のページだけが出す」では足りなかった ── **ページが作るツールバーは、ページが作り直されるたびに作り直される**。`TabView` は隣のページを生かしたまま、状態が変わるたびに作り直すので、その一瞬だけ入りきらず ⋯ に押し出される。ツールバーを**机（`DeskView`）に上げた** ── 1つしか無く、ページが何をしても作り直されない。読み書き・保存・写真・タグ・通知も机の持ち物にした | `ios/Cian/Desk.swift ~ private var chrome: some ToolbarContent` |
| 90 | 2026-09-05 | ノートの長押しメニューは ブックマークに登録する → フォルダへ移す → エクスポート の順に | 手が伸びる順。ブックマークは片手間、移すのは整理、書き出すのは **amber の外へ出る**もので、外へ出るものは最後。**依頼 249 で札を「お気に入り」から改名したとき、ここが鳴った** ── 約束は順番のほうなので、検査は札ではなく並びに張り直した（札だけ変わって約束が生きていた三度目） | `ios/Cian/ContentView.swift ~ ブックマークに登録する[\s\S]{0,900}?フォルダへ移動[\s\S]{0,900}?エクスポート` |
| 91 | 2026-09-05 | メモしたら自動保存してほしい。わざわざ保存しない | 打つのをやめて 0.9 秒で書く（一打ごとに書くと、一文で40回ファイルを書き直すことになり、同期フォルダでは向こうの端末に40回の変化として届く）。**画面を離れるときと、アプリが前面でなくなるときにも書く** ── 電話は断りなくアプリを止めるので、時計だけに任せると最後の一文が消える。保存ボタンは**状態表示に変えた**（●＝書いている／✓＝書いた）── 書かれたかどうか何も言わないノートは、置いて立ち去れない | `ios/Cian/Desk.swift ~ private func later\(` |
| 92 | 2026-09-05 | ミスったときのために Undo / Redo が欲しい | **電話自身の undo を使う**（振って戻す・3本指スワイプと同じ山）── Swift で書き直せば、電話と食い違う2つ目の undo ができる。そのために道具は**文字列を丸ごと入れ替えるのをやめ**、`replace(_:withText:)` で1回の編集として入れるようにした（丸ごと入れ替えは undo の山に載らないので、見出しを押した分だけ戻せなかった） | `ios/Cian/Editor.swift ~ final class Pen` |
| 93 | 2026-09-05 | iPhone のキーボードに矢印が無いのが本気で使いにくい。左下上右（vim の順）で道具に入れて | 長い文をスマホで書くのが辛い理由のほとんどがこれ（虫めがねを引きずってカーソルを置く）。上下は `.layout` 方向 ── 折り返された行に**描かれている通り**に動く。並びは h j k l | `ios/Cian/Editor.swift ~ func step\(` |
| 94 | 2026-09-05 | 表が 2×2 固定で使いにくい。もっといい UI/UX を | **骨組みを置くのが間違いだった** ── 打ち込む表は縦棒を数え続ける表で、揃え方の行（`:---` `:---:` `---:`）は誰も形を覚えていない。列の数・名前・揃えを選ぶ画面にして、縦棒は cian の仕事にした。**入るのはただの Markdown**（cian の表ではなく、どの道具でも読める表）。空欄は全角空白 ── 中身が空の升は描く側によっては消えてしまい、消えた表は「作れなかった」に見える | `ios/Cian/Tabling.swift ~ struct Tabling` |
| 95 | 2026-09-05 | 最初に出てくる「cian」のところをもう少しクールにしたい | **この行は、いちど嘘をついていた。** 実装した `Wordmark`（印＋丸ゴシック 34pt＋中身一行）は、どこかの時点で画面から外れたまま**呼ばれずに残っていた** ── 検査が見ていたのは `struct Wordmark` という**書いてあるかどうか**で、**出ているかどうか**ではなかったので、台帳はずっと「守られています」と言い続けた。検査が宣言を見ていると、こうなる。2026-09-07 に作り直した: 名前は**帯に、綴りそのもので**出す（`ambər`・17pt・シュワだけ琥珀）。**印は並べない**（本人の言葉で「くどい」── 絵と字は同じことを二つの形で言う）。**大きな題にはしない** ── 巻けば縮むかわりに、開くたび一覧を 52pt 押し下げる。ここは一覧を見に来る画面で、帯なら高さは変わらない。フォルダの中に降りたら消えるのは前と同じ ── そこでの問いは「いまどこ」で、名前はそれに答えない。（この行にあった「検索欄は出しっぱなし」も、依頼 308 で畳む形に変わっている） | `ios/Cian/ContentView.swift ~ \*\*印ではなく、名前。\*\*` |
| 96 | 2026-09-05 | フォルダを移動したときにアニメーションが欲しい。パッと切り替わるので押せたか分からない | **一瞬で変わる一覧は、変わらなかった一覧に見える** ── 指が着いた時にはもう中身が違っていて、目が追うものが無いので、もう一度押して確かめることになる。一覧ごと差し替えて、来た方向から滑らせる。**どちらへ行ったかで滑る向きを変える**（両方向で同じに滑るなら、滑らない方がまし）。復元や戻りで場所が変わったときは滑らせない ── 滑りは「あなたがした移動」のためのもの | `ios/Cian/ContentView.swift ~ private func go\(` |
| 97 | 2026-09-05 | 左→右のスワイプで上の階層へ、右→左でそれを戻す（cian → hoge → cian → hoge） | 上がった先を憶えておく（`forward`）── **戻せない操作は、そのうち誰も使わなくなる**。フォルダを手で選んだときは、その先は無かったことにする（新しい方向へ進んだので）。**画面端からだけ**受ける ── 画面いっぱいの横スワイプは行そのものの操作（お気に入り・削除）で、出口は電話が既に置いている場所に置く。`simultaneousGesture` なので**縦スクロールが勝つ**し、指が離れるまで何も決めない | `ios/Cian/NotesStore.swift ~ func back\(` |
| 98 | 2026-09-05 | フォルダ移動のスワイプ、指の移動が長すぎる。もっと短くても反応してほしい | 距離だけで見ないで、**離した瞬間まだ動いている指**を数に入れる（`predictedEndTranslation`）── 指を弾いた人は「もっと先まで行く」つもりで離している。48pt の意図的なドラッグを求めていたのは、スワイプではなくドラッグ | `ios/Cian/ContentView.swift ~ predictedEndTranslation` |
| 99 | 2026-09-05 | 表の画面、列と行の増やす・減らすの見た目を揃えて。文言は「増やす」「減らす」 | 同じことを2つの数にする道具が2つの別の考えに見えないように、1つの形にして両方で使う | `ios/Cian/Tabling.swift ~ private func more\(` |
| 100 | 2026-09-05 | 書く帯は、1段目＝見出し・箇条書き・チェック・太字・画像・⋯、2段目＝undo/redo …（空けて）… 矢印 で右詰め | 1段目は**ノートが何でできているか**、2段目は**動き回るための道具**。分けたのは、片方のつもりの指がもう片方を押さないようにするため。右詰めなのは親指の側だから。写真は上の ⋯ から降ろした（書いている最中に使うものなので） | `ios/Cian/Writing.swift ~ mark\("画像", "photo"` |
| 101 | 2026-09-05 | 新規作成すると title / created / tags が見えるノートができる。プログラムが見ているだけなら書く画面に出さないでほしい | 前書きは**ノートが自分を説明する言葉**で、ノートが言うことではない ── 自分で打っていないものを越えて自分の1行目に辿り着くのは変。書く画面は本文だけを持ち、保存のときに繋ぎ直す。どこで前書きが終わるかは `note::front` の1つの答え（`split`）なので、電話・窓・端末が同じ場所でノートを始める。**チェックは丸ごとに対して**行う ── 行番号はファイルの行番号だから | `crates/amber-core/src/api.rs ~ "split" =>` |
| 102 | 2026-09-05 | 自動保存を既定に。設定で切って手動保存も選べるように | 切ると上のバーに「保存」が出る。**切っていても画面を離れるときに一度だけ訊く** ── 自分で決めたい人でも、消えていいとは言っていない | `ios/Cian/Desk.swift ~ private var autosave = true` |
| 104 | 2026-09-05 | 題を入れずに作ったときの既定のタイトルに、時分秒まで入れてほしい | 日付だけだと午後に3本作ると「-2」「-3」が付くだけで、**どれがどれか分からない**。`created:` は日付のまま ── あれは機械が読む欄で、タイトルは人が読むもの。ファイル名からは `:` が落ちる（Windows で作れない） | `crates/amber-core/src/note.rs ~ pub fn now_stamp\(` |
| 105 | 2026-09-05 | 設定の「保存場所を選ぶ…」を押しても何も起きない | **同じ画面に `fileImporter` が2つあると1つしか効かない**（SwiftUI は後勝ち、負けた方のボタンは何もしない）。2026-09-04 に一度直したときに書いた注意書きの2行下で、同じことを2度目にやっていた ── インポートに負けて、押しても配線されていないボタンに見えていた。**1つにして、何を選びに行くかで受け取るものを変える** | `ios/Cian/ContentView.swift ~ enum Fetching` |
| 106 | 2026-09-05 | フォルダを長押しで、色だけでなく名前の変更と削除もしたい。削除は「中のノートごと消していい？」と訊いてから | 削除は**何本が道連れになるかを数で言ってから**（「中のノート 3 本も一緒に消えます」）。**電話にゴミ箱は無い**ので、これは本物の削除で、取り消せない。`..` を渡してノートの外を消せないよう、パスを絶対化してから根の下にあることを確かめる。名前を変えたフォルダの中に立っていたら、上へ退く ── 消えた場所を見せ続けない | `crates/amber-core/src/api.rs ~ ノートの外は触れません` |
| 107 | 2026-09-05 | フォルダのツリー構造が分かる表示がほしい | 一覧は**一階層ずつ**で、それはフォルダを**使う**ための正しい形。ツリーは別の問い ──「ここに何があって、どこまで深いのか」。字下げは `allBooks` のパスの深さそのもので、組み立てるものは何も無い。数字は下の階層も数える。いま居る場所には印を出す | `ios/Cian/Writing.swift ~ struct Tree` |
| 108 | 2026-09-05 | フォルダに入るとフォルダ名だけになる。パンくずリストでどこにいるか分かるように | **フォルダ自身の名前は「どこ」の答えではない** ── 別々の場所にある「2026」は一覧の上では見分けが付かない。各段は、その階層への戻り道。いま居る段だけは字の色にして押せなくする（同じ色にすると、押しても何も起きないものが押せるものに見える） | `ios/Cian/Writing.swift ~ struct Crumbs` |
| 109 | 2026-09-05 | 「（いちばん上）」という言い方をやめて、素人にも分かる言葉に（デフォルトなど） | 彼の案どおり「デフォルト」に。**2026-09-12 に本人が「（トップページ）」へ変更**（窓も電話も）。ノートを移す先と、お気に入りの棚の両方 | `ios/Cian/ContentView.swift ~ Button\("（トップページ）"\)` |
| 110 | 2026-09-05 | お気に入りに階層を作りたい（棚が「いちばん上」しか無い） | **棚は作れていたが、作りに行く道が隠れていた** ── お気に入り画面からしか作れず、その画面は「お気に入りが1つ以上ある」ときにしか出てこない。だから最初の棚を、要るその場で作れなかった。棚を選ぶ画面に「新しい棚…」を置いた（作って、そのまま入れる。`棚/中の棚` と書けば階層） | `ios/Cian/Stars.swift ~ 新しいブックマークグループ…` |
| 111 | 2026-09-05 | 通知は ⋯ の中ではなく最上段にベルで分かるように | **鳴るかどうかは状態**で、状態はメニューの奥にあると誰も知らない。仕掛かっているときは橙の塗りつぶし | `ios/Cian/Desk.swift ~ ToolbarItem\(id: "bell"` |
| 113 | 2026-09-05 | 窓版でも cian モードを（チェックを押せるように） | HTML の升に**元の行番号を積む**（`data-line`）── `note::set_check` は行番号を取るので、窓に要るのはそれだけ。何番目の升かを数えると、前書きのあるノートでずれる。押すとエディタに戻して**ふつうに保存する**ので、打ち込みと同じ衝突検査を通る | `crates/amber-core/src/markdown.rs ~ data-line=` |
| 123 | 2026-09-05 | iPhone: 「フォルダの構成」の見え方を既定に。中のノートも出す。エクスプローラっぽく。お気に入りフォルダを一番上に | 一階層ずつの一覧はフォルダを**使う**ための形で、ツリーは**知る**ための形 ── ノートアプリでやっていることは、たいてい後者。お気に入りは**フォルダとしては描かない**（⭐️の四角）── 2つ目の居場所であって、ディレクトリのふりをさせると「動かせばファイルも動く」と約束することになる。**探しているときはツリーにしない**（検索結果の木は誰も読まない）。再帰する view は `AnyView` で1か所だけ型を消す ── 自分を含む view は自分を含む型になり、Swift は推論しない | `ios/Cian/Nest.swift ~ struct Nest` |
| 124 | 2026-09-05 | iPhone: 検索を OR / AND に対応。タグは入力欄を押すと選べるように。既定のタグバーは消す | 空白は AND、`OR`（`or` と縦棒も）は OR で、**OR のほうが弱い綴じ** ── `仕事 週報 or 家` は (仕事 AND 週報) OR (家)。みんなそう書いて、誰も説明しない。**問いの意味は `cian-core`**（`note::terms`）── 3つの前端が別々に決めると、2語打つまでは一致する検索欄が3つできる。タグは**打っている場所に出す**（`searchSuggestions`）── 帯は画面の1行を永久に取って、たまに押されるだけだった | `crates/amber-core/src/note.rs ~ pub fn terms\(` |
| 125 | 2026-09-05 | iPhone: 「あたらしい」→「新しい」 |  | `ios/Cian/Making.swift ~ 新しいノート` |
| 128 | 2026-09-05 | （105 の続き）保存場所を選んでも、選んだ後に何も起きていなかった | **開くところしか見ていなかった。** 105 で「押しても何も起きない」を直したとき、確かめたのは**選択画面が開くところまで**で、フォルダを選んだ後は一度も動いていなかった。原因は SwiftUI の作法: 閉じると `isPresented` が false になり、その setter が「何を選びに行ったか」を消し、**そのあとで** completion が走ってそれを読む ── nil なので何もしない枝に落ちる。何を頼んだかは**別に持って**、答えを受け取ってから捨てる。**presentation が自分のコールバックに要るものを消すのはこれで3度目**（`.alert(presenting:)`、`.sheet(item:)`、これ） | `ios/Cian/ContentView.swift ~ private var asked: Fetching\?` |
| 129 | 2026-09-05 | iPhone: Google Drive / Dropbox / iCloud がどこにあるか分からない。探せなくて困る | **一度目の探し方を、画面に書いた** ── 「ブラウズ」から辿る、Drive と Dropbox は「場所」の下、出てこないときは「…」→「サイドバーを編集」。これは amber が代わりにできない設定（「ファイル」アプリのもの）なので、**せめてどこにあるかを言う**。灰色で選べないものがあることも書く（アプリ側の作りなので、こちらでは変えられない）。**「選んだ場所を覚えて一覧にする」という後半は、2026-09-06 に本人が取り下げた**（依頼 253 ── 窓と同じく置き場所は一つに）。取り下げなので、張り直しではなく**約束そのものが変わっている** | `ios/Cian/Where.swift ~ サイドバーを編集` |
| 130 | 2026-09-05 | 保存場所を変えるとき「いままでのノートを丸ごと移しますか」と訊いて、OK なら構成ごと移す | 新しいフォルダは**空のフォルダ**で、それを予期していなかった人は書いたもの全部を見失う。だから訊く ── 何件かも数で言う。移すのは**コピーして、確かめて、それから消す**（プロバイダ間ではこれは rename ではない。向こうに全部書けてから、こちら側に手を付ける。途中で失われたノートは、このアプリがやり得る最悪のこと）。**同じ名前が向こうにあったら1つも動かさない** ── 2つのノート束を混ぜるのは判断で、ここで人の代わりに決めることではない。画像（`attachments/`）と `.cian` も連れて行く（置いていくと、絵のあるノートが全部壊れる） | `crates/amber-core/src/api.rs ~ "migrate" =>` |
| 131 | 2026-09-05 | 「この iPhone の中に戻す」が分からない。どこなのかを言ってほしい | ボタンは「どこなのか」も「なぜ戻るのか」も言っていなかった ── 道を**名前の連なり**で出す（「この iPhone › amber」）。「amber」だけでは *何という名前か* に答えていて、訊かれているのは *どこにあるか* だから。**「開いてきた場所の履歴にする」という後半は、2026-09-06 に本人が取り下げた**（依頼 250）── 窓は置き場所を一つしか持たないので、電話も一つにした。取り下げなので、張り直しではなく**約束そのものが変わっている** | `ios/Cian/NotesStore.swift ~ var trail: \[String\]` |
| 132 | 2026-09-05 | バックアップの zip を選べないので戻せない | 作れて使えない zip は、バックアップではなくただのファイル。選べるものに `.zip` を足して、**フォルダの構成ごと戻す**。**いまあるノートは上書きしない**（先週の写しで今日の仕事を失う戻し方はしない）── 残した件数も言う。展開は一度別の場所へ（半分展開された書庫がノートの中に立たないように）。zip の中は `ノート/…` のように**そのフォルダ自身の名前**で始まるので、1つの名前の下に全部あるときだけ剥がす ── 剥がさないと、戻すたびに中に1階層増える | `crates/amber-core/src/api.rs ~ "restore" =>` |
| 133 | 2026-09-05 | 電話に入れたものを、窓版にも持っていける作りで | **判断は `cian-core` に移した。** 「丸ごと移す」と「バックアップから戻す」は `cian_core::notebook::migrate` / `restore` の1つずつで、電話も窓も同じものを呼ぶ ── 2つ書けば、2つのうち片方だけが「上書きしない」を守る日が来る。窓では移す元を**開いてきた場所の履歴**から選ぶ（電話と同じ理由で覚えている）。戻すのは反対のペインの zip ── 2画面ファイラの「向こう側」がそのまま行き先／出どころ | `crates/amber-core/src/notebook.rs ~ pub fn migrate\(` |
| 148 | 2026-09-05 | iPhone のアプリ名も amber に | 「ファイル」アプリに出るフォルダ名は表示名に従うので、**表示名と説明文は必ず一緒に変える** ── 片方だけ変えると「ファイル → この iPhone 内 → cian」が嘘になる。中の名前（`cian.look` などの設定キー・バンドル ID）は変えない。変えると彼の設定が消える | `ios/Info.plist ~ <string>ambər</string>` |
| 150 | 2026-09-05 | cian と amber を完全に分ける。amber は `~/workspace/amber` で独立 | 「大木に育つ前に別アプリにすべき」。分ける前、**iPhone は 18,128 行の cian-core を丸ごと積んでいた** ── git も svn も SharePoint も差分もアーカイバも。`desktop` 機能は `trash` の依存を切っているだけで、モジュールは一つも切っていなかった。**扉も amber のものにした**（`amber_call` / `amber_free`）── バンドル ID だけは変えない | `crates/amber-ffi/src/lib.rs ~ pub unsafe extern "C" fn amber_call` |
| 151 | 2026-09-06 | amber の窓版を作る（Mac / Windows）。cian の amber モードを外す前に | **`gui/` は持ってこない。** cian の renderer は 11,468 行あるが、それは2画面ファイラの土台（ペイン・シェル・アーカイブ・git・差分・SFTP・PTY）で、amber が使うのはその上の 151 行。持っていくと大木が二本になる。三列（行き先・ノート・中身）専用に書き直すほうが小さい ── 要るのは Monaco と `amber-server` の口だけ。**判断は書かない** ── 絞り込みの意味は `note::terms` に訊く（iPhone と同じ形。三つの前端がそれぞれ「二語」の意味を決めると、二語を打つまで一致して見える検索窓が三つできる） | `gui/renderer.js ~ amber-core` |
| 152 | 2026-09-06 | Gemini に描いてもらったアイコンを amber に取り込む（案2「琥珀の中の Markdown」） | **この絵は式で描けない** ── ガラスは透けて、中に気泡があり、縁に光が回っている。`amber.py` の走査線は角丸と丸端の線しか塗れないので、**画素そのものを原本として持つ**。輪郭はどの物差しでも切れなかった（赤−青では下端が影に 35px 溶け、彩度では上端の光った縁が欠ける）ので、**角丸四角の形を測って当てはめ、それを型にする** ── 四辺が同じ理屈で決まり、どの大きさでも縁が解析的に滑らかになる。絵は 788x846 と正方形ではなかったので、**縦を詰めて**正方形にする（伸ばすと無い画素を作る） | `packaging/amber_icon.py ~ def radius` |
| 153 | 2026-09-06 | 窓でも Ctrl+E でプレビューと書く面を行き来したい（Inkdrop の `view:toggle-preview`） | 組み方は `markdown::to_html`、見た目は前端 ── **`blocks`（iPhone）と同じ行単位の読み方を通る**ので、同じノートが二つの見た目を持たない。逃がし（`javascript:` を落とす・`onclick` も `class` も字にする・色は検査済み6桁だけ）は `to_html` の中の1か所。**重ねずに横へ並べる** ── 重ねて片方を消すと Monaco が幅ゼロのまま測る | `gui/renderer.js ~ function toggleRead` |
| 154 | 2026-09-06 | F12 でノートだけを最大化、F12 か Esc で戻る（Inkdrop の distraction free） | 畳むのは**左の二列だけ**。上の帯を消すと、いま何を開いているのか分からない画面になる。**どこを打っていても効く** ── 書いている最中に大きくしたくなるのだから、エディタの中でこそ効かないと意味がない | `gui/renderer.js ~ function setZen` |
| 155 | 2026-09-06 | 窓にも Markdown の簡易入力を（iPhone の「書く道具」と同じ顔ぶれ） | **書式ツールバーではない** ── ファイルは Markdown のままで、押した跡は打った跡と見分けがつかない。**意味は `markdown::marks`（core）** に置いた ── 窓が自分で `#` を数えはじめると iPhone と押し心地が分かれる。位置は渡さない（JS は UTF-16 の桁、Rust は文字で数えるので絵文字一つで境目がずれる）── **選んだ字そのもの**を渡す。行頭の印は押し直すと外れ、見出しは押すたびに深くなる（`#` → `##` → `###` → 無し） | `crates/amber-core/src/markdown.rs ~ pub mod marks` |
| 156 | 2026-09-06 | vim モードが欲しい。ただし**素のメモ帳が基本** | 既定は入れない ── 知らずに入っていると `i` を押すまで一文字も打てず、それは壊れているのと同じに見える。中身は `monaco-vim`（CodeMirror の vim の移植）── 自前で書くと `ci"` や `.` のような「本物なら動くのに動かない」に必ず当たる。読めなければ素のまま続ける（**入れられないことで書けなくなる理由は無い**） | `gui/renderer.js ~ function setVim` |
| 157 | 2026-09-06 | 読める形の升を、キーボードと読み上げからも押せるように | `<span>` で出していた頃、押せるのに操作要素として名乗っておらず、読み上げはただの字として読み、Tab では辿り着けなかった ── **押せる升が一つ出せないだけで、ノートの半分が触れなくなる**。`<button>` にして `aria-pressed` を積む | `crates/amber-core/src/markdown.rs ~ aria-pressed=` |
| 158 | 2026-09-06 | 番号つきリストを、読める形でも番号で出す | 書いた `1. 2. 3.` が黒丸で出ると、**書いたものと読めるものが違う**ので Markdown が壊れて見える。始まりの番号も渡す ── 途中から続ける手順（`3.` から書きはじめた行）が毎回 1 に戻らない。同じ深さで印が変われば別のリストにする | `crates/amber-core/src/markdown.rs ~ <ol start=` |
| 159 | 2026-09-06 | 行そのものが絵の行を、読める形でも絵で出す | それまで `!<a href>` になっていた（`!` が字のまま残る）。**どこからが絵かは `note::lone_image` の1か所** ── 窓が自分で `![` を探しはじめると、iPhone が絵として積む行を窓が字で出し、同じノートが二つの見た目を持つ。出せない先（`javascript:`）は隠さず字で残す | `crates/amber-core/src/markdown.rs ~ crate::note::lone_image` |
| 160 | 2026-09-06 | アプリの中の印も、渡したアイコンをベースにしたい | **描き直さず、アイコンそのものを小さくして出す**（`amber-mark.png` 128px ── 窓の 54 の2倍と iPhone の 38pt の3倍を両方覆う一枚）。前は葉を四か所に写して描き、揃っているかを `amber.py` の `agree()` が見張っていた ── **それでもアイコンを替えた日に、中の印だけが葉のまま残った。** 見張れていたのは「四つの写しが揃っているか」であって「アイコンと同じか」ではなく、揃ったまま全部が古くなれた。写しを持たなければ、ずれようがない。CSS と SwiftUI の角丸は外す（絵が自分で角丸を持っているので二重に削れる） | `gui/renderer.js ~ amber-mark.png` |
| 161 | 2026-09-06 | iPhone のアクセントを ayu-light / ayu-dark のような黄色に | 暗い側は **ayu-dark の金 `#E6B450` をそのまま**（黒の上で 11:1）。明るい側は ayu-light の `#FF9940` を**そのまま使えなかった** ── 白の上で 2.12:1 しかなく、iOS のアクセントは文字ボタンにも乗るので読めない（Apple 既定の青ですら 3.6:1）。**ayu の金の色相（40°）を保ったまま明度だけ落として `#A87000`**（4.21:1）。ロゴは追従させない（台帳 95）── 印は絵になったので、もう色を拾わない | `ios/Cian/Assets.xcassets/AccentColor.colorset/Contents.json ~ "red" : "0.902"` |
| 162 | 2026-09-06 | 窓の一覧にも並び順を（iPhone にはある。Inkdrop も既定は更新順） | 三つしかないものに開いて閉じる幕は要らない ── **押すたびに次へ**。名前順は `localeCompare(…, {numeric: true})` で、素の `<` は「あ」と「い」も `note-2` と `note-10` も両方まちがえる。**core には上げていない** ── iPhone は Foundation の `localizedStandardCompare` を使っており、Rust には土地を知った自然順が標準に無いので、上げると iPhone の並びのほうが悪くなる。同じ規則を、それぞれの土地の言葉で言っている | `gui/renderer.js ~ const ORDERS` |
| 163 | 2026-09-06 | 読める形のコード枠に色を（Inkdrop は色が付く） | **Monaco の色付けをそのまま借りる。** ハイライタを別に持ってこない ── エディタは既に `rust` も `js` も色分けしていて、同じ言語に二つの色分けを持つと、書いている面と読める面で色が違うノートができる。知らない語彙は素のまま（**色が付かないのは読めないことではない**）。組み直しに追い越されたら捨てる | `gui/renderer.js ~ async function paintCode` |
| 164 | 2026-09-06 | いま何字か見えるように | **数えるのは本文だけ** ── 前書きはノートが自分を説明する言葉で、書いた量ではない。語数ではなく**字数**にしたのは、日本語に語の切れ目が無いから（空白で切ると一段落が「1 語」になる）。題の隣ではなく右端に置く ── ずっと動く数字が題のとなりにあると、視線が題に戻るたびに数字を読んでしまう | `gui/renderer.js ~ function drawCount` |
| 165 | 2026-09-06 | 字の大きさを変えたい（⌘+ / ⌘− / ⌘0） | **書く面と読む面を一緒に動かす** ── 片方だけ動くと、並べたときに同じノートが二つの大きさで出る。キーは `Equal` / `Minus`（位置のキー）で当てる ── JIS では `+` に Shift が要り、`e.key` で当てると刻印どおりに打っても効かない | `gui/renderer.js ~ function setFont` |
| 166 | 2026-09-06 | 探す先を名指ししたい（Inkdrop の `tag:` `book:` `title:` と `-` の打ち消し） | 日本語の見出しも通す（`タグ:` `フォルダ:` `題:`）── 日本語で書いている最中に英字へ切り替えるのは高い。二重引用符で空白を含む一語。**`body:` は入れていない** ── 一覧が持っているのは本文の頭 100 字だけなので、受けると「本文を探したのに見つからない」を作る（奥の一文は `find` の仕事）。**知らない見出しは字のまま** ── `http://…` や「10:30」を打っただけで消える語ができると、探せなくなったことに気づけない。`terms` の返す形を字から `{見出し, 語, 打ち消し}` に変えたので、**窓と iPhone の両方を同時に直した** ── 片方だけ直すと、片方の検索窓だけが `tag:定型` という字を本文から探し続ける | `crates/amber-core/src/note.rs ~ pub struct Term` |
| 167 | 2026-09-06 | 窓に足りていなかった機能を全部入れる（iPhone にはあった） | お気に入り・棚・タグ・文字色・フォルダへ移す・新しいフォルダ・通知・書き出し・削除・フォルダの色・バックアップ・置き場所。**窓ができることを、一つの表にした** ── パレットも ⋯ の献立もそこを見る。二か所に書くと、片方にだけ増えた命令ができて「あるはずなのに無い」になる。ノートを書き換えるものは全部 `editNote` の一本道を通る（前書きを戻し、切り直し、いつもの保存）── **押して付けた星と、打って書いた星が別の扱いになる理由は無い**（衝突の検査も一度で済む） | `gui/renderer.js ~ const CMDS` |
| 168 | 2026-09-06 | 命令を名前で探せるように（コマンドパレット ⌘⇧P） | 覚えなくていいのが値打ち。**小窓は一枚**（選ぶ・打つ・はい／いいえ）── 別々に作ると、微妙に違う「Esc で閉じる」が三つできて、そのうち一つだけ閉じない日が来る。Esc は**手前にあるものから**閉じる（献立 → 小窓 → 最大化） | `gui/renderer.js ~ function sheet` |
| 169 | 2026-09-06 | 目次がほしい（⌘⇧O） | **何が見出しかは core が決める**（`note::blocks`）── 窓が `#` を数えはじめると、`#仕事` というタグの行が目次に出る（空白の有無で決まる）。飛び先の行番号も core が持ってくる ── 何番目の見出しかを数えると、前書きのあるノートでずれる（チェックの升と同じ理由）。書く面ならその行へ、読む面ならその見出しへ | `gui/renderer.js ~ function drawToc` |
| 170 | 2026-09-06 | GitHub 風の注記（`> [!NOTE]`） | 受けるのは GitHub が読める五つだけ ── 増やすと**ここでだけ見える記法**になり、同じノートが GitHub で壊れる。**引用の中の一行目でしか始まらない**（本文に `[!NOTE]` と書いただけの行が消えない）。色だけで区別せず、種類の語を必ず出す ── 色が見えない人にも、白黒で刷ったときにも残る | `crates/amber-core/src/markdown.rs ~ fn alert_kind` |
| 171 | 2026-09-06 | mermaid で図を描きたい | **図のあるノートを開くまで読み込まない**（3.4MB あって、ほとんどのノートに図は無い）。Monaco のローダには渡さない ── 束ねの中に `define.amd` を見て自分から名乗り出る部品が入っていて、ローダはそれを mermaid だと受け取る（実際にそうなった）。**`define` を伏せてから素の `<script>` で読む。** 描けない図は書いた字のまま残す（消すと直しようがない）。地の明暗に合わせる ── 図だけ白いと暗い画面で目を焼く | `gui/renderer.js ~ function loadMermaid` |
| 172 | 2026-09-06 | 書き出し（Markdown / HTML / PDF） | HTML は**一枚で完結**させ、外を参照しない。PDF は**窓そのものを刷らない** ── `printToPDF` は三列ごと刷るので、見えない窓をもう一つ建てて読める形だけを入れる。data: の URL ではなく一時ファイルを読ませる（Chromium は data: を最上位の遷移で渡すと黙って空の頁にすることがある） | `gui/main.js ~ amber:savePDF` |
| 173 | 2026-09-06 | 「上に留める」は入れない。お気に入りが既にそれ | 一覧の上に**別枠で**出す（iPhone と同じ形）。並び順に混ぜて上へ寄せるのではなく別の段にする ── 二つの場所に同じノートが出ると、人はそれを二度消そうとする。**「これは大事」と言う道を二つ持たない。** ついでに、チェックのあるノートには済んだ数を出す ── **数えるだけで、新しい欄は作らない**（進み具合は既にノートの中に書いてある） | `gui/renderer.js ~ class="sect"` |
| 174 | 2026-09-06 | 題を入力させるのをやめたい。「牛乳」だけ書きたいのに題とタグを訊かれるのは変。題を飛ばすと日付が出るのも変 | **書いた人が一度も言っていない名前を、前書きに入れない。** 新しいノートは `title:` も `tags: []` も書かない（`created` だけ残す ── 人が読む名前ではなく、並べ替えが読む欄）。題は **`title:` → 最初の見出し → 書き出しの一行 → ファイル名**。行頭の印は外し（`- 牛乳` の題は「牛乳」）、強調も外し、60字で切る。**題になった行は二行目に出さない**（一行のノートが二行に見える）。ファイル名まで落ちるのは**中身がまだ何も無いとき**だけで、そのときは他に呼びようがない。判断は `note::read` の1か所なので、窓も iPhone も端末も同じ題を見る | `crates/amber-core/src/note.rs ~ fn first_line` |
| 175 | 2026-09-06 | 左上の amber のアイコンと名前は要らない（Inkdrop にも無い） | アプリの名前は**窓の外**（Dock・タスクバー・⌘Tab）が既に言っている。中で二度言うと、そのぶんノートが下がる。mac の信号機のぶんだけ列の上を空ける。**2026-09-07、名前だけ戻した。** 外したのは「アイコン＋ただの名前」で、あれは確かに二度言っているだけだった ── 戻したのは名前に**姿**ができたから（`ambər`・シュワだけ琥珀）で、その綴りは窓の外のどこにも出ていない。**印は戻さない**（本人の言葉で「くどい」）。mac では**高さが変わらない**（34px は信号機のぶんで既に空けてある）。Windows と Linux は 14px しか無いので 30px へ伸ばし、**そこだけ一覧が 16px 下がる** ── 上の理由がそこでは生きているのを承知のうえで、本人が選んだ（Ｗ１） | `gui/index.html ~ \*\*名前だけ置く。マークは置かない。\*\*` |
| 176 | 2026-09-06 | いろんなテーマで表示できるようにしたい（育ててきた琥珀も取り込んで） | 琥珀（既定・OS に従う／明／暗）、ayu（明・中間・暗）、紙（白と黒だけ）。**名前で選んだら、選んだとおりに出す** ──「ayu-dark にしたのに昼は明るい」は選んだことにならない。既定だけが OS に従う。Monaco と mermaid にも**同じ明暗を渡す**（別々に訊くと、替えた日にエディタだけ前の明暗で残る）。足すときは色を全部埋める ── 変数は継承するので、抜けは崩れではなく**混ざり**として出る | `gui/renderer.js ~ const THEMES` |
| 177 | 2026-09-06 | マークダウンの描画が芋臭い。画像の周りにも配慮を | 直したのは三つ。**一行の長さ**（窓を広げるほど一行が長くなり、折り返すたびに目が左端を探す。読みにくさの半分はここで、字体や色の話ではない）。**単位は 2026-09-07 に `em` から `rem` へ変えた**（依頼 338）── `em` は要素ごとの字の大きさで効くので、同じ「34」でも見出し・本文・罫線が全部ちがう幅になり、左端がガタガタだった。止めること自体は変えていない。**見出しの上下**（上を厚く下を薄く ── 見出しは次のかたまりの持ち物）。**絵**（裸で置かない ── 地と同じ色の絵はどこからが絵か分からない。細い枠と丸みと薄い影、`alt` があれば下に添える）。区切り線は端まで引かない ── 全幅の線は「ここで終わり」に見えるが、`---` は「話が変わる」でしかない | `gui/index.html ~ max-width: 34rem` |
| 178 | 2026-09-06 | 書く面でも、画像の行だけ実物を小さく出したい | **字は消さない。** ファイルは Markdown のままで行はそこにある ── 消して絵に置き換えると、パスを一文字直したいだけのときに直しようがない。Monaco の `view zone`（行と行のあいだの空き）に置く。読めない絵は黙って空けず、何が読めなかったかを出す（貼り間違いに気づけるように） | `gui/renderer.js ~ function drawZones` |
| 179 | 2026-09-06 | 読む面でも書けるようにしたい。初学者がこちらだけで完結できるように | 押したかたまりがその場で入力欄になり、上の小さな帯（見出し・箇条書き・チェック・太字・引用）で記号を打たずに変えられる。**元の行は `to_html` が `data-line` / `data-span` で持たせる** ── 何番目のかたまりかを数えると前書きのあるノートでずれる。印はいったん制御文字で置き、最後に属性へ変える（かたまりが何行食べたかは**次のかたまりが始まる場所**を見るまで分からない）。閉じ札には差さない ── 箇条書きの二つ目は前の項目を閉じてから始まる。空にしたらそのかたまりを消す（消し方を別に用意すると、探すことになる）。「＋ ここに書く」を末尾に置く ── 空のノートで手が止まる | `crates/amber-core/src/markdown.rs ~ fn stamps` |
| 180 | 2026-09-06 | マークダウンのサポート（書き方）を出す | 押せる形で出し、**選ぶとその場に入る** ── 読むだけの一覧にすると、読んでから自分で打ち直すことになる | `gui/renderer.js ~ async function cmdSyntax` |
| 181 | 2026-09-06 | タグを文章で入力させるのはいただけない。一覧から選び、無いものだけ足す | すでに `#仕事` があるのに打ち直させると、打ち間違いで**似た名前のタグがもう一つ生える**。押すと付け外し、`＋ 新しいタグを作る` だけが入力。やめたら何も変えない。**札から「…」を外したとき（依頼 192）ここが鳴った** ── 約束は守られたまま札だけ変わった例で、検査を新しい札に付け替えた | `gui/renderer.js ~ ＋ 新しいタグを作る` |
| 182 | 2026-09-06 | 絞り込みが IT プロ向けすぎる。初学者向けに | 「探し方…」から選ぶ ── タグ・フォルダは一覧から選ばせ、記法を覚えなくても絞れるようにした。**「選ぶとその書き方が絞り込みの欄に入る」という後半は、2026-09-06 に本人が取り下げた**（依頼 294 ── 「`tag:仕事` とかになってシステムチックすぎる」）。押しただけなのに機械の言葉が現れ、外すには字を消すことになっていた。取り下げなので、張り直しではなく**約束そのものが変わっている** ── 選んだものは選んだものとして持ち、字にしない | `gui/renderer.js ~ \*\*押して選んだものは、文字にしない。\*\*` |
| 183 | 2026-09-06 | フォルダ・タグ・お気に入りの作り方が分からない。削除もしたい | **できることは前からあった**（`仕事/2026` と書けば階層）が、それを言う場所が画面に無かった ── 使えないのと、あるのに見えないのは、使う人には同じこと。段の見出しに「＋」を付け、**一つも無くても段を出す**（無いと最初の一つを作る道が消える）。右押しで名前を変える／外す。フォルダの名前替えは**中のノートを一本ずつ移す**（フォルダはただのディレクトリなので） | `gui/renderer.js ~ function railMenu` |
| 184 | 2026-09-06 | 「見た目を変える」がノートの右押しに出るのはおかしい | ノートを右押しした人が訊いているのは「このノートをどうするか」で、アプリの色ではない。**⚙ を題字の帯に置いた** ── 見た目・vim・字の大きさ・書き方・バックアップ・置き場所はそちら。⋯ はノートのことだけ | `gui/index.html ~ id="gear"` |
| 185 | 2026-09-06 | 窓の削除は、消さずにゴミ箱へ | **`core` の `delete` は消してしまう**（`remove_file`）── 電話にはゴミ箱が無いのでそれでよいが、机の上では戻せないのは強すぎる。窓は Electron の `shell.trashItem` を通す。フォルダも同じ道 | `gui/main.js ~ amber:trash` |
| 186 | 2026-09-06 | 読む面は、押して開くのではなく**メモ帳のように置いて打てる**ようにしたい。道具の帯も下に出したまま | 面ぜんぶを `contenteditable` にし、落ち着いたら **DOM を Markdown に戻して**いつもの保存を通す。戻せる札は `to_html` が出すものだけなので、逆に読むのも数十行で済む（貼り付けは字だけにして、よそから来た札を入れない）。**戻せないかたまり（枠・表・図・注記・絵）は触らせない** ── `contenteditable="false"` にして、押したら書く面のその行へ送る。**打てるのに保存されない、がいちばん悪い**。飾りは `execCommand`（`<b>` が `**` に戻る）、字そのものを変えるもの（チェック・リンク・表…）は一度ぜんぶ字に戻してから直す ── 見た目の上で組み立て直すと、そこだけ別の作り方が生える | `gui/renderer.js ~ function readToMd` |
| 187 | 2026-09-06 | 自分でも立ち上げたい（Windows の run.bat と、Mac のコマンド） | `./gui/run.sh` と `gui\run.bat`。**両方に `vendor` の用意を足した** ── `gui/vendor/` は git に入れていないので、無いまま起動すると窓は開くが中身が真っ白で、原因が「落としていない」だと画面のどこにも書いていない。README にも書いた | `gui/run.bat ~ node vendor.js` |
| 188 | 2026-09-06 | 描く側の例外が、どこにも出ない | 読み込みの途中で落ちると窓が真っ白になるだけで、端末には何も出なかった ── 開発者ツールを開くまで何が起きたか分からない（実際に白い窓を前に十分ほど探した）。`console-message` を端末へ流す | `gui/main.js ~ console-message` |
| 189 | 2026-09-06 | 読む面で Enter が改行にならない | `document` のキー処理が読む面の Enter まで拾い、**焦点を書く面へ飛ばして**いた。打っている場所は「絞り込みの欄」「書く面」の二つだと数えていて、読む面を数え忘れていた | `gui/renderer.js ~ const inRead` |
| 190 | 2026-09-06 | 読む面で表・注記・引用も書きたい。特に表は行と列を足したい | 表と注記を**触れるかたまり**にした（字に戻せる形をしているので、触らせない理由が無い ── 触れないままだと「読む面だけで完結できる」が嘘になる）。表は caret が入ると**すぐ上に道具が出る**（行/列を足す・消す、寄せ）── **縦棒を数えさせない**。空の升は全角空白で埋める（中身が空の升は描く側によっては消え、消えた表は「作れなかった」に見える）。注記の種類の札だけは打てなくする ── 「注意」を「ちゅうい」に直せると、それは記法を壊す | `gui/renderer.js ~ function tableDo` |
| 191 | 2026-09-06 | 絞り込みに「タグ（複数）」「フォルダ」「期間」がほしい | タグはいくつでも選べ、**選んでいる途中を出す**（出さずに複数選ばせると、押した数を数えることになる）。期間は `updated:` のような記法を増やさない。日付も打たせない（「先週」を `2026-08-30..` に直すのは人の仕事ではない）。**三つを一度に一つしか選べなかったのは 2026-09-06 に直した**（依頼 294）── 三つが引き出しとして常に並び、いくつでも重なる | `gui/renderer.js ~ ここは三つの引き出しが常に並び、いくつでも重なる` |
| 192 | 2026-09-06 | 「xxx…」で終わる札が多すぎる。3点リーダーを一律で無くす | 「…」は「続きがある」の符牒だが、**何が起きるかは言っていない**。`タグを付ける…` → `ノートのタグ設定`、`フォルダを削除…` → `このフォルダをゴミ箱へ` のように、押すと何が起きるかを札そのものに書いた。**畳んだ道具の帯の「…」だけは残る**（依頼 215）── あれは札ではなく「まだある」という印で、押すと開くことは形が言っている。**依頼 213 で札を付け替えたとき、ここが鳴った** ── 約束は生きたまま札だけ変わった二度目の例 | `gui/renderer.js ~ このフォルダを削除` |
| 193 | 2026-09-06 | ⋯ と ⚙ の違いが分からない。歯車が小さすぎる | **形ではなく言葉で分ける** ── 点三つを「ノート ▾」にした。歯車は絵なので、字と同じ大きさだと小さく見える（19px に）。置き場所を訊く小窓は、**いまどこを見ているかを先に出す** ── 「amber のディレクトリはどう決めるのか」が分からなかったのは、決める場所が無かったからではなく、いまどこかが画面に出ていなかったから | `gui/index.html ~ ノート ▾` |
| 194 | 2026-09-06 | mermaid の図が芋臭い。IT 素人でも図を作れるように | 既定の紫と水色は**ノートの地とも琥珀とも合わない**（図だけ別のアプリから貼ってきたように見える）── `base` にテーマの色を渡し、いま出ている色で描かせる。作る側は**よく使う四つだけ**（流れ図・分かれ道・やりとり・円グラフ）を選ばせ、言葉を訊いて骨組みを入れる ── 込み入った図まで画面に載せると「難しいもの」に見えて誰も押さなくなる | `gui/renderer.js ~ async function cmdDiagram` |
| 195 | 2026-09-06 | Monaco の色分けが本体スレッドで走っていた | CSP に `worker-src` が無く、`script-src` が代わりに使われて `blob:` の worker が弾かれていた。通したあとは、Monaco が組み立てる道が `file:///vendor/…`（ファイルシステムの根から）になって取り込みに失敗 ── **絶対の道で渡す**。どちらも動くので気づかないが、長いノートで窓が固まる。`console-message` を端末へ流して初めて見えた | `gui/renderer.js ~ getWorkerUrl` |
| 196 | 2026-09-06 | amber の外にある `.md` を、**単発で**開きたい。左の一覧は出さず、閉じたら元に戻る | 置き場所は動かさない ── 一本開くたびに一覧が入れ替わると「さっきまでのノートが消えた」に見える。索引にも混ぜない ──「フォルダがそのまま索引」の外にあるものを索引に入れると、索引が索引でなくなる。**異例な開き方だと画面が言う**（左の二列を出さず、上に帯と「閉じて一覧に戻る」）── 言わないと、いつもの一本と見分けが付かないまま別のフォルダへ書く。題は前端で決めず `note::read` に訊く（core に一本用の口 `note` を足した）── ここで自分で決めると、外の一本だけ別の題になる | `gui/renderer.js ~ async function openGuest` |
| 197 | 2026-09-06 | `.md` を窓に落としても同じ開き方にしたい | 置き場所の**中**のものは、いつもの一本として開く ── 同じファイルが一覧と客の両方に居ると、どちらに書いたのか分からなくなる。**Electron 32 から `File.path` は無い**ので `webUtils.getPathForFile` に訊く（描く側に「好きなファイルの道を知る」口は作らず、落とされた `File` を渡して訊くだけ）。ついでに `amber note.md` と Finder の「このアプリケーションで開く」も同じ道に繋いだ ── `open-file` は `whenReady` より先に鳴ることがあるので、来た順に溜める | `gui/main.js ~ open-file` |
| 198 | 2026-09-06 | 表示面で打つとチェックリストが消える | **字に戻す処理が、画面のほうを壊していた。** 升（`.box`）と入れ子の箇条書きを `remove()` してから読み、入れ子だけ付け直して**升は付け直していなかった** ── 一度書き戻すと升が画面から消え、次の書き戻しではただの `- やること` になって、ファイルからチェックが消えた。**読むだけで済むものを、動かして読む理由は無い** ── 一切触らずに読むように直した | `gui/renderer.js ~ 画面には一切触らない` |
| 199 | 2026-09-06 | 面の名前を「表示 / 並べて表示 / コード」に | 「読む・書く」は**こちらの都合の言葉**で、使う人から見れば片方は組んだ姿、もう片方は Markdown そのもの。並び順も、ふだん見る順（表示が先）にした | `gui/index.html ~ 並べて表示` |
| 200 | 2026-09-06 | フィルタは打たせない。「言葉で探す」「題」「含まない」は要らない | 言葉で探すのは上の欄がやる ── 同じことを二か所で頼めると、どちらが効いているか分からなくなる。タグもフォルダも**そのものを指す**（名前を打たせない）── 打たせると打ち間違いが「見つかりません」として返り、間違いに見えない。2026-09-06 に小窓ごと引き出しへ移したが、**押して選ぶ・打たせない**はそのまま | `gui/renderer.js ~ 押して付け外し（全部付いたものだけ）` |
| 201 | 2026-09-06 | ノートの既定は「表示」に | 開いてまず見たいのは組んだ姿で、記号の並びではない ── 書きたくなったらその場で打てる | `gui/renderer.js ~ let view = 'read'` |
| 202 | 2026-09-06 | 太字にしたあと、続けて打った字まで太字になる | 選んだところに `<strong>` を掛けると caret はその中に残る ── **飾りのすぐ後ろへ出す**。選んでいなければ「ここから太字」の意味なので、そのまま | `gui/renderer.js ~ 書式の外へ caret を出す` |
| 203 | 2026-09-06 | 水平線や表でノートが終わると、その下に書けない | 表の外側は表の一部ではないので、矢印でも出られない ── **末尾にいつも降りられる一行を置く**。空のままなら字に戻すときに落ちるので、増えも減りもしない | `gui/renderer.js ~ function tailStop` |
| 204 | 2026-09-06 | 図がカーソルの場所ではなくノートの一番下に入る | 帯のボタンを押した瞬間、焦点はボタンへ移り選択も消える ── そのとき「どこか」を訊いても分からず末尾に落ちる。**動いたときに憶えておく**。ボタンは焦点を奪わない（`mousedown` を止める）。入れたあとは、入れたものの次に caret を降ろす | `gui/renderer.js ~ let caretAt` |
| 205 | 2026-09-06 | 図を続けて入れると、前の図が消える | 組み直しで `<pre>` を `<div class="mermaid">` に**掛け替えるとき、元の字を持っていかなかった** ── 字に戻すとき中身がどこにも無く、保存のたびに図が消えた（絵も同じ）。掛け替えるところは必ず持っていく。札を配るのは掛け替えより**先**。そして**元の字が取れないときは書き戻さない** ── 空を返すと黙って消え、気づくのは何回か保存したあと | `gui/renderer.js ~ function keepMark` |
| 206 | 2026-09-06 | 太字にしても保存されない | `styleWithCSS` を真にしていたので、太字が `<b>` ではなく `<span style="font-weight">` になり、字に戻すとき飾りが落ちていた（色のために真にしていた）。**飾りは札で、色は `<font color>` で読む** | `gui/renderer.js ~ styleWithCSS` |
| 207 | 2026-09-06 | 円グラフの見た目が悪い | 既定の派手な12色は琥珀の隣で喧嘩する ── **フォルダと文字色に使っている11色と同じ並び**を渡し、アプリのどこを見ても同じ色の家族にする。境目は地の色で抜き、割合は中に置く | `gui/renderer.js ~ pieStrokeColor` |
| 208 | 2026-09-06 | crmaine に同梱するので、Windows 用の `amber-server.exe` が要る（配布先はネットに出られない社内端末） | 依存はすべて純 Rust だった（`zlib-rs` で C の zlib すら要らない）── `chrono` の Windows 経路だけが `dlltool` を要るので mingw が要る。**組むのは CI だけ**（Windows の上で組み、その場で一往復させ、DLL も見る）。mac からのクロスビルド（mingw）も一度作ったが**捨てた** ── 走るところを確かめられず（mac の上で Windows の実行ファイルは動かない）、見る DLL も CI の四つに対して十三で、**確かめられないほうを残す理由が無かった**。道具（mingw-w64、1.4GB）も要らなくなった | `.github/workflows/release.yml ~ amber-server-win-x64.exe` |
| 209 | 2026-09-06 | 落として置くだけで動くこと（DLL を連れていないこと） | 既定の MSVC ビルドは `VCRUNTIME140.dll` を要求していた ── 素の Windows には無い。**`crt-static` で CRT ごと取り込む**。この食い違いは**走らせるだけでは出ない**（CI の runner には再頒布可能パッケージが入っていて、往復試験は通ってしまった）── 捕まえたのは DLL の検査だけで、入れておいてよかったもの | `.github/workflows/release.yml ~ crt-static` |
| 210 | 2026-09-06 | 変換の Enter で、小窓まで確定してしまう | 「さかな」→「魚」を Enter で確定した瞬間に小窓が閉じ、「魚の目」と打つつもりが「魚」というフォルダが出来る。**変換中の Enter は変換の確定であって返事ではない** ── `isComposing`（見ない環境では `keyCode 229`）で見分ける | `gui/renderer.js ~ e.isComposing` |
| 211 | 2026-09-06 | サブフォルダを「/」で一気に作らせたくない。右押しから作りたい | 「仕事/2026 と書けば階層になります」は**書き方を知っている人にしか通じない**。名前に「/」が来たら全角に落とし、階層は**親を右押しして作る**（フォルダもブックマークの置き場所も同じ形） | `gui/renderer.js ~ この中にフォルダを作る` |
| 212 | 2026-09-06 | 「お気に入り」を「ブックマーク」に。登録時に置き場所を選べて、その場で作れるように | 前は「入れる／外す」と「置き場所を選ぶ」が別の命令で、入れたあとにもう一度探して選ぶ形だった ── **フォルダへ移動と同じ一手**で済む話。「棚」という言い方もやめた（何のことか画面が説明していなかった） | `gui/renderer.js ~ どこに登録しますか` |
| 213 | 2026-09-06 | 献立の名前を、することの名前に揃える | ノートの側は **ブックマークに登録 / ノートのタグ設定 / フォルダへ移動 / 通知設定 / エクスポート / ゴミ箱へ入れる**。amber の側は **テーマ / vimモード / 一括バックアップ / amber保存ディレクトリ変更 / コマンド一覧**（保存先の名前は依頼 511 で「保存ディレクトリの追加・変更・削除」に・本人の言葉・2026-09-12）。テーマと vim と保存先は、**いまどうなっているかを押す前に見せる** | `gui/renderer.js ~ 保存ディレクトリの追加・変更・削除` |
| 214 | 2026-09-06 | vim は歯車の中だけでいい。選んだら憶えていてほしい | 使うのはたいてい一人で、毎日見る帯に居座る値打ちは無い ── 道具の帯から外した。入切は憶える（前から憶えていたが、献立に**いまどちらか**を出すようにした） | `gui/renderer.js ~ async function cmdVim\(` |
| 215 | 2026-09-06 | 道具の帯を整える。コードとコード枠は要らない。フローは画像の隣に | 図は「使う人は使う」もので、**二列目に畳んでおくとあることに気づかれない**。チェックリスト・番号リストは名前のとおりに呼び、番号はチェックの隣へ。畳んでいるときの札は「…」 | `gui/renderer.js ~ ['フロー', '', cmdDiagram]` |
| 216 | 2026-09-06 | 図の種類を増やしたい（キャッチーなので） | 流れ図・分かれ道・**マインドマップ**・**年表**・**予定表**・**四象限**・やりとり・円グラフ の八つ。**書き方は覚えなくていい** ── 言葉を訊いて骨組みを入れる。年表と四象限は日記や「やることの整理」と相性がよく、mermaid を知らない人がいちばん喜ぶところ | `gui/renderer.js ~ quadrantChart` |
| 217 | 2026-09-06 | 文字色は要らない | Markdown に文字色は無く、`<span style>` は GitHub で落ちる ── 「読める形は残る」とはいえ、**記法を増やしてまで残す機能ではなかった**。フォルダの色は残す（あれはフォルダの見分けで、文の飾りではない） | `gui/renderer.js !~ cmdPaint` |
| 218 | 2026-09-06 | mermaid の図は、ふつうの人には更新がクソ難しい。右押しで、図を見ながら直せるように | 作るところ（`cmdDiagram`）は選ぶだけで済むのに、**直すところが字だけ**だった ── `flowchart LR` も `A --＞｜はい｜ B` も覚えていない人にとって、一度作った図は「作り直すしか手が無いもの」で、それは作れると言えない。押すと工房が開き、**左に表・右に図**。表を直すとその場で描き直るので、当たっているかを**保存する前に目で確かめられる**。線の行き先は箱の名前から選ばせる（`A`、`B` のような合言葉を人に打たせない ── 打たせると、消した箱を指したままの線が残る）。押しても右押しでも開く ── どちらだったかを覚えている人はいない | `gui/renderer.js ~ async function studioOpen\(` |
| 219 | 2026-09-06 | 手で書いた凝った図でも、工房を開けないことが無いように | 枝の枝や囲みのある図は表にできない ── そこで閉め出すと「amber で作った図しか直せない」になる。**読めない形は表にせず、字の面を出す**（右の絵は同じように見えながら直せる）。読めなかったことは画面が言う（黙って半分だけ読んだ表を出すと、直して保存した瞬間に読めなかった行が消える ── 気づくのは何日も後） | `gui/renderer.js ~ function studioRaw\(` |
| 220 | 2026-09-06 | 図を直しても、触っていないところが変わらないこと | **表で直せる、とは「字に戻せる」ということ。** 組み直した字が一字でも違えば、その差は保存の瞬間にノートへ入る。`cmdDiagram` の出す八つ全部について「読んで、組み直して、元と同じ」を機械で見る。**二度目も同じか**まで見る（工房は開いて閉じてまた開かれる）── 表にしない行（`dateFormat`、`x-axis`…）は元の字のまま取っておいて戻す | `scripts/diagram-test.js ~ 作った図が、そのまま戻るか` |
| 221 | 2026-09-06 | 幕の上に出したボタンが、押しても効かない | `#top` は窓ごと引きずるための帯（`-webkit-app-region: drag`）で、**上に重ねたものより先に押しを取る ── 重ね順は効かない**。工房は上から 4vh に出るので帯とちょうど重なり、「この形にする」も「やめる」も押せなかった。幕の側で切る | `gui/index.html ~ 重ね順は効かない` |
| 222 | 2026-09-06 | マインドマップと予定表の見た目が悪い | マインドマップだけは `themeVariables` を見ず、灰と藤色で描かれていた ── 琥珀のノートの上で**そこだけ別のアプリ**に見える。枝は円グラフと同じ十一色にし、まん中は琥珀そのものに（色の並びは一か所 `FAMILY` に置いた ── 図ごとに別の並びを持つと、同じノートの中で色の意味が変わる）。予定表は既定の幅だと目盛りが重なって日付が読めない（`09/1009/12…`）── **広く描いてから、入るところまで縮める** | `gui/renderer.js ~ const FAMILY` |
| 223 | 2026-09-06 | 「新しいノート ＋」が絶妙にダサい | 全角の「＋」は字なので、書体によって太さも位置も変わる ── 線で描いた印に替えた。琥珀に薄い階調と、琥珀色の影を一枚。押すと 1px 沈む（押した手応えが無いと、効いたかどうかを画面のどこかで探すことになる） | `gui/index.html ~ #new:active` |
| 224 | 2026-09-06 | ルーラー・行番号を歯車から入切したい | **行番号は入れた**（長いノートで「何行目」と言い合うときに要る）。憶えるので、次に開いても同じ。**ルーラーは入れていない** ── 桁で折り返す決まりがノートには無く、表示面はもともと 34em で measure を止めている。要る日が来たら言ってほしい | `gui/renderer.js ~ async function cmdLineNo\(` |
| 225 | 2026-09-06 | 「新しいノート ＋」が絶妙にダサい。案を見せてから作ってほしい | 四案を実寸・実色・三テーマで並べて見てもらい、**案一**が選ばれた。古く見えていたのは色ではなく**塊であること**で、階調と琥珀色に光る影は、その塊を立体に見せるための飾りだった。塊をやめ、琥珀は 22px の丸だけに残す ── 面積が小さいぶん色は強く効き、字も高さも列の他の行と揃うので、左の列が一本の並びとして読める。押せることは丸の形が言うので、地も枠も要らない | `gui/index.html ~ #new .ring` |
| 226 | 2026-09-06 | 歯車の「いま：」は要らない。右詰めに。vim はオン／オフで | 献立に出ている値は、これから選ぶ値ではなく**いまの値**しかありえない ── 一行ごとに同じ二文字を読ませる意味が無い。右端に揃えるのは、名前のすぐ後ろだと命令ごとに違う位置で始まって目が毎回探しに行くから。行番号も「出している」ではなく**オン／オフ**に揃えた ── 同じ形の設定が二つの語彙を持つほうが分かりにくい | `gui/renderer.js ~ vimOn \? 'オン' : 'オフ'` |
| 227 | 2026-09-06 | 初回起動時に、既定のノートを同梱して表示してほしい（Inkdrop のように） | 何も無い一覧を前にすると、Markdown を知らない人は「何ができるのか」をどこからも知れない ── 説明を読ませるより、**読めて・押せて・書き換えられるノートが最初から入っている**ほうが早い。三枚（案内・覚え書き・設計）とフォルダ二つ。窓と iPhone は**同じ一組**（`packaging/welcome`）を使う ── 電話用に写しを置くと、片方だけ直った日に二つの amber が別のことを言う。置くのは一度きりで、**消したら二度と戻さない**（邪魔だから消したものが起動のたびに生えるのがいちばん嫌われる）。既にノートがある人には置かない | `gui/main.js ~ function seedWelcome` |
| 228 | 2026-09-06 | 見本の中身は『覚悟の磨き方』から。図はデザインパターン（ストラテジー） | 超訳の文そのものは池田貴将さんの著作なので写さず、**松陰本人の言葉**（『士規七則』『留魂録』、下田の歌 ── どれも公有）を引き、本は出会った場所として挙げた。設計のほうは分岐と差し替えを図で見比べ、**「二つしか無いなら `if` でいい」まで書く** ── パターンを知っていることと、そこで使うことは別のこと | `packaging/welcome/覚悟の磨き方.md ~ 士規七則` |
| 229 | 2026-09-06 | iPhone でも表と注記が読めること | 電話は表の行を空白で繋いで「縦棒つきの一文」として描き、`[!TIP]` を字として出していた ── **同じノートが二つの amber で別のものに見えていた**。`Block` に表と注記を足した（升に切るのは核の仕事で、どう描くかが電話の仕事）。表は狭めずに**横へ流す** ── 402pt に四列を押し込むと、升が二文字ずつの縦の列になる | `crates/amber-core/src/note.rs ~ Block::Table` |
| 230 | 2026-09-06 | 一覧の二行目に `**` が生で出る | 題は `inline` を通して飾りを落としていた（依頼 174）のに、二行目だけ取り残されていた ── `**大事**` は四つの星印を出したうえ、**そこは探すときに当たる文でもある**ので 大事 で見つからなくなる。引用と箇条書きの印も落とす（文であることは変わらない） | `crates/amber-core/src/note.rs ~ Nor is the notation` |
| 231 | 2026-09-06 | 図と枠の両方があるノートで、どちらかが描けない | 図の読み込みは `define` を伏せる一瞬を持っていて（AMD ローダに横取りされるため）、Monaco は枠に色を付けるとき言語を後から読みに行く ── 重なると `define is not a function` か「無名の define は一つまで」で落ちる。**旗（`define.amd`）だけ下ろすのでは足りない**（中の部品は関数の有無しか見ていない）ので、**枠の色付けを待ってから図を読む** | `gui/renderer.js ~ paintCode\(\).then\(drawDiagrams\)` |
| 232 | 2026-09-06 | 題が「a」一文字になる | 帯には面の札も字数もタグも並ぶのに、縮む床が題にしか無かった ── そこが全部のしわ寄せを引き受けて「amber へようこそ」が「a」になった。題に床を敷き、縮むのは右端の meta のほうに | `gui/index.html ~ min-width: 9ch` |
| 233 | 2026-09-06 | 円グラフ・年表・マインドマップの色が芋臭い | フォルダの色（`PALETTE`）をそのまま図に流用していたのが原因 ── **7px の点と、円の三割を占める面は、同じ色でうまくいくものではない**。どれも明るさも鮮やかさも同じくらいで、並べると平らな帯になっていた（九〇年代の業務資料の色）。図の側は琥珀を一つめに置いて**明るさをわざと散らす** ── 深い青 → 明るい緑 → 沈んだ紫 → 明るい珊瑚と交互にしたので、隣り合う二つが必ず明暗で分かれ、色の見分けにくい人にも切れ端が数えられる。年表は `cScale` を渡していなくて**灰色の帯が並ぶだけ**だった。**この日の色そのものは半日で捨てた**（依頼 237）── 明暗を散らすために深い色を混ぜたら、今度は「濃くて沈む」になった。守られているのは色の並びではなく「**図とフォルダは別の家族**」と「年表に色を渡す」の二つなので、検査もそちらに張り直した | `gui/renderer.js ~ 'cScale' \+ n` |
| 234 | 2026-09-06 | マインドマップのまん中の色が野暮ったい | ただの丸い橙だった。濃い琥珀で塗り、一段明るい琥珀の輪を掛け、影を一枚 ── **枝より手前にあるものとして見えないと、そこが中心だと形が言っていない**。枝の字の色もこちらで決めた（渡さないと mermaid が塗りから作り、明るい琥珀の枝では「仕事」が薄い字で消えていた） | `gui/renderer.js ~ .mindmap-node.section--1 circle.basic` |
| 235 | 2026-09-06 | 枝葉にさらに枝葉を書けるように。枝の間にも字を置きたい | 字下げを深さの番号に直して読む ── **空白が二つでも四つでも「一段下」は一段下**なので、出てきた字下げを浅い順に並べて何番目かを取る（人が書いた図の空白の数を当てにしない）。工房では「→」で深く「←」で浅く、深さは字下げそのもので見せる（「2」と書くより、ずれている形のほうが枝に見える）。**親のいない孫は作らせない** ── 一段目の次にいきなり三段目を置くと mermaid はその枝を捨て、「足したのに図に出ない」という分かりにくい壊れ方になる。枝と枝のあいだに字は置けない（mermaid に線の札が無い）ので、そう画面に書いた | `gui/renderer.js ~ function settle` |
| 236 | 2026-09-06 | 分かれ道の既定を「左利きですか？」に | 「足りている？→出す／足す」は**何の話か分からない例**だった。ハサミなら、はい・いいえの先が具体的に違うものになるので、骨組みを見た瞬間に「自分の場合はこう」と置き換えられる | `gui/renderer.js ~ 左利きのハサミを使う` |
| 237 | 2026-09-06 | 円グラフの色が濃くて沈む。案を見せてほしい | 三案を同じ数字・同じ大きさで並べて見てもらい、**案一（やわらか）**が選ばれた。明るさを上げ鮮やかさを落とした淡彩 ── 濃い色を並べると円の面積の大半が暗い塊になり、**紙より図のほうが重くなる**。淡いので字は白ではなく濃い墨を載せる（明るい地に白は乗らない）。この十一色は年表の札とマインドマップの枝でも同じものを使う | `gui/renderer.js ~ '#F7BD5C', '#8FC8E8'` |
| 238 | 2026-09-06 | 年表の字が小さくて読めない | SVG の中では 14px だったが、`#read .mermaid` を**文の幅**（34em）に押し込んでいたので `useMaxWidth` が全体を 0.6 倍に縮め、画面上は 8px になっていた。**図は文の幅に従わない** ── 34em は目が一行を追える長さの話で、図はそもそも行ではない。図は図の幅で描かせ、はみ出すぶんだけ横に流す | `gui/index.html ~ 図は、文の幅に従わない` |
| 239 | 2026-09-06 | 四象限の題と軸の言葉を変えたい | 「やることの置きどころ」は何をする図か言っていなかった ── **やることの優先順**。軸も「大きい／小さい」ではなく **影響大／影響小**、「いま／いつでも」ではなく **期限近／期限遠**。四つの区画（すぐやる・段取りする・あとで・誰かに頼む）は新しい軸でも位置の意味が合う | `gui/renderer.js ~ x-axis 期限遠 --> 期限近` |
| 240 | 2026-09-06 | iPhone でも図を出してほしい | 電話は `flowchart LR` と四行の字を灰色の枠で出していた ── 窓が絵を出しているところなので、**同じノートが開く端末によって別のものに読めていた**。描くのは mermaid 自身（`WKWebView`）で、Swift で描き直さない ── 八種類ぶんの実装をもう一つ持てば答えがもう一組でき、食い違うのはマインドマップの配置や予定表の目盛りという**いちばん確かめにくいところ**になる。色も設定も窓と同じものを渡す。図のあるノートを開くまで 3.4MB は読まない | `ios/Cian/Drawing.swift ~ struct Drawing` |
| 241 | 2026-09-06 | 図が描けないときに、何も出さないこと | 束ねに mermaid が無い（`node gui/vendor.js` を通していない木から建てた）ときも、書けない図を書いたときも、**書いた字をそのまま出す** ── 空白を出すと、そのノートがこの端末でだけ中身を失ったように見える。描けない理由も一緒に出す | `ios/Cian/Drawing.swift ~ Say the source, rather than nothing` |
| 242 | 2026-09-06 | crmaine が握っている amber 側の約束を、黙って変えないこと | 四つ ── 描く側に見せている口の名前、エンジンを「画面の隣」に探すこと、印を `../packaging/amber-mark.png` と指すこと、画面の組み立て手順。**黙って変わると「札は出るのに何も起きない」という形で出る**（いちばん辿りにくく、しかも壊したこちらには何も起きない）。名前を変えるなという話ではなく、**変えるときに鳴る**ようにした ── 鳴ったら一声かけてから直す。口は crmaine が使う九つではなく**出している十六を丸ごと**約束にする（使われていないと思った一つを消した日に、それが向こうの九つだった、が起こりうる）。増えたぶんは落とさず「表に足すか決めてください」と言う | `scripts/contract.js ~ 同梱する側との約束` |
| 243 | 2026-09-06 | Release の mac が arm64 だけで、Intel の Mac で動かない | runner が Apple シリコンなので、素直に組むと arm64 の一枚しか出ない ── **落とした人の手元でだけ分かる壊れ方**。`lipo` で二つを束ねて universal に。束ねたと言い張らず、`lipo -info` に両方が居ることを CI で見る | `.github/workflows/release.yml ~ amber-server-mac-universal` |
| 244 | 2026-09-06 | Release に画面の zip がほしい（同梱する側が Monaco を自分で積まなくて済むように） | 同梱する側はいま「タグのソースを落として `npm install` → `node gui/vendor.js`」をしていて、**こちらがエディタの積み方を変えるたびに追随が要る**。積み終えた形で配れば、その追随が要らなくなる。`packaging/amber-mark.png` も同じ zip に入れる（画面は `../packaging/` を指しているので `gui/` だけでは印の出ない画面になる）。`node_modules` は入れない（Electron 本体 200MB 超）。**実行ファイルも入れない** ── 機械ごとに別で、置く場所は同梱する側が決めている | `.github/workflows/release.yml ~ amber-gui.zip` |
| 245 | 2026-09-06 | 不具合が人づてに回ってくる（社内 Windows 端末に配られるので） | 戻ってくる報せはたいてい「amber が変です」だけで、どの版のどのエンジンかは書いていない ── 歯車に **amber について**（版・エンジン・置き場所）を置いた。**画面とエンジンを別々に出す** ── 同梱するときは実行ファイルだけ差し替えられるので二つはずれうる（この機能を作った画面で、実際に 2.4.0 と 2.3.0 がずれていた） | `gui/renderer.js ~ async function cmdAbout` |
| 246 | 2026-09-06 | iPhone の画面が窓と全然違う。最上段は歯車だけ、そこに小さな印 | 前は最上段にフォルダ＋・ノート＋・並べ替えの三つが並び、その下に大きなアイコンと「amber」の見出しがあった ── **どちらも窓には無い形**。作るのは一覧の頭のボタン、並べ替えとフィルタはその下、最上段は歯車と小さな印だけ。アプリの名前は窓の外（Dock・ホーム画面）が既に言っているので、中で二度言うぶんだけノートが下がる | `ios/Cian/ContentView.swift ~ 最上段は歯車だけ` |
| 247 | 2026-09-06 | iPhone も「すべてのノート・ブックマーク・フォルダ・タグ」の順に | 窓の左の列と同じ順に並べた ── **二つの amber で同じものを同じ場所に探せる**。タグは窓にあって電話に無く、探す欄の提案としてしか出ていなかった（「どんなタグがあるか」を見るのに一度打ちはじめる必要があった ── タグは思い出すものではなく、見て選ぶもの）。木（フォルダごとの表示）は既定を切りに ── 木はフォルダの段とノートの段で同じものを二度描くので、窓と同じ形にはならない。要る人は「フィルタ」から入れる | `ios/Cian/ContentView.swift ~ 窓の左の列と同じ順` |
| 248 | 2026-09-06 | iPhone に見本のノートが入っていない | 自動で置くのは**アプリ自身のフォルダを使っている初回だけ**にしてあった ── 既に自分のフォルダを選んでいる人（同期先を向けている人）に置くと、見ていないところに三枚落ちて、何週間かあとにどこから来たか分からないものとして見つかる。それでも「入れてくれ」と言える場所が要るので、設定に **見本のノートを入れる** を置いた。同じ名前は飛ばすので二度押しても増えず、**何枚置いたかを言う**（既にあると本当に何も起きない回があり、黙っていると押せていないように見える） | `ios/Cian/NotesStore.swift ~ func addWelcome` |
| 249 | 2026-09-06 | 「お気に入り」は iPhone にも残っていた | 窓では依頼 212 で「ブックマーク」に改名したが、電話は古い名前のままだった ── **同じものが二つの amber で違う名前で呼ばれていた**。「棚を変える」も「置き場所を変える」に揃えた | `ios/Cian/ContentView.swift ~ 依頼 212 で「お気に入り」から` |
| 250 | 2026-09-06 | バックアップは四種きっちり。既存に別のノートがある場合などを調べて | 窓は `scope: 'all'` を決め打ちで渡していて、**四つあることはエンジンしか知らなかった**（電話には四つとも出ていた）。すべて／フォルダ／タグ／ノート一枚を訊くようにし、四つとも往復で確かめた。**戻すときはいまあるものを消さない** ── 同じ名前があれば今のを残し、何枚避けたかを言う（言わないと「戻したつもりで戻っていない」に見える）。確かめて出た不具合が二つ ── ①復元の一時置き場が pid だけで名前を付けていて、**二つ同時に走ると混ざる**（片方がもう片方の展開途中を消しうる）②フォルダの zip を戻すと**フォルダごとではなく中身が根に散っていた**（形からはノート帳ぜんぶの zip と見分けられないため）── zip の中に「何の範囲か」の札を書くようにした | `crates/amber-core/src/api.rs ~ 範囲の四つは_それぞれ違うものを包む` |
| 251 | 2026-09-06 | バックアップから戻す口が窓に無い | バックアップは取れるのに戻せない、という片道になっていた（電話には両方ある）。エンジンの `restore` は前からあり、窓が呼んでいなかっただけ | `gui/renderer.js ~ async function cmdRestore` |
| 252 | 2026-09-06 | 外のファイルが反映されるようにしてほしい | **同じフォルダを二つの端末で触るのがこのアプリの前提**なのに、外から書き換えたものは窓を開き直すまで出てこなかった ── iPhone で書いた一行が Mac に現れず、「同期していない」ように見える（同期はしていて、見ていなかっただけ）。どのファイルがどう変わったかは読み解かない ── **「何か動いた」だけを伝えて、数え直すのは描く側**。打っている途中のノートは触らない（いま書いているものを向こうの版で黙って置き換えるのが一番悪い） | `gui/main.js ~ function watch` |
| 253 | 2026-09-06 | iPhone の置き場所も窓と同じく一つに | 電話には「開いてきた場所」が八つ並んでいて、**二つの amber で「いまどこに書いているか」の答えが違う形**をしていた。探すのが大変なのは変わらないが、それは選ぶ画面の話で、選んだあとに八つ並べておく話ではない。戻る道（この iPhone の中に戻す）だけ、いまの場所の隣に残した。**その後、依頼 511（2026-09-12）で本人が「複数でよい」と撤回** ── いまは保存ディレクトリごとに「この iPhone の中にする」がある | `ios/Cian/Where.swift ~ この iPhone の中にする` |
| 254 | 2026-09-06 | ノートに取り消し／やり直しを。上の帯に印を | **Monaco の積み木には任せられない。** 表を入れる・図を入れる・升を押す、はどれも `editor.setValue()` で組み直しており、`setValue` は Monaco の積み木を**まるごと捨てる** ── 押した直後に ⌘Z を押しても、そこには何も積まれていない。ノートの字そのものを積んで一本化した。積むのは**保存のたび**（打鍵の 0.9 秒後）で、一続きに打っているあいだは一つにまとまる ── 一文字ずつ積むと、消した一段落を戻すのに何十回押すことになる。**捕捉の段で受ける**（泡の段では、Monaco が自分の textarea で止めることがある）。別のノートを開いたら捨てる ── 別のノートの姿を戻せると、一度の押し間違いで二本まとめて壊れる | `gui/renderer.js ~ async function stepBack` |
| 255 | 2026-09-06 | フォルダの左に、ノートやタグと違うと分かる印を | 色を付けたフォルダにだけ小さな四角が出ていて、**色の無いフォルダとブックマークの棚は字の形しか違わなかった** ── 列は上から下へ読むので、段の見出しを覚えていないと「いま何の一覧を見ているか」が分からない。四つとも形を持たせた（受け皿・星・フォルダ・井桁）。**色は形の上に載せる** ── 色だけで分けると、色の見分けにくい人には何も分からない。名前から「★」「#」は外した（形のほうが言っている） | `gui/renderer.js ~ const RAIL_MARKS` |
| 256 | 2026-09-06 | ノートの履歴。何月何日の何時の姿に戻したい | **自動保存のアプリに履歴が要る理由は、自動保存だから** ── 消したことに気づいたときには、消えたほうが保存されている。取り消し（依頼 254）は窓を閉じれば消えるので、日をまたぐ後悔には届かない。**一世代は「書いていた一区切り」**（最後の打鍵から五分）── 保存のたびに残すと十分書けば数十世代になり、五十世代が一回の執筆で埋まる。置き場所は `.amber/history/`、ノートの隣のただのファイルなので**同期先にもそのまま運ばれる**（Inkdrop は最新の版しか同期しないと言っている）。消し方も**数えて言い切る** ── 新しい五十、または三十日以内（大きいほう）。ノートでもフォルダでも右押しから見られ、フォルダは中のノートの姿をまとめて時系列で出す。**戻す前にいまの姿を一世代残す** ── これが無いと「戻す」は取り返しのつかない操作になり、押すのが怖くなる | `crates/amber-core/src/history.rs ~ pub fn keep` |
| 257 | 2026-09-06 | 自動保存だと世代がすぐ埋まる。⌘S のような明示保存は要るか | **保存としては要らない。** 保存していないノートは窓の中にしか無いノートで、そこで iPhone を持ち上げると書いたものが無い ── 二つの端末で同じフォルダを触る、というこのアプリの前提が崩れる。升を押せばその場で書くなど、他のどこにも保存の概念が無い。**それでも人は反射で ⌘S を押す**ので、意味のある返事を用意した ── ⌘S は「ここを残す」。印の付いた姿は五十世代・三十日の勘定から外れ、古くなっても消えない。自動保存では作れない、人にしか分からない区切り | `gui/renderer.js ~ async function cmdKeepNow` |
| 258 | 2026-09-06 | iPhone にも図の工房を | **窓と同じ解析器を使う。** 八種類の形をもう一組 Swift で書けば答えがもう一組でき、食い違うのはマインドマップの深さや予定表の日付という**いちばん確かめにくいところ**になる。`gui/renderer.js` の読み書きを切り出して `JavaScriptCore` で走らせた ── `scripts/diagram-test.js` が同じ切り出し方で往復を見ているので、電話が使うのは**試験の通ったものそのもの**。WebView ではなく `JSContext` なのは、これがただの関数だから。窓は左に表・右に図だが 402pt にそれは入らないので、**上に図・下に表**。長押しで開く（電話に右押しは無い） | `ios/Cian/Studio.swift ~ enum Mmd` |
| 259 | 2026-09-06 | iPhone でも履歴を見て、戻したい | ノートの長押しから。フォルダの長押しなら中のノートの姿がまとめて出る。**決まりは core が持っている** ── いつ一世代にするか、何世代残すかが窓と違うと、片方の決まりで消したものをもう片方が残っていると思う。電話は並べて、見せて、戻すだけ。**見てから決める**（名前と日付だけで戻すかを決めさせない）。戻す前にいまの姿を一世代残す | `ios/Cian/Past.swift ~ struct Past` |
| 260 | 2026-09-06 | 題なしで作ると、上に日付の入ったノートが起動する | 依頼 174 で前書きから追い出した「書いた人が一度も言っていない名前」が、**ファイル名から戻ってきていた** ── 題が無いときの最後の受け皿がファイル名で、そのファイル名は amber が付けた `2026-09-06 13-07-22` だった。amber が付けた符号だけを見分けて題にしない（人が名づけた `買うもの.md` は、中身が空でも題でいい）。画面は **（タイトルなし）**、一行書けばそれが題 | `crates/amber-core/src/note.rs ~ pub fn made_up_name` |
| 261 | 2026-09-06 | iPhone の初期表示を窓と同じ「表示」に。表示のままでは何もできない | 作ったノートも「表示」で開くようにした。**「電話に `contenteditable` は無いので、一叩きで書く面に入る」で代えた ── その日のうちに間違いと分かった。** 無いのではなく、`WKWebView` の中でならある。逃げ道を作る前に、本当に無いのかを確かめるべきだった（依頼 266 で、窓の面そのものを電話で動かすようにした）。この行が残しているのは「作ったノートも表示で開く」の側 | `ios/Cian/ContentView.swift ~ 作ったノートも「表示」で開く` |
| 262 | 2026-09-06 | iPhone にブックマークとタグの段が初期表示で無い。フォルダ＋をボタン一つで | **一つも無くても段は出す**（窓と同じ）── 無いときに段ごと消えると、最初の一つを作る道がどこにも無くなり、「実装されていないのか、まだ無いだけなのか」が使う人には見分けられない。何をすれば増えるかも、その場に書いた。フォルダを作るのは献立の中に畳んでいたので、押しはじめる前に「どこにあるか」を探すことになっていた ── ボタン一つに | `ios/Cian/ContentView.swift ~ 一つも無くても段は出す` |
| 263 | 2026-09-06 | 窓のフォルダの色が線にしか付かず、分かりづらい | 15px の輪郭の色を見分けることになって、「色を付けた」と気づけなかった（電話は前から塗っている）。**塗る** ── 離れて見ても色で拾える | `gui/renderer.js ~ 色を付けたフォルダは、塗る` |
| 264 | 2026-09-06 | 窓にもベルマークを。通知は画面上部から | 通知は「このノートに」するものなのに、献立の奥にあった ── **仕掛けたことも、仕掛かっていることも見えない**。歯車の左に置き、仕掛けてあるノートでは鐘が琥珀になる（開くたびに献立を開いて確かめなくていい） | `gui/renderer.js ~ const BELL_ICON` |
| 265 | 2026-09-06 | 見本のノートを、いまの Mac と iPhone にも入れたい | 自動で置くのは**まだ一本も無いときの一度きり**（既にノートがある人のフォルダに三枚落とすのは、ただの散らかし）。それでも「入れてくれ」と言える場所が要るので、窓は歯車に、電話は設定に置いた。同じ名前は飛ばすので、二度押しても増えない | `gui/renderer.js ~ async function cmdWelcome` |
| 266 | 2026-09-06 | iPhone でも「表示」で打ちたい | **窓の面そのものを電話で動かす。** 窓の「表示」は `<div contenteditable>` で、そこに直接打てる ── 電話だけ読むだけだと、**同じ名前の面が二つの amber で別のもの**になり、打てると思って叩いた人は何も起きない画面を前に「壊れている」と思う。SwiftUI で書き直さない: 組む側は core の `to_html`、書き戻す側は `gui/renderer.js` から切り出した一組（`richBlock` … `inlineToMd`）── **書き戻しをもう一組 Swift で書けば、同じノートが端末によって別の字に保存される**。失うのはたいてい表と升と図で、気づくのは何回か保存したあと。そのために書き戻しを箱と字だけ見る形に直し、`scripts/paper-test.js` が往復を見ている。絵と図は `amber://` で Swift が給仕する（`WKWebView` は文字列から作った頁に隣のファイルを読ませない）── ついでに**ノートの外は配らない**ことがそこで保証できる | `ios/Cian/Paper.swift ~ struct Paper` |
| 267 | 2026-09-06 | 「表示」の面の書き戻しを、二つ持たない | 窓と電話が同じ一組を使うので、**片方だけ直った日に同じノートが別の字で保存される**ことが起きない。切り出せるように、`armPaper` と `paperToMd` は箱と字だけを受け取る形にした（画面のどこにも触らない）。往復は `jsdom` の上で見る ── 升を読んでも壊さないこと、図の元の字を持っていないときは**書き戻さない**ことまで | `scripts/paper-test.js ~ 打っても文字を失わないか` |
| 268 | 2026-09-06 | フォルダの色をカタカナ十一色に。グリーンはくすんでいる | 名前を**シアン・ターコイズ・ブルー・バイオレット・パープル・ベルガモット・カーマイン・アンバー・マスタード・グリーン・グレー**に（「みどり青」と「青むらさき」は、二つ並べたときにどちらがどちらか言えなかった）。グリーンは `#5E8C42` のオリーブ寄りで、緑というよりくすんだ黄土に見えていた ── `#3FA05C` に。**並びは core が持つ** ── 窓と電話に同じ表を書いていて、両方のコメントに「同じ並び」と書いてあったのに**十一色のうち六色がずれていた**（電話で付けた青が Mac では少し違う青で出ていた）。写しを持てば、いつかずれる | `crates/amber-core/src/notebook.rs ~ pub const PALETTE` |
| 269 | 2026-09-06 | iPhone のフォルダ＋と「フォルダ」が同じ意味。統合して、既存から選ばせて | 印と札が並んでいて、同じことを二度置いていた。押すと**フォルダの構成**が出て、そこで選ぶ／作る ── 名前をいきなり打たせない（たいていは既にあるものへ行きたい）。「フィルタ」からは新しいフォルダとフォルダの構成を外した（フィルタは絞る話で、作る話ではない） | `ios/Cian/ContentView.swift ~ フォルダのことは、この印一つに` |
| 270 | 2026-09-06 | フィルタの右の余白を触ると、フォルダ作成の小窓が出る | 一覧の段は、**中にボタンがあっても段ごと押せる** ── 右の余白を触ると最初のボタンが鳴っていた。段そのものに当たり判定を敷いて、押しをそこで止める | `ios/Cian/ContentView.swift ~ 右の余白は、何でもない場所` |
| 271 | 2026-09-06 | 一覧から右→左のフリックで、開いていたノートへ | 一覧から左へ払うのはノートを開くときと同じ向きの手ぶりなので、**待っているノートのほうを先に見る**（フォルダを一つ潜っただけの「進む」より） | `ios/Cian/ContentView.swift ~ 右から左は「開いていたノートへ戻る」` |
| 272 | 2026-09-06 | 見本のノートに絵の例を。フォルダもタグも作らないで | 入れた瞬間にタグの一覧とフォルダの一覧が汚れるのが嫌だった ── **前書きごと外して、直下に三枚**。絵は amber の印そのもの（`attachments/amber.png`）── 「隣の `attachments/` に置かれ、本文にはリンクだけが入る」を、実物で見せる。電話の設定に見本の入口が**二つ**出ていたのも消した | `packaging/welcome/ambər へようこそ.md ~ attachments/amber.png` |
| 273 | 2026-09-06 | 「前の姿（履歴）」は日本語として変。過去バージョンに | 「姿」はこちらの言い方で、使う人の言葉ではなかった ── **過去バージョン**、**現状バージョン保存**。「ノートのタグ設定」も、ノートの献立に出ているのだからノートは自明 ── **タグ設定** | `gui/renderer.js ~ name: '過去バージョン'` |
| 274 | 2026-09-06 | はい／いいえの小窓に、謎の入力欄が出る | 小窓は一枚しか無く（依頼どおり）、字を打つ欄が常に出ていた ── 二択の返事に欄があると「何か打つものがある」に見え、タグを外すだけの返事で人が止まる。**欄は残して、見せない** ── 上下と Enter を受けているのはそこなので、消すと鍵盤で選べなくなる | `gui/index.html ~ #sheet.bare input` |
| 275 | 2026-09-06 | 窓の鐘・戻す・やり直す・歯車が小さくて見にくい | 15px の線画を 27px の枠に置いていて、鐘か矢印かを見分けるのに目を寄せることになっていた ── 19px に、枠も歯車と同じ背丈に | `gui/index.html ~ 小さすぎた` |
| 276 | 2026-09-06 | 歯車から「フィルタ」は要らない | 絞るのは一覧の頭がやること ── **歯車は amber の設定で、「いま何を見るか」は設定ではない**。2026-09-06 に引き出しへ移したので、いまは命令の表からも外した（三つが一覧の頭に常に出ているものを、命令からも呼べると道が二つになる）── こよみだけは命令として残す | `gui/renderer.js ~ \*\*絞り込みは、命令ではなくなった。\*\*` |
| 277 | 2026-09-06 | 窓のチェックリストを押すと、改行されて表示される | 箇条書きは DOM を直に触るので caret が残り、チェックリストは字を組み直すので**次の行へ降りて**いた ── 同じ帯の隣り合う二つが違う振る舞いをしていた。「この行をそうする」道具は、その行の終わりに残る | `gui/renderer.js ~ landAfter\(at, stay\)` |
| 278 | 2026-09-06 | はい／いいえの小窓は一律おかしい（ゴミ箱・見本・タグ外し） | 小窓は一枚しか無く、字を打つ欄が常に出ていた。**四つまでのときは見せない** ── 四つを絞り込む人はいないし、二択に欄があると「何か打つものがある」に見えて人が止まる | `gui/renderer.js ~ 四つまでのときは、文字を打つ欄を見せない` |
| 279 | 2026-09-06 | iPhone のノート長押しに「削除する」が無い | 消し方は横払いしか無く、**長押しの献立に無い＝消せない**と読める（実際にそう読まれた）。窓の献立にも入っているもの。取り返しがつかないので、必ず訊く | `ios/Cian/ContentView.swift ~ 長押しからも消せる` |
| 280 | 2026-09-06 | iPhone の表示モードで、升の行で改行すると箇条書きになる | `contenteditable` の既定は「同じ種類の次の行」だが、**升は付いてこない**ので `- ` だけの行が生まれる ── 押した人は升を足したつもりで、出てきたのは点。まず「何も無い行に降りる」にした。**その「何も無い行」は 2026-09-06 に本人が取り下げた**（依頼 285）── 点も番号も「次も同じ、空なら降りる」なのに升だけ違い、**やることを続けて三つ書けなかった**。取り下げなので、張り直しではなく**約束そのものが変わっている**。判断は窓と電話で一つ（`checkEnter`） | `gui/renderer.js ~ セルの行で改行したら、次もセル` |
| 281 | 2026-09-06 | iPhone の表示モードにも入力サポートの帯を | 打てる面なのに記号の入れ方が無いと、`#` や `- [ ]` を覚えている人にしか使えない ── 電話の鍵盤にその記号は出ていない。**書く面より少なく出す** ── `execCommand` で確かにできるものだけ（押しても何も起きないボタンは、あることより悪い）。升だけは `execCommand` に作れないので、箇条書きにしてから升を自分で置く | `ios/Cian/Writing.swift ~ 道具の帯は、表示の面にも要る` |
| 282 | 2026-09-06 | 表の見本に、項目の列にも中身を入れて | 空の升は「これで合っているのか」が分からず、打つ前に一度立ち止まる ── 見本の字（`項目`）が入っていれば、上から順に置き換えるだけになる | `gui/renderer.js ~ セルを空で出さない` |
| 283 | 2026-09-06 | 太字・斜体などのショートカットを Inkdrop から | **⌘K リンク・⌘' 引用・⌘1〜4 見出し**（Inkdrop が持っているもの）に揃え、表に ⌘⇧T を足した。書いていて分かったのは、**鍵盤からの一打が読む面で効いていなかった**こと ── 一打の表が帯とは別にもう一組あり、そちらは書く面用しか持っていなかった（⌘B が帯からは効いて一打からは効かない、という見分けの付かない差）。**表は一つ（`MARKS`）**にして、帯に書いた鍵をそのまま引く。見出しの深さは鍵盤からは一打で、帯のボタンは押すたびに深くなるまま（Inkdrop も `toggle-heading-1` … `-4` を別々に持っている） | `gui/renderer.js ~ 表は一つ（\`MARKS\`）` |
| 284 | 2026-09-06 | 表の中で Tab を押したら次の升へ | 表を打つのは「升から升へ」で、行を打つのではない ── 既定の Tab は焦点を面ごと出してしまい、打っている途中で表から追い出される。最後の升で押したら**行を一つ足す**（足し方を探しに行かせない）。caret の居場所は字の節のことも升そのもののこともあるので、両方見る | `gui/renderer.js ~ 表の中の Tab は、次のセルへ` |
| 285 | 2026-09-06 | 窓もiPhoneも、升の行で改行したら次も升に（空でもう一度押したら何も無い行） | 点も番号も `contenteditable` の既定がそうしている ── 押せば次の行が同じ形で出て、空でもう一度押すと降りる。**升だけが違っていた**。揃えるのは形ではなく**押し心地**。判断は切り出し（`checkEnter`）に置き、窓も電話も同じ関数を呼ぶ ── ここで別々に書けば、同じノートを窓で足すと升・電話で足すと点、になる | `gui/renderer.js ~ セルの行で改行したら、次もセル` |
| 286 | 2026-09-06 | ショートカットはどうやって確認したらいい（マークダウンのぶんも） | 鍵は帯の吹き出しと ⋯ の献立に散らばっていて、**全部を一度に見る場所が無かった**。⌘⇧/ で一枚に出す ── 記号・窓のこと・一覧の中、の三段。選べばその場で走る（読むだけの紙にすると、見ながら手で打ち直すことになる）。ついでに、献立が `⌘⇧/` と書きながら**受け口がどこにも無く**、押すと左の列が畳まれていたのも直した | `gui/renderer.js ~ ショートカットの一覧（⌘⇧/）` |
| 287 | 2026-09-06 | ノートを選んで Delete でゴミ箱の小窓を | 一覧を見ているときの Delete は、机の上ではどのアプリでも「これを消す」── 受けていないと、右押しか ⋯ を開くしかない。**必ず訊いてから**（取り返しがつかない）。打っている場所では上で戻しているので、ここに来るのは一覧を見ているときだけ | `gui/renderer.js ~ 消すのは、必ず訊いてから` |
| 288 | 2026-09-06 | 下の道具箱に絵かアイコンを → **やめた**（2026-09-08） | 絵を足し、名前も残した（絵は入口、名前は答え合わせ）。**同じ日に二度動いた** ── 午前「訳の要る字を帯に並べたくない」で絵だけにし、午後「かっこよくなったが、ビギナーには何がなんだか分からない」で**字だけ**に戻した（絵も外した ── 名前が答えを言っているところに絵を添えても、目が二度読むだけ）。**分かることを、訳の都合で捨てない** ── 外へ配る日は、字をその国の言葉に置き換えればいい。`⌗` が「注記」だと分かる人は、どの国にもいない。**この行は残す** ── 「まだ作っていない」と「作って、やめた」は同じ顔をするので。**2026-09-09 に例外を一つ作った**（依頼 419 の絵文字のボタン）── 規則はここに生きていて、例外はあちらに名前と理由が書いてある | `gui/renderer.js ~ b.textContent = name;` |
| 289 | 2026-09-06 | ノートを打っているとき、一覧や左の列は一度押しても移らない（二度押しで移る） | `click` は押し下げと離しが同じ節に当たって初めて出る ── 焦点が外れたことで保存が走り、一覧が組み直され、離す頃には押した行がもう別の節になっていた。**押し下げで開く**（机の上の一覧のふつうでもある）。焦点は移さず、開く前に読む面の打ち跡を先に字へ戻す ── 戻さないと最後の数百ミリ秒ぶんが黙って消える | `gui/renderer.js ~ \*\*押し下げで開く。\*\*` |
| 290 | 2026-09-06 | 上の ⋯ の「通知設定」はベルと重複しているので消して | 同じことを頼む道が二つあると、片方を直した日にもう片方が古いまま残る。ベルは常に上の帯に居て、押せば同じ小窓が出る。命令の表には残す（⌘⇧P から名前で探せる） | `gui/renderer.js ~ \*\*メニューには出さない。\*\* 上の帯にベルが居て` |
| 291 | 2026-09-06 | ブックマークの置き場所が削除できない | 置き場所は空でも残るように core が憶えている（`notebook::add_star`）のに、窓は `drop` を一度も呼んでいなかった ── ノートから外すだけでは中身の無い棚が並び続け、**消せないもの**になっていた。消したのは棚であってしおりではないので、中のノートはブックマークの直下へ移す。名前を変えるときも、下の階層ごと付け替えてから忘れる | `gui/renderer.js ~ \*\*棚そのものも忘れる。\*\*` |
| 292 | 2026-09-06 | 左の列のタグの「#」が全角みたいで格好悪い | 16px の枠に四本線を渡すと線が詰まって、字の「＃」が太って見えるだけになる ── 隣のフォルダも星も形で分かるのに、タグだけ記号を読ませていた。**札の形**にする。段の見出しの「＋」も同じ理由で線に直した（全角の記号は行の高さも幅も字に引かれて、一つだけ大きく沈む） | `gui/renderer.js ~ \*\*ラベルの形。\*\*` |
| 293 | 2026-09-06 | フィルタのタグで絞るとき、字を打つ欄は要らない | ここは押して選ぶ一覧で、打つところではない ── 欄があると「何か打つものがある」に見えて選ぶ手が止まる（はい／いいえの小窓で同じことが起きた、依頼 278）。同じ日に小窓ごと引き出しへ移した（依頼 294）ので、**打つところは初めから無い** | `gui/renderer.js ~ \*\*絞れることが、絞る前から見えている。\*\*` |
| 294 | 2026-09-06 | フィルタは案三（一段の帯）で。タグ・フォルダ・期間を重ねられるように | 三案のモックから本人が選んだ。**絞れることが、絞る前から見えている** ── 「フィルタ」という言葉を覚えなくてよい。三つの引き出しが一覧の頭に常に並び、いくつでも重なる（タグは全部・フォルダはどれか ── ノートは一つのフォルダにしか居ないので、フォルダを「全部」にすると二つ選んだ瞬間に必ず 0 件になる。どちらなのかは引き出しに書く）。選んだものは `tag:仕事` という字にせず、選んだものとして持つ。帯の札には**何で絞っているかを出す**（一つなら名前、二つ以上なら数）── 「タグ 1」では開かないと分からない | `gui/renderer.js ~ \*\*絞れることが、絞る前から見えている。\*\*` |
| 295 | 2026-09-06 | 毎回ぜんぶ出ているとボリュームが大きい。「ノートを探す」を押したら入力欄が出る形に | 帯と探す欄を並べて置きっぱなしにすると、一覧の頭が毎回二段ぶん要る ── **言葉で探すのは、絞るより回数が少ない**。畳んでおいて、ボタン（と ⌘F・`/`）で開く。閉じるときは**必ず空にする** ── 見えない絞り込みが残ると、一覧が減っている理由が画面のどこにも書いていないことになる。空のときの言い方も「見つかりません」から「絞り込みに当たるノートがありません」に分けた（外せば出てくることに気づけるように） | `gui/renderer.js ~ \*\*探す欄は畳んでおく。\*\*` |
| 296 | 2026-09-06 | 日付はカレンダーから、いつからいつまでのレンジで探したい | 「7日以内」のような決め打ちは、**去年の秋**を探せない。こよみを出して、押した日が範囲の端になる ── 次に押した日がどちらに入るかを先に見せ（「いつから」「いつまで」の札）、二度押しで範囲が決まる。片方だけでもよい（「この日から先ぜんぶ」が言えないと範囲は使いものにならない）。前後が入れ替わったら黙って入れ替える（始まりを言い直しているのがふつうで、0 件を返して考えさせる場面ではない）。前月・翌月のはみ出しも押せる ── 月末をまたぐ範囲はよくある。比べるのは秒ではなく `YYYY-MM-DD` の字：秒で比べると「6日まで」が 6日 0時 までになり、その日に書いたものが軒並み落ちる | `gui/renderer.js ~ \*\*いつからいつまでを、押して決める。\*\*` |
| 297 | 2026-09-06 | 新しいノートの題が、一行目を打っても（タイトルなし）のまま | 題は一行目から決まる（`note::title`）ので、一覧の二列目はすぐそう出ていた ── 帯だけが開いたときの値で止まっていて、**同じノートの名前が画面の二か所で食い違って**見えていた。読み直しのたびに書き直す | `gui/renderer.js ~ 開いたときだけでなく、保存のたびに書き直す` |
| 298 | 2026-09-06 | 「保存しました」が出ると、取り消し・やり直しを連続して押せない | 字数と状態が中身の長さぶんの幅を取っていた ── 字が出た瞬間に帯じゅうが左へずれ、いま押したボタンがカーソルの下から逃げる。**一覧の行が一度で開けなかったのと同じ「押した先が動く」不具合**（依頼 289）。幅の決まった枠に住まわせれば、中の字が変わっても外は動かない。1180px の既定の窓では帯が一杯で枠が 0 になったので、名無しの ☰（目次）と ⤢（拡大）を「ノート ▾」へ名前付きで移した ── 記号二つより、名前のほうが見つかる | `gui/index.html ~ \*\*文字数と状態は、幅の決まった枠に住む。\*\*` |
| 299 | 2026-09-06 | 太字などで飾ったあと、続けて打つ字まで飾られる | `execCommand` は選び目を `<b>` の中に残すので、そこから打てば中に入る ── **飾ったのは選んだ字であって、これから打つ字ではない**。飾りの終わりに立っているときだけ外へ出す（途中なら、そこは中の字）。選び目を動かすだけでは足りない ── ブラウザは「いま打つと何になるか」を別に憶えていて、飾りの隣で打った字をその飾りへ吸い込む。憶えているほうも消す。かな漢字は組み始めに出す（組んでいる最中に動かすと変換が壊れる） | `gui/renderer.js ~ 書式の終わりに caret があるなら、その外へ出す` |
| 300 | 2026-09-06 | 引用の中で、何も書かずに Enter を押したら引用から抜けたい | 中で改行すると引用が続くのは既定のとおりで正しい ── けれど**出る道が無かった**。点も番号も升も「空でもう一度押したら降りる」のだから、引用と注記だけ抜けられないのは覚え違いに見える。真ん中で押しても後ろの行は失わない（そこで箱を割り、注記なら種類の札を割った先にも付け直す） | `gui/renderer.js ~ 空の行に降りたら\*\*そこから出る\*\*` |
| 301 | 2026-09-06 | 注記が「表示」モードで直せなかった | 実は打てていた ── **打てないと言っていたのは見た目のほう**。注記と表は触れるようになったのに、指の形（`cursor: pointer`）と「押すと書く面へ飛ぶ」枠だけが**触れないかたまりのまま残っていた**。押すと別の面へ行く印が出ているところを、打てると思う人はいない。顔ぶれは `richBlock()` と揃える | `gui/index.html ~ \*\*注記と表は、ここから外した。\*\*` |
| 302 | 2026-09-06 | 電話版は窓版を正として、機能を揃えてほしい（⋯ の献立） | 電話の ⋯ には「タグ」しか無く、ブックマークもフォルダ移動も履歴も削除も**一覧まで戻って長押し**するしかなかった ── 開いているノートに対してすることなのに、開いている画面からは頼めない。窓の「ノート ▾」と同じ顔ぶれ・同じ順に並べる（ブックマーク・タグ設定・フォルダへ移動・エクスポート・目次・過去バージョン・現状バージョン保存・ゴミ箱） | `ios/Cian/Desk.swift ~ \*\*⋯ の顔ぶれは、窓の「ノート ▾」と同じ。\*\*` |
| 303 | 2026-09-06 | 一つ戻す・やり直すを電話にも（置き場所は電話の都合で決める） | UIKit の取り消しは「打った字」の取り消しで、見出しや升のように面を組み直したところで**積み木ごと消える**うえ、「表示」の面（`WKWebView`）には届かない。窓と同じくノートの姿を積む一本にした。**置き場所は窓と変えた** ── 窓の通り上の帯に六つ並べたら iOS が黙って二つ落とし、消えたのは「表示／コード」とベルだった（落ちたことはどこにも出ない）。下の帯なら幅も足り、**電話の親指は下に居る** | `ios/Cian/Desk.swift ~ \*\*一つ戻す／やり直すは、上の帯ではなく下の帯に。\*\*` |
| 304 | 2026-09-06 | 目次を電話にも | 何が見出しかは core が決める（`note::blocks`）── 電話が `#` を数えはじめると `#仕事` というタグの行が目次に出る。窓は右に居座る列だが、**電話は畳む** ── 393pt の幅で常に一列取るとノートそのものが半分になる。飛び先は desk が持ち、飛ぶのは面それぞれ（「表示」は `scrollIntoView`、「コード」は行頭へ caret）。前書きのぶんを引くのを忘れると、前書きのあるノートでだけずれる | `ios/Cian/Touring.swift ~ 電話は畳む` |
| 305 | 2026-09-06 | 現状バージョン保存を電話にも | 自動保存だと世代が打鍵の切れ目で決まる ── 「ここは残しておきたい」を人が言える道が要る。窓の ⌘S と同じ `keep`（間を置かず・印を付けて）で、印の付いた世代は数の勘定から外れるのであとから流れて消えない | `ios/Cian/NotesStore.swift ~ \*\*いまの姿を、一世代として残す。\*\*` |
| 306 | 2026-09-06 | マークダウンの書き方と amber について を電話の設定にも | 窓の歯車にあって電話に無かった二つ。記号を知らない人がいちばん先に困るのがここで、電話には帯のボタンしか道が無かった。電話では**入れる**のではなく**見せる**だけにした ── 設定の奥から辿ってくる頃には、打っていた場所を離れている（記号そのものは下の帯から入る） | `ios/Cian/Syntax.swift ~ 電話では\*\*入れる\*\*のではなく\*\*見せる\*\*だけ` |
| 307 | 2026-09-06 | 絞り込み（タグ・フォルダ・期間）を電話にも。窓と同じ一段の帯で | 三案から本人が選んだ形（依頼 294）を電話にも。一覧の頭に三つの引き出しが常に並び、いくつでも重なる ── **絞れることが、絞る前から見えている**。タグは全部・フォルダはどれか、期間はこよみで範囲（窓と同じ判断・同じ言い方）。**タグの段は畳んだ** ── 引き出しと段で同じことを頼む道が二つになるうえ、絞った瞬間に段が消えて一覧じゅうが上へずれ、次に押した指が別の行に当たった（窓の「押した先が動く」と同じ） | `ios/Cian/Sifting.swift ~ \*\*窓と同じ形\*\*` |
| 308 | 2026-09-06 | 「ノートを探す」欄も、窓に合わせて畳む | 帯と並べて置きっぱなしにすると、一覧の頭が毎回二段ぶん要る ── 言葉で探すのは、絞るより回数が少ない。閉じたら空にする（見えない絞り込みを残さない）。**`isPresented:` だけでは畳めなかった** ── あの糸が決めるのは「いま打っているか」であって欄が場所を取るかどうかではなく、`.navigationBarDrawer` は `.automatic` にしても**上まで戻れば必ず出てくる**。自前のボタンと並んで**同じことを頼む入口が二つ**になっていた（画面で見て気づいた）。`searchable` そのものを付け外しする | `ios/Cian/ContentView.swift ~ 探す欄を、\*\*押したときだけ\*\*出す` |
| 309 | 2026-09-06 | 窓にあって電話に要らないものを決めた（並べて表示・拡大・前後のノート・字数） | 「まだ作っていない」と「作らないことにした」は画面の上では同じ顔をするので、**どちらなのかを書いておく**。393pt では二面を並べても両方読めない／電話はもともと全画面／左フリックと開いたノートの帯があるのでたどった道は要らない／帯は四つで既に一杯（六つ並べたら iOS が黙って二つ落とした） | `ios/Cian/Desk.swift ~ \*\*電話に置かないと決めたもの\*\*` |
| 310 | 2026-09-06 | フローの各オブジェクトの色を、右クリックから変えたい | **箱そのものを右押しすると色の献立**（フォルダの色と同じ手ぶり・同じ十一色 ── 覚える色の名前を画面ごとに増やさない）。図の余白を右押しすれば、これまでどおり工房が開く。紙の上では**淡く塗って濃い線で囲む** ── 濃いまま塗ると中の字が沈み、読ませるには「どの色なら白い字か」を人が考えることになる。淡い地なら字はいつも同じ濃い墨でいい。工房の表にも「色」の列を足した（献立の名前だけでは色が分からないので、選び口そのものをその色で塗る） | `gui/renderer.js ~ 箱そのものを右押しして色を変える` |
| 311 | 2026-09-06 | その色が、ノートの字として往復すること | `style A fill:…,stroke:…,color:…` の一行で持つ ── GitHub でも同じ色で出る。**こちらが書いた形だけ読み戻す**（`NODE_STYLE`）── 手で書いた凝った `style`（線の太さや破線）を色だけの表に押し込むと、書き戻したときに残りが消える。読めない形なら表にせず字の面で直す。色は箱に付けて持つので、箱を消せばその色の行も消える（消した箱を指す線と同じ扱い）。読み書きは切り出しの中なので、電話も同じ字を書く | `scripts/diagram-test.js ~ 往復しても同じ文字` |
| 312 | 2026-09-06 | 箱の色を、電話の工房からも変えられること | 窓は右押しと工房の両方から変えられる ── 片方だけだと、電話で直したノートから色が変えられないことに気づけない（「電話版は窓版を正として」の続き、依頼 302）。献立の丸は `Colouring.dot` で描く ── `systemImage:` は accent で塗られて**十一個とも同じ色**になる | `ios/Cian/Studio.swift ~ \*\*箱の色も、電話から。\*\*` |
| 313 | 2026-09-06 | amber フォルダをクラウド（iCloud / Dropbox / Google Drive / OneDrive）に置けるように、選べる形で | どのサービスも机の上では**ただのフォルダ**なので、amber は同期の仕組みを一つも知らなくていい ── 知る必要があるのは「そのフォルダがどこにあるか」だけ。`~/Library/Mobile Documents/com~apple~CloudDocs` を覚えている人はいないので、名前で選ばせる。**入っているものだけ並べる** ── 入っていないサービスを並べると、押した人は「入れれば使える」のか「amber が壊れている」のか見分けられない。macOS 12 以降は外のサービスがぜんぶ `~/Library/CloudStorage` に並ぶので、そこを読んで人の言葉に直す（`GoogleDrive-taketan9@gmail.com` → `Google Drive（taketan9@gmail.com）`）。クラウドの直下には置かず、中の「amber」に置く（根っこにばら撒くと、ほかの物と混ざって二度と分けられない） | `gui/main.js ~ \*\*どのクラウドに置くかを、選べるようにする。\*\*` |
| 314 | 2026-09-06 | 置き場所を変えたら、いままでのノートも付いていくこと | **新しいフォルダは空のフォルダ。** そうと知らずに移した人は、書いたものが全部見えなくなったところに立たされる ── **電話は前から訊いていて、窓だけ訊いていなかった**（`migrate` は core にあり、電話だけが呼んでいた）。移せなかったら置き場所も変えない ── 半分だけ移った状態で向こうを見せると、残りが消えたようにしか見えない。数えるのは人が数えるもの（`migrate` が返すのは絵も履歴も含むファイルの数で、6 件のノートが「14 件」になる） | `gui/renderer.js ~ \*\*いままでのノートは、ひとりでには付いてこない。\*\*` |
| 315 | 2026-09-06 | クラウドのノートが、まだ落ちてきていないときに何も言わない | iCloud も OneDrive も「使うときに落とす」が既定で、iCloud はそのとき中身を消して `.買い物リスト.md.icloud` という**札**を置く。名前が違うので amber の一覧に出ず、**ノートが消えたようにしか見えなかった** ── 待てば戻ってくるだけなのに、それを言う口がどこにも無かった。数えて言い、電話は `startDownloadingUbiquitousItem` で落としてくるよう頼む（頼めなくても黙る ── iCloud に置いていないフォルダなら札は出ないし、出たのに頼めないのは向こうの都合で、人にできることが無い） | `crates/amber-core/src/cloud.rs ~ まだ落ちてきていない` |
| 316 | 2026-09-06 | クラウドが作った衝突の控えが、ただの新しいノートに見える | 二台で同時に書くと、amber 自身の衝突検査より先にクラウドが控えを作る。**一覧からは消さずに札を貼る** ── 消すと中身を助け出す道がどこにも無くなる。そして**当てずっぽうで札を貼らない**: `週報 2.md` は拾わない（iCloud も Finder もその名前で控えを作るが、本物のノートと字の上で見分けが付かない）。見逃した控えはただのノートとして読めるが、貼られた本物は「消していいもの」に見える ── 間違える向きを選ぶ | `crates/amber-core/src/cloud.rs ~ 当てずっぽうで印を付けない` |
| 317 | 2026-09-06 | 家族とは、指定した特定のノートだけ共有したい（買い物リストなど） | **分けるのは amber の仕事ではない。** クラウドのフォルダ共有に任せ、amber がするのはその**フォルダに印を一枚置くこと**だけ。それだけで「このノートを共有する」が**そのフォルダへ移すこと**になり、フォルダもタグも履歴も、いまあるものが全部そのまま効く。**置き場所は増やさない**（「置き場所は一つにしたい」は本人が決めたこと）。**ノートには書かない** ── 前書きに `share:` と書くと、共有をやめた日に全部のノートを書き換えることになり、同期先で全部が差分になる。**初めは `.amber/settings.json` に書いていた**が、2026-09-07 に**フォルダの中の印**へ移した（依頼 319）── 動きはしたが相手の amber に何も伝わらなかった | `crates/amber-core/src/notebook.rs ~ \*\*設定ではなく、フォルダ自身が持つ。\*\*` |
| 318 | 2026-09-06 | 共有しているノートが、それと分かること | **書く前に分かるように** ── 家族が読むノートに、そうと知らずに書くことがないように、題の隣に印（開いてからでは遅い）。左の列には「共有」の段が一つ増える。**決めていないうちは出さない**（空の「共有」が並ぶと、壊れているのかまだ何も分けていないのか見分けられない）。**フォルダの段からは外す** ── 二つの場所に同じものが出ると、人はそれを二度消そうとする（ブックマークを別枠にしたのと同じ理由） | `gui/renderer.js ~ \*\*共有は、行き先の一つ。\*\*` |
| 319 | 2026-09-07 | アプリを使う際の人間の設定が多すぎる。設定や同期はアプリ側でほぼ自動に | 数えると**設定が七つ**あり、**四つが amber の外**（Finder・ファイルアプリ）だった ── IT に強くない人には通れない。**七手を二手にする**。核は「どれが共有か」を設定ではなく**共有フォルダの中の一枚**（`.amber-share.json`）に持たせたこと ── 印はフォルダと一緒に旅をするので、**受け取った人は何も教えなくていい**し、機種を替えてもずれない（設定に書いていた頃は、相手が自分の amber に教え直す手が要り、機種ごとにもう一度要った）。これは amber がもともと持っていた考え方（隠しデータベースを持たない）そのもので、そこを外していた。あわせて**棚が無ければ作る**（「共有する」を押した人に、その前に「フォルダを作る」を押させない） | `crates/amber-core/src/notebook.rs ~ \*\*設定ではなく、フォルダ自身が持つ。\*\*` |
| 320 | 2026-09-07 | 名前の設定画面を作らない | 一度しか使わないものを、毎日見る画面に置く値打ちは無い ── **要る瞬間（初めて共有する瞬間）に一度だけ**訊いて憶える。下書きにはこの機械が既に知っている名前を入れる（もう一度打たせない）。**空のままでも共有は動く**（「だれか」になるだけ）。名前は**履歴に付いて回る**のでノートには書かない ── .md はただのマークダウンのまま | `gui/renderer.js ~ \*\*設定画面に置かない\*\* ── 一度しか使わないもの` |
| 321 | 2026-09-07 | Android・iPhone・Mac・Windows でゼロクリックの同期を目指したい | **「同期フォルダを指す」は机の上だけの話だった** ── iOS はアプリ同士が自由に相手のファイルを読めず、Android には iCloud Drive のクライアントが無い。携帯に届かせているアプリは例外なく、サーバを持つか**クラウドの API を直に叩いている**。amber は後者を採る（ノートがただの .md のまま残るのは前者では失われる／カレンダーで通す OAuth の道の二軒目になる／アプリ専用フォルダの権限は審査不要）。**通信は core に入れない** ── core の規則は「判断だけ。I/O と UI に依存しない」で、通信は I/O。core は手順書を出し、繋ぐのは窓と電話。判断は一組のまま、Rust に TLS を積まずに済む。**迷ったら残す** ── 片方で消して、もう片方で書き足したときは書いたほうを残す（消すのは取り返しがつかず、残すのはつかない） | `crates/amber-core/src/sync.rs ~ \*\*決めるだけで、繋がない。\*\*` |
| 322 | 2026-09-07 | 同期は Google Drive の API で。まず繋がない側から | 向こうにあるものの一覧は呼ぶ側が持ってくる（通信は I/O なので core の外）── core は歩いて指紋を取り、**前に合わせたときの憶え**（`.amber/sync.json`）と突き合わせて運ぶ向きを決める。**指紋で比べる、時刻では比べない** ── クラウドから降りてきたファイルの時刻は書いた時刻とは限らず、時計のずれた端末が毎回勝つか毎回負ける。**憶えは相手ごとに分ける**（二つ目の相手が来ても片方が上書きしない）。**全部やるか何もしないかにしない** ── 運べたぶんだけ憶えて残りは次に（電波の悪いところで一本も進まなくなる）。壊れた憶えでも落ちない（合わせ直せば済むことで、そこで落ちるとノートが一本も見られなくなる） | `crates/amber-core/src/api.rs ~ 合わせ方を決める。\*\*繋がない。\*\*` |
| 323 | 2026-09-07 | 電話に反映するときは窓の資材も上げる、を決まりにして再発防止したい | **決まりにするだけでは再発する。** 忘れるから決まりを作るのに、その決まりを憶えていないと効かない ── この repo の作法どおり機械に数えさせる（`scripts/shipped.py`）。実際の失敗はこうだった: 電話はケーブル、窓はリリースと**道が違う**ので、iPhone には毎回入れながら窓は `v2.5.0` のまま 19 件置いていかれ、本人が「ゴミ箱に入れるに入力欄が出る、なんか古い気がする」と気づくまで誰も知らなかった ── **直したはずのものが、直っていないように見える**。数えるのは `gui/` `crates/` `packaging/` に触ったコミットだけ（README を直した日に鳴らしても意味が無く、意味の無い警告はそのうち誰も読まない）。**版を上げるのは Taketan**（自分からは上げない）ので、検査がするのは溜まっていると言うことだけ | `scripts/shipped.py ~ \*\*憶えておく約束にしない。\*\*` |
| 324 | 2026-09-07 | 面を行き来していたら、マーメイドの図が丸ごと消えた（鍵盤では消していない） | **触れないかたまりの「元の字」を、保存のたびに切り直していた。** 組み直したときの行番号（`data-line`）で本文を切って持たせていたが、`syncRead` は caret を飛ばさないために**組み直さずに保存する** ── 上に一行足した次の保存から番号がずれ、図の元の字が**別の場所の字**になり、その次の保存でそれが図の場所へ書き戻されて図が消えた。「表示／並べて／コード／見出し」を行き来すると保存が挟まるので、そこで踏む。**一度持たせたら二度と作り直さない** ── 元の字が変わるのは組み直したときだけで、図を直す工房は必ず組み直す（`readSourceEdit`）。切り出しの中なので電話も同じ。**時刻でもなく、番号でもなく、持っているかどうかで決める** ── ずれた番号は、ずれていることが画面のどこにも出ない | `gui/renderer.js ~ \*\*一度持たせたら、二度と作り直さない。\*\*` |
| 325 | 2026-09-07 | ノートを最大化したときの巻物の摘みが、妙に真ん中に近い | **狭めていたのは字ではなく、巻物そのものだった。** `#read` は自分で縦に巻く箱なので、最大化のときに箱ごと 760px にして真ん中へ置くと、**摘みも一緒に真ん中へ来る** ── 1600px の窓では画面のまんなかに棒が浮いた。読む面の字はもともと `#read > *` が 34em に収めているので（760px より狭い）、箱を狭めることは初めから行の長さに効いておらず、効いていたのは棒の居場所だけだった。箱は端まで伸ばし、字はこれまでどおり真ん中の 34em（見た目は変わらず、棒だけ右端へ）。**書く面は残す** ── Monaco は行が箱の端まで伸びて折り返すので、あちらは箱を狭めることが行の長さそのもの | `gui/index.html ~ \*\*狭めるのは文字であって、巻物ではない。\*\*` |
| 326 | 2026-09-07 | ノートを取り込む道が、窓に見当たらない | **見当たらないのではなく、無かった。** 電話には初めからあり、窓には「バックアップから戻す」しかなかった ── zip でなければ入れる道が無く、ほかのアプリから書き出した .md を持ってきた人は Finder で写すしかない（そのフォルダがどこかは amber が言わないので、まずそこで詰まる）。**同じ名前は上書きせず両方残す**（`週報-2.md`）── 「戻す」は前にあったものを取り返す行いなので同じ名前を飛ばすが、「取り込む」は知らないものを入れる行いで、飛ばすと「入れたはずのものが入っていない」になる。**`週報 2.md`（間が空白）にはしない** ── その形はクラウドが作る衝突の控えで、`cloud::shape` はそれを当てずっぽうで拾わないと決めてある（依頼 316）。元のファイルは動かさない ── 写すだけ。**一本で転んでも残りは運ぶ**（`sync` と同じ考え）── 十本選んだうちの三本目が読めないときに一本も入らないと、人にできることは一本ずつ選び直すことしかない。入らなかった数は黙らずに言う ── 黙ると「八本しか選ばなかった」と思い、気づくのはあとで探しても出てこない日 | `crates/amber-core/src/notebook.rs ~ \*\*同じ名前は、上書きではなく両方残す。\*\*` |
| 327 | 2026-09-07 | 名前の付け直しを、窓と電話で二組書かない | 電話は Swift で `名前-2.md` を組み立てていた。窓にも取り込みを付けた時点で同じ規則が二組になり、二組あるものは必ずずれる ── ずれると**同じノートが端末によって別の名前で入る**（フォルダの色を二度書いて、十一色のうち六色がずれたのと同じ）。判断は core（`notebook::bring`）に一つだけ置き、電話に残るのは**選ばれた URL の鍵を開けている**ことだけ ── それはここでしかできない | `ios/Cian/NotesStore.swift ~ \*\*名前の付け直しは core が決める。\*\*` |
| 328 | 2026-09-07 | 「見本のノートを入れる」が、⚙ の列で探せない | 窓にはもとからあったが、**「amber について」の下に一つだけ離れて座っていた** ── ノートを入れたい人はそこまで下りてこないので、無いのと同じだった（取り込みを探した人が見つけられなかったのはこれも一緒）。**入れる／出す行いを一まとまりにする**（取り込む・見本を入れる・バックアップ・戻す・置き場所）。並びが意味を持たない列では、人は端から順に読むのではなく、近いものの塊を探す | `gui/renderer.js ~ \*\*入れる三つを、並べて置く。\*\*` |
| 329 | 2026-09-07 | 表記を `ambər` に。意味は Advanced Markdown Browser & Editor for Readability | `ə` はシュワー（曖昧母音）── あいまいに置かれた、ただの e の音。**小文字**（U+0259）── 初めに大文字の `Ə`（U+018F）を入れてしまい、まわりの小文字より背が高く出た。並べた見本を見て本人が選び直した。**替えるのは目に映る字だけ。** 中の名前（`amber-server`・`amber-core`・`window.amber`・URL の仕組み・設定キー `amber.notes.seeded`・バンドル ID・リリースの資材名・`~/Documents/amber`・共有の印 `.amber-share.json`）は一つも動かさない ── 動かすと、同梱している crmaine の受け口が黙って外れ、皆の設定と共有の印が「無かったこと」になる（依頼 148 で決めた線を、そのまま引き継ぐ）。**窓では引き出しを先に釘で留めてから名前を替えた** ── `app.setName` は `userData` の置き場所も決めるので、そのまま替えると憶えごと（置き場所・見た目・見本を置いたか）が置き去りになり、開けた人には**初めて開いた日の画面**が出る。**電話は表示名と説明文を一緒に替えた** ──「ファイル」に出るフォルダ名は表示名に従うので、片方だけだと「この iPhone 内 → amber」が嘘になる。名前の意味は「◯◯ について」に置く ── 一度読めば済むものを、毎日見る場所に置かない | `gui/main.js ~ \*\*見える名前は `ambər`、引き出しの名前は `amber` のまま。\*\*` |
| 330 | 2026-09-07 | 版を上げただけの回を、エンジンの鮮度検査が見逃していた | `ios-fresh.sh` は `*.rs` だけを見ていたので、`Cargo.toml` の版を上げた回は黙って通った ── 建った電話の「◯◯ について」が「画面 2.7.0 / エンジン 2.6.0」と出る。**ずれているのが原因の不具合を追うためにその画面を作ったのに、その画面自身が古い数字を言っていた。** 版の数字も見るようにした（この綴りの直しの最中に、実機の画面で見つけた ── 検査は通っていた） | `scripts/ios-fresh.sh ~ \*\*版の数字も見る。\*\*` |
| 331 | 2026-09-07 | 名前のシュワだけ琥珀色にしたい | **色を持てるのは、名前を名前として見せている場所だけ。** ホーム画面の名札も窓の題名も「ファイル」に出るフォルダ名も OS が描き、README は GitHub が `style` を落とす ── 残るのは窓と電話の「ambər について」の二か所。三案から本人が選んだのは**案三**（名札を一覧の頭に、27〜28pt で置く）── 名前が先、数字があと。**題は名札が兼ねる**（上に「ambər について」と書いて下でもう一度 ambər と出すのは、同じことを二度言っているだけ）。窓では**名札だけ HTML を通す** ── 題（`.hd`）と添え書き（`.foot`）は `textContent` のまま置いた。あそこにはノートの名前が入る（「ゴミ箱へ入れる」など）ので、HTML にすると人の書いた字が印になる道が開く | `gui/renderer.js ~ \*\*ここだけ HTML を通す。\*\*` |
| 332 | 2026-09-07 | 明るい地では濃い琥珀、暗い地では明るい琥珀 | `--amber`（#f0a52b）は明るい黄橙なので、白い紙の上の**一文字だけ**だと薄れて沈む。明るい地は #b5760f、暗い地は #f0a52b ── 本人が四つ並べて選んだ。窓はテーマごとの「濃い琥珀」（`--amber-deep`）に乗せる（紙でもあゆでも、その画面の色になる）が、**OS まかせの暗い方と `amber-dark` だけ名指しした** ── 前者は `--amber-deep` を上書きしていないので明るい版の #b5760f が残り、暗い紙の上でただの焦げ茶になっていた。後者はテーマ自前の #e0a94e で、選ばれた色より引っ込む。電話は `BrandSchwa` に二色を持たせた ── `AccentColor` とは別にした（あれはアプリ中のボタンを塗る色で、ここだけ変えたいときに一緒に動く） | `gui/index.html ~ \*\*ここだけ名指しする。\*\*` |
| 333 | 2026-09-07 | 見本のノートの中でも色を付けたい | **付けないと決めた。** 面は `<span style="color:…">` を通すので、字の上では色が付く。付けなかったのは二つ ── ノートの題は**見出しの生の字**から採るので、`# amb<span …>ə</span>r へようこそ` と書くと**一覧にその記号がそのまま並ぶ**（実際に出した）。そして色は一つしか書けず、`light-dark(…)` は面が落とす ── 明るい方に合わせれば暗い画面で焦げ茶に沈み、暗い方に合わせれば白い紙で薄れる。**ノートはただの Markdown**（amber の都合をノートに書かない）という決めごとに戻る | `packaging/welcome/ambər へようこそ.md !~ <span style` |
| 334 | 2026-09-07 | crmaine から開くと「ゴミ箱に入れる」で消えず、「見本のノートを入れる」が `No handler registered` で落ちる | **amber の画面は crmaine の中でも動く。** あちらは自前で `ipcMain` を建てていて、境目は「amber の preload が見せている9つ」で止まっていた ── amber が 23 に増やしても、あちらに知る道が無かった。**押した人には Electron の生の札が出る**（何を間違えたのか、どこにも書いていない。しかも直すのは押した人ではない）。amber 側は**その一行だけ人の言葉に置き換える**。crmaine 側は 12 の口を足し、`scripts/check-amber-bridge.js` が同梱した preload を読んで数えるようにした（あちらの repo・`bfa4d8c`）。**画面は出るので、撮っても分からない ── 押すまで分からない** | `gui/renderer.js ~ \*\*包みだけを剥がす。\*\*` |
| 335 | 2026-09-07 | 「ノートを取り込む」の補足は要らない | 命令の名前が既に言っている。**補足は、名前だけでは足りないときにだけ置く** ── 全部に付けると、読む列が二段になって名前が探しにくくなる | `gui/renderer.js !~ sub: 'よその \.md を写す'` |
| 336 | 2026-09-07 | 同じノートを二人が同時に書いたときは、黙って混ぜて、入ってきたものに印を付ける（案一） | **判断だけ。繋がない**（`sync` と同じ規則）── 三つの姿（分かれる前・こちら・向こう）を渡されて、混ざった字と「どの行が向こうから来たか」を返す。**ノートには何も書き足さない** ── `<<<<<<<` を入れると、混ぜた日からそのノートは amber でしか読めない形になる（ただの Markdown で、GitHub でも VS Code でも開く、が嘘になる）。印は**返り値で**言い、画面が色を差す。**行で見る、字の中までは踏み込まない** ── 一行の中で二人が別の語を直したとき、字で混ぜると「どちらの文でもない一文」ができる。**同じ場所なら両方残す**（迷ったら残す ── 消えたものは取り返せないが、余分に残ったものは読んで消せる）。こちらを先に置く: 自分の書いたものが上にあるほうが、何が起きたか読み取りやすい。**丸ごと採る近道は捨てた** ── こちらが手つかずのとき向こうを丸写しすると `came` が全行になり、一行足されただけの百行のノートで印が何も言わなくなる。長すぎるノートは真ん中を諦めて両方残す（遅くなるより、余分に残るほうがまし） | `crates/amber-core/src/merge.rs ~ \*\*ノートには何も書き足さない。\*\*` |
| 337 | 2026-09-07 | 配る `amber-gui.zip` に見本のノートを入れてほしい（crmaine から） | **同梱する側は写しを持たない**（持つと、amber を入れ替えた日に古い見本が残る）ので、zip に入っていないものは会社端末に届かない ── 押した人に出るのは「見本が入っていません」だけ。入れる位置は `packaging/welcome/…` のまま（あちらは `<画面の隣>/../packaging/welcome` を見る）。**`zip` ではなく Python で組み直した** ── 見本の名前は日本語で、一枚は `ambər へようこそ.md`。Info-ZIP の `zip` は既定で **UTF-8 の印（EFS ビット）を立てない**ので、日本語 Windows で展開すると**名前が化ける**（配りたいのはまさにその端末）。`-UN=UTF8` で立つが、**Apple が入れ替えた zip はその札を知らない**（`short option 'N' not supported`）── 手元で試せないものを CI にだけ書くことになる。`zipfile` は非 ASCII の名前に必ず印を立て、両方で同じものが出る。組んだあと**自分で開いて数える** ── 入れたつもりで空、を配らないため。配ってある v2.7.1 は、**gui/** に触らず見本だけ足して**差し替えた（213 件はそのまま、4 件増）。**この検査が無かったので、三つの版ぶん見本の入っていない zip を配った**。**配る一枚は、タグから組み直せること。** 同梱する側が「配られた zip をそのまま使う」に切り替えたので、揃っているか数えたら二つずれていた。①フォルダの項目 ── `zip -r` は書き、`zipfile` は書かない。ほどく側はたいてい親を勝手に作るが、「たいてい」で配るものではないので書く。②`gui/package-lock.json` が **100 バイト**違う ── CI の `npm install` が lock を書き直し、**書き直されたものがそのまま zip に入っていた**（repo のどのコミットにも無いファイルを配っていた）。`npm ci` にした ── lock のとおりに入れ、lock を触らず、噛み合っていなければその場で落ちる | `packaging/gui_zip.py ~ \*\*`zip` コマンドではなく Python で作る。\*\*` |
| 338 | 2026-09-07 | 「表示」で開くと、左の余白が大きすぎる（並べて・コードでは気にならない） | **余白の話ではなく、左端が揃っていなかった。** `max-width: 34em` は**要素ごとの字の大きさ**で効くので、同じ「34em」でも幅が全部ちがう ── 実測で H1 が左から 32px、本文が 169px、H2 が 90px、罫線に至っては 300px（230px の棒が真ん中に浮いていた）。しかも `margin: … auto` が十か所にあり、かたまりごとに真ん中へ寄っていた。`rem` にして画面の字で一度だけ決め、**真ん中に寄せない** ── 「並べて」も「コード」も字は左から始まるので、面を替えるたびに柱が横へ跳んでいた。直したあとは十種類のかたまりが全部 32px | `gui/index.html ~ \*\*柱は一本。左端で揃える。\*\*` |
| 339 | 2026-09-07 | ノートの目次を右に出すやつが消えている。めっちゃ便利なので戻してほしい | **外したのは自分**（依頼 297 で帯の幅を空けるため、☰ を献立へ引っ込めた）。献立の中では見つからず、「消えた」と言われた ── **献立は探す場所ではなく、知っている人が確かめる場所**。幅は `#meta` を固定してから空いているので、ボタンを帯へ戻した。命令はそのまま（押せる場所が増えるだけで、二つの道にはしない）| `gui/index.html ~ \*\*目次はボタンで出す。\*\*` |
| 340 | 2026-09-07 | 見本のノートの絵が、crmaine で開くと出ない | 絵は `file://` で指していて、**amber 自身の窓では出る**ので撮っても分からない ── 同梱する側の `<webview>` で開くまで分からなかった。**読めなかったら、開いているアプリに読んでもらう**（`fileBytes` ── 同梱する側も持っている口）。返ってきた中身を `data:` にすれば、どの入れ物でも出る。**先に `file://` を試すのは、そちらが安いから** ── 出るなら、大きな絵を毎回 base64 にして持ち歩く理由は無い | `gui/renderer.js ~ \*\*読めなかったら、開いているアプリに読んでもらう。\*\*` |
| 341 | 2026-09-07 | crmaine から「ゴミ箱に入れられません」とだけ出る | `amber:trash` が `false` しか返していなかったので、押した人が見るのは一行だけで**手の打ちようが無かった** ── `trashItem` が落ちるのはゴミ箱の無い置き場所・開いたままのファイル・権限で、どれも人にできることがある。**返す形は増やすだけにした**（`true`/`false` はそのまま、駄目なときだけ `{ ok:false, why }`）── 同梱する側は真偽で受けているので、そこを壊さない。前端はどちらでも受ける | `gui/main.js ~ \*\*なぜ駄目なのかを返す。\*\*` |
| 342 | 2026-09-07 | Windows で、道の区切りを `/` だと思っていた三か所 | core が返す道は土台のもので、Windows では `C:\\Users\\…\\ノート.md`。`split('/')` で切ると**道まるごと**が名前になり、書き出したファイルの名前が道になる／ゴミ箱へ入れる確認に道が出る。在りかを求めるほうは切り落とせずに `''` になり、**Windows でだけ絵が一枚も出ない**。`baseOf` / `dirOf` に集めて、区切りは `[/\\\\]` で見る。**mac では一生出ない不具合**なので、実機で押されるまで分からなかった | `gui/renderer.js ~ \*\*区切りは `/` だけではない。\*\*` |
| 343 | 2026-09-07 | ゴミ箱へ入れられないと、そこで手が止まる | Windows の会社端末は `Documents` が OneDrive に寄せられていることがあり（Known Folder Move）、**そこにはゴミ箱が無い** ── `Failed to perform delete operation` で断られる。ネットワークの置き場所も同じ。黙って断ると、**そのノートは二度と消せない**。**断られたら、訊いてから消す** ── ゴミ箱が「戻せる」ことの担保だったので、それが無い以上そう言う（言わずに消すほうが強すぎる）。核の `delete` を使う ── 電話にはゴミ箱が無いので、前からある道 | `gui/renderer.js ~ \*\*断られたら、行き止まりにしない。\*\*` |
| 344 | 2026-09-07 | チェックリストの最後で改行しても、升が出てこない（箇条書きと番号付きは出る） | **鍵盤の右にもう一つ Enter がある。** フルサイズの鍵盤（会社の机にたいてい載っている）は、数字の並びの脇の Enter を `NumpadEnter` として送る ── Enter の判定が**五か所とも `code === 'Enter'` だけ**だったので、そこを押した人には升の続きが出なかった。**「点と番号は出る」がいちばんの手がかりだった** ── あれは画面が勝手にやっている（`contenteditable` の既定）。こちらの手当てが要るのは升だけなので、手当てが飛ぶと**升だけが出ない**という形で現れる。`isEnter` に集めた（小窓の決定・工房の ⌘Enter・探す欄からの移りも同じ鍵で止まっていた）。**mac の鍵盤には無い鍵**なので、実機で押されるまで分からなかった | `gui/renderer.js ~ \*\*キーボードの右にもう一つある。\*\*` |
| 345 | 2026-09-07 | Ctrl+Z で、caret が関係のない上のほうへぐいーんと動く（表示でもコードでも） | `editor.setValue` は Monaco の caret を 1 行目へ戻し、画面も先頭へ飛ばす ── 三十行目を直して戻すと、**戻ったこと自体は正しいのに、いまどこを見ているのか分からなくなる**。**戻したら、戻った場所へ連れていく** ── 行き先は決まっていて、`firstDiff` が出す「初めて食い違う行」。書く面はその行へ（`revealLineNearTop`）、読む面はその行を跨ぐかたまりへ（`data-line`／`data-span`）。**変わった行が無いときは動かさない** ── 動く理由が無いのに動くのが、そもそもの不具合だった。（一度、書く面の判定を `view !== 'write'` と書いて、書く面でだけ動かなかった ── `gotoHead` と同じ切り分け） | `gui/renderer.js ~ \*\*戻したら、戻った場所へ連れていく。\*\*` |
| 346 | 2026-09-07 | ⌘E で面を行き来すると、見ていない場所に飛ぶ。行き来しても同じ場所・同じ caret に居てほしい | **替える前に、どこに居たかを控える**（`whereAmI`）── 替えたあとでは、前の面の caret も巻き位置も残っていない。控えるのは**ファイルの行番号**（core の行番号はファイルの行、エディタは本文だけなので前書きのぶんを足し引きする）。書く面はその行へ、読む面はその行を跨ぐかたまりへ立つ。**図の中には立たせない** ── 図は触れないかたまりで、caret を置くと押せるものが打てるものに見える。**図の一つ上**の打てるところへ（本人の言葉で「マーメイドのちょっと上にカーソルがあればいい」）。caret が無いときは、いま上に見えているかたまりを使う。**組み直しを待ってから立つ** ── `applyView` が `drawRead` を待たずに返るので、待たずに置くと選び目が古い DOM のものになり、実際に一度も立てなかった | `gui/renderer.js ~ \*\*替える前に、どこに居たかを控える。\*\*` |
| 347 | 2026-09-07 | マーメイドの書き損じで「Syntax error」が画面上部に大量に出て、ノートが左へ潰れる。⌘Z でも戻らず、閉じて開いたら直った | **mermaid は、字が通らないと自分で赤い絵を描いて置いていく。** 置き場所は `document.body` なので、こちらの `catch` は届かない ── 打つたびに描き直すので**一文字ごとに一枚**積み上がり、積まれた絵が幅を持つのでノートが潰れる。戻す（⌘Z）は字を戻すだけで、置いていかれた絵には届かない。`suppressErrorRendering` で絵を描かせず、`render` が測るために建てる仮の箱（`#<id>`・`#d<id>`）は**呼んだほうが片付ける**（成功でも失敗でも `finally` で）。**工房がいちばん積もる** ── 打つたびに描き直すので、通らない字を打っているあいだ一文字ごとに転ぶ。直す前は 12 回で 12 枚残り、直したあとは 0 | `gui/renderer.js ~ \*\*書き損じの絵を、mermaid に描かせない。\*\*` |
| 348 | 2026-09-07 | 工房の「色」の列だけ真ん中寄せ。ほかの列と同じ左揃えに。線の言葉は（なくてもいい）ではなく（なし）に | 上下に並んだ選び口の字が、そこだけ揃っていなかった。添え字は短いほうがいい ── 欄の中に置く字は「何を入れるか」だけ言えば足りる | `gui/index.html ~ \*\*色の列も、ほかの列と同じ左揃え。\*\*` |
| 349 | 2026-09-07 | 窓で直した六つのうち、電話にも効くものを電話にも | **数えたら、効くのは二つだった。** ①書き損じの絵の積み上がり（依頼 347）── 電話も `render` を`try/catch` で包んでいるだけで、`suppressErrorRendering` も仮の箱の片付けも無く、**読む面と図の面の二か所とも同じ形**だった。②面を替えると頭へ飛ぶ（依頼 346）── 電話も面は二つ（`WKWebView` と `UITextView`）。飛ぶ仕組み（`jump`）は目次のために既にあったので、足りないのは「いまどこ」だけ。**替えるときに訊きに行かない** ── 答えを待つあいだ面が止まるので、面の側からこまめに渡しておく。効かないものは効かないと書いておく: 鍵盤の右の Enter（電話に無い）・道の区切り（`/` だけ）・ゴミ箱（電話には無く、前から直に消す）・一行の長さ（電話は `max-width:100%`）・工房の色の列（電話は SwiftUI の献立で、選び口ではない）・Undo（`UITextView.undoManager` が caret を持つ） | `ios/Cian/Paper.swift ~ \*\*いまどこを見ているかを、こまめに伝えておく。\*\*` |
| 350 | 2026-09-07 | 小窓に `**戻せません**` と星がそのまま出ている | **小窓は記号を解さない**（`textContent`）── 自分でマークダウンの星を書いていた。強めたいことは、飾りではなく**言葉の側で**強める（「もう戻せません」）。人に見せる字にマークダウンを書かない、は amber ぜんぶの決めごと（ノートに amber の都合を書かないのと同じ向き） | `gui/renderer.js !~ このまま消しますか ── \\*\\*` |
| 351 | 2026-09-07 | ゴミ箱の無い置き場所で、消すたびに二度訊かれる | `Documents` が OneDrive へ寄せられている机では、断られるのは一度きりではなく**毎回**。「ゴミ箱へ入れますか」→「入れられません」→「では消しますか」を毎回繰り返すのは、**三度目には嘘をついているのと同じ** ── そこにゴミ箱が無いことは、もう分かっている。一度断られたら置き場所ごとに憶えて、次からは初めから正直に訊く（「ここにはゴミ箱が無いので、戻せません」）。**置き場所ごと**に憶えるのは、別のフォルダへ移せばそちらにはゴミ箱があるから。入った回には憶えを消す ── 直った机で、いつまでも疑わない | `gui/renderer.js ~ \*\*ゴミ箱が無いと分かっているなら、初めからそう訊く。\*\*` |
| 352 | 2026-09-07 | 配る zip のフォルダの項目が `0o600` で、Unix だと中に入れない（crmaine から） | **「入れたつもりで空を配らない」と書いた自分が、入り口を塞いだ。** 素の `ZipInfo` は権限を持たないので、そのまま書くと `S_IFDIR` も実行ビットも無い `0o600` になる ── 中のファイルは正しく入っているのに、`unzip` で展開すると `drw-------` で**中に入れない**（`gui/index.html` が「無い」ように見える）。`external_attr` に `S_IFDIR` と実行ビット、それに MS-DOS のフォルダ属性を書く。**Windows は Unix の権限を見ないので、いちばん配りたい相手では起きない** ── 止まるのは同梱する側の CI と、その人の手元だけ。だから配ってから言われるまで分からなかった。組んだあとの検査に「フォルダにフォルダだと書いてあるか」を足した（三つの版ぶん配ってしまった） | `packaging/gui_zip.py ~ \*\*フォルダの項目には、フォルダだと書く。\*\*` |
| 353 | 2026-09-07 | 見張れないことを、同梱する側に自前の帯で言わせない | `amber:watch` が何も返していなかったので、**張れたかどうかを知る道が無かった** ── 同梱する側は自分で帯を出すしかなかった（ゴミ箱と同じ形）。張れたら `true`、駄目なら `{ ok:false, why }`。**言うのは、それを知っているこちら**。見張れないフォルダはある（ネットワーク越し・権限）が、**開かない理由にはならない** ── 言って、そのまま続ける。黙ると、二台で同じフォルダを触っているのに片方が古いまま、が起きても誰も気づけない | `gui/main.js ~ \*\*張れたかどうかを返す。\*\*` |
| 354 | 2026-09-07 | 混ぜたものを、画面に出す（案二 ＋ 消え方①） | **保存のときに `confirm()` で「こちらで上書き／向こうを読み直す」と訊いていた** ── どちらを押しても片方の書いたものが消える。家族で同じ棚を触るのが前提のアプリで、それは強すぎる。いまは黙って混ぜ（core の `merge`）、来た行に**淡い地色**を敷く。**押すまで残す** ── 閉じても出る（見逃した日に気づく道が無くなるので）。**ボタンはノートに一つ**（一行ごとに置くと十行来た日に十回押す）。**憶えるのはこの機械の引き出し** ── 「自分が確認したか」は人ごと・機械ごとで、フォルダに置くと家族の誰かが読んだ時点で全員のぶんが消える。ノートにも書かない | `gui/renderer.js ~ \*\*どちらかを捨てない。混ぜる。\*\*` |
| 355 | 2026-09-07 | 混ぜるときの土台を、履歴用の控えと同じものにしない | はじめ `state.was`（履歴の「保存する前の姿」）を三方向マージの土台に使った ── あれは**保存のたびに動く**ので、自動保存が一度でも通ったあとは「こちらは何も書いていない」ことになり、**向こうで丸ごと上書き**される（試して、足した行が消えた）。土台は専用に持つ（`state.base`）── 動くのは**ファイルと確かに一致した瞬間**だけ | `gui/renderer.js ~ \*\*混ぜるときの土台\*\*（分かれる前）は、別に持つ。` |
| 356 | 2026-09-07 | 来た行の印は、できるだけ細かい単位で | はじめ上の段（`#read` の直下）だけ見て塗ったので、**箇条書きは `<ul>` ひとつで一かたまり**、一行来ただけで**三行とも光った** ── 買い物リストはまさにその形で、それでは何が来たのか分からない（実物を撮って気づいた。それまで「箇条書きなら一行＝一かたまり」と説明していたが、間違いだった）。行の札は `<li>` も持っているので、そこまで降りる。最初の `<li>` は札を持たない（親が同じ行を指す）ので親から継ぐ。降りきれない形は、そのかたまりぜんぶ ── **塗り過ぎるほうが、塗り落とすよりまし** | `gui/renderer.js ~ \*\*できるだけ細かい単位で。\*\*` |
| 357 | 2026-09-07 | 「書きました」ではなく「更新しました」に揃える | はじめ「直し」と書いたが**相手が間違いを正したように聞こえる**（奥さんが買い物リストに足したのは直しではない）。次に amber が前から使っていた「書く」系に寄せたが、並べて見た本人が**「更新」がいちばんしっくりくる**と決めた。**片方だけ替えない** ── 帯とボタンで言葉が割れると、同じことに名前が二つ付く。前からある「同時に書いた控え」「あちらでも書き換えられています」「要るほうを残してください」も、窓・核・電話ぜんぶまとめて更新の側へ寄せた | `crates/amber-core/src/stamp.rs ~ 開いたあとで更新されています` |
| 358 | 2026-09-07 | 混ぜたものを、電話にも出す | 電話も窓と同じ「やめる／それでも上書き」の小窓だった ── どちらを押しても片方の更新が消える。**判断は core の `merge` を呼ぶ**（窓と同じ一組。二組書けば、同じノートが端末によって別の形に混ざる）。帯は**面の上**に置く ── 「表示」でも「コード」でも同じことが起きているので、片方の面の中に入れると面を替えたときに消えたように見える。色は `WKWebView` の中なので外から渡し、**組み直すたびに敷き直す**（札は組み直しで消えるので、一度きり渡すと次に打った瞬間に色が消える）。塗るのは窓と同じ「できるだけ細かい単位で」── `<li>` まで降りる。憶えは `UserDefaults` ── フォルダにもノートにも書かない | `ios/Cian/Desk.swift ~ \*\*どちらかを捨てない。混ぜる。\*\*` |
| 359 | 2026-09-07 | Windows の不具合を、Mac の上でも捕まえられるようにする | **同じ形で二度出した** ── 道の区切りを `/` だと思っていた（依頼 342）と、Enter は一つだと思っていた（依頼 344）。どちらも**mac では一生出ない**ので、実機で押されるまで分からなかった。仮想 Windows を建てる話も出たが、数えたら**今日の四件のうち捕まえられるのは二件**（会社の OneDrive にゴミ箱が無い、zip のフォルダ権限は、VM でも落ちる）── それより**判断そのものが純粋な関数**なので、道と鍵の形を渡せばここで止まる。`gui/renderer.js` から切り出して試す（写すと、写した側だけが直る）。**本物の Windows でも走らせる** ── 「渡した形」が本当に Windows の形かどうかは、Windows でしか確かめられない。CI には既に `windows-latest` が居たので、そこに載せた（依存は要らない）| `scripts/win-test.js ~ Windows で踏んだところを、Mac の上で踏み直す` |
| 360 | 2026-09-08 | ノートを替えても、本文が別のノートに書き込まれないこと | **二本のノートが、前書きだけ自分のまま本文が別のノートになった**（2026-09-07、crmaine の中で。`.amber/history/` から戻せた）。疑われたのは履歴の `keep` だったが、書かれた字は `state.head + editor.getValue()` ── つまり `write` の側。「表示」の面の書き戻し（`syncRead`）が、面に出ている字を**いま開いているノートの字だと思い込んでいた** ── 「コード」の面ではノートを替えても組み直さないので、面は前のノートの字のまま。次に別のノートへ替えた瞬間、それが今のノートへ書き込まれた。同じ道で、「コード」で打った行がノートを替えた瞬間に**組んだ時の字へ戻される**（撮り直して両方出た）。面に「どのノートの字か」の札（`data-of`）を置く ── 組んだとき付け、別のノートを開いたときと「コード」で打ったとき剥がし、**札が今のノートを指すときだけ書き戻す**。遅れて着いた前のノートの組み上がりにも今の札を付けない。`scripts/switch-test.js` が、組み直さずにノートを替える形で踏み直す | `gui/renderer.js ~ if \(!readCurrent\(\)\) return;` |
| 361 | 2026-09-07 | Inkdrop 並みにサクサク動かしたい ── まず測る | **測ったら、疑っていたところは無実だった。** エンジンとの往復（stdin/stdout の JSON）は 1.2ms で、Inkdrop が in-process なこととは関係が無かった。1002 本の棚で突出していたのは**ノートを開く 5.9 秒**。中を割ったら、開くこと自体は 5ms で、時間は全部ついでの仕事だった ── **何も書き換えていないのに保存が走っていた**。読む面は末尾にいつも空の段落を置く（`tailStop`）ので、字に戻すと改行が一つ増える。`syncRead` はそれを「変わった」と読み、ノートを見てから次を開くたびに、見ただけのノートがフル保存され、履歴が一枚積まれ、棚が丸ごと数え直されていた。**人が決められない差**（末尾の空行の数）で保存を走らせない | `gui/renderer.js ~ if \(sameNote\(state.head \+ body, whole\(\)\)\) return;` |
| 362 | 2026-09-07 | （同上）一覧が重い | 名前順の物差しを**呼ぶたびに作り直していた** ── `localeCompare(b, 'ja', {…})` は比べるたびに `Intl.Collator` を建てる。1002 本を並べると一万回建てることになり、それだけで 119ms（使い回すと 7.3ms）。一覧は保存のたびに組み直される | `gui/renderer.js ~ const BY_NAME = new Intl.Collator` |
| 363 | 2026-09-07 | （同上）打ち終えるたびに画面が固まる | **保存のあとに棚を丸ごと数え直していた。** 1002 本で 370ms ── エンジンが 220ms（歩くのに 10ms、一本ずつ読むのに 90ms、880KB を JSON にして渡すのに 100ms）、一覧を組み直すのに 150ms。しかも打ち終えて 0.7 秒後に走る。**自分が書いた一本のことは自分が知っている** ── 変わりうるのは題・書き出し・タグ・大きさ・時刻だけで、どのフォルダに居るか（`rel`・`book`・`shared`）は字を書いても動かない。**題の決め方は core に一つ**（`note` の口）── 窓が「一行目が題」と自前で決めると、一覧と帯で違う名前が出る | `gui/renderer.js ~ await freshenRow\(path\);` |
| 364 | 2026-09-07 | （同上）見張りが自分の書き込みで起きる | 保存するとファイルが動くので、見張り（`fs.watch`）が**自分の書き込みで**起きて、行を直したすぐあとにもう一度棚を数え直していた。**何が動いたかを渡す** ── 名前を落として「何かが変わった」とだけ言うと、受け取った側は数え直すしかない。動いたのがいま自分で書いた一本だけなら、もう新しい。名前は `NFC` に揃えて比べる（mac は濁点を分けて持つ ── 外れても数え直すだけだが、**日本語の名前のノートだけ遅い**になる） | `gui/main.js ~ moved.add\(path.join\(root, name\)\)` |
| 365 | 2026-09-07 | 片付けシリーズ ── 隠しフォルダを一つにする | 履歴は `.amber/history/` に置いていたのに、フォルダの色とブックマークは `.cian/settings.json`、同期の憶えは `.cian/sync.json` に置いていた ── 名前を amber に替えたときに、**片方だけ替えそこねている**。人のノートのフォルダに amber のものが二か所あって、片方が古い名前で残っているのは、いつか片方だけ消される形 | `crates/amber-core/src/notebook.rs ~ root.join\("\.amber"\).join\("settings\.json"\)` |
| 366 | 2026-09-07 | （同上）色もブックマークも同期の憶えも、失わない | **写してから消す** ── `rename` は別のディスクをまたぐと失敗するし、先に消すと途中で転んだ回に何も残らない。**向こうに同じ名前があるなら何もしない**（そちらが新しい）。移すのは棚を開いた時点で、**知らないものを置いた人のぶんは残す**（`remove_dir` は中身のあるフォルダを消さない）。読むときは `.cian` を先に見る ── まだ移していない棚で、`.amber` のほうを先に読むと色が何代か巻き戻る | `crates/amber-core/src/api.rs ~ crate::notebook::tidy\(&dir\);` |
| 367 | 2026-09-07 | 共有の残り ── **共有をやめたノートの行き先** | いままでは「いちばん上へ戻す」で、**もとのフォルダを憶えていなかった** ── フォルダに分けている人ほど「どこへ行った」になる。**ノートには書かない**（ただの Markdown で、家族に渡ったノートに「元は くらし に居た」と書いてあっても相手には意味が無い）── 棚の帳面（`.amber/settings.json`）に一行持つ。憶えるのは共有へ入れるときだけで、**ふつうの「フォルダへ移す」では憶えない**（人が自分で選んで動かしたものに戻し先は要らない）。憶えるのは**移せてから**（移せなかった回の憶えが残ると、次にやめた人が身に覚えのないフォルダへ行く）。戻したら**忘れる**（別のフォルダへ移してからもう一度共有してやめた人が、二回前の場所へ連れて行かれる）。憶えていたフォルダがもう無ければ、いちばん上へ | `crates/amber-core/src/notebook.rs ~ pub fn came_back` |
| 368 | 2026-09-07 | （同上）押す前に、どこへ戻るかを言う | 献立に「いちばん上へ戻します」と出しておいて別のフォルダへ入れるのは、黙って動かすのと同じ。**空は「憶えが無い」ではなく「いちばん上に居た」** ── 取り違えると、上に居たノートが戻ってこない。行き先の有無で言葉を分けていたら、戻し先を憶えるようになった日に**外したのに「共有しました」と言った** | `gui/renderer.js ~ sub: home \? '「' \+ home.split\('/'\).pop\(\) \+ '」へ戻します'` |
| 369 | 2026-09-08 | ノートを開いたとき、上の帯で題を直したい。**タグを付けている人も付けていない人も `title: xxxx` として**、最初の見出しは勝手に変わらないこと | 題の出どころは core が決めている（`title:` → 最初の見出し → 書き出しの一行 → ファイル名）ので、「帯で直す」には**どこを書き換えるか**を決める必要がある。三案を並べ、**本人が「見出しは触らず `title:` を足す」を選んだ** ── 「題を直す」と押した人が期待しているのは題が変わることで、一行目の見出しが書き換わることではない。そのぶん題と見出しが食い違いうるので、**一覧も帯も同じ core の答え**を出す（どちらかが自前で「一行目が題」と決めない）。前書きの組み立ては core（`setfield`）── ここで `---` を書き足すと、前書きの形を決めるところが二つになる。空にしたら `title:` を**外す**（消すのではなく、付けるのをやめる ── 元の決め方に戻る） | `gui/renderer.js ~ key: 'title',` |
| 370 | 2026-09-08 | ノートをまとめて選びたい。`Ctrl+A` で全部（絞り込んでいるならそのぶん）、`Ctrl`+押しで出し入れ | **絞り込んでいるなら、絞り込んだぶんだけ** ── 見えていないものまで選ぶと、次に押した「ゴミ箱へ」が見えていない本に効く。**升は出さない**（本人が案 A を選んだ・Inkdrop も出さない）── 選びはじめた瞬間に行の形が変わると、押そうとしていた行が逃げる。色は**琥珀ではない**（「いま開いている一本」が琥珀なので、同じ色だと二つの意味が同じ顔になる）。テーマは六つあるので、**地に混ぜて作る**（`color-mix`）── 六つに書き足すと、テーマを一つ足した日に一つだけ色の無い一覧ができる。`⌘A` は打っている最中には取らない（エディタと探す欄の「字を全部選ぶ」のほうが強い） | `gui/renderer.js ~ function pickAll` |
| 371 | 2026-09-08 | 選んだノートに、一括削除・同じタグを付ける・タグを外す | **全部に同じタグが無くても動くこと**（本人の問い）。打ち込ませると、二十本のうち十二本にしか無いタグを外したとき「残り八本で何が起きたのか」が誰にも分からない ── **選んだ中に実際にあるタグだけを件数つきで並べる**ので、無いタグは選びようがない。**付いていない本は触らない**（書き換えない ── 同期しているフォルダで、中身の変わらないファイルの時刻だけ動くのがいちばん困る）。終わったら数で言う（「12 本から外しました（8 本はもとから付いていません）」）。一本転んでも残りは進む ── 二十本のうち三本目で止まると、どこまで済んだか誰にも分からない（2026-09-12 に本人が数え方を「件」に統一） | `gui/renderer.js ~ 件中 ` |
| 372 | 2026-09-08 | （同上）まとめて消すとき、二十回訊かれないこと | 一本ずつの `cmdDelete` を二十回まわすと二十回訊かれる ── ゴミ箱の無い置き場所（会社の OneDrive）では、断られてからもう一度で四十回。**数を言って一度承知をもらい**、そのあとは黙って進める。断られてはじめて分かった回だけ、残りをどうするかをもう一度だけ訊く | `gui/renderer.js ~ '。残りをこのまま消しますか。もう戻せません'` |
| 373 | 2026-09-08 | Inkdrop と同じく、いちばん左だけ／二枚目だけ畳めるようにしたい。**歯車からもできること** | 左（`⌘/`）はあったが、一覧（二枚目）は畳めなかった。鍵は `⌘⇧/` ── **同じ鍵の並びに揃える**（畳むことは一つの動きで、畳む相手が違うだけ）。**三枚目の「ノートだけ」は作らない** ── 二枚とも畳めばそこへ行き着くし `F12` が既にある。同じところへ着く道を三本持つと、どれで畳んだかで戻り方が違う画面になる。畳んだ姿は憶える | `gui/renderer.js ~ document.body.classList.toggle\('nolist', listOff\);` |
| 374 | 2026-09-08 | amb\\ər のあらゆる場所で右押しできるようにしたい（Inkdrop を踏襲） | 献立を描く場所が三つ（⋯・左の列・色）に分かれていた ── 画面の外へはみ出さない直しを一か所に入れて残り二つが古いまま、が起こる形。右押しを十か所に増やす前に**一本にまとめた**（`popMenu`）。**指しているものにすることだけを出す**（ノートの行を右押しして amb\\ər の設定は出さない ── それは ⚙）。「表示」の面は指した先で分ける ── 図・表・リンク・升・字。飾りの中身は**道具帯（`MARKS`）から引く**ので、記号を一つ足した日に片方だけ古くなることがない | `gui/renderer.js ~ function popMenu\(items, at\) \{` |
| 375 | 2026-09-08 | （同上）「表示」の面の右押しは、指した先で変わること | 表の上なら表のこと、リンクの上ならリンクのこと、升の上なら升のこと。**同じ命令を二度書かない** ── 表の献立は道具帯の `tableDo` をそのまま呼ぶ | `gui/renderer.js ~ const cell = t.closest\('td, th'\);` |
| 376 | 2026-09-08 | 窓でも複数のノートを開きたい（タブ） | **Inkdrop にタブは無い**（三枚のうちエディタは常に一本、複数見たいときは別ウィンドウ）── モックに「Inkdrop と同じ」と書いたのは私の誤りで、調べて訂正した。倣う先は**電話版**（`Desk.Tab`）で、核に足りないものは無い。帯は題の帯の下（本人が甲を選んだ ── 電話と同じ並び。いちばん上に置くと Windows の掴む帯と二段になる）。**一本のときは出さない**（ふだんの画面を、タブのために一段狭くしない）。**エディタは一台のまま** ── Monaco をノートの数だけ建てるのは高く、替えるときに字と caret と巻き位置を出し入れすれば同じことになる。**タブに戻ったら読み直さない**（打ちかけを失わないための机なので、戻るだけで捨てては元も子もない） | `gui/renderer.js ~ function restoreTab` |
| 377 | 2026-09-08 | （同上）押すたびにタブが増えないこと | 本人が案 A を選んだ ── ふつうに押したら**いまのタブを差し替え**、`⌥` 押しと長押し（電話）と右押しで増やす。`⌘` 押しではない（「まとめて選ぶ」が先に取っている）。**電話は C だった**（`open` が `append` するだけで、入れ替える道がそもそも無かった）── 窓に合わせて A にした。C を窓に持ってくると、1002 本の棚を上から見ただけで 1002 タブになる。**2026-09-12 に依頼 522 で VS Code のプレビュータブの形に**（押しただけなら仮のタブ一枚が入れ替わる・書けば残る）。**さらに 2026-09-14、依頼 555 で本人が丸ごと撤回** ── 実際に動かして仮のタブが分かりにくかったので、**押したぶんだけ増える**形にした（この依頼の「増えないこと」は、もう守っていない）。1002 本の棚の話は残るが、**見たものが残らないほうが困る**、が本人の答え | `ios/Cian/Desk.swift ~ 押したぶんだけ、タブが増える` |
| 378 | 2026-09-08 | （同上）同梱先（crmaine）と鍵がぶつからないこと | 本人が `⌘＋数字` を外した ── crmaine の中で動くので、あちらが欲しがる鍵は取らない。`⌃Tab` も同じ理由で取らない（**mac 標準とはぶつからない**が ── タスク切り替えは `⌘Tab` ── タブを持つアプリはどれもこれを使う）。`⌘W` も取らない。**最初の版では、タブの鍵を一つも取らない** ── 一度配った鍵を取り上げるほうが、あとから足すよりずっと難しい。切り替えは押す、閉じるは ✕・まん中押し・右押し | `gui/renderer.js ~ 同梱先（crmaine）が` |
| 379 | 2026-09-08 | （同上）閉じたら書きかけを捨てないこと・開いていた机に戻ること | この窓は打てば勝手に保存されるので、閉じる前に一度書いてから閉じる（訊かない ── 訊くほうが、この窓の作りに合っていない）。**出していないタブの書きかけも書く**（開き直さずに ── 閉じるためだけに面を組み直すのは高いし caret が飛ぶ）。閉じたあとは**左の隣**へ（そこから来たので ── 端まで飛ぶと居た場所を見失う）。机は憶える（電話と同じ）── 戻すのは**いま棚にある道だけ**（消えた・移した本を並べると、押せないタブが残る） | `gui/renderer.js ~ async function closeTab` |
| 380 | 2026-09-08 | 会社の Windows で amb\\ər からノートを読んだら、**絵が見られない**（「この絵は読めません: attachments/01_rag_start.png」） | `'file://' \+ encodeURI\(道\)` が **Windows の道を壊していた** ── `C:\\Users\\…` を渡すと `file://C:%5CUsers%5C…` になり、`C:` が**機械の名前**として読まれ、円記号は `%5C` に化ける。mac の道は `/` で始まるので**たまたま**斜線が三本になって通っていた（依頼 342・344 と同じ、**mac では一生出ない**形）。円記号を `/` に直して頭に一本足す。`\\\\server\\share` は斜線二本のまま（機械の名前が入る ── 会社の置き場所はネットワークの向こうのことがある）。判断は純粋な関数なので `scripts/win-test.js` で踏み直す（**直す前の版で 5 件鳴るのを見た**） | `gui/renderer.js ~ const fileURL = ` |
| 381 | 2026-09-08 | （同上）読む面では出るのに、コードの面でだけ「読めません」と言っていた | 絵を出す道が**二か所にあって、助け船（`fileBytes`）が片方にしか無かった** ── 読む面は `file:` が届かなければ同梱する側に読んでもらうが、コードの面（行の下に出す実物）はそのまま諦めていた。道の直し方も、片方だけ直せば済んでしまう形。**一本にした**（`showPicture`）── 言うのは、手を尽くしてから | `gui/renderer.js ~ function showPicture` |
| 382 | 2026-09-08 | 撮った画面を貼ったら、`attachments/` に置かれるか（問い） | 置かれる ── ただし**「表示」の面では、置かれるのにリンクが入らなかった**。絵を置く道（`attach`）だけが面を見ておらず、`put` は「表示」の面で黙って帰る ── **誰も指していない絵が `attachments/` に溜まる**。道具帯のほかの記号は面を見ているので（`onRead\(\) \? readPut : put`）、ここだけが揃っていなかった。貼り付けの受け口も「表示」の面で帰っていた（**画面を撮って貼るのは、いちばん「表示」の面でやりたいこと**） | `gui/renderer.js ~ if \(onRead\(\)\) await readPut\(\`!\[\]\(\$\{r.link\}\)\`\);` |
| 383 | 2026-09-08 | 「表示」の面のまま書けるのが amb\\ər の芯だから、そこを違和感なく・バグなく・美しく極めたい | まず**往復の総当たり**を建てた（`scripts/round-test.js`）── 組む側は core、戻す側は窓と電話が共有している切り出し。**75 通り中 23 が戻らなかった**。一つずつの形は `paper-test.js` が通っていて、落ちたのは**往復と組み合わせ**。実物でも踏んだ ── 「表示」の面で一文字打っただけで、打っていない行が五種類書き換わり、同期していれば全部むこうへ差分として飛ぶ。**二周させる**のは、形が動き続けるものを捕まえるため（引用の中の枠が、保存のたびに `` ` `` を一つずつ増やしていた） | `scripts/round-test.js ~ 二周目` |
| 384 | 2026-09-08 | （同上）段落の中の改行が、空白になって保存される | core が段落の行を空白で繋いでいた（`para.join\(" "\)`）── GitHub（CommonMark）はそうするが、amber は「表示」の面をその姿から字に戻すので、**三行で書いた段落が、一文字打った瞬間に一行になる**。**改行は改行として描く**（本人が決めた・「圧倒的に案 A」）。ファイルは一文字も変わらない ── 描き方だけの話。行末の空白二つと `\\` は、**印としてそのまま憶える**（`data-hard`）── 見え方は同じでも人が打った字 | `crates/amber-core/src/markdown.rs ~ fn breaks` |
| 385 | 2026-09-08 | （同上）人が書いた記号を、丸めずにそのまま戻すこと | `\*` `\+` の点が `-` に、`1\)` が `1.` に、`1. 1. 1.` が `1. 2. 3.` に、`\[X\]` が `\[x\]` に、`\*\*\*` `\_\_\_` が `---` に、`:---` が `---` に丸まっていた。**どれも Markdown としては同じ意味で、見え方も同じ** ── けれど家族の誰かが `\*` で書く人なら、こちらが開くたびにその人のノートが書き換わり、向こうに「更新されました」と出る。**本人が「丸めずにいこう」と決めた**（2026-09-08）。core が元の印を憶え（`data-mark`・`data-sep`・`data-box`・`<ol start>`）、戻すときにそのまま書く | `crates/amber-core/src/markdown.rs ~ let mark_at = format!` |
| 386 | 2026-09-08 | （同上）行頭の全角空白（字下げ）が消える | 段落の前後を落とすのに `trim\(\)` を使っていた ── JavaScript も Rust も、`trim\(\)` は**全角空白を削る**。字下げは人が打った字なので、削るものを半角の空白と tab と改行に絞る（窓と core の両方） | `gui/renderer.js ~ const edges = ` |
| 387 | 2026-09-08 | （同上）`~~~` の枠が枠にならず、中身が Markdown として解釈される | GFM は三つ以上の `` ` `` と `~` のどちらも枠にするが、core は `` ``` `` しか知らなかった ── `~~` が取り消し線に読まれ、中に `- ` があれば点になる。**コードが Markdown として読まれる**ので、見え方だけの話では済まない | `crates/amber-core/src/markdown.rs ~ for mark in \["```", "~~~"\]` |
| 388 | 2026-09-08 | （同上）引用の中のコード枠が壊れ、保存のたびに形が変わり続ける | 外は引用・中は枠。外を触れるままにすると、中を字に戻すのは `blockToMd` の仕事になり、あちらは枠を知らない（知らない札は中身だけ取る）── `` ` `` が行の途中のコードとして読み直され、**一周目で中身だけ、二周目でさらに `` ` `` が増えた**。同期していれば毎回差分。**中に枠や図を抱えたかたまりは、外ごと触らせない** ── 元の字は外が持っている（`data-md`）ので、一文字も失わない | `gui/renderer.js ~ return !!node.querySelector\('pre, .mermaid'\);` |
| 389 | 2026-09-08 | 「表示」の面で Tab を押したら、一覧は一段深く・段落は字下げ（`PAPER.ja.md` 六章の乙） | いままで**焦点が面から飛んでいた**（表の中でしか受けていなかった）── 芯の 1「面の上の一打は、字の上の一つの記号に対応する」から外れる。**一覧の中は子リスト、外は字下げ**（Inkdrop も同じ向き）。字下げは**全角空白**（`　`）── 半角の空白と tab は四つ揃うとコード枠になるので使えないが、`　` はただの字で、何段でも枠にならず GitHub でも同じ幅で出る。**上に項目が無ければ深くしない**（Markdown に親の無い入れ子は書けない）。見出しでは何も起きないが、**受けて止める**（焦点が飛ぶのは事故） | `gui/renderer.js ~ function checkTab` |
| 390 | 2026-09-08 | （同上）行頭の Backspace は、その行の記号を一つ外す | 既定に任せると、**途中の項目は前の項目と繋がる** ── 升が一つ黙って消え、二つの「やること」が一行になる。どの位置の項目でも同じでなければ、押すたびに結果を見る癖がつく。見出しは段落になる（前の段落には繋がない）。引用・注記は**最初の行の行頭だけ**出る ── 段落の中の改行は `<br>` なので二行の引用は一つの節で、丸ごと出すと一打で二行ぶんの `>` が外れる。**触れないかたまりのすぐ下では、何も起きない**（芯の 2 ── caret の無いものが一打で消えるのは事故） | `gui/renderer.js ~ function checkBack` |
| 391 | 2026-09-08 | （同上）押した跡が字に落ちるかを、機械で確かめる | `round-test.js` は**触らずに**往復させる。こちらは**押してから**往復させる（`key-test.js`）── 押した跡が字に落ちるか、押していない行が動いていないか。**升の記号（`☑`）を、行頭の判定で「前にある字」と数えていた**のを、この試験が捕まえた ── 升のある行の行頭がいつまでも行頭にならず、記号を外す一打が効かなかった | `scripts/key-test.js ~ 押した跡が文字に落ちるか` |
| 392 | 2026-09-08 | （鍵の続き）電話でも同じ押し心地にすること | `checkTab` と `checkBack` は**切り出しの中**にあるので、電話は建てるたびに同じものを持っていく（写しを持たない）── あとは**繋ぐだけ**だった。行頭の Backspace は `keydown` と `beforeinput` の両方で受ける（iPhone の鍵盤の delete は `keydown` を出さず `deleteContentBackward` として来ることがある）。**電話に Tab は無い**ので、下の帯に「一段深く」「一段浅く」を置く ── **一覧のボタンの隣**に（帯は横に流れて一度に六つしか見えず、後ろに置くと流れた先に埋もれる）。`keepMark` も切り出しの中へ移した ── 外に置いていたので、電話の束ねに入っていなかった | `ios/Cian/Paper.swift ~ deleteContentBackward` |
| 393 | 2026-09-08 | （鍵の続き）Enter ── 見出しの次は段落、表のセルは下へ、`⇧Enter` は段落の中の改行 | **見出しは一行のもの**（Word も Docs も Notion も、次は段落）。**途中で押しても後ろは段落** ── 既定は見出しを二つに割るが、「見出しの途中で Enter」はたいてい「打ち終えて本文に降りたい」のに caret が末尾に無かっただけ。**表のセルに改行は書けない**（Markdown の表は一行一行）ので、既定に任せると表を壊すか改行を黙って落とす ── 表計算と同じく下のセルへ、最後の行なら行を一つ足す（**セルの数は上に合わせる** ── 行ごとに数が違う表は GFM で崩れる）。`⇧Enter` は段落の中の改行（core が改行を改行として描くようになったので・依頼 384）。**電話は Return が改行**で、新しい段落は下の帯から（本人が決めた・「改行二回で段落」は取らない） | `gui/renderer.js ~ function checkReturn` |
| 394 | 2026-09-08 | （同上）行が見つからないところで、箱そのものを行として扱わないこと | `lineAt` が `div` を数えていたので、表のセルの中では `#read` そのものが返っていた ── 「行が見つかった」ことになり、**セルの中の Enter が段落の Enter として通って、表の中に `<br>` が入った**。試験が捕まえた | `gui/renderer.js ~ return line && line !== box \? line : null;` |
| 395 | 2026-09-08 | 電話の升の中に、小さいチェックがもう一つ見えて気持ち悪い | 電話は枠を CSS で描いている（指で押せる大きさが要る）のに、**中身の字（core が入れる `☑`）もそのまま出していた** ── 塗った枠の中に小さいチェックが重なる。字を出さず、済みの印は枠の上に自分で引く。窓は枠を描かず字だけを出すので、あちらでは起きていなかった | `ios/Cian/Paper.swift ~ box\[aria-pressed=true\]::after` |
| 396 | 2026-09-08 | ブックマークが空のときに、どうすれば登録できるかを言うこと | 「まだありません」だけでは、登録する道が画面のどこにも書いていない。**横払いは長押しより手数が少ない**ので、そちらを先に言う（払う向きまで書く ── 左右のどちらかは、やってみるまで分からない） | `ios/Cian/ContentView.swift ~ ノートを右へ払って ★ を押すと登録できます` |
| 397 | 2026-09-08 | 電話の文字が全般的に小さい | 窓には字の大きさがある（⌘+ / ⌘−）のに、電話には無かった ── 同じノートが端末によって読みやすさで分かれる。**既定は「iPhone に合わせる」** ── 大きい字にしている人の設定を、こちらが黙って上書きしない。「表示」の面（`WKWebView`）は SwiftUI の段を知らないので、**同じ増え方を px で渡す** ── 一覧だけ大きくなって本文が小さいままだと、「大きくした」が半分しか効いていない | `ios/Cian/CianApp.swift ~ enum Size` |
| 398 | 2026-09-08 | 電話の「表示」の面に、caret を動かす矢印が欲しい（iOS の caret 移動が指では難しい） | 「コード」の面は `UITextView` なので **iOS が鍵盤の上に純正の帯（∧ ∨）を出す**が、「表示」の面は `WKWebView` なので出ない ── 同じアプリの中で、面によって手が変わっていた。**流れない一列**にする（本人が「下の帯が二列になってもよい」と言ったのはここ ── 打っている間ずっと使うものを記号の帯に混ぜると、流れた先へ行ってしまう）。動かすのは `selection.modify` に任せる ── 自分で節を渡り歩くと、升や飾りの境目で止まる | `ios/Cian/Paper.swift ~ const move = \(dir\) =>` |
| 399 | 2026-09-08 | いつか端末の言葉に合わせて世界へ配りたい。**言葉にしなくていいところは言葉にせず、手ぶりと絵で案内したい** | 目と目にバツ（`eye` / `eye.slash`）は「隠す／見せる」に読める ── ここで替わるのは**組んだ姿と記号そのもの**で、隠す話ではない。**押すと何になるかを出す**（いま「表示」なら `</>`）。`</>` は世界のどこでもコードで、訳が要らない。**一つの絵に一つの意味** ── 面の切り替えが `</>` を取ったので、行の中のコードは波括弧に分けた（同じ絵が二つの意味を持つと、押すまでどちらか分からない）。「表示」側は `doc.richtext` を試したが、実機では**「あ」の入った札**に見えて字の入った絵になったので、行の並んだ絵にした ── **絵で案内する、を絵が裏切っていた** | `ios/Cian/Desk.swift ~ chevron.left.forwardslash.chevron.right` |
| 400 | 2026-09-08 | Windows の窓で `⌘` が出ていて「なんか違う」 | あちらに `⌘` という鍵は無いので、**押しようがない案内**が出ていた ── mac では一生出ない（依頼 342・344・380 と同じ形で、四度目）。鍵の字が **26 か所に mac の記号で書き込まれていた**うえ、`index.html` の吹き出し（`title=`）にも直に書いてあった。**表には mac の記号で書いておく**（一つの書き方に揃えておけば、増やす人が迷わない）── 出すときに一本で言い換える。記号だけでなく**繋ぎ方も違う**（mac は詰めて `⌘⇧O`、Windows は `Ctrl+Shift+O`）。記号のあとの言葉は `+` で繋がない（「⌥ 押し」は説明であって鍵の並びではない） | `gui/renderer.js ~ const keyText = ` |
| 401 | 2026-09-08 | 「たたむ」の置き場所をもっと近くに（絵にする話は、同じ日に取り下げになった） | 「たたむ」は右端で、**押した指からいちばん遠かった** ── 記号は左、ボタンは右。**記号のすぐ右**へ移した（記号の列の続きに見えるほうが「もっとある」も伝わる）。畳んでいるときの「…」は、押すまで何が出るか分からなかった ── **「ほかの記号」**にする（押す前に分かる）。一度は山形の絵にしたが、**同じ日に字へ戻した**（依頼 288 の註） | `gui/renderer.js ~ more.textContent = moreMarks \? 'たたむ' : 'ほかの記号';` |
| 402 | 2026-09-08 | （鍵の続き）矢印で、触れないかたまりを跨ぐこと | 図や枠の中に caret が入ると、**押せるものが打てるものに見える**（`PAPER.ja.md` 六章の芯の 3）。端に居るときだけ跨ぎ、行の途中では既定のまま一文字ずつ動く。**跨いだ先が無いなら、置き場所を作る** ── 図で始まるノートの上に一行足す道が、いままで無かった（末尾には `tailStop` があったのに・依頼 203 の裏返し）。**常に置く** ── 「caret が近づいたときだけ出す」もありうるが、増えたり消えたりするものは往復の試験で数が合わなくなる。空のままなら字に戻すとき落ちるので、ファイルは増えも減りもしない | `gui/renderer.js ~ function headStop` |
| 403 | 2026-09-08 | 触れないかたまりとリンクは、**押すと吹き出し**（図・枠・絵・リンクで同じ形） | 前は図と枠で違うことが起きていた ── 図は工房、枠は並べて表示のその行へ。**どちらも「押した」だけなのに行き先が違う**。リンクは押した瞬間に外へ飛んでいて、**字を直したい人に道が無かった**（右押しを知らない人が多い ── Notion・Google Docs は吹き出し）。**覚えることを一つにする**。「消す」を置くのは、図や枠を消すのに「コード」へ行かせないため（表示のまま消せること）── **訊かない**（一つ戻すで戻せるし、押した人が「消す」を選んでいる。選びは意思・芯の 2） | `gui/renderer.js ~ function dropBlock` |
| 404 | 2026-09-08 | 日本語の変換中に、打っている字が保存されないこと（`PAPER.ja.md` 六章の丁） | `input` は**変換の一字ごとに来る**ので、書き戻しの間合い（窓 700ms・電話 500ms）が**変換の途中で切れうる** ── そこで保存すると、未確定の字がファイルに入る。同期先に「あいう」が届き、次に「愛」が届く ── 履歴が確定前の姿を積み、混ぜる側には「向こうが二度書いた」に見える。**間合いが切れても待ち、確定してから数え直す**。組み直しも同じ（未確定の字が消える）── 用事は預かって確定してから通す。Monaco は自分の中で変換を扱うので、**エディタからも受ける**（`onDidCompositionStart`）── 読む面の `compositionstart` だけでは、コードの面の自動保存が止まらない | `gui/renderer.js ~ let composing = false;` |
| 405 | 2026-09-08 | （同上）選んで消すとき、表のセルの数が変わらないこと | `blockToMd` は行ごとにセルを数えて書くので、**列の数が行ごとに違う表は GFM で崩れる** ── `contenteditable` の既定は、セルをまたぐ選びを消すときにセルや行そのものを消したり繋げたりする。表計算の Delete と同じにする（**中身を空にして、セルは残す**）。**表の外から中へ跨ぐ選びは、何も起きない** ── 「表の途中までを消す」に正しい答えが無いので、答えないほうがよい | `gui/renderer.js ~ function checkCut` |
| 406 | 2026-09-08 | （`PAPER.ja.md` 六章の丙）同じボタンで、付けると外す | 引用の中で「引用」を押しても**付けるだけ**だった ── 長い引用をまとめて外す道が無く、行頭の Backspace で一行ずつしか出られない（本人が求めた道）。Word の太字と同じ手触りにする。**範囲を選んでいれば、選んだ行ぜんぶ**（途中の行だけ選べば、そこで引用が割れる）。見出し・箇条書き・升も同じ形 | `gui/renderer.js ~ function unwrapBlock` |
| 407 | 2026-09-08 | （同上）一行は、見出しか項目か、どちらか一つ | 見出しの行を箇条書きにすると `- ## 見出し` になりうる ── **`blockToMd` は一覧の中の見出しを知らない**ので、見た目は見出しのまま、保存すると黙って落ちる。点を付ける前に見出しを段落へ落とす ── **落とすなら、押した瞬間に見えて落ちる**（本人：「自分で選んだ操作だから驚かない」）。太字は逆に**見出しを飛ばす** ── 見出しは既に太いので、`## \*\*見出し\*\*` は見た目が変わらないのに同期先では差分になる | `gui/renderer.js ~ function flattenHeads` |
| 408 | 2026-09-08 | （`PAPER.ja.md` 六章の己）升を押したら、その瞬間に裏返ること | 押したものが何拍か置いて変わるのは「効いたか分からない」の入口。**升の状態は DOM が既に持っている**（`aria-pressed`）ので、そこを先に裏返し、core の答えはあとから受ける。**組み直さない** ── 見た目は既に裏返っていて、組み直せば caret が飛び、長いノートでは一瞬止まる（行番号は動いていない ── 升の一文字が変わっただけなので、札を持たせ直すだけでよい）。効かなかったときは**見た目を戻す**（裏返したままにしない）。電話も同じ ── あちらは `tab.text` が変わると組み直すので、「次に来る字はもう出してある」と受ける印を持たせた | `gui/renderer.js ~ box.setAttribute\('aria-pressed', String\(done\)\);` |
| 409 | 2026-09-08 | （同上）人が書いた生の HTML が、保存のたびに削られないこと | `PAPER.ja.md` は「知らない札は `blockToMd` が中の字だけ取り、札を黙って落とす」と書いていたが、**確かめたら起きなかった** ── core の `to_html` は札を全部逃がして字にするので（`esc`）、`<details>` は `&lt;details&gt;` として**ただの字**で面に出る。往復の試験に四通り足して見張る（生の HTML・`<sup>`・`<script>`・逃がした記号）。**前提が違っていたので、直しは入れずに PAPER を訂正した** ── 起きないものを直すと、起きない場所に註が残る | `scripts/round-test.js ~ '生の HTML'` |
| 410 | 2026-09-08 | （依頼 403 の電話版）表示の面で、触れないかたまりとリンクは**叩くと訊く** | 電話は**一叩きで何も起きなかった** ── 図に行くには 450 ミリ秒の長押ししかなく、それを知らない人には壊れて見える（押せそうなものが押しても動かない）。リンクは一叩きで外へ飛び、**字を直す道が無かった**。窓と同じ顔ぶれ・同じ言葉にする ── 二つの amber で別のことを覚えさせない。**面の中に描かず iOS の小窓で訊く** ── 自前の吹き出しは鍵盤や選び目の丸とぶつかる。押されたものは面の側が憶え、選ばれたら `window.did` が戻る | `ios/Cian/Paper.swift ~ messageHandlers.menu` |
| 411 | 2026-09-08 | 電話の表示の面で打っても、**絵が消えないこと** | `inlineToMd` は段の中の `<img>` を捨てる（`case IMG` は空を返す）── 窓はそれでよい。`findPictures` が `<img>` を `<figure>` で包み、`keepMark` が元の字（`data-md`）を移すので、**包みが丸ごと返る**からだ。**電話は包んでいなかった** ので、表示の面で一度でも打つと絵が黙って消えた ── 消えるのは保存のあとで、消したつもりの無い人には理由が見えない（mac では一生出ない類・四度目）。窓と同じ順で、**札を配ったあとに包む** | `ios/Cian/Paper.swift ~ keepMark\(img, fig\)` |
| 412 | 2026-09-08 | ノートを**複製**したい（本人・Inkdrop と揃える） | 下書きの型を持っている人が、毎回それを開いて全部写していた ── 写すのは道具の仕事。**中身はそのまま、`created` だけ今日**（`updated` は落とす）── 写しは今日できたもので、元の日付を名乗ると並べ替えが嘘をつく。**題に「（コピー）」を書き足さない** ── 題は書いた人の言葉で、amber の都合をノートに書かない（芯の 1）。名前は `名前-2.md` と空いているところへ（`create_new` で取りにいくので、上書きしない）。**絵は写さない** ── 同じフォルダの同じ一枚を二つのノートが指すだけで、`delete` は `.md` しか消さないので片方を消してもう片方の絵が欠けることは無い。決めるのは core 一か所 | `crates/amber-core/src/note.rs ~ pub fn duplicate\(` |
| 413 | 2026-09-08 | 絵の大きさを指せるようにしたい（本人・Inkdrop と揃える） | 書き方は **Marp と同じ**（`!\[width:200px\]\(猫.png\)`・`w:` `h:` も）── ここで新しい記法を作ると、その行は amber の外で通じない。GitHub でも VS Code でも、ただの絵として崩れずに読める書き方にする（芯の 1・ノートはただの Markdown）。**指示は題から外して `style` へ** ── 外さないと「width:200px」が説明文として絵の下に出る。**片方だけ指されたら、もう片方は `auto`** （釣り合いを崩さない）。長さは**数と知っている単位だけ**しか通さない ── `style` に人の書いた字をそのまま入れる道を作らない。読めないものは指示と見なさず題に残す（見えなくなるより、見えるほうがよい） | `crates/amber-core/src/markdown.rs ~ fn picture_size` |
| 414 | 2026-09-09 | 「何をしますか」から、**ノート・フォルダ・タグ・見出し**にも飛べるように（本人） | ここに来る人が探しているのは「やること」より「行き先」のことが多い ── 名前を打てば全部ここに出る、がいちばん覚えることが少ない（VS Code も Inkdrop もそうしている）。**一つの箱に混ぜる** ── 種類ごとに窓を分けると、打つ前に「これは何を探すところか」を思い出す仕事が増える。並びは近いものから: すること → いま開いているノートの見出し → ノート → フォルダ → タグ。見出しが上なのは手元の話だから（画面の下にあると、そこまで目が降りない）。**2026-09-12（依頼 507）に、見出しとノートの段は外した**（見出しは目次、ノートは一覧の探す欄が受け持つ）── 残るのはフォルダとタグ | `gui/renderer.js ~ ── フォルダへ` |
| 415 | 2026-09-09 | 小窓の一覧は、**絞るのは全部から・描くのは頭だけ** | 「何をしますか」がノートを抱えたので（依頼 414）、ここに来る数は**その人のノートの数**になった ── 千本持っている人は一打ごとに千行を組み直す。**探す範囲は狭めない**（打てば下のものも上がる）。**隠したぶんは一行で言う** ── 黙って切ると「無い」に見える。矢印と Enter が歩くのも描いてある行まで（描いていない行へ下りると、選び目が画面の外へ消えたように見える） | `gui/renderer.js ~ const SHEET_ROWS` |
| 416 | 2026-09-09 | 小窓の**束の見出しには、選び目が止まらない** | 「── すること」に選び目が乗ったまま開き、開いた瞬間の Enter が空振りした（実際にそうなった）。押しても何も起きない行が選ばれているのは、壊れているのと見分けがつかない。見出しは床（背景・当たり）も持たない ── 押せるものと同じ形をしていると、押せると読める | `gui/renderer.js ~ const pickable = shown.filter` |
| 417 | 2026-09-09 | **テンプレート**が欲しい（本人・「今の見本ノートみたいな感じ」） | **「テンプレート」というただのフォルダ**にした ── 型のための新しい入れ物を作らない。中のノートは一覧にも普通に出るし、開いて直せる（置き場所は一つ・芯の 1）。名前は決め打ちの一語で設定にしない ── 設定にすると「どこに置けば型になるか」が人によって違い、見本ノートにも書けない。**写す仕組みは「複製」と同じ**（core の `duplicate`）で行き先だけが違う ── 別の道にすると、`created` を今日にするのを片方だけ直した日に二つの作り方が食い違う。**型のフォルダを見ているときはいちばん上へ作る**（そこに作ると型が増えるだけで、書いたものがどこにも出てこない）。電話は「新しいノート」の中に「型から」の段 ── **一つも無ければ段ごと出ない** | `gui/renderer.js ~ async function cmdTemplate\(` |
| 418 | 2026-09-09 | **絵文字**を入れたい。補助の帯からも（本人・「絶対バズる」） | **ノートに残るのは絵文字そのもの**（本人が甲を選んだ・2026-09-09）── `:tada:` を書いて表示のときだけ絵にする道は採らない: amber の外では意味の分からない字が残るし、「表示のまま書ける」との相性も悪い（絵から名前は一意に決まらない）。**入口は板だけ**、`:` を打つ補完は入れない ── 本人「僕でも :tada とか打たない」。覚える記法が増えるだけで、ノートに残るのは同じ一文字。**表は amber が持つ** ── 外の何かを取りに行かない（会社の窓に閉じた機械では出られない）。**全部は載せない**: 日本語で名前を付けたぶんだけ ── 名前の無い絵文字は探せないので、載っていても同じ。足りないものは端末の絵文字盤から打てる（入る字は同じ） | `crates/amber-core/src/emoji.rs ~ pub const GROUPS` |
| 419 | 2026-09-09 | 窓の道具の帯で、**絵文字のボタンだけは絵**（本人が甲を選んだ） | 依頼 288 は「帯は字だけ、絵は置かない」で、その規則はいまも生きている。**ここだけ例外にする理由**: 「絵文字」と書くより 😀 のほうが速く、しかも**訳が要らない** ── 押すと出てくるものの見本が絵そのもので、絵が名前より多くを言っている唯一の道具だから。ほかのボタンに絵を戻す話ではない。**電話は SF Symbol の顔にする** ── あちらの帯はぜんぶ一色の記号で揃っていて、そこに色の付いた 😀 を置くと記号の帯に絵が一つ落ちているように見える | `gui/renderer.js ~ if \(icon\) b.textContent = icon;` |
| 420 | 2026-09-09 | **絵の大きさを、ビギナーでも変えられるように**（本人） | 依頼 413 で記法（`w:200px`）は入れたが、**それは記法を知っている人の道**だった。絵を押して「大きさ…」→ 小さめ・中くらい・大きめ・はばいっぱいから選ぶ。**数は訊かない** ── 「200px」と打てる人は記法で書けるので、ここに来るのは打てない人。**幅だけを指す**（縦も訊くと、釣り合いを自分で守る仕事になる）。選んだ結果は `!\[猫 w:200px\]\(…\)` という**打てる字**として残る ── 押す道と打つ道が同じところへ着くので、片方で付けた大きさをもう片方で直せる。書き換えるのは core 一か所（窓と電話が別々に文字列をいじると、片方の付けた形をもう片方が読めない） | `crates/amber-core/src/markdown.rs ~ pub fn set_picture_size` |
| 421 | 2026-09-09 | **Web クリッパー**（本人・甲乙丙をやる／丁は使われ方しだい） | 甲＝ブラウザで選んでコピー → amber に貼ると見出し・一覧・リンクが生きたまま入る。乙＝URL を渡すと取りに行って一本のノートに。丙＝iPhone の共有シート。**丁（ブラウザ拡張）は見切る** ── amber が口を開けて待つ形になり、会社の端末では拡張もポートも通らない（本人「基本はスクショで貼り付けでなんとかなる」）。**字に直すのは `blockToMd` 一本** ── 面の書き戻しと同じ道を通す。`webClean` がやるのは「均す」ことだけ（要らない札を落とし、入れ物をほどき、行き先を絶対の道にする）── 二本目の変換器を書かないので、片方だけ直した日に貼り付けと書き戻しがずれない | `gui/renderer.js ~ function webClean\(` |
| 422 | 2026-09-09 | 読む面に貼るときは**組んだ形のまま**、書く面には**字で** | 均した HTML を字（`## 段取り`）にして読む面に入れたら、`##` がそのまま字として出た（実際に出た）── 読む面が食うのは組んだ姿で、字ではない。**貼られた枠には元の字を持たせる** ── 枠は触れないかたまりで、`paperToMd` は `data-md` が無いと**何も書き戻さない**: 貼った瞬間から保存が黙って止まる | `gui/renderer.js ~ insertHTML` |
| 423 | 2026-09-09 | 取り込んだノートの**出どころは、本文の最後に字で** | 前書きに `source:` を足す道は採らない ── amber の都合をノートに書かない（芯の 1）。`出典: \[host/path\]\(url\)（2026-09-09 に取り込み）` なら、メモ帳で開いた人にもそのまま読める。日付は**その機械の今日** ── `toISOString` は世界標準時で、日本の朝に取り込むと前書きの `created` と一日ずれた（実際にずれた）。**絵は落としてこない**（本人が決めた）── `attachments/` へ落とすと一本取り込むたびにフォルダが重くなり、消す道も無い | `gui/renderer.js ~ に取り込み）` |
| 424 | 2026-09-09 | 同じ名前の関数を二つ置かない | `landAfter` が二つあった ── 後ろの宣言が前を上書きするので、`readSourceEdit` の末尾が**升の献立のほうの関数**を呼び、番号を渡して落ちていた（`box.closest is not a function`）。表示の面からチェックリスト・リンク・表・水平線・絵の大きさを直すたびに落ちる。**落ちるのは書き終えたあと**なので字は入り、caret だけがどこかへ行く ── 画面には何も出ないので、総ざらいを回すまで誰も気づかなかった | `gui/renderer.js ~ function landOnBox\(box\)` |
| 425 | 2026-09-09 | **全部の動作チェック**を、いつでも同じ手順で回せるように（本人） | 単体の検査が通っているのに実物で落ちる、が何度もあった ── 落ちるのはたいてい「押したときにしか通らない道」で、そこは単体の試験が触れない。`scripts/walk.sh` が八十とおりを実際に動かし、三つを見張る: 例外・`console.error`・**触ったあとまだ字に戻せるか**（戻せないノートは保存が黙って止まる）。**本人のノートと設定には絶対に触らない** ── 隔離した `$HOME` を渡し、`appData` は `$HOME` を見ないので設定は写しを取って必ず戻す | `scripts/walk.sh ~ 設定を戻すのは、何があっても` |
| 426 | 2026-09-09 | 動作チェックは、**見ていないところを名指しで言う** | 「ぜんぶ通りました」は「ぜんぶ見ました」ではない ── 一度目の総ざらいは命令 39 のうち 21、右押しは 8 か所のうち 1 か所しか触っていなかったのに、報せは「81 とおり通った」だけだった。数え上げて突き合わせたら、右押し七か所・まとめて選んだ献立・後回しの命令九つ・**異常系ぜんぶ**が白紙だった。**触れないもの**（OS の小窓が出る八つ）と**まだ検査が無いもの**（窓と電話の字の一致・混ぜる・速さの回帰・大きいノート・文字コード・Windows の実物・初回起動）は、毎回そう言う ── 見えていない抜けが、いちばん危ない | `.claude/skills/check-all/SKILL.md ~ まだ誰も見ていないところ` |
| 427 | 2026-09-09 | **窓と電話が、同じノートを同じ字に戻すこと**（本人・「ずれているなら変だもんね」） | 字に戻す一本（`paperToMd`）は両方が同じ切り出しを使うが、**その前後の糊は別々に書いてある** ── 窓は `gui/renderer.js`、電話は `ios/Cian/Paper.swift` の中の JS。片方だけ足すと、同じノートが端末によって別の字で保存される。実際になった（電話だけ絵が消えた）── 往復の試験は窓の糊しか通していないので素通りした。**電話の糊を抜き出して、両方を同じ入力に通して突き合わせる**。名前がぶつかっていないかも見る（同じ名前で書き直すと、片方だけ直した日に静かにずれる） | `scripts/agree-test.js ~ デスクトップ版と iPhone で文字が違います` |
| 428 | 2026-09-09 | 触ったあとの**往復で、字が一文字も変わらないこと** | 面を行き来しただけで字が変わる、が実際にあった（段落の改行が空白に・点が `-` に・番号が振り直され）── 同期しているフォルダなら全部むこうへ差分として飛ぶ。一文字打ってから・升を押してから・絵文字・絵の大きさ・貼り付け・記号のあとで、それぞれ往復する。**この検査は二度空振りした**: 面を替えるだけでは書き戻しが走らず何も見ていなかったのと、往復に使うノートが走査の前半で既に書き換わっていて壊れやすい行が残っていなかったの。どちらも**変異させて初めて分かった** ── 検査を足したら必ず壊して鳴るのを見る（鳴らない検査は、無いより悪い） | `scripts/walk.mjs ~ 何もしない往復では` |
| 429 | 2026-09-09 | **UTF-8 でないノートも、一覧に出ること**（BOM 付きも、前書きが読まれること） | 一覧は頭だけ読む速い道で、`read_to_string` は UTF-8 でなければ何も返さない ── そのまま素通りさせていたので、**Shift_JIS で書かれたノートが amber から丸ごと消えていた**。`read` op は通るので「開けるのに一覧に無い」という形で、どこから探せばいいのかが画面のどこにも出ない。落ちたときだけ文字コードを見る側で読み直す（ほとんどのノートは UTF-8 なので、速い道はそのまま）。**BOM も字として残っていた** ── 一行目が `\\u{feff}---` になり前書きが前書きに見えず、題も `tags:` も読まれずに前書きぜんぶが本文の書き出しとして一覧に出ていた。総ざらいで見つけた | `crates/amber-core/src/note.rs ~ BOM は本文の文字ではない` |
| 430 | 2026-09-09 | 総ざらいは、**開けたことを確かめてから触る** | 開けなかったとき前のノートが開いたままで、そこに打って「通りました」になっていた ── 一覧から丸ごと消えているノート（依頼 429）を、あやうく見落とすところだった。あわせて `walk.sh` は**エンジンを作り直してから**窓を出す: 窓は起動時の実行ファイルを掴んだままなので、直したはずの判断が効かないまま通ってしまう（実際になった） | `scripts/walk.mjs ~ 一覧にありません（開けませんでした）` |
| 431 | 2026-09-09 | **大きいノートでも保つこと**（開く・面を替える・打つ・目次） | 一万二千行のノートで、それぞれに時間の上限を置く ── 速さは一度測ったきり（開く 236 倍）で、**遅くなったことに気づく仕掛けが無かった**。上限は「人が待てるか」で決める（開くのに三秒かかったら、もう道具ではない）。**通っても測った時間を出す** ── 遅くなっていく気配は、落第になる前に見えていたほうがよい。いまは 開く 203ms・コードへ 128ms・表示へ 782ms | `scripts/walk-harness.mjs ~ 大きいノート（一万二千行）で測ったもの` |
| 432 | 2026-09-09 | **初めて開いた人の道**を、実物で通すこと | 総ざらいはいつもノートのある場所から始まるので、「一本も無いところから」を誰も通っていなかった ── そこは置き場所を決め・見本を入れ・最初の一本を開くまでが一続き。見るのは五つ: 置き場所が決まる／見本が入る／**読める形で出る**（組めずに白いまま、を許さない）／字に戻せる／**二度開いても増えない**。設定も消してから始める ── 残っていると前に決めた置き場所を思い出して、初めての人の道を通らない | `scripts/first-run.sh ~ 初めて開いた人が見るもの` |
| 433 | 2026-09-09 | 同じノートを二か所から書き換えても、**どちらも消えないこと**（実物で） | core の試験は混ぜる判断を見ているが、**窓を通した本物**は誰も通していなかった ── 走査が「向こうの端末」の役をやり、窓が開いたままのノートを横から書き換える。画面の字とファイルの中身の**両方**で、こちらの字と向こうの行が残っているかを見る。壊して確かめた: 混ぜるのをやめさせると「向こうの行が消えました」と鳴り、ファイルの中身も出る | `scripts/walk.mjs ~ 向こうが足した行` |
| 434 | 2026-09-09 | **Windows でも core の試験を回すこと** | 往復（`round-test` ほか）は字を渡すだけなので一台で足りるが、`cargo test` はそうではない ── core はファイルを開き、道を組み立て、改行と文字コードを見る。そこは機械によって振る舞いが変わり、**「mac では一生出ない」類を四度踏んでいる**（`file://`・鍵の名前・絵の道・改行）。組めることだけ見て通していた。あわせて js の試験が `.exe` も探すようにした ── 見つからないと「エンジンがありません」と言って**黙って飛ぶ**（通ったように見えて何も見ていない） | `.github/workflows/release.yml ~ core の試験（Windows）` |
| 435 | 2026-09-09 | 書き出した HTML は、**本当に一枚で完結すること** | 献立は「一枚で完結」と言っているのに、絵は `attachments/…` という**隣を指す道**のままだった ── 書き出した HTML を人に送ると、送られた側では絵が出ない。**言っていることを本当にする**（絵を data: で中へ入れる）。PDF は元から入っていた（刷る側が道を解いていた）。落として来られない絵は道のまま残す ── 消すと「あったはずのものが無い」になる。OS の小窓を実際に押して見つけた | `gui/renderer.js ~ function inlinePictures` |
| 436 | 2026-09-09 | Markdown で書き出したものが、**元のノートと一バイトも違わないこと** | 「ノートそのまま」と言いながら、**最後の改行が落ちていた**（100 バイトが 99 になる）── 面が持っているのは最後の改行の無い姿で、core は保存のときにそれを足している。git に置いている人には毎回そこが差分になる。OS の小窓を実際に押して見つけた | `gui/renderer.js ~ ファイルと同じ文字にする` |
| 437 | 2026-09-09 | 初めて開いた人に、**名前の絵**を見せる（本人・「かっこいいのは丙」「色はつけておしゃれしたい」） | 見本ノートの頭に `ambər` を点で置く。**`ə` は本物** ── 手で描くと `AMBER` になってしまい、**自分で名前を名乗り間違える**ことになる（本人が気にしたのもそこ）。**本人が ANSI Shadow の絵を持ってきた**（2026-09-09）ので、そちらへ差し替えた ── 直したのは三つ: b の縦棒の頭が ə の上に迷子になっていた／a の左端が一列ずれていた／ə が a と見分けにくかった（**上が左に開いていて a と同じ形**だった ── ə は上を丸く覆う）。`\u0060\u0060\u0060amber` の枠だけ琥珀色にする ── メモ帳でも GitHub でもただの枠として読めて、**覚える記法は増えない**（囲みの中に入るのは字だけ）。踏んだのは三つ: Monaco が知らない語彙も span で包んで色を上書きする（黒いまま出た）／半分の高さの記号は**行間ゼロ**でないと縦棒が繋がらず横縞に割れる／43 列は電話では切れる（「ambə」で終わっては締まらない）ので、電話だけ字を小さくする | `packaging/welcome/ambər へようこそ.md ~ \u0060\u0060\u0060amber` |
| 438 | 2026-09-09 | 枠の絵は、**行間ゼロでないと読めない** | 記号（`█` や `╚═╝`）は字の枡いっぱいに描かれるので、行が離れると縦棒が繋がらず**字が横縞に割れる**。1.0 と 1.15 と 1.3 を並べて見て、1.0 だけが字になった。電話は幅 46 列が入りきらないので、そこだけ字を小さくする（初めて開いた人が見るのが「ambə」では締まらない） | `gui/index.html ~ 行間をゼロに` |
| 439 | 2026-09-09 | 電話でも **Web から取り込める**こと（依頼 421 の乙・電話の側） | 窓にだけあると、同じ amber が端末によって別の道具になる。**字にするのは窓と同じ一組**（`paper.js` の `clipTitle` / `bestPart` / `webToMd`）── Swift で二本目の変換器を書けば、同じページが端末によって別の字で残る。そのために見えない `WKWebView` を一つ建てるが、二つ気をつける: 切り出しは `WKUserScript` で入れる（HTML の字に混ぜると、ページ側の `</script` 一つで途中で切れる）・**道は付けない**（`amber://` を渡すと、その道を配る係が居ないので頁がそもそも建たず、切り出しも入らない ── 実際に「取りに行っています…」から動かなくなった） | `ios/Cian/Clipping.swift ~ webToMd\(bestPart\(html\), base\)` |
| 440 | 2026-09-09 | 本文らしいところは、**均す前に選ぶ** | `webClean` は `article` も `main` も皮を剥がすので、均したあとに名乗りを探しても**絶対に見つからない** ── いちばん確かな手がかりを、使う前に自分で捨てていた（`bestPart` の一つ目の枝は、ずっと死んでいた）。選ぶ前に要らない札だけ落としておく（`webDrop`）── 頁の中の長い台本が、字の量で本文に勝ってしまう | `gui/renderer.js ~ 均す前の HTML を受ける` |
| 441 | 2026-09-09 | 出どころの宿の名前は、**窓と同じ形**で書くこと | JS の `new URL(…).host` は番号を付ける（既定の 80 と 443 のときだけ落とす）が、Swift の `URL.host` はいつも落とす ── 同じページを取り込んでも、電話の一行だけ番号が消えていた（`127.0.0.1:8777/` と `127.0.0.1/`）。依頼 427 の「窓と電話は同じ字」は、本文だけの話ではない | `ios/Cian/Clipping.swift ~ 窓と同じ形にする` |
| 442 | 2026-09-09 | 電話の「Web から取り込む」で、**OS の小窓を出さない**（本人が丙を選んだ） | 押しただけでクリップボードを覗くと、iOS が毎回「ペーストしてよいか」と訊いてきて、**amber の言葉ではない小窓が先に一枚出る**（しかも amber からは消せない）。覗くのをやめ、代わりに**貼り付けのボタン**を置く ── 人が押したこと自体が iOS への答えになるので、訊かれない。ボタンを置けるのは紙（`sheet`）だけなので、小窓はやめた。ボタンに出る字（「ペースト」）は iOS が決めるもので変えられないため、何をするボタンかは下の一行で言う | `ios/Cian/Where.swift ~ PasteButton\(payloadType: String.self\)` |
| 443 | 2026-09-09 | **画面に出る言葉は、凝った言い換えをしない**（本人・「道 とか 持ち物 とか 独創的だなｗ」） | 中の文章（この台帳やコードの覚え書き）では「道」「面」「窓」でよいが、**人が読む画面では URL は URL、クリップボードはクリップボード**。窓は前から「ページの URL を貼ってください」と言っていて、電話だけが「ページの道」になっていた ── 依頼 427（窓と電話は同じ字）は、画面の言葉にも効く | `ios/Cian/Where.swift !~ 道を貼って` |
| 444 | 2026-09-09 | 同期が置いていく**書きかけのものを、ノートとして並べない** | 隠しファイル（`.` で始まるもの）は歩く側が落としているので、iCloud の札も macOS の相棒も LibreOffice の錠も出ない。**落ちないのは Windows 側の作法**で、Office と同じ `~$` で始まる置き土産は隠しにならず、`x` という題のノートが一本増えていた。**同期がぶつかった控えは落とさない**（`段取り (競合コピー…).md`）── あれは人の書いた字で、消えていいものではない | `crates/amber-core/src/note.rs ~ n.starts_with\("~\$"\)` |
| 445 | 2026-09-09 | 電話の読み書きは、**クラウドに一言通してから**（PLANS 一の残り） | ノートのあるフォルダは Files の向こう側（iCloud・Drive・Dropbox）のことがある。黙って読むと**まだ降りていないファイルは「無い」ことになり**、黙って書くと書いている最中にあちらが差し替える。`NSFileCoordinator` を通すと、iOS が降ろし終えるまで待ち、書いている間はあちらを止める。**通すのは一本のノートを読むところと書くところだけ** ── 一覧はフォルダを歩くだけで、毎回まるごと待たされる意味が無い。**通せなくても読み書きは続ける**（手元のフォルダなら作法は要らない） | `ios/Cian/Bridge.swift ~ coordinate\(readingItemAt:` |
| 446 | 2026-09-09 | mac の **Dock に ambər を一枚**。押せば、そのときの最新が起きる（本人） | `scripts/dock.sh` が `~/Applications/ambər.app` を作る。**中身は写さない** ── 写すとその日の姿で固まるので、束ねの中身は repo の `gui/` を指す。起きる前に `cargo build -p amber-server` を通すので、午前に直した op が午後の窓に無い、も起きない（窓は**新しいほうの実行ファイルを採る**ので debug で足りる）。作り方は electron-packager と同じで、Electron 一式を写して名前と絵と中身を差し替え、**署名し直す**（Apple Silicon は中身をいじった束ねを黙って起動しない） | `scripts/dock.sh ~ ln -s` |
| 447 | 2026-09-09 | **鍵だけで一周できるか**を、実際に押して確かめる（本人） | 鍵は二か所に書いてある ── 見せる側（`CMDS` の `key`）と、効かせる側（`keydown` の `if` の並び）。別々なので片方だけ直る日が来る。実際に **`⌘⇧/` を二つの命令が名乗っていて**、`⌘⇧/` は mac の「ショートカット一覧」（`⌘?`）が先に取るため、**一覧を畳むは献立にもパレットにも出ているのに一度も押せなかった** ── `⌘⌥/` に移した。見張りは二つ: 同じ鍵を二つが名乗っていないことと、表に載っている鍵を押すと窓の姿が変わること | `scripts/walk.mjs ~ 同じキーを、二つの命令が名乗っていない` |
| 448 | 2026-09-09 | **電話の総ざらいを自動で**（本人） | 窓の総ざらいは画素を押していない ── **窓の関数**を押している。電話も同じにする: 画面の下の関数（`NotesStore`・`Cian`・`Clipping`）を片端から呼ぶ。画素を押す道（XCUITest）を採らなかったのは、**ボタンの位置が変わるたびに検査が壊れる**から ── 壊れた検査は直されず、そのうち誰も走らせなくなる。見たいのは「押せるか」ではなく「押した先が壊れていないか」で、それは関数の側にある。**走査はアプリの後片づけを肩代わりしない** ── `reload()` を気を利かせて呼ぶと、読み直しを忘れた不具合をこちらが隠す（実際に `remove` から読み直しを取り除いても「落ちたものはありません」と言った）。**画素でしか触れないもの**（吹き出し・記号の帯・写真選び・共有シート）は手のまま | `scripts/walk-phone.sh ~ simctl launch --console-pty` |
| 449 | 2026-09-09 | **ノートから使われていない画像を削除する**（本人・甲／歯車の中だけ／今日のぶんも出す） | 貼ったノートを消しても・その行だけ消しても、画像は `attachments/` に残る。消す道がどこにも無く、使っているうちにフォルダだけが重くなっていた。**小さく見せて、選んで消す**（名前だけの一覧では、消していいかを誰も判断できない）。数えるのは core（`spare`）で**窓と電話は同じ一組** ── 同じフォルダを両方から見ても同じ枚数が出る。**枠の中に書いてある道も「使っている」に数える**（出はしないが、消していい理由にはならない）。**読めなかったノートがあれば言う** ── 「使われていない」はぜんぶ読み切って初めて言えることで、黙って少なく数えるのがいちばん危ない。窓は OS のゴミ箱へ入れる（戻せる）、電話にはゴミ箱が無いので訊くときにそう言う。フォルダの右押しには置かない（本人） | `crates/amber-core/src/spare.rs ~ 数えるだけ。消さない。` |
| 450 | 2026-09-09 | 画面では**「絵」ではなく「画像」**と言う（本人・「絵って言われもなぁ」） | 依頼 443 の続き。中の覚え書きでは「絵」でよいが、**人が読む画面では画像は画像**。図（`mermaid`）と絵文字はそのまま ── あれは別のもの | `gui/renderer.js !~ '絵の大きさ'` |
| 451 | 2026-09-09 | **読めない字を使わない**（本人・「釦ってなんだい？読めなくてさｗｗ」） | 依頼 443・450 の続き。「釦」は明治ごろのボタンの当て字で、**いまはまず使わない** ── 画面に二か所出ていた（「帯の釦から」ほか）。画面だけでなく、**覚え書きと台帳もぜんぶボタンに直した** ── 読めない字は、書いた本人以外には無いのと同じ。同じ筋で危ないのは「小窓（＝ダイアログ）」「幕」「帯」だが、こちらは本人が使っている言葉なので残す | `gui/renderer.js !~ 釦` |
| 452 | 2026-09-09 | 画面では**「こよみ」ではなく「カレンダー」**（本人・「こよみは古風すぎる」） | 依頼 451 の続き。あわせて名前を一つに戻した ── いま月表が出るのは**期間で絞るとき**なので、そこは「期間で絞る」だけにする。これから足す本物のカレンダーが「カレンダー」を名乗るので、**同じ名前が二つあると見分けられない** | `gui/renderer.js !~ こよみ` |
| 453 | 2026-09-09 | カレンダー ── **ひと月ぶんを、一度で返す**（本人・「カレンダー取り込み＋確認はそろそろあってもいい」） | 画面が要るのは「この月の、どの日に、何があるか」だけ。いま乗るのは **amber が既に知っているもの**（前書きの `remind:` と `repeat:`、それとノートを書いた日）── **語彙を増やさない**。よそのカレンダー（Outlook ほか）は、この返す形に**足す**かたちで入るので、画面は繋ぐ前から作れる。**書いた日はその機械の日付で**（世界標準時ではなく）── 日本の朝に作ったノートが前の日に並ぶ理由は、見た人には分からない。三十一日の繰り返しは短い月では最後の日へ寄せる（`due_on` と同じ答え） | `crates/amber-core/src/month.rs ~ 語彙を増やさない` |
| 454 | 2026-09-09 | カレンダーの画面 ── **月の表と、その日の中身**（本人が甲を選んだ・「マークダウンの特徴を生かして綺麗に」） | 月の表に予定を並べ、日を押すと右にその日が開く。**予定を押したらノートへ** ── 予定は入口で、書くのはノート（amber がカレンダーアプリになるのではなく、カレンダーがノートへの入口になる）。**何もないところを押したら足せる** ── 足すのは新しいノートで、日付は前書きの `remind:` に書く（依頼 73 の語彙のまま・カレンダーのためだけの置き場所を作らない）。**予定に出ているノートを、下でもう一度出さない**（同じ一本が二度並ぶと、二つあるように見える）。道は出さない ── 読めない長さになるうえ、知りたいのは中身のほうなので、一行目を添える。**ノートを見るところは奪わない** ── 面（表示／コード）はノートのもので、カレンダーはノートではないので、上に重ねて出す | `gui/renderer.js ~ カレンダーはノートではない` |
| 455 | 2026-09-09 | カレンダーを**電話にも**（依頼 454 の電話の側） | 窓と同じ形 ── 月の表、日を押すとその日、予定を押すとノートへ、何もないところで足す。数えるのは core の `month` で**窓と同じ一組**なので、同じフォルダを両方から見れば同じ予定が同じ日に並ぶ。**`Text` に数をそのまま渡さない** ── SwiftUI は土地の決まりで桁を区切るので、年が「2,026年」になった（実際になった）。名前は `Calendaring` がもう居た（期間を選ぶ月表）ので、あちらを `Spanning` に改名した ── 依頼 424 と同じ形で、今度はコンパイラが止めてくれた | `ios/Cian/Calendaring.swift ~ Text\(verbatim:` |
| 456 | 2026-09-09 | **よその予定表を読む**（本人・「普通の人が普通に見えるカレンダーにしよう」） | 会社の Outlook は打ち切った（AVD の中に居て手が届かない・会社の予定は crmaine の領分）。代わりに**どこの予定表でも読める形**（iCal / RFC 5545）を一つだけ持つ ── Google も iCloud も書き出した `.ics` も中身は同じなので、**口が一つあればどこでも読める**（アカウントも鍵も要らない）。**読むだけ**：向こうが正でこちらは写しなので、書けるふりをしない（押したら「直せません」と言う）。**返す形は `month` と同じ**にして、画面は混ぜて並べるだけ。**時間帯の表は抱えない** ── 抱えると古くなった日に静かに一時間ずれる。`Z` だけその機械の時間へ直し、あとは書いてあるとおりに読む。**アドレスは鍵と同じ扱い**で、その機械の設定にだけ持つ（ノートにも `.amber/` にも書かない ── あそこはフォルダと一緒に旅をする） | `crates/amber-core/src/ics.rs ~ 時間帯の表を抱えない` |
| 457 | 2026-09-09 | よその予定表を**電話でも**（依頼 456 の電話の側） | 窓と同じ ── URL を一本貼れば、同じ予定が同じ日に並ぶ（読むのは核の `ics` 一つ）。アドレスはこの iPhone の中だけ（`UserDefaults`）で、ノートにも `.amber/` にも書かない。**月を替えたあとに返ってきた答えは捨てる** ── 網の返事は遅れて来るので、そのまま入れると先月の予定が今月に混ざる | `ios/Cian/Calendaring.swift ~ 月を替えたあとの答えは捨てる` |
| 458 | 2026-09-09 | 画面の言葉を**総ざらいして、独特な言い回しを外す**（本人・「君独自の言い回しがないかもチェックして欲しい」） | 依頼 443・450・451 の続きで、今度は**全部の文字列を出して読んだ**（窓と電話で 980 件）。外したもの: 「面」→ 画面のボタンと同じ「表示」「コード」（ボタンが 表示/コード と言っているのに、説明だけ「面」と言っていた）・「升」→ チェック／マス・「帯」→ 下のボタン・「頁」→ ページ・「道を写す」→ 場所をコピー・「控え」→ コピー。**ボタンや命令の名前が常体なのは直さない**（「削除する」「共有する」は名前であって文ではない） | `gui/renderer.js !~ の面で直して` |
| 459 | 2026-09-09 | **私の造語を、普通の言葉に**（本人が名前を決めた） | 小窓→ポップアップ・工房→ツール・置き場所→保存場所。**「棚」は二か所で別のものを指していた** ── 家族と分けるほうは実際にフォルダなので**フォルダ**（左の列がそう言っている・本人は「ディレクトリ」と言ったが、それだと「フォルダ」と二つの名前になる）、ブックマークのほうはフォルダではないので**グループ**。**「注記」は使えない** ── あれは注記の仲間ぜんぶの名前（下のボタンがそう言っている）なので、その中の一つには当てられない。**昔の依頼の検査が二件当たらなくなった**（依頼 110・253）── 中身は生きていて、言葉が変わっただけなので、検査の字だけ直した | `ios/Cian/Stars.swift ~ 新しいブックマークグループ…` |
| 460 | 2026-09-09 | **この iPhone の予定表を、読み書きとも**（本人・「一般的なカレンダーは読み書きどっちもやりたい」「iPhone はすぐにやろう」） | iOS の設定に足してあるカレンダー（Google でも iCloud でも）を、そのまま読んで、そのまま書ける ── **amber は鍵を一つも預からない**（アカウントを持っているのは iPhone のほうで、借りるだけ）。よその予定表（iCal）との違いは一つ、**こちらは書ける**こと。**足すのはこちらへ** ── 普通のカレンダーとして期待されるのはそれなので、許可があるときは iPhone の予定表に足す（無いときだけノートを作り、小窓にそう書く）。**許可は押されて開いたときに訊く** ── 起きた瞬間に訊くと、何のために訊かれたのか分からないまま断られる。走査は小窓を押せないので、`simctl privacy grant calendar` で先に許可を出しておく | `ios/Cian/Phone.swift ~ 鍵を一つも預からない` |
| 461 | 2026-09-10 | ノートの**途中**にカーソルを置いて絵文字を入れたら、**ノートの頭に入った**（本人が見つけた） | `focus()` は caret を連れてこない ── 板を押した時点で焦点はそちらへ移り、読む面の選択は消えるので、`focus()` だけして入れると先頭に落ちる。**依頼 204 と同じ形**（あちらはかたまりを憶えて直した）だが、絵文字は「いまカーソルが居るところ」に入るものなので、**かたまりでは足りない** ── 字と字のあいだそのものを憶えて、入れる直前に戻す。**電話にも同じ穴があった**（しかも覚え書きに「憶えている caret に入る」と書いてあり、それが嘘だった）ので、**切り出しの中に一組だけ置いて**両方から呼ぶ。あわせて走査に「途中に書く」を足した ── **これまで、入れるものは端でしか確かめていなかった**（端に入るなら途中にも入るだろう、は成り立たない） | `gui/renderer.js ~ caret を連れてこない` |
| 462 | 2026-09-10 | **この Mac の予定表を、読み書きとも**（本人・「mac もやろう」） | Electron から EventKit は呼べないので、**小さな実行ファイルを一つ連れていく**（`mac/amber-cal.swift` → `target/mac/amber-cal`）。AppleScript で「カレンダー」アプリに話しかける道もあるが、あちらはアプリを起こす必要があり月ぶんを読むのに何秒もかかる ── **電話と同じ EventKit** なら、同じ予定表を窓と電話から見て同じ予定が並ぶ。道具に判断は置かない（何を混ぜるかは窓と core の仕事で、あれは OS への口だけ）。**許可が無くても、カレンダーは開く** ── 自分の予定まで消えるのがいちばん困る。**Windows はまだ道が決まっていない**ので「この機械には予定表がありません」と答える。窓と電話で同じ言葉（`here`）を使う | `mac/amber-cal.swift ~ iPhone と同じ EventKit` |
| 463 | 2026-09-10 | 注記の見出しを**普通の言葉に**、バージョンの印を**保護**に（本人が名前を決めた） | 備忘（NOTE）・ヒント（TIP）・重要（IMPORTANT）・注意（WARNING）・警告（CAUTION）。**名前は三か所にあって、しかもずれていた** ── 核と窓は「ノート／こつ／大事／注意／危険」、電話だけ「おぼえておく／こつ／大事／注意／あぶない」で、同じノートが端末によって違う言葉で出ていた（本人が言葉を直そうとして見つかった）。`words-test` で三つが揃っているかを見張る。あわせて残っていた私の癖を外した: 姿→バージョン・写す→コピー・字→文字・はばいっぱい→幅いっぱい・「一覧でこの本を選ぶ」→ノート | `scripts/words-test.js ~ 同じものを、同じ名前で呼んでいるか` |
| 464 | 2026-09-10 | 走査の一歩目が、**時々落ちていた** | 窓が出た直後の一回目は、エンジン（子）がまだ起きていないことがあり、`reload` しても一本も読めない回があった ── 一度きりで見ていたので、**時々鳴る検査**になっていた。**それは無いより悪い**（鳴っても「また不安定か」と読み飛ばすようになる）ので、起きるまで待つようにした | `scripts/walk-harness.mjs ~ 時々鳴る検査は、無いより悪い` |
| 465 | 2026-09-10 | 注記の五つを、**電話でも色で分ける** | 窓は五色に分けていたのに、**電話だけ一色**だった ── 名前を直したときに並べて見て気づいた（依頼 463 と同じ形で、窓と電話がずれていた）。名前が違うだけで見た目が同じだと、警告と備忘が並んだときに見分けられない。窓と同じ色を当てる | `ios/Cian/Paper.swift ~ 五つを色で分ける（窓と同じ色）` |
| 466 | 2026-09-10 | mac の予定表の許可が、**小窓も出ずに断られていた**（OS の小窓を実際に押して見つけた） | macOS は、**なぜ要るのかを持っていない素の実行ファイル**に予定表を触らせない ── しかも「許可しますか」を出さずに、いきなり断る。だから使う人には「許可がありません」としか見えず、許可する道がどこにも無かった。名札（`NSCalendarsFullAccessUsageDescription`）を**実行ファイルの中に埋め**（`-sectcreate __TEXT __info_plist`）、**署名する**（署名の無いものへの許可を macOS は憶えていられない）。**呼ぶ側の束ねにも名札が要る** ── `npx electron .` の素の Electron からは、埋めても出ない。Dock の一枚（`dock.sh` が作る束ね）には入れてある | `scripts/mac-build.sh ~ 名札を実行ファイルの中に埋める` |
| 467 | 2026-09-10 | 窓の**左の列に「カレンダー」が無かった**（本人・「カレンダーってどこだろう？」） | 命令の表には入れたが、**左の列に置くのを忘れていた** ── パレット（`⌘⇧P`）を知っている人しか辿り着けない。電話は一覧の同じ段に出しているので、窓も同じ場所に置く（絵の段階で決めたのは「左の列にカレンダー」だった）。**カレンダーは行き先ではない** ── 押しても一覧の絞り込みは変えない（変えると、押したあとに一覧が空になって何が起きたか分からない） | `gui/renderer.js ~ カレンダーは行き先ではない` |
| 468 | 2026-09-10 | カレンダーを**日・週・月**で見られるように（本人） | 週と日は**時間が縦に並ぶ** ── 「いつなら空いているか」は月の表からは読み取れないし、家族の予定を重ねたときにいちばん効くのがそこ。**見方は憶える**（週で暮らしている人を、開くたびに月へ連れ戻さない）。**動く幅も見方に合わせる**（週を見ている人の「次」は次の週）。**朝が見えているところから始める** ── 開いた瞬間に真夜中が出ていると、毎回スクロールしてから見ることになる。**終日の段は「予定」だけ** ── その日に書いたノートを混ぜると、時刻つきの予定が上と下に二度並ぶ。週と日で**何もないところを押すと、その高さの時刻で足す** | `gui/renderer.js ~ 朝が見えているところから始める` |
| 469 | 2026-09-10 | 予定の**終わりの時刻**も読むこと | 週と日の表では、それが帯の高さになる ── 無いと一時間の予定も三時間の予定も同じ高さで、時間の表にした意味が半分になる。持っていないもの（amber 自身の `remind:` は「その時刻」しか持たない）は三十分ぶんの高さにする（潰れて読めないより、少し大きいほうがよい） | `crates/amber-core/src/ics.rs ~ 週と日の表で高さになる` |
| 470 | 2026-09-10 | 予定が**数千・数万に溜まったら**、毎回ぜんぶ読み直すのは重すぎないか（本人） | 重かった ── 二万本で測って、月を替えるたびに **415 ミリ秒**。原因は二つ。一つ、**同じファイルを二度読んでいた**（題を読むのに一度、`remind:` を読むのにもう一度、どちらもまるごと）。二つ、頭の六十行が欲しいだけなのに**最後まで読んでいた**。読むのは一度・要る行までにして 415→**58 ミリ秒**。**憶えておいて、変わったものだけ読み直す** ── 日付と大きさが同じなら前に読んだものを使う。ただし**秒までしか見ない**と、同じ一秒のあいだに長さも変えずに直した題が古いまま出る（そこが速さより怖いので、細かく見る）。**索引をファイルに書かない** ── `.amber/` はフォルダと一緒に旅をするので、別の端末で古い索引を読むことになる | `crates/amber-core/src/month.rs ~ 変わっていなければ、読み直さない` |
| 471 | 2026-09-10 | カレンダーに**グループの見方**を（横に時間、縦に人）。会社の Outlook の予定は、別の道具が三十分おきに書き出す **CSV** から読む（本人） | 「いつなら空いているか」は、人ごとに一段でないと読めない ── 一人ぶんの縦の表を頭の中で重ねる作業を、毎回やらせないため。**取りに行かない** ── 置いてある一枚を読むだけで、書き戻しもしない（会社の網の中にしか無い紙なので、家では必ず読めない ── そのたびに騒がない）。列の取り決めは `docs/team-csv.ja.md`。ただし**その形だけを読むようには作らない**。決め手になるところ: **鍵は名前ではなくメール**（同姓・改姓・全角半角で揺れる）。**予定の無い人も段を持つ**（段ごと消えると、書き出せていないのか本当に空なのかが読めない ── いちばん知りたいのがそこなのに）。**「空き時間」と取り消しは予定ではない**。**何日もある終日は日ごとに出す**（初日にしか出さないと、二日目から居るように読める）。**終日の日付は時差で直さない**（前日にずれる）。**件名の中の読点で列がずらない**（ずれると、その行から先の全部が横にずれる）。**いつ時点の紙かを画面に出す**（三十分おきに置き換わるので、これが無いと古い紙を今の予定だと思って読む）。**中身の見えない予定は縞にする**（「取れなかった」と「見せてもらえていない」は違う） | `crates/amber-core/src/team.rs ~ 段は、予定より先に立てる` |
| 472 | 2026-09-10 | 週の表が、**月をまたぐと半分白紙**になっていた（471 を作っている途中で見つけた） | 九月三十日から十月四日までの週で九月ぶんだけを読むと、十月の四日が空のまま出る ── 予定が無いのか、読んでいないのかが画面からは区別できない。**出している範囲がまたぐ月を、ぜんぶ読む**。読み直しの重さは 470 の憶えで消えているので、またぐ週だけ二か月ぶん歩いても効かない | `gui/renderer.js ~ 週は月をまたぐ` |
| 473 | 2026-09-10 | 「みんな」ではなく**グループカレンダー**。**そこから何人かに絞って**出せること。それと、CSV を読む道は**素人さんの目に触れないところ**へ（本人） | 十五人の段から三人を探すのは、目でやる仕事としては重い ── **名前を押せば引っ込み**、「人を選ぶ」で戻せる（**引っ込めたその場で戻し方を言う** ── 押して消えたものの戻し方が画面のどこにも無いのが、いちばん困る）。選んだ人は憶える。CSV の二行は**合言葉（`TEAM_WORD`）を打つまで、どこにも出ない** ── 初めて amber を見た人の一覧に会社の話が混ざると、何の話なのか分からないまま面食らう。合言葉を**日本語にしない**のは、変換の途中で偶然出てしまわないため | `gui/renderer.js ~ 合言葉のあるものは、打たれるまで出てこない` |
| 474 | 2026-09-10 | 色の名前を**間違えても、何も言わずに効かなくなっていた**（終日の帯が一日ぶん敷いてあるのに、画面には何も無かった） | `var(--ink3)` は本当は `--ink-3`。字の色なら親から継いだ色になり、**背景なら透明になる** ── 描いたはずのものが無い、という形でしか現れない。日曜の赤（`--bad`）も、そもそも定義が無いまま呼んでいて、ずっと出ていなかった。**呼んでいる名前が、どこかで定義されているかを数える**。中身が正しいかは見ない（そこは目で見るしかない）。ついでに、段の名前を `row` にしていたのもやめた ── 一覧のノート一行が同じ名前を持っていて、そちらの余白が段にも効き、目盛りだけ 16 ずれていた。**一般的すぎる名前は、いつか必ず当たる** | `scripts/paint-test.js ~ どこにも定義がありません` |
| 475 | 2026-09-10 | 合言葉を**毎回打たせないでほしい**。件名の取れない予定に「空」と出るのは変（本人・「何もない予定のことを空ってでるの？」） | 合言葉（473）は「**まだ読んでいない人**の一覧に会社の話を混ぜない」ためのもので、もう読んでいる人のためのものではない ── 読んでいるあいだは、**いつ時点かの字がそのまま入口**になる（読み直す／別のファイルにする／やめる）。読んでいなければ、その字ごと出ない。「空」もやめた ── 直前の行で「空き時間」を落としているので、同じ字を使うと「空いている」と読める。そこに帯が出ている以上、**予定はある** ── 見せてもらえていないなら「非公開」、読み取れなかっただけなら「件名なし」 | `gui/renderer.js ~ 合言葉は、ここでは訊かない` |
| 476 | 2026-09-10 | チームの紙は**開いたときに一度読む**。そのあと読み直したいときは、日付ではなく**「更新」を押したとき**。あとは**毎時十分**に読みにいってよい（本人・元の CSV が毎時零分に置き換わる） | この紙は「今日から何日ぶん」の**一枚**で、月では分かれていない ── 月を替えるたびに読み直すと、途中で置き換わったときに**月によって時点の違うものが並ぶ**。前の月へ戻ったら紙が入れ替わっていた、が画面の上でいちばん分かりにくい壊れ方なので、**読むのは人が読めと言ったときか、決めた時刻だけ**にする。**零分ちょうどには読まない** ── 書いている途中の紙を読むと、列の揃っていない半分だけの表になる。十分待てば書き終わっている。**次の約束は、起きてから置く**（先に置くと、寝ていた機械が起きたときにまとめて鳴る）。**読めなくても、前に読めたものは捨てない** ── 網の外に出た人の画面から段が黙って消えると、「読めていない」のか「予定が無い」のかが区別できない | `gui/renderer.js ~ 日を替えたときには読まない` |
| 477 | 2026-09-10 | **題を押してもタイトルが打てない**。**「この日に予定を足す」を押しても何も起きない**。カレンダーを開いているのに、左の列は「すべてのノート」が光ったまま（本人） | 三つとも別の理由。一。`#top` は窓ごと引きずる四角（`-webkit-app-region: drag`）で、そこから抜けるには **`no-drag` と書くしかない** ── 何も書かないと `none` になるが、`none` は「引きずらない」ではなく「切り抜かない」。OS が先に押しを取るので `onclick` は一度も鳴らない。**作った押し（`el.click()`）は OS を通らない**ので、総ざらいはずっと素通りしていた ── 見るのは押しの通り道ではなく**四角のほう**にした（同じ穴が目次の釦にもあった）。二。小窓（`#veil`）が z-index 30 で、カレンダー（60）の裏に開いていた ── 開いてはいるが暗幕の下なので、画面の上では「押しても何も起きない」にしか見えない。三。行き先は変えないまま、**開いているあいだだけ光らせる**（どこを見ているかは画面が答えるべき） | `gui/index.html ~ 引きずらない」ではなく「切り抜かない` |
| 478 | 2026-09-10 | カレンダーが**小さい**。右のその日の欄は要らない。**ノートと同じ場所**に出せないか（本人・「F12 で最大化してさらに見やすくなるだろうし」） | 小窓として重ねると左右が狭く、全画面にしても広がらない ── 予定を眺めるのは「ちょっと開く」ではなく「しばらく居る」ことなので、**面の一つ**にしてノートと同じ広さで扱う。右の欄は落とした（同じことが表の中に出ている）。**地を押しても閉じない**（小窓ではないので、何もないところを押すのは「閉じる」ではない）。**ノートを開いたらカレンダーからは出る**（同じ場所なので、開いたノートが裏に隠れる）。一時間の高さは 2.1rem → 3rem。**色は五か所に別々に書いてある** ── 種類を一つ足すとどこかを書き忘れる（チームの紫が、月の升目と平の週表で琥珀のまま残っていた）。同じ予定が見方によって別の色になるのはいちばん困るので、**五つの見方ぶん揃っているかを数える** | `scripts/paint-test.js ~ 見方によって別の色になります` |
| 479 | 2026-09-10 | **Mac と iPhone のノートの同期を、実際に動かして確かめたい**（本人） | 手元で両方を同じフォルダに向けて（電話の入れ物は Mac の中の実在のフォルダなので、雲を挟まずに同じ形が作れる）、書いて・直して・ぶつけた。**運ぶところは無事だった** ── 字も絵文字も往復し、二台が同じノートを別々に直しても**突き合わせて両方残った**。壊れていたのは**気づくところ**で、三つ出た。一。**電話は、戻ってきても読み直さない** ── 引き下げれば来るが、そこに新しいものがあると知らない人は引き下げようと思わない。二。**窓は、最後に保存したノートへの外からの変更を、そのあとずっと無視する** ── 跳ね返りを打ち消す道（`lastWrote`）を憶えたまま消していなかった。「窓で保存 → 電話で直す → 窓は何も知らない」がこれ。三。**読み直しが先に「開いている行」を繋ぎ直す**ので、そのあとの「変わったか」が新しい値どうしの見比べになり、いつも「変わっていない」になっていた ── 開いているノートの字だけが黙って古いまま残る | `gui/renderer.js ~ 見比べる相手を、先に控える` |
| 480 | 2026-09-10 | 電話が、**戻ってきたときに読み直す**こと（479 の一） | 同じフォルダを窓と電話が触るので、電話を置いているあいだに向こうで書かれる ── 読み直さないと、開いた瞬間の一覧が古いまま出る。**「引き下げれば来る」では気づけない**（そこに新しいものがあると知らない人は、引き下げようと思わない）。**打ちかけを抱えているときは触らない**（打っている字を下から書き換えない）。一覧の `body` は型を追いきれる限界に居るので、`ViewModifier` に出す ── 一行足すだけで組めなくなる。`scenePhase` の `onChange` は呼ぶ場所で新旧どちらの形に解けるかが変わるので、**前に出るという報せを直に受ける** | `ios/Cian/ContentView.swift ~ 戻ってきたら、読み直す` |
| 481 | 2026-09-10 | **同じ操作を、考えうる限りすべての場所で**（行頭・行中・行末・文書の頭／途中／末尾・一行だけのノート…）確かめる検査を、いつものチェックに足す（本人）。正しい挙動が分からないものは勝手に決めず、本人に訊く | 網（`scripts/walk.sh grid`）── `grid-page.js` が窓の中で caret を置いて押し、`grid.mjs` が判断する。決めごとの無いところは落第にせず**「見たまま」**として報せに集める（本人に見せて訊くための材料）。行ごとの記号・Tab・升は**行のどこで押しても同じ結果**であることを見張る | `scripts/grid.mjs ~ const SAME_ANYWHERE = new Set\(` |
| 482 | 2026-09-10 | （481 の一）読む面で記号を押しても、**字が消えない**こと ── 段落で「番号リスト」を押すと段落が消え、一覧の途中の行頭で Backspace を押すと入れ子が消え、項目で「見出し」を押すと字が消え、表のセルで「箇条書き」を押すとセルが空になっていた（網が捕まえた） | Chromium の `insertOrderedList` は隣に一覧の無い段落で `<p><ol>…</ol></p>` を作り、字に戻す側が段落の中の一覧を飛ばしていた。**判断は切り出しの `blockAs` に一組**（窓と電話が同じものを呼ぶ）── 段落の中に潜った一覧は外へ出す（`tidyLists`）、字に戻すときも落とさない、記号を外すとき入れ子は下に残す、項目を見出しにする前に点を外す、**表のセルでは一覧にしない**（受けて何もしない） | `gui/renderer.js ~ function tidyLists\(` |
| 483 | 2026-09-10 | 網で見つかった決めごとの無いところ、本人の答え（2026-09-11）── ①注記を入れたら**打てる空の行を中に置き caret をそこへ**、③点の項目で番号（番号で点）は**その一行だけ種類を替える**、④チェックリストは**caret の一行だけ**（点・番号・引用・見出し・字下げは外して升に）、⑤項目で引用は**段落にしてから引用に**、⑥貼り付けは**セルには字だけ・項目には字だけ（改行ごとに項目）** | 判断は切り出しに一組（`checkLine`・`switchItem`・`fillAlerts`・`pasteLines`・`blockAs`）── 窓の帯も電話の帯も同じものを呼ぶ。空の注記は札だけ書く（`> ` の行を書くと次の保存で `>` に変わり、同期先に差分が一度飛ぶ）。表のセルでは升も一覧も何もしない | `gui/renderer.js ~ function checkLine\(` |
| 484 | 2026-09-11 | 網の決めごとの残り（本人の答え）── ②行末の Delete は**次が記号付きの行なら何も起きない**（段落と段落は繋がる）、⑦⑪**空の記号だけの行はファイルに書かない**（面には見えるが、字を打つまで）、⑧注記の途中の Enter は引用と同じ**改行**、⑨字下げた段落を項目にしたら字下げは外す、⑩表のセルで一覧・見出しのボタンは何もしない | `checkDel`（項目・セル・箱の終わり、次が素の段落でないとき止める）・`blockToMd`（字の無い見出し・項目は書かない）・`quoteEnter`（引用・注記の途中は `<br>`）。どれも切り出しの中 ── 窓と電話で一組 | `gui/renderer.js ~ function quoteEnter\(` |
| 485 | 2026-09-11 | （網の二周目）入れ子を持つ項目の行末で Delete を押すと入れ子を吸い込む／済んだ升の行頭で Enter を押すとチェックが空の行に移る | 項目の「終わり」は入れ子の手前（`atOwnTail` ── `textContent` は子の字まで含むので `atTail` では終わりにならなかった）。升の行頭の Enter は**上に空の升を置き、字は自分の升と一緒に残す**（後ろへ送ると `- [x] やった` が `- [ ] やった` になる） | `gui/renderer.js ~ function atOwnTail\(` |
| 486 | 2026-09-11 | 網を電話の「表示」の面でも回す（窓で通ったことは電話で通ったことにならない） | `ios/Cian/Mesh.swift` ── 本物の面（`Paper.page`）を同じ配管で建て、`walk-phone.sh` の最後に七百五十六とおり押す。決めごとの表は窓の `grid.mjs` の写し。あわせて電話にも行末の Delete（`checkDel`）と選んで消すときの表の守り（`checkCut`）を繋いだ。見出しにした行の字下げは外す | `ios/Cian/Mesh.swift ~ enum Mesh` |
| 487 | 2026-09-11 | 混ぜ方は **Git のマージの仕組みを可能な限りそのまま**取り込むこと。別々の場所は混ぜ、同じ場所は両方残して人に選ばせる。一致は空白を含めた完全一致 | `merge.rs` を `git merge-file` の写しに置き換えた（Myers ＋ `xdl_change_compact` の塊ずらし ＋ `xdl_do_merge` の重なり判定 ＋ zealous の同じ行の外出し ＋ 三行以下のあいだの巻き込み）。ぶつかった場所は `spots`（こちらの行の範囲と向こうの行の範囲）、前書きの鍵は `fields` で返す。**Git を正解に、でたらめな直し四百通りで一字一句同じ**ことを見張る試験と、失わないことの性質の試験（三千通り）を置いた | `crates/amber-core/src/merge.rs ~ fn git_merge_file_と結果が一致する` |
| 488 | 2026-09-11 | 同じ場所を両方で直したら、**競合として見せて、どちらを採るか選ばせる**。削除も同じ三択の形（こちらを残す／〇〇を残す／両方）。相手は「その版を書いた人と端末」の名前で。タグも同じ | 窓の読む面に、ぶつかった場所ごとの選び口（`placeGadgets`・`.gadget` は字ではないので `paperToMd` が飛ばす）。帯（`drawBand`）に数と前・次・ぜんぶ。ぶつかった場所は**行の中身**で憶える（行番号は打つたびに動く）。前書きの鍵は帯の中で（`chooseField`・タグの「両方」は和集合）。混ぜる前のこちらは履歴に必ず残す（`gap: 0`） | `gui/renderer.js ~ function placeGadgets\(` |
| 489 | 2026-09-11 | 同期は **Google Drive の API を amber の中から**（案 甲）。使う人は ⚙ →「同期」→「Google でサインイン」→ ブラウザで「許可」→ 窓に戻る、だけ。URL を打たせない・Google の設定画面を触らせない | `gui/drive.js` ── OAuth 2.0 の PKCE（毎回その場で作る使い捨ての合言葉）と 127.0.0.1 の折り返し。鍵は `safeStorage` で暗号化して `userData/drive.token` に。クライアント シークレットは repo に置かず、要るなら `userData/google.json` から。切れた鍵は黙って新しくする。やめると Google 側の許可も取り消す。偽の Google で一周する試験（`scripts/drive-test.js`） | `gui/drive.js ~ function createDrive\(` |
| 490 | 2026-09-11 | 同期の**上げ下ろし**（Drive の一覧 → 手順 → 運ぶ → 憶える）。押すのは同期だけで、あとは保存の三秒後・三十秒ごと・窓に戻ったときに勝手に運ぶ。**同じ行を両方で直したら両方残して選ばせる**（`merge` の写し・分かれる前の姿は `synced` が `.amber/base/` に取っておく）。向こうで消したノートはこちらのゴミ箱へ、こちらで消したら向こうのゴミ箱へ | `gui/renderer.js` の `syncNow`（判断は core の `syncplan`・運ぶのはここ・運べたぶんだけ `synced`）。開いているノートが下りてきたら、打ちかけでなければ自分で開き直す（見張りは保存直後の変わりを自分の跳ね返りとして捨てるので）。`gui/drive.js` の `list / upload / download / trash`（`appProperties` に `rel` と `print` と `by`）。偽の Drive（`scripts/fake-drive.js`）を相手に、上げ・下ろし・直し・同じ行のぶつかり・選び・消しを順に押す走査（`scripts/walk-sync.mjs`・`walk.sh walk-sync` で単独でも回る） | `scripts/walk-sync.mjs ~ 同期：同じ行を両方で直すと、両方残って選び口が出る` |
| 491 | 2026-09-11 | 同期の様子を**一覧のいちばん上に**（案甲）。始める前は色つきの列「まだ同期していません」＋「同期をはじめる」「あとで」（あとで＝この窓のあいだだけ畳む。次に開いたらまた出す）。運んだ直後は数秒「同期しました ── hh:mm／アップロード2本・ダウンロード1本」。ふだんは一行「同期しています ・ 最終 hh:mm ・ メールアドレス」。困ったときは赤い列「インターネットに繋がっていないようです。次回接続時に同期します。」＋「接続確認する」。画面の字に 上げる／下ろす／運ぶ を使わない | `#syncsay`（列）と `#syncmark`（一行）。`drawSyncState` が様子から描き分ける（`syncTroubleFace` が困りごとを人の言葉と押すものに）。偽の Drive に `/_break`（繋がらない体）を足し、走査で四つの姿を押す（`walk-sync.mjs`） | `gui/renderer.js ~ 接続確認する` |
| 492 | 2026-09-11 | **ファイル名は題に合わせる**（乙）。Drive のファイル名と amber の題が合わず探しにくかった（`2026-09-06 19-18-30.md`）。決めごと: 改名は題の欄から出たとき／名前の元は一覧に出ている題（`title:`→見出し→一行目）で、見出し・一行目のノートは離れたとき／使えない字は全角に／同じ題は `買い物.2.md` で番号は加算／絵・履歴・憶えも付いてくる／同期は削除＋新規ではなく改名として運ぶ／時刻名のノートは一度だけ揃える | `crates/amber-core/src/naming.rs`（`title_name`・`settle`・`relocate`・`tidy_names`）、`sync::plan_with_moves`（ID で改名を見つける・両方で変えたら向こうに従う）、api の `settle`/`syncmove`/`tidynames`、Drive の `rename`（同じ ID で名前と親を付け替え）。窓は `settleName`（欄から出た・別のノートを開く・窓から出る・十秒手が止まった）。走査は `nameAuto` で切って改名の段だけ入れる | `crates/amber-core/src/naming.rs ~ pub fn settle\(` |
| 493 | 2026-09-11 | カレンダーの言葉と小窓（本人）。⚙ にカレンダーは要らない（左の列に居る）。「予定を登録する」の小窓は一枚で「タイトル」（小窓の見出しも「予定を登録する」── 2026-09-13 に本人が「予定を足す は AI 的」と言ったので、ボタンと同じ字に揃えた）。時刻は打たせず**プルダウン**、終日に印を入れたら時刻は選べない、開始を選んだら終了は一時間後、空のままなら入力エラー。予定の三択は「タイトルを修正する」「予定を削除する」。「訊きます」は変 → 普通の日本語。⚙ の「よその予定表を読む」→「カレンダー設定追加」、「読むのをやめる」→「カレンダー設定解除」、「ノートから使われていない画像を削除」→「不要添付削除」（説明に元の言葉）。**2026-09-12 の見直しで本人が「使われていない画像」「予定を修正する」に変更** | `askEvent`（`#evform`・十五分刻み・終日で無効化・開始→終了＋1h・空はエラー）。Mac の予定表には終わりの時刻も渡す（`amber-cal add` の五つ目）。ノートに持てるのは始まりだけ。走査は小窓を一枚で押す | `gui/renderer.js ~ name: '使われていない画像'` |
| 494 | 2026-09-11 | カレンダー（本人）: 出す予定表を絞れるように（Google の誕生日は要らない）、表示が小さく色が淡くて読みにくい、土日を出すかは設定で、日・週・月・みんなの表で**二度押しと右押し**すると押した場所に予定を足す／押した予定を直す | ⚙「カレンダー表示設定」（`cmdCalSettings`・どの予定表を出すか＝`calHide` をどの見方にも効かせる `calShown`・「土日を出す」＝`calWeekend`）。Mac の予定表の名前は `amber-cal calendars`。字を大きく色を濃く（`#calbox`）。二度押し＝`calSpotAt`/`calItemAt` で押した日・時刻か予定を取り、足す／直す。右押し＝同じものを献立で。週と日の表の一度押しは日を選ぶだけに | `gui/renderer.js ~ function calSpotAt\(` |
| 495 | 2026-09-12 | 隣の cian は選べるテーマが三倍以上ある。**全テーマを全く同一に合わせたい**（amber も cian と同じに） | cian の十八の配色（`cian-core/src/theme.rs` の `PRESETS`）と窓の三つの装い（白磁・陰翳・端末譲り）を `gui/palettes.js` に写し、amber の変数（十五）に組み替えて着せる（`themeVars`・`setTheme`）。並びも cian と同じ。琥珀の三つは頭に残す。ayu と紙は cian の表に譲った。写しが古くなれば `scripts/themes-test.js` が鳴る（`--write` で作り直す） | `gui/palettes.js ~ const CIAN_PALETTES` |
| 496 | 2026-09-12 | フォルダへ移したノートも、同期では「消して新しく上げる」ではなく**名前が変わった**として運ぶ（依頼 492 の残り） | `move` op に `root` を渡すと、改名と同じ一本（`naming::carry`）で履歴の棚・共有から戻る場所・同期の憶えを連れて行き、次の同期が `movethere`（Drive は同じ ID で名前と親フォルダを付け替え）になる。窓の四か所の `move` は `root` を渡す | `crates/amber-core/src/naming.rs ~ pub fn carry\(` |
| 497 | 2026-09-12 | 絵（`attachments/` の中）も同期で運ぶ ── 絵を貼ったノートが、向こうの端末で絵だけ欠けていた | `sync::assets` が `attachments/` の絵をノートと同じ手順書に乗せる（道は `仕事/attachments/段取り-123.png`）。bytes は描く側を通さず主の側で読み書き（`driveUploadFile`／`driveDownloadFile`・仮の名で書いてから改名）。両方が変わった絵は混ぜられないので、こちらを残して向こうのものは `名前.2.png` として隣に置く。分かれる前の姿は取っておかない | `crates/amber-core/src/sync.rs ~ pub fn assets\(` |
| 498 | 2026-09-12 | グループカレンダーは人ごとに色を変えて（花木は青・山田はピンク・佐藤は黄のように）。個人のカレンダーは既定の色を選べるように（いまは淡い緑、本人はオレンジが好き） | 段ごとに `--lane`（`laneColor`: 決めてあればその色、無ければ並んだ順に五色（青・ピンク・黄・緑・紫）を回す）。名前の右押しで色を選ぶ（`calColors`）。個人カレンダーの色は「カレンダー表示設定」の「個人カレンダーの色」で十色から（`calHereColor` → `--cal-here`） | `gui/renderer.js ~ function laneColor\(` |
| 499 | 2026-09-12 | iPhone の配色も cian と同じ二十一に揃える（本人「進めておいて」） | `ios/Cian/Palettes.swift` を `themes-test --write` が窓と同じ元・同じ算数（`palettes.js` の `amberVarsOf`）から作る。設定の「見た目」の下に「配色」。選ぶと明暗はその配色に従い、面（WKWebView）は `window.setPalette` で十五の変数を差し、面の地もその配色に。tint はシアンのまま（依頼 75）、iOS の一覧などの地は iOS の色のまま（明暗だけ従う） | `ios/Cian/Palettes.swift ~ enum Palettes` |
| 500 | 2026-09-12 | iPhone でも同期（Google でサインイン一回・上げ下ろし・改名・絵・両方向の消し）。窓と同じ作り | `ios/Cian/Drive.swift`（OAuth 2.0 PKCE・`ASWebAuthenticationSession`・折り返しは iOS 用クライアントの URL スキーム・鍵はキーチェーン・Drive の一覧／上げ／下ろし／ゴミ箱／改名は窓の `drive.js` と同じ札）と `Syncing.swift`（窓の `syncNow` の写し・判断は core の `syncplan`・打ちかけでない札だけ読み直す）。設定に「同期」、一覧の頭に一行。保存の三秒後・三十秒ごと・前に出たとき。走査（`walk-phone.sh`）は偽の Drive を相手に上げ・下ろし・直し・改名・絵・消しを押す | `ios/Cian/Syncing.swift ~ func now\(` |
| 501 | 2026-09-12 | iPhone にも、同じ行を両方で直したときの選び口を（本人「まだ無いものは今のうちに」） | 札（`Desk.Tab`）が `spots`・`fields`・`who` を持ち（行の中身で・引き出しに憶える）、帯に「〇〇と同じところを N か所で直しています」と「選ぶ」。小窓（`Choosing`）で一つずつ「こちらを残す／〇〇を残す／両方」、上に「ぜんぶ」。選ぶ算数は窓の `chooseSpot`／`chooseField` の写し（`Desk.rowsOf`・`spotAt`）。保存の衝突でも同期のぶつかりでも同じ札 | `ios/Cian/Choosing.swift ~ struct Choosing: View` |
| 502 | 2026-09-12 | iPhone でも、ファイル名を題に合わせる | 電話には題の欄が無いので、**離れたとき**（札を閉じる・別の札に替える）に core の `settle` で揃え、置き場所を開いたときに `tidynames` で時刻名を一度だけ揃える。改名した札は新しい道の札に差し替える。同期は改名として運ぶ（`movethere`） | `ios/Cian/Desk.swift ~ func settle\(` |
| 503 | 2026-09-12 | iPhone の設定の文言を窓版に合わせる | 「ambər 保存ディレクトリ変更」「ノートを取り込む」「カレンダー設定追加」「不要添付削除」（説明に「ノートから使われていない画像を…」）。「見た目」と「配色」の二つの選び口は、窓の「テーマ」と同じ一つの表（琥珀 ── iPhone に合わせる／明るい／暗い、そのあとに cian と同じ二十一・同じ鍵）に | `ios/Cian/Where.swift ~ Picker\("テーマ"` |
| 504 | 2026-09-12 | iPhone: フォルダを作る印を「フォルダ」の見出しの右に（パッと探せなかった）。「ノートを探す」が小さい → 最初から入力欄を出す。タグ・期間・フォルダの札が小さい → 大きく | 探す欄は題の下の検索の帯（iOS の標準・いつも出す）。作る印は「フォルダ」の見出しの右（`folder.badge.plus`）。絞り込みの札は `.subheadline` と広めの余白（`Sifting`） | `ios/Cian/ContentView.swift ~ accessibilityLabel\("フォルダを作る・選ぶ"\)` |
| 505 | 2026-09-12 | 並びを決めた。iPhone: ノートを探す → タグ／フォルダ／期間 → 同期の様子 → カレンダー → ＋新しいノート → すべてのノート → ブックマーク → フォルダ。窓の左の列: カレンダーをノートと切り離していちばん上に | 窓は `drawRail` でカレンダーの行き先を名札のすぐ下に（ひと呼吸の隙間のあと「新しいノート」→「ノート」の段）。電話は一覧の頭の段をその順に組み直した（`ContentView`） | `gui/renderer.js ~ カレンダーは、いちばん上に一つ` |
| 506 | 2026-09-12 | 「テンプレートから新しいノート」を選んでも動かない（テンプレートが無いと帯に一言出るだけだった）。見本のノートをテンプレートにできないか、良い案は | 無いときは小窓: 「見本のテンプレートを入れる」（週報・議事録・買い物リストの三枚・`packaging/templates`・窓は `amber:templates`、電話は束ねの `templates`）／「今開いているノートをテンプレートにする」。どのノートも「このノートをテンプレートにする」（⋯ の献立・電話は長押し）で「テンプレート」フォルダへ写せる。電話の「新しいノート」の小窓は、型が無くても「テンプレートから」の段を出して見本を入れられる | `gui/renderer.js ~ async function cmdToTemplate` |
| 507 | 2026-09-12 | ⌘⇧P／Ctrl+Shift+P の表から「カレンダー」「このノートの見出し」「ノートを開く」を外す | カレンダーは左の列のいちばん上、見出しは目次（⌘⇧O）、ノートは一覧の探す欄が受け持つ。残るのは すること・フォルダへ・タグで絞る | `gui/renderer.js !~ ── このノートの見出し` |
| 508 | 2026-09-12 | iPhone: カレンダー・新しいノート・すべてのノートの三つの段は、大きさと色を揃える（すべてのノートの印だけ色が無い・字の大きさがばらばら） | 三つとも印は 26pt の枠にアクセント色、字は 16pt の太め（`ContentView` の一覧の頭） | `ios/Cian/ContentView.swift ~ 三つの段は同じ大きさ・同じ色` |
| 509 | 2026-09-12 | 窓の左の列でも、カレンダー・新しいノート・すべてのノートの大きさと色を揃える | `.dest[data-kind="cal"]` と `[data-kind="all"]` を `#new` と同じ字（太め・同じ大きさ）に、印はアクセント色で 18px | `gui/index.html ~ 同じ大きさ・同じ色\*\*（本人・2026-09-12）` |
| 510 | 2026-09-12 | 窓でカレンダーを押したとき、「すべてのノート」の光りが残る。カレンダーだけが光って、元の光りは消えてほしい | `drawRail` の `on` は `calOn` のあいだ偽。閉じれば行き先の光りが戻る | `gui/renderer.js ~ カレンダーを出しているあいだは、カレンダーだけが光る` |
| 511 | 2026-09-12 | **保存ディレクトリを複数に**（本人が決めた乙・案ア・案B）。会社の SharePoint（WebDAV）と家の Drive のように、同期する場所としない場所を並べたい。同期の入れる切るは保存ディレクトリごと（この保存ディレクトリは Drive、これは同期なし ── iCloud・OneDrive はこれから）。入口は ⚙「保存ディレクトリの追加・変更・削除」一つで全部（一覧 → 一つ → 同期先／名前／場所／外す） | `state.places`（`[{name,dir,sync,at}]`・前の `root` 一つから引き継ぐ）。フォルダは絶対の道で持ち、core には `relOf` で相対を渡す。左の列は二つ以上のときだけ保存ディレクトリが親の行（Drive の札つき）。同期は保存ディレクトリごとに `syncPlace`（帳面はそれぞれの `.amber/sync.json`・Drive はいちばん目が `ambər` 直下、二つ目から `ambər/<at>/`）。運んだ列だけ名前を頭に付け、ふだんの一行には出さない。ぜんぶ同期しないなら灰色の一行。見張りは `main.js watch(roots)` で並び。走査「保存ディレクトリ：」十一段。**電話も同じ**（`NotesStore.places`・前の bookmark 一つから引き継ぐ・フォルダの段の頭に切り替えの一行・設定に「保存ディレクトリの追加・変更・削除」と一つずつの画面・`Syncing` は場所ごと・電話の走査 九段） | `gui/renderer.js ~ name: '保存ディレクトリの追加・変更・削除'` |
| 512 | 2026-09-12 | 窓にも自動保存の入切を（基本は入。自発的に切れるように・本人） | ⚙「自動保存」で入切（`autoSave`・憶える）。切っているあいだは打っても書かず、帯の上に「保存」が出る。ノートから離れるときは一度だけ「保存しますか」（電話と同じ決まり）。走査「自動保存：」 | `gui/renderer.js ~ function cmdAutoSave` |
| 513 | 2026-09-12 | 新しいノートは電話の形（先にタイトル・タグ・テンプレート。選ばなくても作れる）を窓にも。案内は初心者に優しく（本人） | 窓に `#newform`（`askNewNote`・`cmdNewNote`）── タイトル・タグの札・前に使ったタグ・テンプレートの並び（無ければ見本を入れる）。「作成」だけでも作れる。電話の `Making` の案内も書き直し。走査「新しいノートの小窓：」 | `gui/index.html ~ id="newform"` |
| 514 | 2026-09-12 | 片方にしか無かった小さな穴を埋める（本人「穴は実装する」） | 窓: ⚙「同期」に「いま同期する」。電話: 予定の終了時刻・「このノート一枚をバックアップ」・フォルダの献立に「この中にフォルダを作る」「家族を招待」（「ファイル」で開く）・Drive の空き不足の言い分・始める前の色つきの列「まだ同期していません／同期をはじめる／あとで」。終了は打たせずプルダウン（本人・2026-09-12） | `ios/Cian/Calendaring.swift ~ Picker\("終了"` |
| 515 | 2026-09-12 | 電話にもカレンダー表示設定（出す予定表・土日・個人カレンダーの色）と、週／日の表を（穴を埋める・本人） | `CalPrefs`（窓と同じ鍵 `me`/`here:`/`away:`・十色）と `CalSettings` の紙（歯車から）。`Calendaring` に月／週／日の切り替え（週は月曜からの段・日はその日だけ・← → は表に合わせて動く）。土日を出さないと月〜金の五列 | `ios/Cian/CalPrefs.swift ~ struct CalSettings` |
| 516 | 2026-09-12 | 電話のエクスポートも窓と同じ三つ（Markdown・HTML・PDF）に | `Exporting`（core の `html` → 窓の `onePage` と同じ CSS・絵は `data:` で一枚の中へ・PDF は `UIPrintPageRenderer` で A4 の頁に）。ノートの ⋯ →「エクスポート」が三択に | `ios/Cian/Exporting.swift ~ static func pdf` |
| 517 | 2026-09-12 | 「ambər フォルダ以外のノートを開く」を「ほかの場所のノートを開く」に。スマホ版にも置く（本人・絵で決めた） | 窓: 命令の名前と帯の字。電話: 設定に一行 →「ファイル」で .md を一本選ぶ → `NotesStore.openOutside`（鍵を開けたまま・一覧には入れない）→ 開いた札の上に「ほかの場所のノートを、一時的に開いています」の帯。エクスポート／インポートも対の言葉に統一（ノートを取り込む → インポート、Web から取り込む → Web からインポート） | `ios/Cian/NotesStore.swift ~ func openOutside` |
| 518 | 2026-09-12 | パソコン版のノートの上の帯に、最大化・最小化の印を（本人「めっちゃ使う気がする」）。前に見た／次に見たノートは窓からも外す。「全部まとめて見る」は「すべてのノート」と同じなので消す | `#zenbtn`（⤢／⤡・F12 と同じ `setZen`）。`back`／`fwd` の命令と ⌘← ⌘→ を外した。電話の並び順の献立から「全部まとめて見る」を外した。ぶつかりの一つずつも「こちらの記載を反映する／〇〇の記載を反映する／両方を反映する」。テーマの脚註の cian の一文は消した | `gui/index.html ~ id="zenbtn"` |
| 519 | 2026-09-12 | Win 版は exe、Mac 版は app で配れるように。cian・crmaine の Electron の組み方を採る | `scripts/pack.js`（網に出ない・Electron の一式を写して名前と絵と中身を差し替え・出口で数える）。`--mac` → `dist/ambər.app`（署名はその場のもの）＋zip、`--win --electron … --server …` → `dist/amber-win-x64/amber.exe` ＋zip（絵は Windows で rcedit）。`resources/app/` は `gui/`＋`packaging/`＋エンジン（`gui/` の隣に置くと engine.js が最初に見る） | `scripts/pack.js ~ function fillApp` |
| 521 | 2026-09-12 | 家族とノートとカレンダーを共有したい（自分の端末をまたぐのは大満足） | まず土台の一つ: Drive の一覧は **自分のものだけ**（`ownedByMe`）── 家族が共有したノートが自分の ambər に紛れ込まないように。共有の形は案を見せて決める（共有フォルダを保存ディレクトリに、が本命） | `gui/drive.js ~ ownedByMe === false` |
| 522 | 2026-09-12 | タブの増えすぎに VS Code の**プレビュータブ**の決まりを（本人「いい感じ」・絵で決めた）。× で閉じる、献立は「このタブを残す／このノートを閉じる／このノートより右のものを閉じる／このノート以外をすべて閉じる」、説明は人の言葉で | 窓: 一覧から押しただけのノートは**仮のタブ**（斜体・薄い字・`pre`）で開き、次を押すと入れ替わる。書く・題を直す・一覧で二度押し・⌥ 押し・「このタブを残す」で本のタブ。仮は憶えない。電話: `Desk.Tab.preview`（点線の丸札・斜体・下に一言）、長押しの献立、書いたら本に。走査「仮のタブ：」窓三段・電話二段。**2026-09-14、依頼 555 で本人が仮のタブを撤回**（「実際にうごかしてみて分かりにくかった」）── 残っているのは **× で閉じる**と献立の三つ（このノートを閉じる／このノートより右のものを閉じる／このノート以外をすべて閉じる）で、「このタブを残す」は消えた。検査もそちらに付け替えた | `gui/renderer.js ~ このノートより右のものを閉じる` |
| 523 | 2026-09-13 | 予定に**人のタグ**を付けたい（太郎・次郎…）。ただし**題には書かない** ── Google カレンダーを横から見られたときに氏名が画面に出てしまう（本人） | 置き場所は**メモ欄のいちばん最後の、タグだけの行**（`caltag`）。**読むのも書き換えるのもその一行だけ**で、人が書いた文章には触らない。行の途中の `#` は拾わない ──「#1 の件」が名前になる。**amber の中に予定 ID の対応表は持たない**: EventKit の `eventIdentifier` は端末ごとに違い、端末をまたいで揃う `calendarItemExternalIdentifier` は繰り返し予定の全インスタンスで同じで（「今週だけ次郎」が書けない）、作り直された予定では孤児になる ── **壊れても画面には何も出ない**。メモ欄なら、タグは予定そのものに付いて旅をする。改行は元のメモに合わせる（Exchange から来たメモを LF で書き戻すと、触っていない行まで差分になる） | `crates/amber-core/src/caltag.rs ~ いちばん最後の一行だけ` |
| 524 | 2026-09-13 | （同上）Mac の予定表の道具が、メモ欄を読めていなかった | `amber-cal` は題・場所・予定表名・ID しか返しておらず、タグの置き場所そのものが見えていなかった。`month` の各行に `notes` を足し、書き換える口（`amber-cal notes <id> <字>`）と、足すときに渡す道（`add` の六つ目）を付けた。**切り出しはここでしない** ── メモのどこがタグ行かの判断は core に一枚だけ置く（写すと、窓と電話で二つの読み方ができる） | `mac/amber-cal.swift ~ "notes": e.notes \?\? ""` |
| 525 | 2026-09-13 | グループカレンダーを **amber が作れる**ように（依頼 521 の土台） | Mac のカレンダーでは Google に新規カレンダーを作れない（ファイル → 新規カレンダーの下は iCloud だけ）ので、**API で作るしかない**。`gui/gcal.js` ── 作る・訊く・名前を変える・消す、の四つだけ。**予定の読み書きはここを通らない**（端末のカレンダー経由。amber が作ったカレンダーは Google アカウントの繋がった Mac と iPhone に降りてくることを実機で確かめた）ので、**通信が要るのは作るときと招待するときだけ**。スコープは `calendar.app.created` ── Drive の `drive.file` と同じ「自分で作ったものだけ」の形で、本人のコンソールで**非機密**（審査不要）と確かめた。**名前から探し直さない** ── この許可に一覧を読む力は無いので、二度押せば二枚できる。憶えるのは呼ぶ側 | `gui/gcal.js ~ アプリが自分で作った二次カレンダーだけ` |
| 526 | 2026-09-13 | （同上）カレンダーの許可を、**ノートの同期しか使わない人に訊かない** | はじめのサインインは `drive.file` だけ。カレンダーの許可は**要る瞬間に足しにいく**（`signIn({ scope })`）。同意の画面に並ぶ数が増えるほど、押す前に引き返す人が増える。**`include_granted_scopes` を必ず付ける** ── 無いと、足しにいった瞬間に Drive の許可が消えて**同期が黙って止まる**。憶えるのは「頼んだ許可」ではなく**向こうが返した許可**（断られたものを持っていると思い込むと、使う瞬間まで気づけない） | `gui/drive.js ~ include_granted_scopes` |
| 527 | 2026-09-13 | グループカレンダーを作る入口は、**設定の中ではなくカレンダーの画面に**（本人）。ついでに「設定に入っているもののうち、カレンダーや共有の設定は設定から出す、または設定以外からも触れるようにしたい」（本人・狙いは IT に明るくない人なので、アプリや画面をまたいで操作させない前提でいく） | カレンダーの見出しに一つ（`.groupbtn`）── まだ無ければ「グループを作る」、あれば「グループへ招待」（**帯に収まる長さで** ── 12 文字の「グループカレンダーを作る」だと見出しが二行に折れて、全部が窮屈になった。撮って見て分かった）。**押すのは一回**で、サインインもカレンダーの許可も主（Node）が面倒を見る。作る前に「いまの予定は一つも動きません」と言う ──「共有」を押した瞬間に何が起きるか分からないのがいちばん怖い。作ったあと**二段あることをその場で言う**（招待は Google の画面。人を招待する口には審査の要る許可が要るので、一般公開のときに amber の中へ入れる）。名前は `ambər グループ`（本人が決めた） | `gui/index.html ~ 設定を開いてください」と言わない` |
| 528 | 2026-09-13 | カレンダーの帯を畳む（本人が絵を見て**案ア**を選んだ）── 決めた設計にはまだ「自分だけ／グループ／両方」が入るのに、帯はもう一杯で、足すと**二行に折れて全部が窮屈になる** | 三つやった。**一、並べて表示を「見方」の仲間に入れた**（`日｜週｜月｜並べて`）── あれは見方の一つなので、日・週・月と同じ並びにいるのが素直。ボタンが一つ減り、光るのも一つで済む。月から押されたら週へ移す（月の表を人ごとに割ると、一人ぶんの升目が字より小さくなる）。**二、件数を帯から外した** ── 表を見れば分かる。ただし**空のときだけは表の真ん中で言う**（何も無いのか、読めていないのかが分からないのがいちばん困る）。段を増やさず**上に重ねる**（カレンダーは縦が命）。**三、「グループを作る」に印を添えた**（本人）── 左の列の共有と同じ二人の形。同じものには同じ形を使う | `gui/index.html ~ data-view="crowd"` |
| 529 | 2026-09-13 | **自分だけ／グループ／両方**の絞り込みと、**塗り分け**（決めごとのとおり）── ローカルの予定とグループの予定を一枚に並べ、どれがグループに見えているかを画面で言う | 帯に `自分だけ｜グループ｜両方`（既定は両方・**グループカレンダーが無ければ出さない**＝選べないものを見せない）。**塗ってあるものがグループに見えている予定**（`.ev.sh` / `.ev.lo`）── 色はタグ（誰の用事か）にだけ使うので、見えているかどうかは塗りで言う（10px の印は月の表の一行では小さすぎる）。どの予定がグループのものかを当てるのは `inGroup` **一か所だけ**（予定表の名前で当てる ── EventKit が返すのは端末側の題で、Google の id ではない）。「招待」は**グループを見ているときだけ**帯に出す ── 作るのは一生に一度なので居座らせない（居座らせたら帯が折れた）。**字は割らせず帯のほうを折る**（`white-space: nowrap` ＋ `flex-wrap`）── 既定だとボタンが縮んで「自分だ／け」と割れる | `gui/renderer.js ~ function inGroup` |
| 530 | 2026-09-13 | **電話も並行して進める**（本人「PC版がうまくできても携帯版が横幅は短くてどうしようとか、色々あると思う」） | 窓で決めた形が電話で成立するかは、電話で見るまで分からない ── 実際この日、窓だけで**帯が折れる・字が割れる・印が小さすぎる**を三度踏んだ。電話は幅が半分以下なので、もっと早く壊れる。**絞り込みと塗り分けを窓と同じ決まりで電話にも**入れた: `CalPrefs.inGroup`（窓の `inGroup` と同じく**予定表の名前で当てる** ── ここがずれると同じ予定が端末によって違う顔をする）、`CalPrefs.visible` に絞り込みを足し、月の表とその日の一覧で**グループのものだけ塗る**。切り替えは段をもう一つ（`segmented`）── **グループカレンダーが無ければ出さない**（電話は縦が短いので、一段でも空けられるなら空ける） | `ios/Cian/CalPrefs.swift ~ static func inGroup` |
| 531 | 2026-09-13 | 電話の絞り込みは**段を増やさない**（本人が絵を見て案ウ）。回る順は**両方 → 自分だけ → グループ → 両方**、初期は両方 | 二段にすると月の表が **85pt** 下がる（画面の一割・実機で測った）。電話は縦が命なので、`月｜週｜日` の**右に一つ**置いて押すと回す（`sidePill`）。回る形の弱いところは「ほかに何が選べるか」が押すまで分からないことなので、**長押しで三つ出す**（`contextMenu`）── 押し先が一つで済む軽さは残したまま、全部を見る道も残す。字は出しっぱなし（「両方」と書いてある）ので、**いま何を見ているかは常に読める** | `ios/Cian/Calendaring.swift ~ private var sidePill` |
| 532 | 2026-09-13 | **電話でもグループカレンダーを作れるように**（本人）── 「IT 素人に向けたサービスなので、**PC をそもそも持っていない可能性が大いにある**」 | 一度は「作るのは窓だけ、電話は見るだけ」で入れたが、電話でしか始められない人がいる以上、始められないのは致命的。電話には既に Google の道（`Drive.swift`）があるので、そこに二軒目を乗せた（`makeGroupCalendar`）── **サインインを二度させない**。カレンダーの許可は**要る瞬間に足す**（`calendar.app.created`・非機密）。`include_granted_scopes` を必ず付ける（無いと Drive の許可が消えて同期が黙って止まる）。憶えるのは頼んだ許可ではなく**向こうが返した許可**。入口は**設定の中ではなく、カレンダーの「予定を登録する」の隣** ── 作ったら消え、グループを見ているときは「グループへ招待」になる。押す前に「**いまの予定は一つも動きません**」と言う | `ios/Cian/Drive.swift ~ func makeGroupCalendar` |
| 533 | 2026-09-13 | **実機検証の手法を、本人が自分で回せる形で**（本人「こっちでもやれることを勧めるよ」） | 文章より**一本のコマンド**（`scripts/phone-install.sh`）── 組んで、入れて、**いつ切れるかまで言う**（無料の個人チームなので 7 日で起動しなくなる。言わないと、ある朝いきなり動かなくなる）。**シミュレータには入れない** ── 実機に入れたつもりでシミュレータを見ていた、を道で分ける（この日に実際にやった）。**識別子は二つある** ── `devicectl` が返すものと `xcodebuild` が受けるものは別物で、取り違えると「その端末は見つかりません」で止まる（実際に止まった）ので、名前で突き合わせて両方持つ。新しい端末は**持ち主自身に「信頼」を押してもらう**（人の端末なので代わらない）。何をどう押して確かめるかは `docs/on-device.ja.md` | `scripts/phone-install.sh ~ 識別子は二つある` |
| 534 | 2026-09-13 | 実機で二つ出た（本人）── ① グループを作っても「グループへ招待」が出てこない（別の画面へ行って戻ると出る）② 招待を押すと Google カレンダーのアプリが開くだけで、招待する画面に行けない | ① **`UserDefaults` を画面から直に読んでいた** ── SwiftUI に「変わった」が伝わらないので、描き直されるまで出てこない。`@State` で持ち、憶える先は `CalPrefs` のまま（画面が見るのはこちら）。② パソコン版の設定ページの URL を開いていた。**URL のほうは合っていた**（本人の本物のカレンダーで確かめた ── 組んだ字と Google が出す URL が一字一句同じ）。**駄目だったのは、iPhone が `calendar.google.com` を Google カレンダーのアプリに渡してしまうこと** ── アプリは設定ページを出せない。iPhone でも共有そのものはできる（アプリの設定から）ので、電話では **URL を当てにいかず、行き方を言う**（メニュー → 設定 → そのカレンダー → ユーザーまたはグループを追加）。窓は URL のままでよい | `ios/Cian/Calendaring.swift ~ URL を当てにいかず、行き方を言う` |
| 535 | 2026-09-13 | **グループカレンダーを消す道が、画面のどこにも無かった**（本人「消し方がわからないぞ」）。やめるだけでなく**実際に消す**（本人） | 作る道だけ付けて、やめる道を付けていなかった ── amber の決まり（押す前に行き先を言う・戻し方をその場で言う）に反していた。**本当に消える**（持ち主が消すと、グループの人の画面からも消える。中の予定も一緒に消えて戻せない）ので、押す前にそう言う。窓は **⚙ の中**（一生に一度で戻せない操作を、毎日押すものの隣に置かない）で、**カレンダーの名前を打たせてから**消す。電話はカレンダーの献立に赤い一行で、確認を一枚。グループを持っていない人には出さない（`need: 'group'`） | `gui/renderer.js ~ async function cmdDropGroup` |
| 536 | 2026-09-13 | Google の画面で先にカレンダーを消してから amber で「消す」を押すと、**「Not Found」と出て外せない**（本人・実機） | 二つ悪かった。**一、生の HTTP の言い分を人に見せていた**（amber は人の言葉で言うと決めてある）。**二、もっと悪いのはこちら ── もう無いものを消そうとして失敗で止まり、amber の憶えだけが永久に外せなくなっていた。** 人が Google の画面で先に消すことはある。**もう向こうに無ければ、それは消し終わっている** ── 404／410 は「済み」として扱い、憶えを外す。ただし**黙って「消しました」とは言わない**（まだあるのに消したように読める）ので「もう Google にありませんでした」と言い分ける | `gui/gcal.js ~ もう向こうに無ければ、消し終わっている` |
| 537 | 2026-09-13 | グループカレンダーまわりの**画面の文章が「AI 丸出し」**（本人・「憶える とか。人間風に校正しないとまずいレベルで変」）。作ったカレンダーが端末に降りてくるまでは**約 3 秒**だった（本人・実機で計測） | 依頼 443・450・451・458・459 の系列を、また踏んだ。直したもの: 「憶え」→**設定**（「amber の憶えからも外しました」→「amber の設定からも解除しました」）、「一枚できます」→「新しいカレンダーを**作ります**」、「いまの予定は一つも動きません」→「**今ある予定はそのままで、共有されません**」、「消す」→**削除**で統一、「できました」→「**完了しました**」、「わかりました」→「**OK**」、「返事がありません」→「**Google から応答がありません**」、「打ってください」→「**入力してください**」、「もう Google にありませんでした」→「**Google カレンダー側ですでに削除されていました**」。**3 秒なので「少し時間がかかることがあります」は消した** ── 数秒のことを断らない | `ios/Cian/Calendaring.swift ~ amber の設定からも解除しました` |
| 538 | 2026-09-13 | **招待された側の amber が、グループカレンダーを知らない**（本人の指摘から分かった ── 「妻の iPhone に入れないと検証しようがなくない？」） | 名前を憶えているのは**作った端末だけ**だった。招待された人の端末にはカレンダーが降りてくるのに、amber から見ると「よその予定表」と見分けが付かず、**絞り込みも色分けも出ない** ── これでは共有できているように見えない。名前が `ambər グループ` で決まっているので、**端末の予定表にその名前があれば見つける**（ノートの共有で決めた「受け取る側は自動で見つける」と同じ形・人に選ばせない）。**訊くのは一度だけ**（断った人に毎回訊かない）。**招待と削除は作った人だけ** ── 招待された側は Google 側の id を持たないので、共有設定にも行けないし消せもしない。持っていない道を画面に出さない | `gui/renderer.js ~ async function findGroupCal` |
| 539 | 2026-09-13 | **誰の用事かを、色で分ける**（決めごとのとおり）── タグに色を付け、予定の一行をその色で塗る | どこを読むかの判断は core（`caltag`）に一本。**まとめて訊ける形にした** ── 月の表には予定が何十本も並ぶので、一本ずつ訊くとその数だけ行き来する。色は**決めていなければ十色を順に配る**（設定を開かなくても色分けされた表が見られる。使う前に決めさせない）。決めた色はこの端末だけが憶える。**二人以上の用事は、左の帯を人数ぶんに分ける** ── どちらか片方の色にすると嘘になる。タグが無ければ何も差さず、いままでの出どころ別の色のまま。`var(--c)` には**既定値を置く** ── 定義の無い `var()` は黙って透明になるので（依頼 474）、差し損ねた日に予定が消える | `gui/renderer.js ~ function tagPaint` |
| 540 | 2026-09-13 | **色に名前を付け直し、十色から八色に**（本人）。青は水色と、オレンジは赤と似ているのでやめる。二人の用事の出し方は案ア | 名前は **ローズ・アンバー・リーフ・バイオレット・シアン・カーマイン・セピア・スレート**（アンバー以下の五つは本人が決め、ローズ・セピア・スレートはこちらの案）。**選べる数が多いほど、どれにするかで止まる**ので減らすほうがよい。自動で配る五色は **ローズ・アンバー・リーフ・バイオレット・シアン**。**依頼 498（2026-09-12・本人の決め）の「青・ピンク・黄・緑・紫」と「個人カレンダーはオレンジが好き」を、本人の了解の上で更新した** ── 訊かずに消していない。**青をやめたので、青はよその予定表だけの色になった**（人のタグに青が出ないぶん、青い予定は「自分では直せないもの」と読める）。二人以上の用事は**塗らない**（薄く塗ると、隣の「その色ひとりの用事」と紛らわしかった ── 実際に本人が誤認した）。左の帯だけを人数ぶんに分ける。**取りこぼしが一つあった**（2026-09-14 に走査で出た）── 段の色を見る走査が旧五色（先頭が青）と旧名称「ピンク」のままで、この日から鳴り続けていた。色そのものを書かず、**並んだ順に `LANE_COLORS` を回す**という決まりのほうを見る形に直した（何色を回すかは別の話） | `gui/renderer.js ~ 名前で呼べる八色` |
| 541 | 2026-09-13 | 二台目の iPhone が、一覧に出ているのに**黙って飛ばされた** | `devicectl` を字で読んでいたせいで、名前の「の」が `?` になり、`xcodebuild -showdestinations` 側の名前と一致しなかった ── **合わないと分かるのではなく、何も起きない**のがいちばん悪い。`scripts/devices.py` が JSON で訊いて、**入れるときの識別子と組むときの識別子を両方**返す形にした（名前で突き合わせない）。入れられなかったときは、そう言う | `scripts/devices.py ~ 名前で突き合わせない` |
| 542 | 2026-09-13 | 実機に入れられないとき、**こちらが当て推量の理由を並べていた**（「端末がロックされているか、つながっていません」）── 実際の理由はデベロッパモードが切れていたこと | **知っているのは端末のほう。** `devicectl` の JSON には `developerModeStatus` も `pairingState` も入っているので、**そのまま渡す**（「デベロッパモードが切れています（設定 → プライバシーとセキュリティ → デベロッパモード → オン → 再起動）」）。入れる前に分かるので、組むところまで行って失敗しない。機種も型番（`iPhone16,2`）ではなく売っている名前（`iPhone 15 Pro Max`）で出す | `scripts/devices.py ~ 入れられない理由は、端末が知っている` |
| 543 | 2026-09-13 | 二台目の端末で「署名の書類にこの端末が入っていません」で止まる | 新しい端末は、はじめは書類に入っていない。`-allowProvisioningUpdates` を付けると Xcode が Apple に登録しにいく ── **本人の Apple ID に「この端末で開発する」と記録される**（Xcode からいつでも消せる）ので、何が起きるかをコードにも書いておく。これで二台目（家族の端末）にも入るようになった | `scripts/phone-install.sh ~ allowProvisioningUpdates` |
| 544 | 2026-09-13 | **予定を登録するとき、どこに入れるかを選ばせる**（決めごとの丙）── 自分だけか、グループと共有か。グループなら**だれの用事か**も | 小窓のいちばん先に「どこに入れる」を置き、**どちらも選ばれていない状態で開く**。選ぶまで「登録する」は押せない（既定を持たせると、その既定のまま押した予定が出る。出す・出さないは、押し間違えたときに戻す道が無いほうの話）。**押せないボタンは押せないように見せる** ── 見た目が同じだと「押しても何も起きない」になる（実際にそう出た）。「だれの」は**グループに出すときだけ**訊く（誰にも見えない予定に、誰の用事かを書く意味がない）。その場で名前を足せて、この端末が憶える。グループカレンダーが無ければ、選ぶものが無いので出さない。書き込む先は `amber-cal add` に予定表の名前を渡して指す ── 端末に降りたカレンダーは名前でしか指せない | `gui/renderer.js ~ どちらも選ばれていない状態で開く` |

| 545 | 2026-09-14 | **電話でも、予定を登録するときに行き先を選ばせる**（依頼 544 の電話の側） | 窓と同じ丙 ── 小窓のいちばん先に「どこに入れる」、グループを選んだときだけ「だれの」。**どちらも選ばれていない状態で開き**、選ぶまで「登録する」は押せない（押せないボタンは、押せないように見せる）。**名前を憶えているだけでは出さない** ── その予定表がこの端末に降りてきているときだけ（作った直後や、Google のアカウントをまだ iPhone に足していないうちは無い）。選ばせておいて、押した先で「予定表がありません」と断るのがいちばん悪い。名前で指した先が見つからないときは、黙って自分の予定表に入れず、どの予定表が無いのかを言う ── 共有するつもりの予定が誰にも見えないところに入るほうが、ずっと悪い。書く先は `EKCalendar` を**名前で**引き当てる（端末に降りたカレンダーは名前でしか指せない ── 窓が `amber-cal add` に名前を渡すのと同じ）。メモ欄の字を作るのは core の `caltagset` で、窓と同じ一枚に訊く ── ここを電話で書き直すと、同じ予定のメモが端末によって違う形になる。憶えた名前は `amber.calTagsKnown` にこの端末だけが持つ | `ios/Cian/Calendaring.swift ~ どこに入れる` |
| 546 | 2026-09-14 | 「予定を足す」は AI っぽい ── 人の言葉に直す（本人・「このチャットだからだよね？」） | 押すボタンは「＋ 予定を登録する」と言っているのに、開いた小窓の見出しだけ「予定を足す」だった ── **同じ一つの操作が、押す前と押したあとで違う名前を名乗っていた**。小窓をボタンに揃えた。台帳の依頼 493 も直す（元の字のままだと、次に読んだ人が「本人の指定だ」と思って戻す）。ついでに自分が書いたぶんだけ ── 予定の「＋ 足す」を「＋ 名前を追加」、電話のタグの「足す」を依頼 181 と同じ「新しいタグを作る」に。**表と図の「行を足す」「箱を足す」は触っていない** ── あれは依頼 190 の本人の言葉 | `gui/renderer.js ~ askEvent\('予定を登録する（'` |

| 547 | 2026-09-14 | **電話でも、だれの用事かで色を分ける**（依頼 539 の電話の側） | 予定のメモ欄を `Slot` まで持ってきて、そこから切り出す ── 切り出すのは core（`caltag`）で、窓とまったく同じ一枚に訊く（写すと、同じ予定が端末によって違う顔をする）。訊くのは**グループカレンダーの予定だけ**、しかも**一度にまとめて**（`caltag` は字の一覧を受ける）── 予定の数だけ扉を叩かない。自分だけの予定に人の名前は入らないし、よその予定表のメモまで覗く筋合いが無い。色は窓と同じ決まり ── **一人ならその色、二人以上なら帯を分ける**（二人の用事は二人のものなので、片方の色にすると嘘になる）。字には乗せず**左に細い帯**を立てる（升目の字は 8pt で、色を乗せると読めなくなる）。見た名前は憶える ── 次に登録するとき打たずに選べる。**選びは小窓が持つ** ── `@Binding` を直に触ると一つ選ぶたびに外の画面まで描き直されて、選んだ印が消える（シミュレータで出た）。`.buttonStyle(.plain)` を付けたら押せるのが字のところだけになったので `.contentShape` も足した（これも画面で出た） | `ios/Cian/Calendaring.swift ~ func readTags` |

| 548 | 2026-09-14 | 並べて表示の段を、**予定表ごとではなく人ごと**に（本人・「グループスケジュールを選択すると人ごとに表示されるのが自然な気がした」） | **タグが無いうちは、いままでどおり予定表ごと**（本人が決めた ── 「カレンダーを同期した直後ってタグの記載は何もないよね？　タグができたら予定表から分離するイメージ」）。名前が付いた予定だけが予定表の段から抜けて、その人の段に立つ ── 使う前に段を増やして見せても、何のことか分からない。**二人以上なら、両方の段に出す**（本人が決めた）── 家族旅行は太郎の用事でもあり次郎の用事でもあるので、その日その人が何をしているかを段で読める。人の段は予定表の段より上（グループを見ていて知りたいのは「だれの用事か」で、どこから来たかではない）。段の色は**予定の色と同じ鍵**（`tag:<名前>`）なので、段の色を変えると予定の色も変わる ── 同じ人が段と予定で違う色になるのがいちばん困る。段の左の印は `.who.tag` にも要る（付け忘れると、人の段だけ印の無い表になる） | `gui/renderer.js ~ その人の段へ` |

| 549 | 2026-09-14 | 並べて表示で「グループ」が光っているのに、会社の予定がそのまま並ぶ（自分だけ／グループ／両方が効いていない）。あわせて「自分のノート」の空の段は出さない（本人） | `crowdLanes` だけが `calShown()` を通さず `calSlots` を直に読んでいた ── **絞り込みのボタンが、並べて表示では押しても何も起きなかった**（依頼 477 と同じ形。月・週・日では効いていたので、気づきにくい）。段の組み立ての手前で同じ決まりを通す。**引っ込めた段（`calHide`）はここでは落とさない** ── 落とすとその段が「人を選ぶ」の一覧からも消えて、戻す道が無くなる（落とすのは最後の一行のまま）。空の段を消すのは **`me` だけと名指しする** ── チームの段は空でも残す決まりがある（依頼 471・段ごと消えると「書き出せていない」と「本当に空」が読めなくなる）。逆の決まりが二つ同居するので、どちらかに寄せない。**なお並べて表示は予定だけを並べるので（`kind === 'note'` は段に入れない）、この変更で「自分のノート」の段は出なくなる** ── 日付を書いたノートも並べたいなら、それは別の決めごと | `gui/renderer.js ~ lanes.delete\('me'\)` |

| 550 | 2026-09-14 | **ネットに出られない会社の Windows で amber を組みたい**（本人・cian と同じ形のコマンドで） | 組む道具（`scripts/pack.js`）は既にあったが、**あの機械では三つ足りなかった**。一、`--zip` が `python3` を呼んでいた ── Windows にあれは無い。PowerShell の `Compress-Archive` は有るが**日本語の名前に UTF-8 の印（汎用ビット 11）を立てない**ので「はじめにお読みください.txt」が化ける。`scripts/zip.js` を書いて自前で組む（`zlib` だけ・印を必ず立てる・縮まないものは素のまま入れる）。二、**`gui/vendor/` は git に入っていない**（`npm install` が置いたものを写す形）ので、網の外では作れない ── `scripts/offline-kit.js` が家で持ち込み一式（HEAD＋vendor＋Electron＋エンジン＋組み方.txt）をまとめる。コミットしていない変更は入らないので、まとめるときにそう言う。三、写す前に足りないものを言う（出口の `verify` だけだと、Electron を二百メガ写したあとで落ちる）。あわせて**cian の綴りを受ける** ── `--platform win32` は `--win`、`--engine` は `--server`（手が憶えているほうが正しい・同じ入口が二つになるのは承知の上で、綴りだけの話なので受ける）。エンジンは CI が既に **CRT ごと静的**に組んでいる（release.yml）ので、置くだけで動く | `scripts/zip-test.js ~ 局所ヘッダにも UTF-8 のマークが立っている` |

| 551 | 2026-09-14 | v3.0.0 の札を打つ前の試し組みが、**Windows だけ落ちた** | `team.rs` の「取り決めの見本がそのまま読める」が **`10:00` と書いてあった** ── 時刻つきの予定は現地時刻に直す決まり（依頼 471）なので、**UTC の機械が読めば `01:00` が正しい**。つまり**東京の机でしか通らない試験**だった。CI は Windows でしか試験を回さず、あそこは UTC なので、`team.rs` が入った日（依頼 471〜476）から落ちていた ── v2.13.0 の札はその前に打たれていたので、誰も踏んでいなかった。直したのは試験のほうで、**`+09:00` の一瞬を、その机の時刻に言い直してから照らす**。終日の予定はそのまま `2026-09-11` で照らす（あちらは時差で直さない決まりなので、literal が正しい）。UTC・東京・ロサンゼルス・キリバス・ロンドンの五つで回して確かめた。**先に札を打っていたら、落ちたリリースが残っていた** | `crates/amber-core/src/team.rs ~ 東京の机でしか通らない試験` |

| 552 | 2026-09-14 | 試し組みが Windows でもう一件落ちた（依頼 551 が隠していた）── **直した題が、古いまま月の表に出る** | 憶えておくしるしは「更新時刻と長さ」だが、**更新時刻はどこでも細かいわけではない** ── NTFS はシステム時計の刻み（約 15ms）でしか進まず、HFS+ は一秒、FAT は二秒まで。同じ刻みのあいだに**同じ長さで**書き替えると、しるしが一字も変わらず、依頼 470 が防ごうとしたそのものが起きる。見分ける道が無いので、**新しすぎるしるしは初めから信じない**（二秒）── git が「racily clean」と呼んで同じことをしている。読み直すのは「たったいま書かれた数本」だけなので、憶えておく甲斐（二万本で 415ms → 58ms）は残る。時計は一回の数えごとに一度だけ見る（一本ごとに見ると、数えているあいだに時が進んで、同じ一回の中で信じる／信じないが分かれる）。試験は**時刻を手で書き替える前に戻して**、粗い時計の状況をどの機械の上でも作る ── 前の試験は mac の細かい時計に寄りかかっていた | `crates/amber-core/src/month.rs ~ racily clean` |

| 553 | 2026-09-14 | **会社では git も gh も使えず、GitHub から zip を落とすことしかできない**（本人）── 展開するだけで動くものを Asset に置いてほしい | 持ち込んで組む道（依頼 550）は残したまま、**組んだものを置く**道を足した。CI に `windows` の段を一つ ── エンジンは matrix が組んだものをそのまま使い（二度組むと、配る一枚と資材の一枚が別物になりうる）、`npm ci` → `vendor.js` で積み、Electron を落として `scripts/pack.js` に通す。**ここでしか組めない理由が二つある**: `gui/vendor/` は git に入っていない（網の外では作れない）、exe の絵と名前は PE のリソースなので `rcedit` を Windows の上で掛けるしかない。**Electron の版は lock から取る** ── ここに数字を書くと、`package.json` を上げた日にここだけ古いまま残り、画面と本体の版が違う一枚ができる（配ったあとにしか出ない食い違い）。出口で、絵と名前が焼けたか（`ProductName` が `ambər`）と、zip に要るものが入っているかを数える | `.github/workflows/release.yml ~ exe の名前が埋め込まれていません` |

| 554 | 2026-09-14 | カレンダーを出しているときに「すべてのノート」やノートを押したら、**カレンダーは消えてノートの表示に変わってほしい**（本人・「今は『閉じる』を押さないとノートが表示されなくて結構面倒」） | ノートを押す道には前から決まりがあった（依頼 478）が、**行き先（すべてのノート・フォルダ・タグ）を押す道は `calOn` に一切触っていなかった** ── 一覧が絞られても月の表が出たままで、絞った結果が見えない。閉じ方は `calShut` に一本あるので、そこを通す（ここで書き直さない）。**ついでに前からの穴が出た** ── `applyView` はカレンダーのあいだタブの帯を畳むが、**畳みっぱなしで誰も戻していなかった**。タブが二枚あっても、カレンダーから戻ると帯ごと消えていた（「閉じる」でも同じ）。出すかどうかを決めるのは `drawStrip` 一本なので、`calShut` からそちらに訊く。**測り方でも一度転んだ** ── 一覧の行は `onclick` ではなく `onmousedown` で開く（依頼 477）ので、`el.click()` では届かず「ノートを押しても閉じない」と読み違えた。合成した押しは、本物と同じ出来事を出すこと | `gui/renderer.js ~ 行き先を選んだら、カレンダーからは出る` |

| 555 | 2026-09-14 | **仮のタブをやめる**（本人・「あー。。仮のタブってことか。やめようかな。実際にうごかしてみて分かりにくかったんだ」） | 依頼 522（VS Code のプレビュータブ）と、その前の依頼 377（押すたびに増えないこと）の両方を裏返す ── **押したぶんだけタブが増える**。置く先は `⌥` 押しと同じ「いまのすぐ右」で、たどった順と並びが合う。**窓と電話の両方から外した** ── 同じ操作が端末で違う顔をするほうが悪い（扉は二つ、答えは一つ）。落としたもの: 斜体と点線の見た目、`pre`／`preview`、「このタブを残す」、書いたら本にする道、仮は憶えないという決まり（**いまは全部憶える**）、電話の `fresh`／`pin` の引数（増やす道が既定になったので用が無い）。**きっかけは「タブが2つしか出ない」という報せ** ── 壊れていたのではなく、決めたとおりに動いていた。本人が気づけない印は、印として弱い | `gui/renderer.js ~ 押したぶんだけ、タブが増える` |

| 556 | 2026-09-14 | タブの上限（本人・「古いタブから自動で閉じるなら50個とかかなぁ。。やりすぎ？」） | **やりすぎではない ── 性格が違う。** 50 枚も並べば帯はとうに読めないので、あれは日々の片付けの道具ではなく**網**で、ふだんは「このノート以外をすべて閉じる」で片付ける。網が効くのは、片付けを忘れて何百本も見て回った日だけ（依頼 377 が心配していた 1002 本の棚）── だから低くしない。**低くすると、まだ使っているタブが黙って消えるほうの害が出る。** 触らないものが二つ: **いま出しているタブ**と、**書きかけを抱えたタブ**（閉じる前に書き戻す道はあるが、打っている途中のものを黙って片付けるくらいなら、上限を超えているほうがよい ── 全部書きかけなら一枚も閉じない）。「古い」は並び順ではなく**最後に見た順** ── 新しいタブはいまのすぐ右に入るので、左にあるものが古いとは限らない。**黙って消さない**（押していないのにタブが減るのは、画面の上では「勝手に閉じた」にしか見えない） | `gui/renderer.js ~ ネットワークであって、道具ではない` |

| 557 | 2026-09-14 | **会社の Windows で起動した**（本人）。動作検証の項目がほしい | `docs/windows-check.ja.md` ── **mac では一生出ない型から順に**並べた（道の区切り・鍵盤の名前・絵の道・改行・時計の粗さ）。本命は五つ: 一、amber 自身が書くファイル名が日本語のまま置かれるか（zip の中の名前は確かめたが、**書くほうは初めて**）。二、メモ帳で開いて行が繋がらないか（`CRLF` と `LF` の繋ぎ目）。三、JIS の鍵盤と IME（変換キーは `Zenkaku`／`Process`／`Unidentified` のどれにもなる）。四、絵（依頼 380 で会社の Windows だけ読めなかったところ ── **直したが、直った姿を本物の Windows で見ていない**）。五、依頼 552 の粗い時計（**踏んだのは CI の中だけ**）。あわせて「ネットが無いときの断り方」も項目にした ── 配布先が出られない端末なので、**断り方が製品の一部**。画面のボタン名は実物と突き合わせた（「新しいノート」「同期をはじめる」「ambər について」「保存ディレクトリの追加・変更・削除」）── 無いものを探させない | `docs/windows-check.ja.md ~ mac では一生出ない` |

| 558 | 2026-09-14 | **会社の Windows で、書いたノートが真っ白に見える**（本人・検証二の二）。ファイルには残っているのに、画面からは消える | **変換の途中で別のノートを押すと、打った字がどこにも残らずに消えていた。** `syncRead()` は「変換中は書き戻さない」で黙って戻る ── 打っている最中はそれで正しい（未確定の字はまだ人の字ではない）が、**去るときに黙って落としてよい理由にはならない**。しかも一覧は `mousedown` で開く（依頼 477）ので、**確定は押しのあとに来る** ── 待っていても間に合わない。**去るときだけは、変換中でも画面に見えている字をそのまま拾う**（未確定なら読みの仮名で残るが、消えるよりはるかにましだ）。日本語を打つ人は「一区切り打って、まとめて変換」なので、一度に消える量が大きい ── 本人の画面では一本まるごと空に見えた。走査に一段（変換中に移って、戻って、字が残っているか）。**その段は使い捨ての一本を自分で作って消す** ── はじめ試しのノートを書き換えていたが、題が一行目から決まるので名前が変わり、後ろの段を二度巻き込んだ | `gui/renderer.js ~ 去るときは別` |

| 559 | 2026-09-14 | 表の左上の升で下矢印を押すと**右上**へ行く。見た目どおり**左下**へ行ってほしい（本人・検証三） | 表の中に上下の決まりが無く、`contenteditable` の既定（次の字へ）がそのまま効いていた ── **表は格子に見えているのに、動きだけが一列に並んだ字のまま**だった。Tab が「次の升へ」なのと同じ理由で、上下も見えている形に合わせる（同じ列の、真下・真上の升）。**升の中で折り返しているときは、まず升の中を動く** ── 升の高さで見分ける（詰めの厚みに寄りかからない）。表の外へは出さない（最後の行より下・最初の行より上は既定に任せる ── `checkArrow` が表そのものを跨ぐ） | `gui/renderer.js ~ function checkCellArrow` |
| 560 | 2026-09-14 | 表示の面で `⇧Enter` が**一回では効かず、二回押さないと改行しない**（本人・会社の Windows・検証三） | `keyCode === 229` は「IME が処理中」の古い合図だが、**Windows では変換していなくても付いてくる** ── 日本語入力を載せているだけで、確定済みの行末で押した `⇧Enter` が 229 で届く。これを変換中と見て手当てを飛ばすと、既定の `<br>` が一枚だけ入り、**段落の末尾の `<br>` 一枚は行にならない** ── だから二回押して初めて見える。**変換が本当に開いているかは、こちらが知っている**（`composing` は `compositionstart`／`end` で立てている）ので、229 はその裏付けがあるときだけ信じる（`isComposing` は仕様どおりの合図なので常に信じる）。読む面の四つ（Enter・消し・矢印・Tab）を同じ一本に揃えた。**手元で `keyCode 229` を送って再現し、直してから鳴らないことを見た** ── Windows そのものでは確かめていない | `gui/renderer.js ~ const imeBusy` |

| 561 | 2026-09-14 | crmaine が **team-calendar-csv.md（v0.3）**の形で CSV を出せるようになった。その一枚を、**美しいカレンダーで**表示してほしい（本人） | **読む側はもう合っていた** ── 重複の鍵は `(owner_mail, uid, start)` で実装済み（v0.3 が足した人の鍵）、`cancelled` は任意の列なので消えても落ちない、BOM と CRLF も通る。本物の形の一枚（BOM・CRLF・`cancelled` 無し・同じ会議が複数人に重複・非公開・終日）を作って通し、**382 行を 46 ミリ秒で読み、15 段・181 枚を 7 ミリ秒で描いた**。`docs/team-csv.ja.md` は v0.1 のままだったので、**crmaine のリリースの一枚をそのまま写した**（写しを手で書き直さない ── 同じ契約が二通りになる）。core に v0.3 の見本と「同じ会議は出席者の人数ぶん残る」を足した。画面は二つ直した。**一、`show_as` を一度も使っていなかった** ── 仮の予定（`tentative`）が確定と同じ顔で並ぶ。「いつなら空いているか」を読む紙なので、**仮は点線の枠・地は塗らない**（休みや在宅は題がそう言っているので触らない。空きは core が落とす）。**二、依頼 471 で約束した縞が、一度も画面に出ていなかった** ── 段の色を塗る規則が `background:` の**一括指定**で、`background-image` を道連れに `none` へ戻していた。しかもそちらが強いので、縞を後ろへ動かしても直らない（**札は付いているのに計算後は none**、と DOM で見てやっと分かった）。地の色は `background-color` で置くように直した。走査は**計算後の見た目**を見る ── 札が付いているかだけ見ていると、この形は通る | `gui/index.html ~ 一括指定を使わない` |

| 562 | 2026-09-14 | CSV を読んだあとの四つ（本人）── 並べて表示の**段を上下に並べ替えたい**／**人を選ぶを憶えてほしい**／日・週・月が**自分じゃない人の予定**になっている／**三十分の会議の件名が読めない** | **一、段の並べ替え。** 段の名前を右押しで「上へ／下へ」。憶えるのは**いま出ている並びそのもの**（「この段を一つ上へ」だけを憶えると、人が増えた日に意味が変わる）。憶えた並びが先、居ないものはいままでの決まりで後ろに続く ── **CSV は毎日書き換わるので、消えた人の穴が空かない。二、人を選ぶの記憶。** 仕組みは前から入っていて、**窓を建て直しても戻ることを実測した**（`calHide`・`calOrder`・`calMe`・時間帯の四つとも）。**その後、本人が「初期化されるか確認せずに言った。実際に初期化されていたわけじゃない」と取り消した**（2026-09-14）── 幽霊だったので、追わない。**三、自分はこの人**（本人が「自分の予定だけ出す」を選んだ）。段の名前の右押しから決める。日・週・月ではチームの紙をその人のぶんだけに絞る（`calShown` の一か所）。**並べて表示は絞らない** ── あれは全員を見るための面。**四、読みやすさは四つとも採った**（本人）: 出す時間帯を設定に出して既定 8〜19（会社の予定表は真夜中を一日も使わないのに、24 時間ぶん敷くと見える幅が半分以下になる）、狭い札は時刻を省いて件名だけ（時刻は置かれている位置が言っている）、札は二段（時刻を上・件名を下）、一時間の幅を 84 → 168px。**時間帯の外の予定は落とさず端に寄せ、点線で「位置は本当ではない」と言う** ── 落とすと「その日は空いている」と読める | `gui/renderer.js ~ どれが自分か` |

| 563 | 2026-09-14 | **自分の予定が「予定表」という名前で出てくる**（本人）。書き出す側のマクロで、設定シートの字に置き換えられないか | **置き換えは書き出す側（crmaine）の仕事**なので、こちらからは触らない ── 取り決めが「変えるときは両方のセッションで合わせてください」と言っている当のことになる。依頼文を本人に渡した。**あわせて、こちらでも段の呼び名を変えられるようにした** ── Outlook は自分の予定表を「予定表」と返すので、書き出す側が直るまで読めないし、**同姓の人が二人来た日にも効く**。段の名前を右押しして「呼び名を変える」、**空にすれば書き出された名前に戻る**。**鍵はメールのまま**なので、呼び名を変えても予定との結びつきは動かない（取り決めのとおり、名前は揺れる） | `gui/renderer.js ~ const laneName` |

| 574 | 2026-09-14 | CSV を使ってみての四つ（本人）── **上へ／下へが引っ込めた段まで数える**／**日・週・月に自分じゃない人の予定が出る**（「これはダメだな」）／**重なった予定の字が重なって読めない**／**並べて表示でもまだ読めない** | **一、上下は見えている段で数える** ── 引っ込めた段まで数えると、その人のぶん何度も押すことになる。画面で一つ上に見えているところへ一回で行く。引っ込めた段は並びのまま置いていく（戻したとき前に居た場所へ戻る）。**二、自分を決めるまで、日・週・月にチームの紙を一件も出さない**（前は全員ぶん出していた ── **自分じゃない誰かの予定が、自分の欄に出ていた**）。空のときは「自分を決めると、ここに自分の予定が出ます」と表の真ん中で言う ── 黙って空だと「読めていない」と見分けがつかない。並べて表示は絞らない。**三、重なるものは重ねない** ── 縦の表（日・週）は**横に分け**、並べて表示（日）は**縦に積む**（軸が逆なので）。どちらも**塊ごとに割る** ── 朝に三つ重なった日に、夕方の一件まで三分の一にしない。**四、それでも切れる札は残る**ので、**押せば中身が全部読める小窓**を出す（時刻・件名・場所・だれの・仮や非公開の別）。道（`data-at`）だけでは足りない ── **読むだけの予定には道が無い**ので、描くときに番号を振って札から予定へ戻れるようにした | `gui/renderer.js ~ 重なるものは、縦に積む` |
| 575 | 2026-09-14 | **太字と斜体の重ねがけを、窓でも iPhone と同じに読む**（本人「iPhone に合わせて欲しい」） | 同じノートが二つの扉で違って見えていた ── iPhone は Apple の `AttributedString` に渡していて `***両方***` を太字＋斜体に読むが、窓は自前の走査で `**` が先に食い、`<strong>*両方</strong>*` ── **太字の頭にアスタリスクが一字生えて出ていた。** `***` を `**` より先に見て、閉じる `***` が無ければ何もせず下へ落ちる（`***閉じていない**` は今日と同じに読まれる）。**閉じは三つ数える** ── 二つで手を打つと、そこから三つ飛ぶので中の字が一つ落ちる（変異テストで最初に黙ったのがこれで、検査を一つ足した）。iPhone 側は触っていない、揃えたのは窓 | `crates/amber-core/src/markdown.rs ~ find_run3\(&chars, i \+ 3, c\)` |
| 576 | 2026-09-14 | **ノートを改名・移動しても、絵の名前は変えない**（本人「めちゃくちゃややこしくなりそうだし、バグの温床になりそう」） | 前は絵を `attachments/<幹>-<時計>` という**名前**でノートに結び付けていて、幹が変わるたびに絵も改名していた。**ノート二本が同じ絵を指していると、触ってもいないほうが壊れる** ── 改題すると残ったほうのリンクが外れ、フォルダ移動すると絵ごと連れ去られる（どちらも手元で再現した）。いまは**本文が指しているものを運ぶ**（`spare::points_at` ── 「使われていない画像」と同じ読み方。二か所で別々に読むと、片方が見落とした絵をもう片方が消す）。**同じフォルダの改名では絵に一切触らない。** ほかのノートも指している絵は動かさず**写す** ── `..` の付いた道を一本も作らないため（`..` を許すとノートが自分の外のフォルダに依存し、同期の単位が崩れ、「誰が指しているか」も保存ディレクトリ全体を見ないと答えられなくなる）。名前の形で見分けないので、OneNote から写した `<ページ名>_001.png` も付いてくる。依頼 492 の「絵も付いてくる」は**ノートから絵が見え続けること**（本人に確認）。あわせて `spare::resolve` が**リンクを最初の空白で切っていた**のを直した ── amber 既定の `2026-09-06 19-18-30-1.png` が、指されているのに「使われていない画像」に並んでいた | `crates/amber-core/src/naming.rs ~ pub\(crate\) fn bring_pictures\(` |
| 577 | 2026-09-15 | **OneNote に繋がらないとき、何が起きたか分かるようにする**（レビュー依頼から・繋がる前に効くもの） | 三つ直した。**一、落ちたわけが記録に残っていなかった** ── 繋がらない理由も、依頼 572 で足した bit の見立ても `sys.exit("わけ")` で stderr にしか出ておらず、docs が薦める定時の呼び方（`pythonw.exe`）に stderr は無い。記録には「開始」の一行だけが残り、**落ちたことと落ちていないことが見分けられない** ── `--log` を足した理由（依頼 564）が達していなかった。思わぬ落ち方の追跡も同じなので `main` ごと包む。**二、`--probe` の結果をファイルに落とすと途中で死んでいた** ── `✗` も `ambər` の `ə` も cp932（日本語 Windows の既定）に無く、画面に直に出すぶんは平気でも**ファイルへ向けた瞬間に cp932 になる**ので、失敗した行を書こうとして `UnicodeEncodeError`。**いちばん知りたい行で止まる。**画面の字を UTF-8 に寄せ、`--probe` に `--out` も要らなくした（写真を手打ちさせずに済む）。**三、`--probe`・`--list`・`--dry-run` が鎖に阻まれていた** ── 固まっている回を調べるための道具が、その固まっている回のせいで断られる。書く回だけが鎖を取る | `scripts/onenote2md.py ~ def use_utf8\(` |
| 591 | 2026-09-15 | **`.one` がどちらの形式か、フォルダを指すだけで数える（`--peek`）**（本人「OneNote が使いにくすぎて」「死ぬほど使いにくくて」） | `.one` には**二つの形式がある**（Apache Tika の試験資材 4 本で確認）── `109ADD3F…` は公開仕様（MS-ONESTORE）で、ヘッダを読むとノードリストの根がファイル内に収まる（**歩ける**）。`638DE92F…` は Office 365 由来の別もの２で、同じ並びで読むと指し先がファイルの外へ飛ぶ。**どちらかで、ファイルから直に読む道（案C）の重さがまるで変わる。**見るのは 48 バイト目からの 16 バイトだけなので、**会社のノートを外へ出さずに決められる** ── `--peek <フォルダ>` が下まで歩いて形式ごとに数え、**意味まで言う**（「ぜんぶ公開仕様」「混ざっています」「一本も…」「SharePoint にしか無いのかも」）。道を一つずつ打ち直させない。**crmaine のバイナリ保険は効いていなかった**ことも分かった ── 4 本すべてで出てくるのはゴミで、`_looks_like_meaningful_text` が「CJK の範囲にあれば本文」と見るので、でたらめなバイト列がそのまま通る。amber の案A（字だけでも取る）は消えた | `scripts/onenote2md.py ~ def peek\(` |
| 592 | 2026-09-15 | **`.onepkg` の中まで見る（`--peek`）**（現場から。ノートブックをまとめて出せるのは `.pdf` `.xps` `.onepkg` だけだった） | **`.onepkg` は入れ物**（CAB）で、中に `.one` が並ぶ ── 外から見ても形式は分からない。Windows 標準の `expand` で開いて、中の `.one` を同じ手で数える。**書き出した `.one` が公開仕様なら話が変わる**ので、そこを一行で確かめられるようにした（SharePoint 上のものは`638DE92F…` だったが、OneNote が書き出したものは `109ADD3F…` かもしれない ── 落とした見本の `testOneNote4.one` がその形だった）。**開けなければ、わけを言って数だけは出す** ── 黙って 0 本と数えない。CAB でなければ開きにいかない。**なお「ノートブック単位で `.mht` が出せる」と言ったのは誤りだった** ── OneNote は「ノートブック全体は `.pdf` `.xps` `.onepkg` のみ」と断る。docs にも書いた | `scripts/onenote2md.py ~ def unpack_onepkg\(` |
| 593 | 2026-09-15 | **ambər が読める形に揃える（画像の置き場所と名前）**（本人「amber でインポートして読めるよう画像の配置場所は名称にも気をつけて欲しい」） | 三つ外れていた。**一、画像がアンダースコア**（`9月の定例_001.png`）── ambər は**「幹 + ハイフン」**で自分の画像を見分ける（`note::attach` が `format!("{}-{stamp}.{ext}", file_stem(&stem))` で書く）ので、**ノートを改名・移動した日に画像が付いてこない。** ハイフンにした。**二、幹が 120 字**だった ── ambər の `file_stem` は 60 で切るので、**長い題のページだけ幹がずれる**（そのノートだけ画像が迷子になる）。60 に揃えた。**三、説明が `image`** ── ambər が自分で貼るのは `![](…)` で、`image` と埋めると**その字が題の指示（`![猫 w:200]`）の場所に居座る。**規則の正本は Rust（`note::file_stem` / `attach`）で、Python は写し ── **写しである以上ずれる**ので、走査が「同じ答えになるか」と「正本にその規則がまだあるか」を両方見る。あわせて古い画像の片付けを**番号の形まで見る**ようにした ── ハイフンは名前の中にも出るので、`会議-補足-001.png` が `会議-*` に当たってしまう（アンダースコアのときより広く当たる）| `scripts/onenote2md.py ~ def amber_stem\(` |
| 594 | 2026-09-15 | **COM を通らずに、書き出したファイルから写す**（本人「このままスクリプトを作成しきって欲しい」） | COM は諦めた（依頼 589 ── 登録も実体も取り次ぎ側も白いのに呼ぶと落ち、クイック修復でも変わらなかった）。**書き出した `.onepkg` の中の `.one` は公開仕様**（依頼 592）なので、そちらを直に読む ── `scripts/onestore.py`。ヘッダ → `fcrFileNodeListRoot` → FileNodeListFragment → FileNode を辿り、ObjectDeclaration のプロパティ集合から題と本文を取る。**意味の表は記憶で当てない** ── ノードは Apache Tika の `FndStructureConstants.java`、プロパティは `OneNotePropertyEnum.java`。見ずに書いて二度誤診した（ノード名を取り違えて「数が合わない」、`TextExtendedAscii` を UTF-16 で読んで「本文が取れない」── **名前のとおり 1 バイト文字だった**）。**`.one` は改訂の履歴を丸ごと持つ**ので、素直に歩くと同じページが何度も出る（見本では題が `This is impo` →（五回）→ `Section3HeaderTitle` と書き換わった跡が並んだ）── **空間＝ページ、最後の改訂が「いま」。**同じ題のページは `名前 (2).md` にずらす（上書きすると字が消える）。Tika の期待値（`Section3HeaderTitle` / `Section3TextArea1` / `Section3TextArea2` / `way too much information about poptarts to handle.`）と一致した。**まだ平文**（見出し・太字・表・画像・並び順は次） | `scripts/onestore.py ~ def pages\(` |
| 595 | 2026-09-15 | **`ModuleNotFoundError: No module named 'onestore'`**（現場から。走らせた場所が違った） | `import onestore` に頼っていた ── `sys.path` に `scripts/` が入るとは限らないので、**走らせる場所によって落ちる。** 道を名指しして読む（`load_onestore`）。見逃した理由がはっきりしている: **走査が偽物を `sys.modules` に差し込んでいた**ので、**本物の読み込みを一度も試していなかった** ── 偽物が本物より甘いと検査は嘘をつく、六度目。走査を「本物の `load_onestore` を通す」に変え、**よその場所（別の cwd）から子processで走らせる**検査も足した。あわせて、隣の一枚が無いときは**黙って進まずに言う**（`git pull` は済んでいますか）| `scripts/onenote2md.py ~ def load_onestore\(` |
| 596 | 2026-09-15 | **保存ディレクトリが道のまま出る／パネルの幅を変えたい／巻き物の棒が浮いて見える**（本人・現場から） | **一、道がまるごと出ていた。** `dir.split('/').pop()` は Windows の道（`C:\\Users\\…\\OneNote`）を一つも切れず、**まるごと返る** ── 左の帯に道が並んで読めない。`leafOf` が `/` と `\\` の両方で切る。**二、幅を掴んで動かせるようにした** ── もともと `#rail 208px` `#list 336px` の固定で、**仕掛けそのものが無かった**。線の上に幅ゼロの掴み手を置く（帯を挟むと中身が痩せ、境目が二本に見える）。**狭すぎ・広すぎは止める**（一覧は 160px 未満で題が読めず、画面の 45% を越えるとノートが痩せる）。窓の大きさと同じ道で憶える。**二度押しで元に戻る** ── 動かしすぎた人が掴み直さずに戻せる。**三、巻き物の棒を地に溶かした** ── 既定は白い帯と丸い摘みで、琥珀色の地の上に別の板が乗って見える（本人「浮いて見える」）。地は透かし、摘みだけを線の色で。角の升も出さない | `gui/renderer.js ~ function leafOf\(` |
| 597 | 2026-09-15 | **セクションの名前が化ける**（本人「各セクション名の日本語はメチャクチャ文字化けしている」「実際に出力されているセクション＝ディレクトリもとんでもなく文字化けしている」） | **本文は無事で、名前だけが化けていた** ── 犯人は `expand`。CAB から出すときに、名前を機械の符号で書いたつもりで別の符号として扱う。中身は素通しなので、**壊れるのはファイル名＝セクション名だけ**（見え方が「一部だけ化ける」になって、符号の問題だと分かりにくい）。**中身は `expand` に開かせて、名前はこちらで読む** ── CAB の目録（`CFFILE`）を自分で歩き、旗 `_A_NAME_IS_UTF`（0x80）が立っていれば UTF-8、立っていなければ cp932 で読む。目録と実物は**大きさで突き合わせる**（名前が化けている以上、名前では繋げない）。**CAB でなければ開きにいかない・読めなければ空を返す** ── 分からないことを分かったように言わない | `scripts/onenote2md.py ~ def cab_names\(` |
| 598 | 2026-09-15 | **多層のセクショングループが一段に潰れる**（本人「`400_打合せ/月_定例/20260909` こんな感じで多層構造になっているとき、`400_打合せ` くらいのレイヤまでしか出力できてないんじゃないかな」） | **そのとおりだった。** 開いた先を `rglob("*.one")` で集めていたので、**どのフォルダに居たかを捨てていた** ── セクショングループが消えて全部が一段に並び、名前も化けていた（依頼 597）ので**取りこぼしたように見えた**（実際には出ていたが、どれがどれだか分からなかった）。CAB の目録の名前には `\` でフォルダが入っているので、そこから道の並びを取って**そのままフォルダに戻す**。目録が読めなかったときは、開いた先の相対の道で代える ── **黙って何も出さないよりはよい** | `scripts/onenote2md.py ~ def opened_sections\(` |
| 599 | 2026-09-15 | **見出し・太字・箇条書き・画像を落とす**（本人「実際の装飾や画像も取り込んで欲しい」） | 平文しか出していなかった（依頼 594 の積み残し）。`.one` のプロパティから拾う ── 段落の書式（`ParagraphStyle`）の名前が `h1`〜`h6` なら `#`、`Bold`/`Italic` の旗から `**…**` `*…*`（両方なら `***…***`）、`ListNodes` があれば `- ` か `1. `、`OutlineElementChildLevel` で字下げ（6 段まで）。**画像は ambər の決まりどおりに置く**（`attachments/<幹>-001.png`・幹は 60 字・説明は空 ── 依頼 593）。**同じ中身なら書き直さない**ので、二度目からは更新時刻が動かない。作成日時も front matter に入れた（`.one` は 1980-01-01 からの秒で持つ）| `scripts/onestore.py ~ def as_markdown\(` |
| 600 | 2026-09-15 | **表を落とす**（本人「OK,それじゃ表を頼むぜ」） | 表・行・升は `ObjectGroup` に**木の順で並ぶ**ので、参照をたどらずに「表が始まった／行が始まった」と数えていけば組める。jcid は記憶で当てず Tika の `JCIDPropertySetTypeEnum.java` から取った（表 `0x22` / 行 `0x23` / 升 `0x24` ── ここを取り違えて二度誤診している）。決めたことが三つ。**一、見出しの行は空で置く** ── Markdown の表は見出しの行が要るが、OneNote が「1 行目は見出し」と決めているかは `.one` から素直に引けない。**1 行目を使うと、そのデータが一行消える**（消えるより空の行が一本増えるほうがいい・COM の道と同じ・依頼 578）。**二、升の中の改行は空白で繋ぐ** ── Markdown の表に改行は入らず、`<br>` は ambər の画面に字として出る。**三、升の数を行ごとに揃える** ── 揃っていない表は画面の上で崩れる。あわせて**表の中の字を本文に二度出さない**（升をたどるときに拾うので、そのままだと同じ字が表の外にも並ぶ）| `scripts/onestore.py ~ def table_markdown\(` |
| 601 | 2026-09-15 | **セクションごとに、中身のないノートが一本できる**（見本で走らせて気づいた） | `.one` の**空間がぜんぶページとは限らない** ── 頭にセクションそのものの空間が一つあり、題は持つが本文が無い。そのまま出すと、**セクションごとに空の `.md` が一本**できて、ambər の一覧に「開いても何も無いノート」が並ぶ。ページの空間には `Page`（0x0B）が入っているので、そこで分ける。あわせて、**1 ページも取れなかったときに黙らない** ── 見本 4 本のうち 3 本は `638DE92F…`（読めない形式・依頼 591）で、`0 ページ` とだけ出ていた。形式の名前を添えて言う | `scripts/onestore.py ~ o\["jcid"\] == JC_PAGE for o in whole` |
| 602 | 2026-09-15 | **会社向けの一枚では、同期とカレンダーの同期を閉じる**（本人「会社でビルドする際には、『同期』に関する機能や表示はすべてクローズにしたい」「『Google カレンダー』なども同様会社の環境では見えなくて良い」「Asset にアップしてもらう資材には見えないまたは機能を削ぎ落としたものにしてほしい」） | 版を一つ増やした ── `pack.js --edition office` が配る一枚に `gui/edition.json` を置き、画面はそれを見て**外へ運ぶものを出さないし走らせない**（Google Drive の同期・iCal の購読＝カレンダーの同期・グループでの共有）。**「献立から消す」では足りない** ── amber には ⌘⇧P があり、そこは名前で探す道なので、蓋をするのは一か所（`canRun`）にして両方の道をそこに通した。**訊きにも行かない** ── サインインの有無を訊くこと自体が外の鍵入れを開けにいくことなので、`loadSync` ごと止める。**残したもの**: カレンダーの面そのものと、チームの CSV（会社の Outlook が書き出した一枚を読むだけで、外へは何も出さない ── 依頼 473）。リリースは**二枚**出す（`amber-win-x64-*.zip` と `amber-win-x64-office-*.zip`）── 名前もフォルダも分ける。同じ名前だと、平らに並ぶ籠の中で片方が片方を黙って上書きする | `scripts/office-test.js ~ '会社向けの1 つに「' \+ id \+ '」は無い'` |
| 603 | 2026-09-15 | **保存ディレクトリが横に長すぎる**（本人「２つ３つ４つと増やしたときに保存ディレクトリの見た目が横に長くなりすぎない？」「設定の amber についてや、保存ディレクトリの追加・変更・削除に見えているぞ」） | 三つ重なっていた。**一、`shortPath` が Windows の道を一度も切っていなかった** ── `split(/)` なので `C:\Users\t502960\Documents\OneNote` は**まるごと**返る。`leafOf` で踏んだのと同じ取りこぼし（依頼 596）。`/` と `\` の両方で割り、畳んだあとの区切りは**その道が使っているほう**に戻す（混ぜると別の道に見える）。**二、添え書きが小窓からはみ出していた** ── 小窓の幅は 560px の決め打ちなのに `.sub` が縮まないので、名前ごと右へ押し出されていた。名前は縮めず（それが行の顔）、添え書きだけ `…` で畳む。**三、「amber について」が道を `・` で繋いだ一本の行**だった ── 増えるほど横に伸び、**不具合を伝えるときに添える三つのうち、いちばん読めない一つ**になっていた。一行ずつに割った。あわせて、一覧から「同期しない」の四連を消した（何も繋いでいない人に同じ字が四本並ぶ意味は無い）。一行ずつにしたら行が四つを越えて**絞り込みの欄が生えた**ので、`bare` を真で渡す | `scripts/win-test.js ~ Windows の家の下は ~ に畳む` |
| 604 | 2026-09-15 | **F5 で読み直す**（本人「保存ディレクトリの中身をごっそり削除したときなど、表示がなかなかアンバー側に伝わらない。F5 で更新するとかの機能はほしいね」） | 見張り（`fs.watch`）は、**見張っているフォルダそのものが消えると落ちる** ── そのとき `eyes` から外れるだけで張り直していないので、外でまとめて消した・同期が下ろした・ネットワーク越しのフォルダが一度切れた、のどれでも**画面だけが古いまま**残る。押せば必ず読み直す道を置いた（F5・⌘R／Ctrl+R）。**見張りを張り直してから**数え直し、開いていたノートが消えていれば閉じる（無いノートを見せ続けると、次の保存で作り直してしまう）。**打ちかけは触らない** ── 読み直しで消えていいのは、まだ書いていない字ではない。**どこを打っていても効く** ── エディタの中に居るときこそ押される鍵で、そこで素通りすると「効かない」として出る（⇧付きは掴まない ── 別の鍵が効かなくなる） | `scripts/win-test.js ~ F5 で読み直す` |
| 605 | 2026-09-16 | **読みにくい言い回しを画面から外す**（本人「AI的な表現・文言・案内があれば修正をお願いしたい。題・鳴る・釦など」） | 依頼 451（「釦ってなんだい？読めなくてｗｗ」）の続き。**画面の中で言葉が割れていた** ── `index.html` は前から「空のままなら、本文の1行目が**タイトル**になります」と書いているのに、`renderer.js` は「**題**を直す」「（**題**なし）」と言っていた。同じものを二つの名前で呼んでいたので、タイトルに寄せた（13 か所）。**探すときも「タイトル:週報」で打てる**ようにした ── 画面がそう呼ぶなら、そう打てないとおかしい（「題:」も受け続ける・前から打てたものは取り上げない）。**前のバージョンの小窓も割れていた** ── 中の三つは「このバージョンを見る／戻す／保護する」なのに、題だけ「9/12 14:03 の**姿**」だった。ほかに、本文の意味で使っていた「字」を「文字」へ（「元の字が取れません」ほか 6 か所）、「終日に**印**を」を「チェックを」へ。走査の出す言葉も平たくした（「鳴った／黙った」→「気づいた／見逃した」）。**台帳の過去の行は直さない** ── あれはその日に何があったかの記録で、書き換えると記録でなくなる | `gui/renderer.js ~ name: 'タイトルを直す'` |
| 606 | 2026-09-16 | **見出しと表の中に書いた言葉が探せない**（総ざらいで見つけ、本人が決めた） | 探し先は「タイトル＋タグ＋抜粋」で、**抜粋は `#` の行・表・画像・コード枠を外している** ── あれは一覧の二行目に出す**一行の要約**で、見出しも表も「何でできているか」であって「何について書いたか」ではないから。**その判断は変えない。** けれど「書いたのに探せない」は別の話で、`## 来期の見通し` や `ＣＰＵ` `８コア` といった升の中身は、書いた本人にとっては書いたことそのもの。**出す字と探す字を分けた** ── `Note::findable` に見出しと升だけ集め、`haystack` にだけ足す。一覧の二行目は一文字も変わらない。**飾りは落とす**（`## **来期**の見通し` を探す人は「来期」と打つ）。**区切りの行（`--- :--:` だけの行）とコード枠の中は拾わない** ── 後者を拾うと「Markdown の書き方」を書いたノートが例文ぜんぶで当たる。読むのは `read` が既に取っている頭の数十行だけで、ファイルは二度読まない | `crates/amber-core/src/note.rs ~ pub findable: String` |
| 607 | 2026-09-16 | **裸の URL がリンクにならない**（総ざらいで見つけ、本人が決めた「「表示」の面だけリンクにする」） | GitHub と違い、`[字](url)` と書いたものしかリンクにならなかった。押せるようにした ── ただし**ファイルの字は一文字も変えない。** `[https://x](https://x)` に書き換えると、打っていない記号がファイルに増え、ほかのアプリで開いた人には別の字に見える（同期していれば、それが全部むこうへ差分として飛ぶ）。`Inline::Bare` という別の札にして、HTML には `data-bare="1"` を付けて出し、**書き戻す側（`inlineToMd` ── 窓と電話が共有している一組）はそれを見て URL の字だけを戻す。** 終わりの見切りは日本語の文に合わせた ── 空白・閉じ括弧・鍵括弧・句読点で止め、末尾の終止符は文のほうに返す（`…/a。` や `…/a.` は開けない行き先になる）。`javascript:` は今までどおり通さない。往復の走査（`round-test.js`）に六とおり足した | `gui/renderer.js ~ c\.dataset\.bare` |
| 608 | 2026-09-16 | **OneNote から色とリンクも落とす**（本人「リンクと色は欲しい。フォントサイズや太字、取消線などはもう実装済み？」） | 訊かれて確かめたら、**太字も斜体も取り消し線も一度も落ちていなかった**（依頼 599 で入れたつもりだった）。旗は本文のオブジェクトではなく**書式（`ParagraphStyle`・jcid 0x4D）のほう**が持っていて、こちらは本文から読んでいた ── 見本の `.one` で数えたら、`Bold` `Italic` `Strike` `FontColor` `Hyperlink` は**ぜんぶ 0x4D にしか無かった。** 根は `propset` が**参照の指し先を捨てていた**こと ── `rgData` には「何個ぶん」しか置かれず、指し先は property set の頭の並びに入っているのに、そこを読み飛ばして全部 `("ref", 1)` という中身の無い札にしていた。頭の並びを持っておき、参照の型が出てきた順に配る。そのうえで本文 → 書式をたどる（`style_of`）。**色は MS-ONE の COLORREF** ── 4 バイトの最後が `0xFF` なら「自動」で、それは「黒を指定した」ではなく「指定していない」（ここを色として出すと、ノートの全部の行が span に包まれる）。最後が `0x00` のときだけ前の三つが赤・緑・青。ambər の書き方（`note::first_color` が読む一つの形）で巻く。**巻く順は、リンクがいちばん内側・色がいちばん外側** ── 逆にすると `[**字**](url)` や `**<span…>字</span>**` になり、印と札が噛み合わない。見本で確かめると、日付と時刻の行だけが `#767676` になった（OneNote の見た目どおり）。**落とせないもの**: 蛍光ペンと文字の大きさ（ambər に書き方が無い）| `scripts/onestore.py ~ def style_of\(` |
| 609 | 2026-09-16 | **使われていない読み手を、電話から外す**（本人「いらない」） | `ios/Cian/Reading.swift` の `Reading` は SwiftUI で組んだ二つ目の Markdown の読み手で、**どこからも呼ばれていなかった**（「表示」の面を `Paper.swift` に一本化した名残・250 行）。残しておくと「読み手が電話にもう一つある」ように見えるうえ、**こちらは `AttributedString(markdown:)` を使うので逃がしの読み方が窓と違った** ── 使われていない食い違いは、いつか誰かが繋いだ日に本物の食い違いになる（実際、docs には「iPhone は逃がしを読むので窓と食い違っている」と書いてあって、それはこの死んだ画面の話だった）。**`Block` だけ残す** ── `NotesStore.blocks(of:)` が core の答えを組み立てる型で、窓の面にも電話の面にも要る | `ios/Cian/Reading.swift !~ struct Reading: View` |
| 610 | 2026-09-16 | **繋がらない道を丸ごと外す**（本人「不要なロジック削除」「丸ごと外す」） | `onenote2md.py` は 2,593 行で、**そのうち約 1,700 行が COM の道**だった ── OneNote に直接繋ぐ側、その診断（`--probe` 29 行・`--check`・`--forget`・登録と bit の見立て）、返ってきた XML を Markdown にする側。本人の会社の端末ではどうやっても繋がらず（依頼 589 ── 登録も実体も取り次ぎ側も権限も bit も白く、クイック修復でも変わらなかった）、代わりに `.onepkg` の中の `.one` を直に読む道が通った。**797 行になった。** 走査も 2,249 → 661 行、変異テストも 190 → 62 とおり（残りは「置換できず」＝撤去済み）。**pywin32 も 32 bit の Python も要らなくなった。** 台帳の 23 行は消さずに `REQUESTS.withdrawn.ja.md` へ移した（あの日に何があったかの記録なので）。**消しすぎも出た** ── COM の器を使っていた走査を落としたら、`chosen` `amber_stem` `use_utf8` のような**生きているコードの検査まで一緒に消えていた**（変異テストが 11 件「見逃した」と言って捕まえた）。足し直してある | `scripts/onenote2md.py !~ def connect_onenote\(` |
| 611 | 2026-09-16 | **押すのは一回にしたい**（本人「バッチファイルを１回叩けば全部やってくれたり、UI をだして、コマンドを手打ちすることなく OneNote を md に吐き出したい」） | `onenote2md.bat` をダブルクリックすると**小さい窓**が出る（`scripts/onenote_ui.py`）── 取り込むものと出力先を押して選び、「取り込む」を押すだけ。進み具合と結果もそこに出て、終わったら「出力先を開く」。`.onepkg` をバッチに**放り込めば窓も出ない**（既定の出力先へそのまま取り込む）。**`tkinter` だけで組む** ── Python に最初から入っていて、網に出られない会社の端末でも何も足さずに動く（見た目より、入っていることのほうが値打ちがある）。**変換は子 process で、別の糸から** ── 同じ糸でやると窓が固まって「落ちた」ように見える（`.onepkg` 一本で数分かかる）。**走らせる一行は Tk の外に出した**（`command()`）── 中に置くと Tk の要る機械でしか確かめられず、押したときに何が走るのか誰も試せない。呼ぶのは `sys.executable` ── `py` を探し直すと、窓を出した Python と別の Python で走りかねない（会社の端末には何本か入っている）。バッチは先に `chcp 65001` を打つ（日本語 Windows の既定は cp932 で、セクションの名前が化けたまま画面に出る）。既定の出力先は `ドキュメント\OneNote` ── **ambər の既定の保存ディレクトリの下には掘らない**（本人が決めた）| `scripts/onenote_ui.py ~ def command\(` |
| 612 | 2026-09-16 | **ゴミ箱に捨てたノートがタブに残り続ける**（本人）／**右押しの献立が、押した場所で違う**（本人「どちらも同じポップアップにできる？」） | **一、消えたタブ。** 消す道は**四つ**あり（⋯ から一本・選んでまとめて・フォルダごと・同期が向こうの削除を下ろしたとき）、**どれもタブに触っていなかった。** 四か所に同じ一行を足すと三か所目で忘れるので、**数え直したあとの `reload` で一度だけ見る**（`dropGoneTabs`）。**`closeTab` は使えない** ── あれは書きかけをファイルへ落とすので、**消したはずのノートが書き戻って生き返る。** 書かずに外す。**「一覧に無い＝消えた」ではない** ── 外付けを抜いた・ネットワークの保存ディレクトリが一度切れた回も一覧からは消えるので、**困っている保存ディレクトリの下と、単発で開いている一本（`guest`）は触らない**（そこを閉じると、繋ぎ直したとき机が空になっている）。たどった道（`trail`）からも抜くが、**作り替えずに抜く** ── 同じ並びを見ている `trailAt` がずれる。**二、右押し。** 題の右押しだけが手書きの四つ（タイトルを直す・ファイル名を写す・場所をコピー・Finder で表示）で、一覧の行とも ⋯ とも違うものが出ていた ── 同じノートを右押ししているのに、押した場所で出るものが変わる。**四つを命令の表へ移した**ので、一覧からも ⋯ からも ⌘⇧P からも同じ十七が出る（題の右押しは `openMenu` を呼ぶだけになった）。Windows では「エクスプローラーで表示」と言う。改名とフォルダ移動のタブは実機で確かめた ── どちらもその一本のまま付いてくる | `gui/renderer.js ~ function dropGoneTabs\(` |
| 613 | 2026-09-16 | **マウスを乗せても選び目が動かない／乗せたときの手応えを全般に**（本人「↑や↓ボタンで はい や いいえ の選択が変わってハイライトも変わるが、マウスがオンボードされても選択のハイライトが変わらない」「マウスが上に乗っている時の挙動を全般的に見直し」） | 小窓（`askYes` / `askPick`）の選び目は**鍵盤だけが動かしていた** ── 「はい」に乗せて押しているのに、光っているのは「やめる」のまま。押す直前に画面が言っていることと、押して起きることが食い違う。**いちばん怖いのは、まさにそこ**（ゴミ箱の確かめ）。乗せたら選び目を移す（`onmouseenter`）── **鍵盤と同じ場所（`at`）を動かす**ので、乗せてから Enter でもそこが選ばれる。**描き直さずに印だけ移す** ── 一行ごとに組み直すと、千行の一覧でマウスを動かすたびに全部を描く。あわせて**指の形だけ出ていて色が一つも動かなかった五つ**に手応えを付けた: カレンダーの予定（週と日の帯・月の升目・終日の段・グループの帯と升目）・「使われていない画像」の升・帯の小さなボタン・ぶつかった場所の選び口。**予定の地の色は塗り替えない** ── 色は誰の予定かを言っているので、上書きすると読めなくなる（外枠と浮きで言う）| `gui/renderer.js ~ row\.onmouseenter = \(\) => \{` |
| 614 | 2026-09-16 | **コードの枠が狭くて横棒が出る／押すだけで写せる札がほしい**（本人「横幅にものすごく余裕があるのに、狭いコード表記になって横スクロールが表示されている」「押下するだけでコピーできるコピーボタンがあるといい」） | **枠は文の幅に従わない。** `#read > *` の 34rem は「目が一行を追える長さ」の話で、**コードはそもそも文ではない** ── 折り返さないので、狭い枠に入れると画面が空いていても横棒が出る（実測で、面 977px に対して枠が 544px しか使っていなかった → 913px に）。図で同じ判断を既にしている（`.mermaid`）。**表も同じ** ── 升は折り返せるが升の数は折り返せないので、四列あれば必ず横棒が出る。**写す札は枠の中に置いてよい** ── 枠は触れないかたまりで、書き戻すのは `data-md`（元の字）なので、札の字が本文に混ざらない。**乗せるまで出さない**（読んでいるあいだ字の上に札が浮かないように）。図の枠には付けない ── あれは押すと工房が開く場所で、写したいのは図であって字ではない。**写す字は画面から拾わない** ── 読む面の枠は Monaco が色を付けたあとの姿で、**改行が `<br>`・空白が `&nbsp;`** になっている（実物で確かめた ── 最初それで拾って、写した字が一行に潰れた）。元の字から囲みだけ外す | `gui/renderer.js ~ function codeOf\(pre\)` |
| 615 | 2026-09-16 | **注記の小窓に絞り込みの入力欄は要らない**（本人） | 五つを絞り込む人はいない。押して選ぶだけの一覧にした（`bare`）。同じ形は「amber について」でも一度直している（依頼 603）── **`bare` を渡さないと `items.length <= 4` で自動に決まる**ので、行が五つになった日に欄が生える | `gui/renderer.js ~ GitHub でも同じ形で出ます., true\)` |
| 616 | 2026-09-16 | **Excel から貼ると絵になる／白いマーカーが付く／不思議なところで改行する**（本人） | 三つとも別の理由だった。**一、Excel は同じ一回のコピーで三つ載せてくる** ── 選んだ範囲の絵（PNG）、表（`text/html`）、タブ区切りの字。窓は「絵があれば絵」で採っていたので、**表を貼ったつもりが画面写真**になっていた。見分けるのは**字の有無** ── 画面を撮って貼る回（⌘⇧4）だけが字を一つも載せてこない。ウェブの画像を右押しでコピーした回も字が無いので、いままでどおり絵として入る。**二、白いマーカーは Excel の `style="background:white"`** がそのまま残っていたもの（琥珀の紙 `#fffdf8` の上では、白がマーカーを引いたように見える ── 実物の HTML で確かめた）。**ambər が知っている飾りは `color:#rrggbb` だけ**なので、それ以外の属性は落とす。**黒は色として残さない** ── Excel も Word も既定の字に `color:black` を付けて寄こすので、残すとノートじゅうが span で埋まる。**三、升ひとつは表ではない** ── Excel は升を一つ写しても `<table>` で寄こすので、文の途中に貼ると段が割り込んで**そこで行が切れる**（本人の言う「不思議なところで改行」）。升が一つなら字だけ入れる（末尾の改行も落とす ── Excel は `text/plain` にも一つ足してくる）| `gui/renderer.js ~ function justAPicture\(data\)` |
| 617 | 2026-09-16 | **セクションの名前が化ける／階層がばらばら／中身がほとんど入らない／ローディングバーが動かない**（本人「よめない漢字がずらーっとばけちゃっている」「400_ｘｘｘｘというセクションが３つできあがっている」「中身もほぼほぼ入っていない」「ローディングバーは全く動かなかったので、非表示または削除でよい」） | 四つのうち三つは**同じ一か所**から来ていた ── `.onepkg`（CAB）の目録を読む頭の位置。**ファイルの数は 28 バイト目、26 バイト目は「フォルダの数」。** 26 を数えていたので目録はフォルダの数（ふつう 1）で切れ、残りは `expand` に出させたファイルを**大きさで突き合わせる**逃げ道に落ちていた ── そこで名前は `expand` が付けた化けたものになり（`expand` は日本語の名前を壊す）、階層も取り違えた。走査は**同じずれで偽物の CAB を組んでいた**ので、検査は間違いを一緒に抱いて黙っていた（偽物が本物より甘い、七度目）。いまは**頭も目録も中身も全部こちらで読む** ── CFHEADER・CFFOLDER・CFFILE・CFDATA を自分で辿り、MSZIP は塊ごとに前の 32KB を辞書にして解く（`expand` を呼ばないので mac でも開ける ＝ **走査で本物の CAB を組んで確かめられる**）。名前は**旗を見る前に UTF-8 を試す** ── OneNote は `_A_NAME_IS_UTF` を立てずに UTF-8 で書いていて、それを cp932 で読むと「数字はそのまま、漢字だけ読めない」という現場の顔になる。**中身が入らない**のは別口で、`.one` の改訂は「変えたところ」しか持たないことがあり、最後の改訂だけを見ると本文も `Page` の札も前の改訂に置きっぱなし ── 題だけのノートや、ページまるごとの取りこぼしになる。**OID で重ねて**（新しいほうが古いほうの居た場所に座る）拾い直す。**回っているだけの棒は外した** ── 一度も動かず、測っていないものを測っている顔で見せていた。代わりに `[3/12] 道 ── n ページ` を本体が言い、窓はそれを読んで上の一行に出す | `scripts/onenote2md.py ~ n_folders, n_files, flags = struct\.unpack\("<HHH", d\[26:32\]\)` |
| 618 | 2026-09-16 | **`` ``` `` を入れる札を、注記の右に**（本人「注記の右に、コードみたいな文言で ``` を入力できるボタンを設けてくれないかな」「囲みじゃなくて、コードブロックがいいな。長いけど入らない？」） | **名前は「コードブロック」**（本人が決めた）。帯のすぐ上に「コード」という**面の札**があるので、**そこだけの「コード」は置かない** ── 同じ字が二つあると、押す前にどちらの話か分からない。**入るかは測った**: 札は 107px、二列目ぜんぶで 530px。**窓 1100px までは一段**で、1080px から帯が横に流れる（前からある振る舞いで、行が消えるわけではない ── 一列目も同じ幅で流れる）。左の列と一覧を畳めば 900px でも一段に戻る。**中身は空で出す** ── 表のように見本の字を入れると、枠の中では**消すところから始まる**（枠に入れたいのは自分の字で、言い換えるための見本ではない）。**実機で二つ見つけた。** 一、**行の途中では枠にならない** ── `` ``` `` は行の頭に無いとただの字で、`もとの一行。```` と繋がって入ったときは**枠が開かないまま次の行を飲み込み**、「表示」の面に空っぽの枠が出た。打っている行に字があるときだけ、先に行を改める（行の頭なら余計な空行を足さない）。二、caret は枠の中へ ── 入れたあと、そのまま打てないと札の意味が半分になる | `gui/renderer.js ~ function putFence\(\)` |
| 619 | 2026-09-16 | **`<summary>` で文書を折りたたみたい／押して開け閉めしたい**（本人「コードがめちゃくちゃ長くて見にくいんだよね」「折り畳んだ文書はクリックしたりスマホでタップして開いたり閉じたりしたい」） | **記法は `<details>`。** GitHub がそのまま畳む形で、メモ帳で開いた人にも「畳んであるもの」と読める ── ambər だけの記号を作ると、同じノートがよそで壊れる。core が読むようにした（それまでは山括弧が字として出ていた）。**行ぜんたいが札のときだけ**畳みはじめる（本文の途中に `<details>` と書いた行が消えないように・注記と同じ筋）。中身はふつうに組み直すので、**畳んだ中に枠も表も図も入る** ── 畳みたいのは、たいてい長い枠。見出しを書かなければ「詳しく」と出す（押すところが字として要る）。**入れ子も数える** ── 中の `</details>` で外が閉じると、そこから下がぜんぶ畳みの外へ出る。**段落の次の行でも畳みになる** ── 切らないと段落に飲み込まれ、山括弧が字で出た（実機で出た。囲みが `fence_lang` で切れているのと同じ筋）。**書き戻しはまるごと元の字で返す**（`richBlock`）── 畳んである中身を組み直すのは「見えていないものを書き換える」ことで、閉じたまま保存しただけで中が変わるのがいちばん怖い。**開け閉めは見え方だけ** ── 押しても書きかけにならず、面も替わらない（実機で確かめた）。**一覧の二行目に札を出さない** ── `<details>` と `</details>` は形の話。**見出しは残す**（人が付けた名前なので、抜粋にも探し先にも入る）。見た目は窓と iPhone で同じものを置いた（三角は自分で出して、開き具合で回す） | `crates/amber-core/src/markdown.rs ~ pub fn is_details_open\(` |
| 620 | 2026-09-16 | **絵や図が出力されていない／表もぐちゃぐちゃ**（本人・現場から） | **二つとも「並び順で当てていた」のが元だった。** 一、**絵は指し先にある。** `PictureContainer`（`0x20001C3F`・型 8 ＝参照）も `EmbeddedFileContainer`（`0x20001D9B`）も参照なのに、**画像オブジェクト自身のバイト列**を `.png` として書き出していた ── 出てくるのはプロパティ集合の生バイトで、**一枚も開けない。** 指し先をたどるようにした（依頼 608 で参照が引けるようになっていた）。あわせて **`FileDataStoreObject` の頭 36 バイト**（`GUID(16)+長さ(8)+未使用(4)+予備(8)`・Tika の `deserializeFileDataStoreObject` と同じ形）を外す ── 被せたまま書くと 36 バイトずれて開けない。**拡張子は中身から決める** ── OneNote の絵はたいてい名前を持たず、名前だけ見ていると JPEG を `.png` として置くことになる。**絵として名乗らない中身は置かない**（開けないファイルをノートごとに並べない）。二、**表は参照でたどる。** 表 → 行 → 升 → 中身は `ElementChildNodes`（`0x24001C20`・型 9 ＝参照の列）で繋がっているのに、**並んでいる順**で「表が始まった／行が始まった」と数えていた ── 作り物の見本では通るが、**本物は改訂をまたぐと順が入れ替わる。** 升の中は `升 → アウトライン要素 → 本文` と下がるので、字に当たるまで降りる（輪は憶えて止める）。「表の中の字を本文に二度出さない」も同じ指し先で数える。**指し先の無い表だけ、並び順で拾い直す**（古い形の逃げ道）。走査は**並びを入れ替えても同じ表になるか**を見る ── そこが本題なので | `scripts/onestore.py ~ def kids_of\(p\)` |
| 621 | 2026-09-16 | **OneNote の取り込みを ambər の中に**（本人「今アップされている資材をもとにビルドしたけど、OneNote の取り込みってでてこない」「それじゃお願いできるかな」） | ⚙「インポート」の隣に**「OneNote を取り込む」**。見取り図で通した三段 ── 取り込むもの（ドキュメント・ダウンロード・デスクトップ直下の `.onepkg` を新しい順に＋ファイル／フォルダを選ぶ）→ 書き出し先（初回だけ訊いて憶える。**ドキュメント／OneNote を新しい保存ディレクトリとして足す**のがおすすめ。憶えた場所がどの保存ディレクトリの中でもなくなったら、また訊く）→ セクションごとの ✓／▸（閉じても裏で続く）。**読み手は借り物**（`onenote_parser` 2.0・MPL-2.0、新しい `crates/amber-onenote`）── 自前の `onestore.py` は公開仕様しか読めず本物の見本で 0 ページだったが、これは FSSHTTP 包みの `.one`・目次つきのフォルダ・`.onepkg`（CAB）を読む。**書くところは自前**: 走りごとの太字・斜体・取り消し・色（黒と自動は出さない）、リンクは部品の `hyperlinks()`、見出し、点と番号の入れ子（子の字下げは親の印の幅）、**箇条書きでない段落は字下げしない**（4 つで Markdown がコードの塊と読む ── 本物の見本で踏んだ）、表は空の見出し行（依頼 578）、絵は `attachments/<ページ>-001.jpg`、添付ファイルは**中身ごと**置いてリンク。名前は `note::file_stem`、同じ題は `-2`（上書きしない）。**添付の名前から空白と括弧を抜く** ── `%20` にしたら窓が二重に直して絵が出なかった（窓で踏んだ）。**取り込みは二本目のエンジンで** ── エンジンは一本の糸なので、大きい一冊を読む間ノートの一覧も保存も止まる。Windows の道は `/` と `\\` の両方で「保存ディレクトリの中か」を見る（入れ子を作らない）。試験の見本は本物の `.one`（MIT と MPL・`tests/samples/README.md`）、`.onepkg` は試験の中で本物の一冊を CAB に包んで作る。**Python の道（`onenote2md.bat`）は、本人の端末でこちらが通るまで残す** | `gui/renderer.js ~ id: 'onenote', name: 'OneNote を取り込む'.*run: cmdOneNote` |
| 622 | 2026-09-16 | **Ctrl＋「＋」で文字が大きくならない**（本人「Ctrl+「＋」で文字を大きく、Ctrl+「ー」で文字を小さくって実装してくれてたっけ？」） | 実装はあったが、**大きくする側は `e.code === 'Equal'` だけを見ていた**。`e.code` は US 配列での位置で、**JIS の「＋」は `Semicolon` の位置**（`; ＋ れ` のキー）、`Equal` は「＾ へ」のキー ── 刻印どおりに押すと効かず、＾を押すと大きくなっていた（前の注釈は「JIS では + は Shift を要る」と書いていて、位置の読み違いに気づいていなかった）。`Semicolon` も拾う（ブラウザも JIS では Ctrl＋; で拡大する）。`Equal` とテンキーの＋は残す。「ー」（`Minus`）と Ctrl＋0 は前から効いていた。本物の窓で、一覧の上とノートを書いている最中（Monaco の中）の両方で押して、大きさが 1 段ずつ動くのを見た。戻し方の案内も Windows では「Ctrl0」と出す（前は ⌘0 のまま） | `gui/renderer.js ~ e\.code === 'Equal' .. e\.code === 'Semicolon' .. e\.code === 'NumpadAdd'` |
| 623 | 2026-09-16 | **文字を大きく・小さくするボタンを、ノートの上とカレンダーに**（本人「ノートの画面上部ってまだ余裕あるかな？もしあれば、文字サイズを大きくするボタン、小さくするボタンを設けてもらえないかな」「カレンダーのページにも…サイズ的に難しければ無理にいれなくてよい」） | **ノート**: 帯の「表示／並べて表示／コード」の右に「A−」「A＋」を同じ形の枠で置いた。鍵と同じ一本の道（`setFont`）を通すので、書く面と読む面が揃って動く。端（-4・+8 段）まで来たらその向きを薄くする。ノートを開いていないとき（カレンダーを見ているときも）は出さない ── 見えていないノートの字を変えるボタンは押しても何も起きないのと同じ。**カレンダー**: 帯の「閉じる」の左に同じ二つ。**ノートとは別の段で憶える**（`calFontStep`、-3〜+6）── 月の升は狭いので、ノートと同じだけ大きくすると予定がはみ出す。升の中の字は `em` なので、表の土台を一つ変えれば揃って動く（帯のボタンは大きくしない ── 帯が折れて押した先が逃げる）。カレンダーを見ているときは Ctrl＋「＋」「ー」「0」もカレンダーの字を変える。**開いただけでは設定を書かない**（戻すだけの `quiet` では `remember` しない ── 前は `setFont` が必ず書いていた）。**踏んだ**: カレンダーのボタンを「日・週・月」と同じ `.seg` の枠に入れたら、`.seg button` の受け口が先に拾って何も起きなかった ── 受け口を前に置いた。本物の窓で、ノートの二つの面・週・月のそれぞれで押して大きさが動くのを見た | `gui/renderer.js ~ closest\('\.fup'\)\) \{ setCalFont\(calFontStep \+ 1\)[\s\S]*const seg = e\.target\.closest\('\.seg button'\)` |
| 624 | 2026-09-17 | **OneNote の取り込みで「何も出なかった」**（メンバーから・本人経由「.onepkg ではなく .one を取り込んだところ、何も出なかったそうだ。裏で動いているのか？」） | **読めなかったとき、小窓が 0.03 秒で閉じていた**（本物の窓で測った）。理由は下の札で言っていたが、札は 2 秒で消え、頭に長い道が付いていて理由まで読めない ── 何も起きなかったのと同じ顔。いまは**小窓に残す**（閉じていても開き直す）: 題「『x.one』を取り込めませんでした」、✗ の行に人の言葉の理由、次の行に**元の文言**（問い合わせで打ってもらえば原因が分かる）、下の添え書きに**どうすればいいか**（行の添え書きは一行で切れるので、切れない添え書きへ）。理由の言い換え: Windows の『別のプロセスが使用中』（os error 32・OneNote が開いている `.one`）／形として読めない（空・途中で切れた・OneDrive 上にだけある）／権限が無い ── どれも最後は「OneNote から .onepkg を書き出して選ぶ」へ。セクションが一つも無いときも同じ形で残す。**読んでいる間は「▸ 読んでいます…（大きいノートブックは数分かかります）」の行を出す** ── 題だけの小窓は止まっているのか動いているのか分からない | `gui/renderer.js ~ run\.trouble = oneTrouble\(why\(e\)\);\s*oneShow\(\);` |
| 625 | 2026-09-17 | **窓: タブが溢れると、見切れたタブへ行けない**（本人「ノートを開きまくって、タブが溢れかえっている時、見切れているタブにアクセスできず不便だ。タブを右や左にスクロールまたはフリックして動かせるようにできないかい？」） | 帯は前から横に動く作り（`overflow-x: auto`）だったが、**棒を隠していて、ふつうのマウスのホイールは縦にしか回らない** ── Windows のマウスでは見切れたタブへ行く手段が無かった（Mac のトラックパッドの横なぞりだけ効いていた）。三つ足した: **ホイールで横に動く**（縦の回しを横へ。横なぞりはそのまま）／**掴んで引っ張る**（5px 動いたら引っ張りに変わる。引っ張り終わりの一押しはタブを開かない ── そのため**タブを開くのを押し下げから押し離しに**変えた。押した瞬間に開くと、掴んだだけで開く）／**端の ‹ ›**（溢れているときだけ出る。見えている幅の八割ずつ。端まで来たら薄くなる）。本物の窓で 20 枚開いて、ホイール・› ・引っ張り（タブが開かないこと）・ふつうのクリック（開くこと）を確かめた | `gui/renderer.js ~ box\.addEventListener\('wheel'[\s\S]*box\.scrollLeft \+= e\.deltaY` |
| 626 | 2026-09-17 | **スマホ: 開いたノートのタブが見切れている**（本人「タブを右や左にスクロールまたはフリックして動かせるようにできないかい？ PC版もスマホ版もだ」） | 電話の帯は前から横にはじける（シミュレータで 3 枚開いて、左右どちらにもはじけるのを見た）。**見えていなかったのは、いま開いたノートのタブ** ── 一覧から開くと帯が作り直され、そのとき `showing` はもう決まっているので `onChange` が鳴らず、開いたばかりのタブが右端の外に居た。帯が出たとき（`onAppear`）とタブの数が変わったときにも、いまのタブを真ん中へ送る。直したあと、同じ手順（04 → 01 → 10 と開く）で 10 のタブが見えるのを確かめた | `ios/Cian/Desk.swift ~ \.onAppear \{ to\.scrollTo\(desk\.showing, anchor: \.center\) \}` |
| 627 | 2026-09-17 | **窓: 開いているノートの一覧**（本人「PC版でも一覧は出せるかい？」） | 帯の右端に「N 件 ▾」。押すと小窓に開いているノートが並ぶ（いまのものに ●、ほかは場所を添える）。**打てば絞れる** ── 何十枚の中から探すのは、名前を打つのが早い。選ぶとそのタブへ。⌘⇧P の一覧からも「開いているノートの一覧」で呼べる。名前は帯と同じ出し方（`tabName`）── 帯と一覧で名前が違うと、どれがどれか分からない。本物の窓で 20 枚開いて、一覧が 20 行・「その07」と打って 1 行になるのを確かめた | `gui/renderer.js ~ el\('stripall'\)\.onclick = \(\) => cmdTabList\(\)` |
| 628 | 2026-09-17 | **スマホ: 開いているノートの一覧**（本人「実装してほしいぞ」） | 帯の右端に「N ⌄」。押すと開いているノートが並び（見出し「開いているノート（N 件）」）、いまのものに ✓、選ぶとそのタブへ。**高さは帯に合わせる** ── 縦に伸ばす指定を置いたら帯が画面の半分まで膨らみ、本文が消えた（シミュレータで踏んで直した）。シミュレータで 2 枚開いて、一覧を出し、もう一方を選んで切り替わるのを確かめた | `ios/Cian/Desk.swift ~ 件）"\) \{\s*ForEach\(desk\.tabs\) \{ tab in\s*Button \{\s*desk\.showing = tab\.id` |
| 629 | 2026-09-18 | **うっかり更新しないように、ロックがほしい**（本人「アンバーのノートやディレクトリにロック機能ってつけてくれたっけ？ 間違って更新しなくなるように 1 手間いれるような仕組みを期待している。すべてのノートやディレクトリは対象とせず、ユーザが自分で指定したものだけ」） | 無かった（似たものは「いまのバージョンを保護」だけで、書き換えは止まらない）。憶え方は本人が決めた ── **ノートは前書きの `locked: true`**（ファイルと一緒に動くので、同期しても iPhone でも会社の PC でも錠。メモ帳で開いた人にも見える）、**フォルダは目印のファイル**（`.amberlock`・中のノートとサブフォルダぜんぶに効き、あとから増えたノートにも効く）。**止めるのは core** ── 窓も電話も `api::call` に来るので、前端で見た目を止めても片方から書けてしまう。**門は `write` に一つ**（印を足す・タグを付ける・分けるは字を返すだけで、ファイルに落ちるのは必ず `write`）。`new` / `copy` / `move` / `delete` / `image` / `bring` / `restore` は置き先も見る。外し方も本人が決めた **二つ**: 「今だけ編集する」（`unlock: true` を添えて書く。錠は残る）と「ロックをやめる」（前書きから外す／目印を消す）。**保存で錠が落ちない** ── 保存は前書きごと書き直すので、`locked: true` の行が消えた字を書いても core が書き戻す（試験で踏んだ） | `crates/amber-core/src/api.rs ~ fn keep_out\(p: &serde_json::Value, path: &std::path::Path\)` |
| 630 | 2026-09-18 | **窓のロック**（依頼 629 の窓側） | 錠のノートを開くと、面の上に帯 ──「このノートはロックされています」／「「仕事」はロックされたフォルダです」と、**今だけ編集する**・**ロックをやめる**。打てなくするのは見た目ではなく**打てる場所そのもの**（Monaco は `readOnly`、表示の面は `contenteditable` を開かない、帯の札は断る、`save()` も断る）。かける道は「ノート ▾」（このノートをロックする／やめる）とフォルダの右押し。左の列の錠のフォルダには 🔒 を添える（右押しするまで分からないのでは、かけたことを忘れる）。**「今だけ」はノートを離れたら終わり** ── タブへ戻る道は途中で返るので、`afterTab`（一覧から開く道とタブへ戻る道の両方が通る場所）で見る（本物の窓で踏んだ） | `gui/renderer.js ~ function canEdit\(\)[\s\S]{0,200}return !lockNow\.locked.{4}unlockedNow\.has\(state\.open\.path\)` |
| 631 | 2026-09-18 | **電話のロック**（依頼 629 の電話側） | 窓と同じ帯（🔒 と「今だけ編集する」）、⋯ の献立に「このノートをロックする／ロックをやめる」。「表示」の面は `armPaper` に渡す旗で**入力欄そのものを開かない**、「コード」の面は `.disabled`、`Desk.save` は錠なら書かない（今だけのぶんだけ `unlock` を添える）。フォルダの錠は**目印のフォルダごと**外す ── ノートの前書きを触っても上のフォルダの錠は外れない。シミュレータで見た: 錠のノートは打っても字が入らずファイルも変わらない → 「今だけ編集する」で打てる → 保存しても `locked: true` が残る | `ios/Cian/Desk.swift ~ if tabs\[at\]\.locked && !tabs\[at\]\.freed \{ return nil \}` |
| 632 | 2026-09-18 | **スマホからもフォルダをロックしたい**（本人「スマホ版でもロックできるようにして欲しいぞ」） | フォルダを長押し →「このフォルダをロックする」／「ロックをやめる」（窓の右押しと同じ言葉・同じ場所）。錠のフォルダは一覧で 🔒 を添える ── 長押しするまで分からないのでは、かけたことを忘れる。**どれが錠かは一覧の返事から**（`notes` が `locks` を返す）── フォルダごとに訊き直すと、一覧を組むたびに何十回も往復する。上のフォルダの錠が効いているときは、その名前で「「仕事」のロックをやめる」と言う（下で外そうとしても外れない）。シミュレータで見た: かける → 目印のファイルができ一覧に 🔒 → 中のノートに「「仕事」はロックされたフォルダです」の帯 → ⋯ から「フォルダのロックをやめる」で目印が消え、帯も消える | `ios/Cian/ContentView.swift ~ Label\("このフォルダをロックする", systemImage: "lock"\)` |
| 633 | 2026-09-18 | **取り込んだ表の見出しが `**hoge**` のまま出て、しかも白くて読めない**（本人・本物の見本から） | 一つの原因だった ── **色の `<span>` の中に太字の `**` を入れていた。** 生の HTML の中は Markdown として読まれないので、星印が字のまま出る。直したのは二か所。**一、飾りを巻く順**（`amber-onenote`）── 色はいちばん外、印はその中（`<span …>**hoge**</span>`）。**二、色の中の印を読む**（`amber-core` の `Inline::Colored`）── 前は中身を字として escape していた。これは ambər 自身の「文字に色をつける」でも同じで、**表の升の中で色を付けた字は、太字が効かず記号がそのまま出ていた**（実測）。加えて**白に近い色は運ばない** ── OneNote の表の見出しは「濃い地に白い字」で、地の色は Markdown の表に持ち込めない。白だけ運ぶと明るい紙の上で見えない字になる（明るさ 236 以上は落とす。色を落としても太字は残る） | `crates/amber-core/src/markdown.rs ~ Inline::Colored \{ text, color \} => \{[\s\S]{0,200}inline_html\(&text\)` |
| 634 | 2026-09-18 | **Ctrl＋ホイールで文字の拡大・縮小**（本人「無意識に実施したんだけど有効じゃなかったので、ちょっと違和感があった」） | どの窓でもそうなっているので、手が先に動く。ホイールに `ctrlKey`（Mac は ⌘ も）が付いていたら、字の大きさを一段。**`preventDefault` は要る** ── 入れないと Electron が窓ぜんぶを拡大し、帯も列も膨らむ。**一目盛りで一段** ── マウスは一目盛り 120 ほど送ってくるので、割り算で数えると一目盛りで二段も三段も飛ぶ（本物の窓で測って直した）。トラックパッドの摘みも同じ形で来るが刻みが小さいので、40 たまって一段。カレンダーを見ているときはカレンダーの字（鍵と同じ振り分け・依頼 623） | `gui/renderer.js ~ if \(!e\.ctrlKey && !e\.metaKey\) return;[\s\S]{0,120}e\.preventDefault\(\)` |
| 635 | 2026-09-18 | **OneNote の升（チェックボックス）が、ただの字になっていた**（本人・現場から） | OneNote では升も「ノートタグ」の一つで、段落に付く。形の名前に `CheckBox` が入っているものを升として `- [ ] ` / `- [x] ` で写す（済みかどうかは `item_status().completed()`）。**名前で見分ける** ── 形は八十ほどあり（`GreenCheckBox`・`YellowStarCheckBox` …）、枝を書き写すと部品が一つ足した日にそこだけ升にならない。公開の見本（MPL・`checks/handwriting_recognition.one`）で、済み・未済の両方が出ることを試験にした | `crates/amber-onenote/src/lib.rs ~ fn ticked\(t: &RichText\) -> Option<bool>` |
| 636 | 2026-09-18 | **会社の `.one` が読めない**（本人が現場の文言を手打ちで渡した ──「Malformed OneStore data: Unexpected node (parsing Revision): ObjectInfoDependencyOverridesFND …（revision.rs:208）」） | 借りた読み手が、**その節を決まった場所でしか飛ばしていなかった** ── 全体 ID 表（GlobalIdTable）の直後だけ。本人の `.one` はそれ以外の場所にも持っていて、一冊まるごと読めなかった。この節が持つのは参照の数え（MS-ONESTORE 2.5.20）で、中身を読むのに要る値は無い ── **どこに出てきても飛ばす。** 本家（`msiemens/onenote.rs`）には 2.0.0 の後も直しが入っていないので、**読み手の写しを amber の中に置いた**（本人が決めた・`crates/vendor/onenote_parser`、MPL-2.0、直しは `PATCHES.ja.md` の一行だけ）── 網に頼らず、こちらで直して配れる。写しの試験 65 件も一緒に回る。**現場のファイルでは、まだ確かめていない**（手元に同じ形のものが無い ── 公開の見本 10 本はどれもこの節をあの場所に持っていない） | `crates/vendor/onenote_parser/src/onestore/desktop/objects/revision.rs ~ \} else if let FileNodeData::ObjectInfoDependencyOverridesFND\(_\) = current \{` |
| 637 | 2026-09-18 | **取り込んだ画像が小さくて読めない／押したら原寸で見たい**（本人「OneNote の画像サイズが、インポートするとものすごく縮小されて…アンバーでは画像をクリックしたら、実際の画像サイズに拡大してポップアップ表示する、みたいなことはできないかなぁ」） | **ファイルは原寸のまま入っている**（取り込みは一度も縮めていない ── 実測: 1600×900 の絵は 1600×900 のまま置かれる）。小さく見えるのは**紙の幅に合わせて描いている**ため（`#read img { max-width: 100% }`）で、字の入った画面写真は縮むと読めない。**押したら原寸の窓**を出す ── 地を暗くし、はみ出したぶんは転がして見る（縮めるなら開く意味が無い）。下の帯に「何 × 何 px」と、画面に合わせる／大きさ…／消す／閉じる。Esc でも閉じる。**前は押すと小窓（大きさ…／消す）**だったが、その二つは原寸の窓の中に置いた ── 押して真っ先にしたいのは「大きく見る」ほう（依頼 420 の道は残っている） | `gui/renderer.js ~ function openLens\(fig\)` |
| 638 | 2026-09-18 | **電話でも原寸で見たい**（依頼 637 の電話側） | 画像を叩いたときの小窓の先頭に「原寸で見る」。全画面で、縮めずに指で送って見る（下の帯に大きさと、画面に合わせる／閉じる）。道は `![説明](attachments/x.png)` の丸括弧から取り、**外の URL は開かない** ── ここはファイルを見せるところで、網に出る道ではない。シミュレータで確かめた: 叩く → 小窓 →「原寸で見る」→ 1600×900 がそのまま出て、送って見られる | `ios/Cian/Writing.swift ~ Button\("原寸で見る"\) \{ peeking = picURL\(held\?\.at \?\? ""\) \}` |
| 639 | 2026-09-19 | **会社へ持ち込むのは一枚だけにして、社内では組むだけにしたい**（本人「crmaine や cian は cian-src を持ち込んで `node gui/pack.js --out dist --platform win32 --electron … --engine …` で社内ビルドしている。これってアンバーでもできない？ 持ち込む資材は amber-src.zip だけで、amber-gui.zip・amber-server-win-x64.exe.zip・amber-win-x64-office-<版>.zip を作りたい」） | **できる。** 一式（`scripts/src-zip.js` → `amber-src.zip`・10MB）に、画面（`gui/` ＋ `vendor/` ＝ npm が要らない）・印と見本（`packaging/`）・組む道具・**エンジン一枚**（＝ Rust が要らない）・許諾・叩く一行（`はじめに読んでください.txt`）を入れる。**Electron は入れない** ── 百メガあり、会社には cian のぶんが既にある。会社では一行（`scripts/build-win.js --electron …`）で**三つ**出る。`amber-gui.zip` は `packaging/gui_zip.py` と**同じ顔ぶれ・同じ中身**（突き合わせた ── フォルダの項目と `LICENSE` まで含めて 222 枚一致）だが、**python を呼ばない**（会社の Windows にあれは無い）。`pack.js` の版は `Cargo.toml` が無ければ `gui/package.json` を見る（一式に Cargo.toml は入れないため）。**CI は、その一式から会社と同じ一行で組んで三つ数える** ── 配ってから「あちらで組んだら落ちた」がいちばん高くつく。Electron ごと持ち込む重い道（`offline-kit.js`・依頼 550）はそのまま残す | `.github/workflows/release.yml ~ node scripts/build-win.js --electron \$env:GITHUB_WORKSPACE/electron-dist` |
| 640 | 2026-09-19 | **会社でも「直して・組んで・試す」までやりたい。ただし容量はシビア**（本人「全部やりたいなぁ。。ただ容量はシビアだからしっかり見定めて決めたい」） | 測ってから決めた ── Rust の自己完結版（windows-gnu）358MB／依存の実体（`cargo vendor`）99MB／ソース 12MB／画面の部品 16MB。**二つに分ける**（本人が決めた）: 軽い `amber-src.zip`（10MB・組むだけ）と、`amber-dev.zip`（**377MB**・直せる）。中身は Rust の一枚・ソース一式（試験と台帳ごと）・依存の実体・出来合いのエンジン・`.cargo/config.toml`（依存は隣の `vendor` から取る ＝ **網に出ない**）。**MSVC 版ではなく自己完結版**を配る ── 会社に Visual Studio があるか分からないため。**CI が毎回、その組み方でエンジンを組んで動かしてから包む**（`{"amber":"3.1.7","desktop":true}` と答えるところまで）── 358MB を運んでもらってから「組めない」が、いちばん高くつく。**包むのは git が知っているものだけ** ── 前は「この名前は飛ばす」の引き算で選び、CI の `vendor/` と `rustdist/` を二度包んで 746MB になった（足し算なら、知らないものは入りようがない）。回らないもの: jsdom を使う試験（npm が要る）と iPhone 側（Mac と Xcode が要る） | `.github/workflows/release.yml ~ cargo build --release -p amber-server --target x86_64-pc-windows-gnu` |
| 573 | 2026-09-14 | **登録まわりが全部白なのに、呼ぶと落ちる**（現場の往復から）| 型ライブラリ本体は `syskind 3（Win64）版 1.1` で**ファイルから読める**、取り次ぎ側（`HKCR\Interface\{IID}\TypeLib`）も**版 1.1 を指している** ── 登録は白。残るのは呼び方。Microsoft の資料は `XMLSchema` について「**版を明示せよ、空で渡すな**」と書いているのに、こちらは空で渡していた（空だと OneNote が「いまの版」を探しにいく）。`xs2013 = 2` を**明示する形を先に試す** ── `_xml_call` は元から複数の形を試して「XML が返ったほう」を採るので、変えたのは並べる形だけで、通っていた端末の動きは変わらない。走査に **schema を省くと断る偽物**を足した。`int なら何でも通す`偽物では甘く、**`[out]` の置き場所に info（整数）が入った形まで通ってしまい、schema を落としても黙った** ── 偽物が本物より甘いと検査は嘘をつく（この session で三度目）。あわせて `--probe` に**呼び方を四通り試して結果を並べる**節を足した ── **当てにいかず、機械に言わせる** | `scripts/onenote2md.py ~ XS_2013` |
| 641 | 2026-09-20 | いったん amber の開発はやり切ったと思う。今残っているゴミファイル・不要ファイルの整理をして欲しいことと、GitHub にアップされている過去の資材の削除、不要ファイルの削除をお願いしたい。さらに GitHub の Asset に今後アップする資材を君と壁打ちしたいぞ。会社でビルドすることを考えると、もう少し絞れるような気がするんだ。（壁打ちの中で）**Windows アプリも同期や Google カレンダーの機能は不要だぞ** | **資材を13点から5点に絞った（684MB → 約57MB）。** 物差しは、同じ日に cian で決めたのと同じ**「その機械で作れないものだけ置く」**（cian 依頼 208）。会社への道が「`amber-src.zip` を運んで`node scripts\build-win.js --electron <Electron>` を叩く」に決まった時点で、Windows の出来合い二枚（計 252MB）は**あの機械が自分で作れるもの**になった。残したのは `amber-src.zip`（会社の入口）・`amber-dev.zip`・`amber-gui.zip` と `amber-server-win-x64.exe.zip`（同梱する側の crmaine が**会社の端末でブラウザで落とす** ── あちらには git も gh も無く、`fetch-amber.js` は作者の手元専用）・`SHA256SUMS.txt`。**外した理由**: 出来合い二枚は会社で組める ── しかも `build-win.js` の既定は**office 版だけ**なので、**同期・iCal 購読・グループ共有は最初から入らない**（本人の「Windows に同期と Google カレンダーは不要」は、この道を選ぶだけで満たされる）。裸の `amber-server-*` 三枚は**zip が通る網は裸も通るが、逆は通らない**。mac と Linux のエンジンと `amber-cal-mac` は本人の Mac で組める。**ジョブは何も削っていない** ── 資材は今までどおり全部上がり、要る日は `gh run download` で取れる。**足し算で選ぶ** ── 「この名前は置かない」の引き算だと、明日 CI が増やした資材が黙って Release に混ざる（`amber-dev.zip` を包むときに同じ形で踏んだ）。**指紋は絞ったあとで書く** ── 置かないものの指紋が並んでいたら、落とした人は「落とし損ねた」と読む。あわせて **`amber-dev.zip` から Rust 本体（375MB）を抜いた**（396MB → 40MB・本人が選んだ）── あれは amber の版が変わっても 1バイトも変わらず、版ごとに置き直すと同じ 375MB が札の数だけ積み上がる（v3.1.2〜v3.1.7 で実際に 1.66GB 積んで、消した）。代わりに**版を焼き込んだ URL** を一式の「はじめに読んでください」へ一行書く ── 網の外なのは**会社の端末**で、落とす人の手元は外に出られる。落とすのは一度だけ。版は **CI がその版で「組めた」を見た rustc のもの**をそのまま渡すので、会社で入る Rust と確かめた Rust が同じになる。**過去の資材も消した**: Actions の成果物 215本・5.35GB と、v3.1.2〜v3.1.7 の資材 67点・1.66GB（**札とコミットは残した** ── 組み直せる）。**依頼 553（「展開するだけで動くものを Asset に置いてほしい」）は、ここで本人が取り下げた** ── 「会社では組む」と決めた以上、出来合いを置く相手が居なくなったため。553 の検査は CI の**組む段**を見ているので、外しても黙ったまま通る（＝検査が依頼より弱い）。行は消さない ── あの日に何があったかの記録なので、**ぶつかっていることをここに書く**。依頼 602（会社向けは同期を閉じる）と 639（持ち込むのは一枚だけ）は、そのまま生きている。手元は `scripts/__pycache__`・空の `.claude/worktrees/`・`target/` の iOS 4ディレクトリ 882MB。追跡しているファイルにゴミは無かった（未参照の画像も道具も無し） | `.github/workflows/release.yml ~ (?s)(?=.*keep="amber-src\.zip amber-gui\.zip amber-server-win-x64\.exe\.zip amber-dev\.zip")(?=.*node scripts/dev-kit\.js --rust-version)(?=.*name: Rust の落とし先が在るか)` |
| 642 | 2026-09-20 | そのうえで **AI 的な表現が残っていないかスクロールして**もらえない？ 判断に困る部分は僕に聞いて欲しい。（壁打ちの中で）**窓、走らせられない機械、口、扉、通りすがりに何も決めない、など大体全部変だぞ。日本語として自然な文書を期待したい**／**どこかにだけ今までの言葉を残すと君が混乱するだろう？基本は全部普通の日本語にしたい**／試験の名前も**日本語に揃える** | **まず機械が混ぜた字を二つ直した** ── `gui/index.html` の「幅を持つ帯を挟むと、**그**のぶん」と `scripts/dev-kit.js` の「npm が要るので**회**社では」。どちらも日本語のまんなかにハングルが一字入っていて、読めてしまうので目では滑る。全ファイルを走査して、ハングル・キリル・簡体字だけの字・半角カナを探した ── 残りはゼロ。LLM がよく書く日本語（「〜することができます」「以下のとおり」「シームレス」等）は一件も無かった。**本題は語彙のほう。** 数えたら、**本人の逐語の引用 94 件（2,289 字）の中に `窓` `電話` `扉` `献立` `札` `印` は一度も出てこない**（使っているのはタブ 6・ボタン 4・スマホ 2・iPhone 2・画面 1・ファイル 1・フォルダ 1）。そして台帳に**「道 とか 持ち物 とか 独創的だなｗ」**という行が残っていた ── 本人はあれを僕の造語として面白がっていたのであって、採用していたわけではなかった。**repo に 422 回あるから正しい、は自分の癖を総意と取り違えていただけ。** 用語表を本人と確定: 字→文字/テキスト・面→画面・窓→画面/デスクトップ版・道→パス・印→アイコン/マーク・電話→iPhone・札→ラベル/タグ・一枚→ファイル/ビルド・棚→フォルダ・機械→端末/環境・網→ネットワーク・組む→ビルドする・献立→メニュー・扉→入口・走り→ラン・飾り→書式・部品→パーサー・**升→セル または チェックボックス**（同じ語が二つの意味で使われていて、機械的な置換では壊れる）。英語のコメントも同時に日本語へ ── `crates/amber-ffi` は **95% が英語**で、冒頭が分かれる前の `"cian's notes"` のままだった。**`-D warnings` が snake_case で鳴る**ので、試験名の ASCII は小文字にする。**台帳の 546 件のうち 42 件が、書き換える比喩そのものを当てていた** ── 本人の判断で「検査の当てる字も一緒に書き換える」。追従させたぶんは毎回変異テストを回して、鳴るのを見てから進む。**`packaging/welcome/` は触らない** ── 利用者が読む本文で、松陰の引用も入る | `crates/amber-ffi/src/lib.rs ~ iPhone からノートの機能を呼ぶための入口。` |

## 増やすとき

依頼を受けて何かを入れたら、**その場でここに1行足す**。あとでまとめてやると、
まとめる前に自分で消す。番号は cian の台帳と続きにしてある ── 同じ日に
両方へ足したとき、どちらの話か番号で分かる。

**検査を足したら必ず変異テストする**（`scripts/mutate.sh FILE FROM TO`）。
壊した版で検査が落ちるのを見てから戻す。「直したから通った」と
「検査が何も見ていないから通った」は、画面の上では同じ顔をしている。
