# amber、三つの大玉

`REQUESTS.ja.md` が**果たしたことの台帳**なら、こちらは**これから果たすことの
台帳**。決まったことと、まだ決まっていないことと、**やらないと決めたこと**を
分けて置く ── 「まだ作っていない」と「作らないことにした」は、画面の上でも
コードの上でも同じ顔をするので、どちらなのかを書いておかないと来月の自分が
迷う。

決まったら `REQUESTS.ja.md` へ移り、検査が付く。ここに残っているものは、
**まだ機械が見張っていない**。

最後に詰めた日: 2026-09-06

---

## 三つ全部に効く前提

**amber には通信が一行も無い。** `amber-core` の依存は `anyhow` /
`serde_json` / `chrono` / `encoding_rs` / `zip` の五つだけ。HTTP も、アカウントも、
鍵の置き場も無い。これは偶然ではなく、**ノートがただのファイルでいられる理由
そのもの**。同期は「置いたフォルダのクラウドがやること」で、amber は知らない。

三つのうち **カレンダーだけが、この線を破る**（本人が承知のうえで決めた）。
クラウドと共有は、線の内側で作れる。

ほかに動かさない前提：

- **ノートはただの Markdown。** amber の都合はノートに書かず `.amber/` に置く。
- **窓と電話は同じ答えを出す。** 判断は core か、切り出した JS に一本で置く。
- **窓を正とする。** 電話が違ってよいのは、画面の大きさに理由があるときだけ。

順番は **クラウド → 共有 → カレンダー**。共有はクラウドの上に乗り、カレンダーの
いちばん大きい判断（通信を入れる）を最後に回せる。

---

## 一. クラウドの置き場所

どのサービスも机の上では**ただのフォルダ**なので、amber は同期の仕組みを一つも
知らなくていい ── 知る必要があるのは「そのフォルダがどこにあるか」だけ。

### 決まったこと（入った）

| | |
|---|---|
| 選び方 | **名前で選ぶ**。`~/Library/Mobile Documents/com~apple~CloudDocs` を覚えている人はいない |
| 並べるもの | **入っているものだけ**。入っていないものを並べると「入れれば使える」のか「壊れている」のか見分けられない |
| 見つけ方 | macOS 12 以降は外のサービスがぜんぶ `~/Library/CloudStorage` に並ぶ。`GoogleDrive-taketan9@gmail.com` → `Google Drive（taketan9@gmail.com）` |
| 置く場所 | クラウドの**直下ではなく中の「amber」**。根っこにばら撒くと、ほかの物と混ざって二度と分けられない |
| 引っ越し | **いままでのノートが付いていく**（`migrate`：絵も履歴も `.amber` も）。移せなかったら置き場所も変えない |

依頼 313・314。

### 決まったこと（入った・二回目）

**クラウドの置き土産を、黙って見せない**（`amber-core::cloud`）。

- **落ちてきていないノート** ── iCloud は中身を消して `.買い物リスト.md.icloud`
  という札を置く。名前が違うので一覧に出ず、**ノートが消えたようにしか
  見えなかった**。数えて「N 件、まだ落ちてきていません」と言い、電話は
  `startDownloadingUbiquitousItem` で落としてくるよう頼む。
- **衝突の控え** ── `買い物リスト (Taketan の競合コピー 2026-09-06).md` を
  見分けて「競合」の札を貼る。**一覧からは消さない**（消すと中身を助け出す
  道がどこにも無くなる）。
- **当てずっぽうで札を貼らない** ── `週報 2.md` は拾わない。iCloud も Finder
  もその名前で控えを作るが、本物のノートと字の上で見分けが付かない。見逃した
  控えはただのノートとして読めるが、貼られた本物は「消していいもの」に見える。

### 残っていること

- **読み書きを `NSFileCoordinator` に通す**（電話）。いまは札を見つけて落として
  もらうところまで。File Provider（Dropbox・Drive）は開いた瞬間に materialise
  するので実害は薄いが、iCloud は違う。
- **控えを見比べる画面。** いまは札を貼るだけ。見比べて片方を残す道は、
  **二（共有）のマージと同じ仕組み**になるので、そちらで一緒に作る。

### 直せないこと

**電話の Google Drive はフォルダを渡さない**ので、置き場所として選べない
（「ファイル」アプリで灰色になる）。これはあちら側の作りで、amber からは変え
られない ── 画面にそう書いてある。

---

## 二. ノートの共有

基本は自分だけ。**家族とは、指定したノートだけ**（買い物リストなど）。

### 決まったこと

**共有の棚を一つ持つ。** それ自体がクラウドの共有フォルダで、そこに置いた
ノートだけが家族に見える。共有の仕組みはクラウドに任せ、amber は「どこに
置くか」と「共有中だと分かる印」だけ持つ。左の列に「共有」が一つ増える。
移せば共有、戻せば自分だけ。

**サーバは立てない。アカウントも作らない。**

### 同時に書いたとき ── 黙って混ぜる（案一）

amber は履歴を持っているので「**二人が分かれる前の姿**」を知っている。だから
衝突コピーを作らずに混ぜられる ── これができるノートアプリは多くない。

- ふだんは**黙って混ぜて、あちらから来た行に細い帯**を付ける。上に一行だけ
  「あちらの 2 行を入れました」。押せば何が来たか見えて、履歴から丸ごと戻せる。
- **店で訊かれない**のがいちばん大事。買い物中に「どちらを残しますか」は最悪。
- **同じ行を二人が別々に直したときだけ**、その一行について訊く。足した行・
  消した行・チェックの入り切りは機械が正しく決められるが、「パン」と「食パン」の
  どちらが正しいかは決められない。

`<<<<<<<` を書き込む案は**採らない**（下の「やらないこと」）。

### 三人以上

できる。マージは「分かれる前の姿 ＋ こちら ＋ あちら」の三つを見るので、
人数が増えても**二人ずつの繰り返し**になるだけ。

**「誰が」の名前は、ノートに書かない。** 名前は設定に一度だけ書き、**履歴**
（`.amber/history/`）に記す。履歴は共有フォルダの中にあるので名前も一緒に
旅をするのに、**.md はただのマークダウンのまま**。ノートを GitHub に貼っても
誰の名前も入っていない。

### 決まったこと（入った・見える形）

**いまのルートの中の一つのフォルダを、クラウド側で共有してもらう**（依頼 317）。
二つ目のルートは持たない ── 「置き場所は一つにしたい」は本人が決めたこと。

- amber が憶えるのは `.amber/settings.json` の `share` 一行だけ。**ノートには
  書かない**（共有をやめた日に全部のノートを書き換えることになる）。
- 「このノートを共有する」＝**そのフォルダへ移す**。フォルダもタグも履歴も、
  いまあるものが全部そのまま効く。
- 題の隣に「共有」の印（**書く前に分かるように**）。左の列に「共有」の段。
  フォルダの段からは外す（二つの場所に同じものを出さない）。

**使い方は二段。** ① クラウド側でそのフォルダを家族に分ける ② amber に
どれかを教える。相手も自分の amber のルートの中に、受け取ったフォルダを
置く必要がある。

### まだ決まっていないこと

- **混ぜる（案一の三方向マージ）。** ここが本番。いまは衝突があると
  「競合」の札が出るところまで（依頼 316）。
- **共通の祖先が消えたとき。** いまの履歴は 50 世代・30 日。長く電源を切って
  いた端末は祖先を失って混ぜられない ── **共有ノートだけ保持を長くする**か、
  祖先が無いときは訊くか。
- **「誰が」の名前**をどこで一度だけ書かせるか（設定に一つ）。
- **共有をやめたノートの行き先。** いまは「いちばん上へ戻す」── もとの
  フォルダは憶えていない。
- 三人ぶんのアカウントは要る（iCloud のファミリー共有・Dropbox・Drive、どれも
  子どものアカウントに共有できる）。

---

## 三. カレンダー

読めるだけでなく**書ける**。書いた内容はカレンダーにも上がる。家族で共有する
カレンダーとして使う。加えて**仕事（Teams）**も。

### 決まったこと

**窓でも書く。Google も。** ここで初めて amber に通信が入る。

- **電話：EventKit 一つで全部。** iOS の設定にアカウントが足してあれば、Google も
  iCloud も Exchange も一律に読み書きできる。**amber は鍵を一つも預からない**。
- **窓：OAuth ＋ REST の道が一本。** そこに **Google Calendar API** と
  **Microsoft Graph** の二軒が乗る（どちらも OAuth2 ＋ REST ＋ JSON で同じ形）。
- **iCloud だけ仲間外れ。** REST が無く CalDAV しか無いので、窓で書けるように
  するのがいちばん割に合わない ── **iCloud は電話に任せる**が筋。

### Teams について

**Teams はカレンダーを持っていない。** 画面に出ているのは Exchange（Outlook）の
カレンダーそのもので、Teams 会議も「参加リンクの付いたただの予定」。だから
「Teams と繋ぐ」ではなく「**Microsoft 365 と繋ぐ**」になる。

| したいこと | できるか | 要るもの |
|---|---|---|
| 予定を読む・書く | ○ | Graph `Calendars.ReadWrite`（本人の同意） |
| **会議招集 ＋ Teams 会議付き** | **○** | `POST /me/events` に出席者と `isOnlineMeeting: true` / `onlineMeetingProvider: "teamsForBusiness"`。**会社アカウントだからできる**（個人の Outlook.com はこれだけ不可） |
| **空き時間**（誰がいつ空いているか） | ○ | `getSchedule` ── 本人の同意だけ |
| 相手が共有してくれたカレンダー | ○ | `Calendars.Read.Shared` ── 本人の同意だけ |
| グループカレンダー・全員ぶん | △ | たいてい**管理者の承認** |

招集は**本人からの招集として届く**（主催者は本人、amber の名前はどこにも出ない）。
ただし**テナントの監査ログには「amber が触った」と残る** ── 受け取る人には見えず、
管理者には見える。その二つは両立する。

### いちばんの関門は、コードではない

**会社のテナントが「利用者が第三者アプリに同意すること」を許しているか。**
切ってある会社は珍しくなく、そのときは管理者に amber を承認してもらうまで
Graph の道は一切開かない。

**そして作ってみるまで分からない** ── 繋いだときに同意画面が出るか、「管理者の
承認が必要です」が出るかで一発で判明する。だから OAuth の最初の版は、
**確かめるための版**でもある。

### まだ決まっていないこと

- **カレンダーは「面」か「ノート」か。** 予定ごとにノートを作るのは論外（何百本に
  なる）。月表示の面を足すのか、一枚のノートに書くのか。
- **語彙は半分できている。** 前書きに `remind: 2026-09-10 09:00` と
  `repeat: weekly wed 09:00` が既にある。「ノートに日付を書くと予定になる」は、
  新しい言葉をほとんど増やさずに言えるかもしれない。
- **鍵の置き場**（窓の keychain / Windows の資格情報マネージャー）。
- **broker 認証（WAM / Authenticator）。** 会社がこれを使っている見込み。
  Conditional Access が「準拠した端末から」を求めると、ふつうのブラウザ越しの
  OAuth は**弾かれる** ── そのときは端末側の broker を通すしかない。
  Windows は MSAL に native broker がある（Electron からも呼べる）。mac の
  個人機は、そもそも準拠端末にならず**永久に通らない**可能性がある。
  **つまり仕事のカレンダーは、会社の Windows（crmaine が配られている先）で
  だけ動く形になりうる。** 繋いでみるまで分からないのは変わらない ──
  弾かれたときの `AADSTS` の番号がそのまま答えになる。
- **繰り返しの予定・終日・時間帯**の扱い。ここは必ず面倒になる。
- 段階の刻み方。読むだけの版（公開 ICS リンク：OAuth も承認も要らない）を
  先に出すか。

---

## やらないと決めたこと

**書いておかないと、来月の自分が「まだ作っていないもの」として拾い直す。**

- **`<<<<<<<` を .md に書き込む**（git の衝突マーカー）── 実装はいちばん楽で
  一行も失わないが、**保存された .md に記号が残る**。直し忘れるとそのまま家族に
  同期される。ノートアプリがやることではない。
- **サーバを立てる／アカウントを作る。** 共有はクラウドに任せる。
- **本当の共同編集（CRDT）。** サーバと接続が要り、「ただの Markdown ファイル」で
  なくなる。
- 電話の**並べて表示・ノートだけを大きく・前後のノート・字数**（依頼 309）。

---

## 増やすとき

決まったら**その場でここを直す**。実装が終わったら `REQUESTS.ja.md` へ移して
検査を付け、こちらからは消す ── 両方に残すと、片方だけ古くなる。
