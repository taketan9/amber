[package]
name = "cian-ai"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
description = "Optional AI helper for cian: Azure OpenAI over a bundled Python broker-auth script"

[dependencies]
anyhow = { workspace = true }
# For `proc::quiet`: a probe that opens a console window on Windows is a
# window the user did not ask for. See `cian_core::proc`.
cian-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3.27.0"
