# 撤去した依頼 ── OneNote に直接繋いでいた頃のもの

**2026-09-16、本人が「丸ごと外す」と決めた。** `scripts/onenote2md.py` から
COM の道（OneNote に直接繋ぐ側）を外したので、そこを見ていた約束の行き先は
こちら。**行は消さない** ── あの日に何があったかの記録で、消すと記録でなくなる。

**なぜ外したか。** 本人の会社の端末では、どうやっても繋がらなかった（依頼 589
── 登録も実体も取り次ぎ側も権限も bit も白く、Office のクイック修復でも
変わらなかった）。落ちていたのは OneNote の側で、こちらからは直せない。
**代わりに、OneNote が書き出した `.onepkg` の中の `.one` を直に読む道**
（依頼 594 以降）が通り、そちらは COM 以外の問題も四つ一度に解いた ──
網に出られなくても動く・Office が要らない・ノートブックを開いておく必要が
無い・mac でも動く。**pywin32 も 32 bit の Python も、もう要らない。**

**ここは検査しない。** `scripts/requests.py` が読むのは `REQUESTS.ja.md` の
表だけで、この表は入っていない（`REQUESTS.moved.ja.md` と同じ扱い）。
理由は嘘をつかないため ── 撤去したものに当てになる検査は書けないし、
書けば**通っているのは検査であって約束ではない**という一番悪い形になる。

**もし OneNote の側が直って、また繋ぎたくなったら** ── `git log` に全部ある。
外したのは 2026-09-16 のコミット一つで、1,700 行ほど（本体 1,706 行・走査
1,588 行）。戻すなら、そこから拾ってくればいい。

| # | 日付 | 頼まれたこと | なぜ | どこにあったか |
|---|------|------------|------|--------------|
| 520 | 2026-09-12 | OneNote から出した Markdown を、セクションなどのフォルダ構成ごと ambər に | `scripts/onenote2md.py`（本人が別の席で作ったものを、ambər の保存ディレクトリとして読める形に改修）: 同じページは同じファイル・変わっていなければ書かない・LF・SharePoint で通る名前・絵は隣の `attachments/`・`--prune`・`--flatten-groups`。読ませ方は ⚙「保存ディレクトリの追加・変更・削除」で出力先を足す（同期しない） | scripts/onenote2md.py ~ def place_for（撤去） |
| 564 | 2026-09-14 | OneNote を**1時間ごとに、更新があったノートだけ**、フォルダ構成ごと Markdown へ（本人） | 写す道具は依頼 520 で既にあった ── 足りないのは**定時で回す**ぶんで、そこは道具の作りが変わる。**誰も見ていない**時間に走るから、`--prune` の取り違えが一度でも起きると気づくのは何日も後になる。三つ直した。**一、`--prune` が出力先ぜんたいを歩いていた** ── `--notebook` で絞った回は、絞られなかったノートブックが今回一枚も出てこないので**全部「消えたページ」に見えて全滅する**（ノートブックを閉じていた回も同じ）。歩いた範囲の中だけを見る。**二、鍵のかかったセクションと、取得に失敗したページを残す** ── **見えなかった場所は、消えた場所ではない。**COM が一度しゃっくりしただけで前回の写しが消えるのは、写しとして嘘。三、**一度に一本だけ**（OS の鎖 ── PID を書いたファイルだと、電源が切れたとき誰も外せない鎖が残る）。最初の一回は全ページ書くので一時間で終わらないことがある。あわせて `--sync`（読む前に OneNote へ同期を頼む ── 開いているだけのノートブックは電話で直しても古いままで、こちらは「変わっていない」と見て飛ばす）と `--log`（画面に誰もいないので、落ちたことが残らなければ落ちていないのと見分けがつかない）。**mac では一行も動かない道具だった**ので、COM を偽物に差し替える走査を書いた（`scripts/onenote-test.py`・54 項目）── そして最初に鳴らなかったのは`__pycache__` で、壊して戻したはずの原本ではなく**直前に壊した版の `.pyc`** が走っていた。手順は `docs/onenote.ja.md` | scripts/onenote2md.py ~ def prune\(scope: list,（撤去） |
| 565 | 2026-09-14 | **写したいものだけ選びたい**（本人・「全部のノートじゃなくて、抽出したいノートだけ指定するってことはできるかな？」） | ノートブック単位は依頼 520 の `--notebook` で既にできた。足したのは**セクション単位**の `--only` / `--skip` ── 当たるのは **OneNote 側の道**（`ノートブック/グループ/セクション`）で、`--flatten-groups` が畳んだ出力の名前ではない（畳んだ名前に当てると、**同じ `--only` が旗の有無で違うものを拾う**）。`--skip` が `--only` より強い ── 逆にすると「外したはずのものが混ざる」ほうの事故になる。選ぶ前に見るための `--list`（道とページ数、外れるものに `×`）。**肝は絞りと `--prune` の噛み合わせ** ── 絞って外したセクションは今回一枚も出てこないので、素直に組むと**前回の写しがまるごと消え、次の回も絞りは同じなので二度と戻らない**。守りは道ではなく**ページ ID**（階層 XML に並んでいる）── 道で守った最初の版は、あとから `--flatten-groups` を付けて出力の形が変わった回に、外したセクションを消していた（**走査が見つけた**。手では踏んでいない）。鍵のかかったセクションだけは中が見えず ID が出ないので、そこは道で守る | scripts/onenote2md.py ~ if keep.covers\(r, oid\)（撤去） |
| 566 | 2026-09-14 | **会社の Windows で `AttributeError: OneNote.Application.GetHierarchy`**（本人・現場から）。その前は `サーバーの実行に失敗しました` | 二つとも「繋がらない」に見えて、中身が違う。**前者は繋がっている** ── OneNote の型ライブラリの登録には中身のない `1.0` の枝が混ざっていて、pywin32 がそれを掴むとメソッドの名前が引けない（pywin32 issue 1488）。レジストリを消せば直るが**会社の端末はグループポリシーが書き戻す**ので、こちらで避ける ── 版（1.1）を名指しして `EnsureModule`、駄目なら `EnsureDispatch`、それも駄目なら素の `Dispatch` の**三段**。**繋がったことと話が通じることは別**なので、`GetHierarchy` が見えるところまで確かめてから返す。束ね方が変わると `[out]` の渡し方も変わる ── 早い束ねは戻り値、遅い束ねは置き場所を渡し、**その位置はメソッドごとに違う**（`GetHierarchy` は 3 番目、`GetPageContent` は **2 番目**）。両方試して**XML が返ったほうを採る** ── 例外の有無だけで見分けると、間違った位置に渡しても例外にならず「XML でない何か」が返る形を取りこぼす（走査に性悪な偽物を置いた）。後者（`0x80080005`）は権限のずれで、昇格した窓からは昇格していない OneNote に繋げない ── 直し方は docs に書いた（タスクの「最上位の特権」も外す）。**`connect_onenote` は走査が届いていなかった**ので、`win32com` ごと偽って梯子を確かめるようにした | scripts/onenote2md.py ~ gencache.EnsureModule\(ONENOTE_TYPELIB, 0, major, minor\)（撤去） |
| 567 | 2026-09-14 | **`PermissionError` で makepy が作り置きを書けない**（本人・現場から）。`C:\Program Files\Python39\...\win32com\gen_py\...temp` | pywin32 は makepy の写しを `site-packages\win32com\gen_py` に置く。Python が `Program Files` に入っていると管理者でないと書けない ── そして**管理者の窓で一度作る手は使えない**。その窓は今度は OneNote に繋げない（依頼 566 の `0x80080005`）。**二つの制約が別々の窓を要求していて、交わらない。**だから書ける場所へ逃がす（`%TEMP%\amber-gen_py-3.x`）。**書けるかは実際に一つ置いて消して確かめる** ── `os.access` は Windows のフォルダについて読み取り専用属性しか見ず、ACL で拒まれる場所でも True を返す。逃がすのは **`win32com.client` を読む前**でなければ意味がない（gencache は読み込みの時点で道を見る）。その並びは mac では実行の上で観測できないので、**並び順そのものを走査が見る**。`os.access` に戻す壊し方は置いていない ── POSIX では正しく答えるので mac では鳴らず、黙る壊し方を並べると「鳴らないのが普通」になる | scripts/onenote2md.py ~ def _gen_py_somewhere_writable\(（撤去） |
| 568 | 2026-09-14 | **逃がした先の makepy が読めない**（本人・現場から）── `No module named 'win32com.gen_py.0EA692EE-…x0x1x1'` | 依頼 567 で作り置き先を逃がしたが、**動かしたのは書く先だけだった。** pywin32 は `import win32com` した時点で `win32com.gen_py` モジュールを作り、`__path__` を**そのときの** `__gen_path__` で焼き付ける（`gen_py.__path__ = [__gen_path__]`）。だから makepy は新しい場所へ書き、`__import__("win32com.gen_py.…")` は古い場所を探す ── **書けたのに読めない。**両方を揃えて移す。あわせて `importlib.invalidate_caches()`（pywin32 は呼ばないので、作ったばかりのフォルダが Python の作り置きの中では空のまま見える）と、`ImportError` のとき一度だけのやり直し。**走査の偽物が本物より甘かった**のが見逃した理由 ── `gen_py.__path__` を持たない偽 `win32com` では「書く先だけ動かす」誤りが素通りする。偽物を本物と同じ形にして、`got is good` だけでなく**どの段で繋がったか**まで見るようにした（一段目が転んでも三段目が同じものを返すので、梯子を落ちたことが見えなかった） | scripts/onenote2md.py ~ gen_py.__path__ = \[at\]（撤去） |
| 569 | 2026-09-14 | **makepy は通ったのに、まだ「GetHierarchy が見えない」**（本人・現場から）| `EnsureModule` が通っても、`Dispatch` が早い束ねを返すとは限らない ── ProgID → CLSID → 生成された型、の対応が引けないと遅い束ねのまま返る。なら**生成された型のほうから包みにいく。**型の名前は決め打たない（`IApplication` とは限らず、版で変わる）── **`GetHierarchy` を持っている型を探す。探しているものの名前で探すのが、いちばん壊れにくい。**かぶせたあとに**実物を見る** ── 型の上に名前があるだけの皮があり、`dir()` は名前順なので本命より先に当たりうる。あわせて **`--probe`** を足した ── **二度当てにいって二度外した**ので、推し量るのをやめて事実を見る口を作る。壊れた端末で使う道具なので、**一行ずつ守って途中が転んでも最後まで出す**（最初の版は `import win32com` が無い端末で丸ごと落ちた ── mac で走らせて分かった）。走査も二つ直した: 落ちたときに例外のまま抜けると**走査ごと止まり「検査が無い」と同じ顔になる**ので NG にする。壊し方に `mod = pass` のような**構文を壊すもの**を書くと、同じ理由で黙って見える | scripts/onenote2md.py ~ def _wrap_with_generated\(（撤去） |
| 570 | 2026-09-14 | **皮はかぶさったのに、呼ぶと『ライブラリは登録されていません』**（本人・現場から。`TYPE_E_LIBNOTREGISTERED`）| 端末には型ライブラリが **`1.0` と `1.1` の両方**登録されていた（`--probe` が出した）。並んでいるだけでは**どちらが使えるか分からず**、片方は実体を指していない ── その皮で包むと `GetHierarchy` は見えるのに呼ぶと落ちる。1.1 → 1.0 の順に試す。**そして合格の合図を変えた ── 見えることではなく、実際に呼んで答えが返ること。** この session で三度、`hasattr` を合格にして先へ進み、三度とも現場で別の顔をして転んだ。同じ間違いを設計から外す。訊いた答えは捨てずに`connect_onenote()` が `(相手, 最初の階層)` で返す ── ページ数の多い棚を二度歩かない。`--probe` も深くした（版ごとの実体の道とファイルの有無、両方の版で包んで**呼ぶ**まで）。走査の型も一つ増えた: **同じものが返るせいで見分けられない**検査は、何が返ったかではなく**どの道を通ったか**を見る（版の取り違えも、やり直しの有無も、最後は同じ相手が返るので `got is good` では素通りした） | scripts/onenote2md.py ~ first = get_hierarchy\(app\)（撤去） |
| 571 | 2026-09-14 | **`--probe` の写真から原因を突き止める**（本人・手打ちが無理なので画面の写真）| 写真は読めた。決め手は二行 ── `1.1 / lcid 0 / **Win32** = …ONENOTE.EXE\3` と `版: … **64 bit** (AMD64)`。**型ライブラリの登録は bit ごとに枝が分かれていて、64 bit のプロセスは `Win64` を見る。** 片方しか無いと、皮は正しくかぶさるのに呼んだ瞬間に落ちる ── いちばん読みにくい形。**これはコードでは直らない**（環境の食い違い）ので、`--probe` が**見たことから結論まで言う**ようにした（`_arch_verdict`）。あわせて三つ直した: `ProgID → CLSID` は `pythoncom.CLSIDFromProgID` が無いので `pywintypes.IID` で引く。型ライブラリの道は末尾に**資源の番号**（`…EXE\3`）が付くので、落としてから在処を見る ── **番号ごと渡すと、在るものまで「無い」と言い、見立てをまるごと変える嘘になる**。Office の bit も出す（`ClickToRun\Configuration\Platform`）。直し方は docs に二つ書いた ── Python の bit を合わせるか、**自分のユーザーにだけ**足りない枝を足すか（`HKCU` なら管理者は要らない）。**レジストリを触る話なので、こちらからはやらない** | scripts/onenote2md.py ~ def _arch_verdict\(（撤去） |
| 572 | 2026-09-14 | **繋げないときに、こちらが知っていることを黙らない**（現場の往復から）| 何度も「繋がりません」だけを返して、本人に写真を撮らせた。**レジストリを見れば「この bit では無理」と分かる**ことがあるのに、それを言っていなかった ── 知っていることを黙るのは、報せとして不誠実。繋げない回の報せに `_arch_verdict` を載せる。**分からないときは黙る**（Windows でない・枝が読めないときは空を返し、何も言わない）── 分からないことを分かったように言うほうが、黙るより悪い。あわせて docs に「`Win64` を足しても直らない場合」を書いた ── 型ライブラリが Win32 のものしか無いと、枝を足しても 64 bit からは読めない（`TYPE_E_CANTLOADLIBRARY`）。out-of-process の COM なので、**32 bit の Python から 64 bit の OneNote は問題なく触れる** | scripts/onenote2md.py ~ def _registered_arches\(（撤去） |
| 578 | 2026-09-15 | **OneNote から出てくる Markdown そのものを直す**（レビュー依頼から） | 配管（COM・差分・`--prune`・絞り）には検査が並んでいたのに、**道具の目的そのものである変換には主張が七つしか無く、変異テストは一つも無かった**。見つけて直したもの: **表の見出しの行**（`hasHeaderRow` を見ておらず、無いのに 1 行目を見出しにしていた ── **そのデータが一行、表から消える**。画面の上では「一行目が濃いだけ」に見えるので気づけない）。**升の中の改行**（`<br>` で繋いでいたが、ambər は本文の札をぜんぶ字にするので `<br>` という字が画面に出ていた ── 空白で繋ぐ）。**コードの枠**（`inline_md` を通していたので `if x > 0:` が `if **x** > 0:` になり、**写したコードに無い字が混ざっていた**）。**リンクの URL**（`href` を空白で切っており、`file:///C:/My Documents/…` が途中で切れて別の場所を指していた）。**インクの言い方**（本文中と頁の直下で二つあり、docs は片方しか知らなかった）。**`created` の時差**（UTC の頭 10 字を切っていたので、**東京では朝 9 時前に作ったページが前日に並ぶ**）。走査を 131 → 156 項目、変異テストを 16 通り足して全部鳴るところまで見た。**行頭の記号の逃がしは、やっていない** ── ambər がバックスラッシュを読まないので逃がすと `\\#` の `\\` が字で出る（iPhone は読む。窓と食い違っている） | scripts/onenote2md.py ~ def plain_md\(（撤去） |
| 579 | 2026-09-15 | **繋がるかどうかを、手で打ち直せる長さで言う**（本人「僕は君に手打ちで情報を渡すからファイルに大量におとしてもあまり嬉しくない」） | `--probe` は 29 行出す。繋がらない端末の姿を残らず並べる道具としては要るが、**現場から手で打ち直して渡す人に出す注文ではなかった** ── ファイルに落として送る道（依頼 577）も、この人の渡し方には合っていない。`--check` は数行で言う: Python と Office の bit、型ライブラリの枝、COM サーバーの実体、そして繋がらないなら**次の一手を一つだけ**。**足すレジストリの道は、こちらで埋めて出す** ── 繋がらない端末の前に立っている人に「docs を見て `--probe` の値をそこから写して」は通らない。戻す一行も並べる。繋がったなら一行（開いているノートブックの数と名前）。あわせて**ストア版（OneNote for Windows 10）が同居している**ことを見るようにした ── 会社の端末は両方入っていた。**COM を持つのはデスクトップ版だけ**なので、ふだんストア版で書いていると、繋がっても一冊も見えない（「繋がったのに何も出ない」の正体）。`--check` も `--probe` も見出しの一行を出さない ── 打ち直すぶんを一行でも減らす | scripts/onenote2md.py ~ def _next_move\(（撤去） |
| 580 | 2026-09-15 | **`--check` が「登録は白」で行き止まる**（現場から。枝は `win32 win64` の両方あった） | 依頼 579 の `--check` には穴が二つあった。**一、枝を版ごとに見ていなかった。** `_registered_arches` は版をまたいで束ねるので、**「1.0 に win64、1.1 に win32」でも「両方ある」に見える** ── こちらは 1.1 から読む（依頼 570）ので、そこに枝が無ければ落ちるのに、登録は白に見える。**いちばん読みにくい形を、こちらで作っていた。**版ごとに出し、**読みにいく版**で判じる（ほかの版に有るなら「助けにならない」と言う）。**二、呼んだときの答えを捨てていた。** 登録が全部白なら残る手がかりはそれだけ（依頼 573 で踏んだ形）なのに、`connect_onenote` の四つの答えを飲み込んで「登録は白。残るのは権限か…」と一般論を並べていた ── 現場で行き止まる。`CannotConnect`（`SystemExit` のまま）が四つの答えを持ち帰り、`--check` がそれを出す。番号から分かるものはその場で名指しする（`0x80080005` は権限のずれ、`TYPE_E_CANTLOADLIBRARY` は実体が今の bit から読めない、など）── **知らない番号には決めつけない。** あわせて変異テストを九つ足し、**構文を壊す壊し方を一つ書いてしまって黙った**（依頼 569 と同じ罠）ので替えた | scripts/onenote2md.py ~ def _typelib_tree\(（撤去） |
| 581 | 2026-09-15 | **登録は白、実体が読めない**（現場から。`TYPE_E_CANTLOADLIBRARY`） | 枝は `1.0` `1.1` とも `win32 win64` が揃い、COM サーバーも在った。それでも名指しの二つは `-2147312566`（タイプ ライブラリ／DLL の読み込みエラー）、`gencache` は「makepy を手で走らせろ」、**素の `Dispatch` は繋がるのに `GetHierarchy` が見えない** ── 四つの答えが揃って一つのことを言っている: **登録の話は終わっていて、読めないのは実体のほう。** docs に書いてあった「`Win64` を足しても直らないことがある」がそのまま出た形で、答えは**32 bit の Python**（out-of-process なので OneNote は 64 bit のままでよい）。`--check` はこの番号のときだけ**枝の道と在処**を並べる ── ここで `--probe` へ送ると 29 行を手で打ち直させることになる（依頼 579）。**同じ道を二つの枝が指していたら、片方は写しだと言う** ── 32 bit の実体しか無いところに `Win64` を足すと、登録は白くなるが読めるようにはならない。ほかの番号のときは道を出さない | scripts/onenote2md.py ~ def _lib_paths_note\(（撤去） |
| 582 | 2026-09-15 | **網に出られない端末で整える／OneNote は 365 とストア版を両方使う**（本人「オフライン環境なのでインターネットに接続して pip install するようなことはできない」「どちらも開いて操作していた。今後もそうする」） | 依頼 581 の答え（32 bit の Python）を、**網の無い端末で実行できる形**に書き直した。`pip install pywin32` はそこでは何も起きない ── 要るのは**どのファイルを持ち込むか**で、それはこの Python の版と bit で決まる。`--check` が**その wheel の名前を言う**（`pywin32-*-cp39-cp39-win32.whl`）── 取り違えやすいのは bit なので、**`win32` が 32 bit・`win_amd64` が 64 bit** と添える。入れ方も `--no-index --no-deps` で。docs に「32 bit の Python を入れる（オフラインの端末で）」── 持ち込むのは installer と wheel の二つだけ、`DLL load failed` のときの後始末も。**そして `--check` が pywin32 の言い分を飲み込んでいた**（答えが無いまま止まると何も出ない）── 走査が見つけた。**ストア版の言い方も変えた。** 両方入れて両方使うのは**そのままでよい** ── 困るのはどちらで開いているかだけなので、「使うな」ではなく**「365 側でも開いておく」**と言う | scripts/onenote2md.py ~ def wheel_hint\(（撤去） |
| 583 | 2026-09-15 | **32 bit の Python は、この道具のためだけに**（本人「永続的に 32 ビットにはしたくない。一時的に使うような手順にしてもらえない？」） | **半分そうなっていて、半分なっていなかった。** `py` は版を名指ししないかぎり 64 bit を選ぶので、入れても既定は替わらない ── だが docs にそう書いていなかったうえ、**installer の「Add python.exe to PATH」に印が付いたままだと `python` が 32 bit を指す**。いちばん替えたくないところなので、手順に「印を付けない」「自分だけに入れる」を書いた（管理者も要らない）。**消し方も書いた** ── 設定→アプリから削除、64 bit には何も起きない。あわせて `--check` が、**入れたあとの一手をこちらで言う**ようにした ── 入れた人は 64 bit の癖でもう一度同じことを叩き、同じ答えが返る。ランチャー（`py -0p`）に訊いて 32 bit が居れば `py -3.9-32 … --check` の一行を出す。**居なければ言わない。**診断の道具は壊れた機械の上で使うので、**ランチャーが居なくても落ちない** ── その壊し方は走査ごと落として黙ったので、本物で叩く検査を先に置いた（依頼 569 と同じ罠、三度目）| scripts/onenote2md.py ~ def _thirty_two_bit_here\(（撤去） |
| 584 | 2026-09-15 | **枝の先にファイルが無いのに、bit の話を続けていた**（現場から。32 bit で叩いたら `→ **無い**` が並んだ） | `--check` に穴が三つ。**一、版を一つしか出していなかった** ── `_typelib_path` は最初に見つかった版を返すので、**落ちたのは 1.1 なのに 1.0 の道を見せていた**（依頼 580 で束ねて見て踏んだのと、同じ形）。版 × bit をぜんぶ出す。**二、「一つも無い」を見ていなかった。** 登録が指す先にファイルが一つも無いなら、枝が何本あっても読める道は一本も無い ── **bit の話でも枝の話でもない。**そこへ「32 bit の Python を使う」を被せるのは、要らない道へ人を送ること。在るか無いかを先に見て、無いなら Click-to-Run の入れ場所から実体を探す ── **よそに在れば「登録が古い、Office を修復」、どこにも無ければ「デスクトップ版が入っていない」。三、写しの話も同じで、在処が無いときは要らない。**走査では、作り物が薄くて一つ黙った（枝が版ごとに一本ずつだと、守りを外しても写しの話が元から出ない）── **偽物が本物より甘いと検査は嘘をつく**、四度目 | scripts/onenote2md.py ~ def _lib_files_gone\(（撤去） |
| 585 | 2026-09-15 | **在るファイルを「無い」と言った**（現場から。`dir` では在るのに `--check` は `→ **無い**`） | **レジストリは走っている処理の bit で見え方が変わる**（WOW64）。`--check` は自分の見え方しか読んでいなかったので、32 bit の Python で叩いた回に32 bit 側の枝だけを見て「無い」と言った ── **在るファイルを無いと言うのは、見立てをまるごと変えてしまう嘘**（依頼 571 で資源の番号を落とさず踏んだのと同じ形、二度目）。`KEY_WOW64_64KEY` / `KEY_WOW64_32KEY` で**両方の見え方を読み**、**違うときだけ**どちらの話かを添える（同じなら一つにまとめる ── 同じものを二度見せると、人が読む行が倍になる）。これで「32 bit では読める枝が無い」と「そもそも実体が無い」を**画面の上で見分けられる。**走査では、偽物が `_typelib_values` を丸ごと差し替えていて**その中身を一度も試していなかった**ので、`_read_typelib` の側を偽る検査を足した | scripts/onenote2md.py ~ def _read_typelib\(（撤去） |
| 586 | 2026-09-15 | **四つの答えのうち、少ないほうを採っていた／見ていないことから結論を出していた**（現場から。32 bit では三つが `0x80080005`・64 bit では pywin32 が無い） | **一、答えを数えていなかった。** `_from_answer` は表に並べた順で拾っていたので、**四つのうち三つが「サーバーの実行に失敗しました」と言っているのに、一つだけの「登録されていません」を採った** ── 数えてから決める（同じ数なら表の順）。**二、`COM サーバー` を pywin32 で引いていた。** 入っていない Python では引けず、それを **`**無い**`** と言い、さらに「入れ直すか修復する」と**見てもいないことから結論を出していた**。ProgID → CLSID はレジストリだけで引ける。在るかは三通りにして（在る／無い／**読めない**）、**読めないときは何も結論しない** ── 分からないことを分かったように言わない。**三、Office の bit を 32bit の見え方で読んでいた**ので、32 bit の Python では「読めない」になっていた（`KEY_WOW64_64KEY` で読む）。この二つは mac では壊れないので、変異テストは置かず註を残した（`os.access` と同じ） | scripts/onenote2md.py ~ n = joined.count\(needle\)（撤去） |
| 587 | 2026-09-15 | **登録を通さず、ファイルから型ライブラリを読む**（現場から。32 bit でも 64 bit でも `TYPE_E_CANTLOADLIBRARY`） | 枝は `1.0` `1.1` とも `win32 win64` が揃い、`1.1` の指す先のファイルも在り、素の `Dispatch` は繋がる（管理者の窓では OneNote が前に出てくる）。それでも名指しの二つが読み込みエラー ── **32 bit に替えても同じ答えだったので、bit の話ではない。**`LoadRegTypeLib` が転ぶのと、**その資源に型ライブラリが入っていない**のは別のことなので、梯子に一段足した ── **`ファイルから型ライブラリを読む`**（`pythoncom.LoadTypeLib(<道>\\3)` → `makepy.GenerateFromTypeLibSpec`）。`gencache` が言う「makepy を手で走らせろ」を、こちらで走らせるのがこれ。**在る道だけ試し、駄目なら道ごとの言い分を持ち帰る** ── そこで初めて「資源が読めない」と分かる。あわせて、番号ごとの言い分から bit の話を外に出した ── **32 bit で叩いている人に「32 bit を使え」と言い続けるのは、一度通った道へまた送ること。**走査の偽物に `pythoncom` と `makepy` を足した（無いと新しい段が `ImportError` で素通りし、何を試したのか分からないまま次へ落ちる ── 偽物が甘いと検査は嘘をつく、五度目）| scripts/onenote2md.py ~ def by_file\(（撤去） |
| 588 | 2026-09-15 | **「繋がるのに呼べない」を、そう言う**（現場から。写真で読んだ画面に `呼ぶと落ちる` が三つ並んでいた） | `--check` は五つの答えを並べていたが、**その中の `呼ぶと落ちる` を読んでいなかった。**これは「束ねるところまでは通っていて、落ちているのは呼んだ瞬間」という意味で、**繋がらないのとは別の話** ── 束ね方をいくら変えても直らない。依頼 587 で足した`ファイルから型ライブラリを読む` の段まで `呼ぶと落ちる` だったので、**型ライブラリはファイルから読めている**（資源の話でもない）。残るのは取り次ぎ側（marshaling）で、`HKCR\\Interface\\{IID}\\TypeLib` が壊れた版を指していれば、本体が読めていても呼んだ瞬間に落ちる ── 依頼 573 で `--probe` に足した節を、`--check` に出すようにした。**指している版が、名指しでも読めなかったほうなら**そう言い、生きている版を見させる一行を道ごと出す（戻す一行も）。**確かなことが分かったら、当て推量は並べない** ── 「資源に型ライブラリが入っていない」「片方は写し」を後ろに足すと、確かなほうが埋もれる | scripts/onenote2md.py ~ def _interface_note\(（撤去） |
| 589 | 2026-09-15 | **できない修復を勧めていた／取り次ぎ側は生きていた**（現場から。取り次ぎ側は `1.1` を指していた） | 依頼 588 の見立ては外れた ── `HKCR\\Interface\\{IID}\\TypeLib` は **`1.1`**、つまり**名指しで読めたほう**を指していた。だが外れ方が答えになる: 取り次ぎ側が白いのに呼ぶと落ちるなら、**名前を引くのも呼びを受けるのも OneNote 自身**なので、**落ちているのは向こう側** ── 自分の型ライブラリを読めていない。こちらの束ね方で直せるところは、もう無い。そう言うようにした。**そして、ずっと「オンライン修復」を勧めていた** ── あの端末は網に出られない（依頼 582）のに。**そこではできないことを、何度も勧めていた。**`クイック修復`（手元のファイルから焼き直す・**網が要らない**）を先に言い、オンライン修復は「それでも駄目なら、ただし網が要る」と添える。docs も直した | scripts/onenote2md.py ~ def _repair_lines\(（撤去） |
| 590 | 2026-09-15 | **作り置きを捨てる一行（`--forget`）と、残る手を安い順に**（現場から。クイック修復でも変わらなかった） | 「`%LOCALAPPDATA%\\Temp\\gen_py` を消す」と docs に書いてあっても、**そこには無い** ── こちらは書ける場所へ逃がしている（依頼 567）ので、人に二つのフォルダを行き来させることになる。`--forget` が両方消す（**捨てても困らない**・次に繋いだときに作り直される）。あわせて、取り次ぎ側まで白いときの言い方を「残っている手を、**安いほうから**」に ── 1. 作り置きを捨てる（ただ）2. Office の登録を焼き直す（クイック修復・網が要らない）3. `--probe` を写真で（**そこにだけ出るものが三つある**: 版ごとの実体、生きている相手が名乗る型ライブラリ、呼び方を四通り試した結果）| scripts/onenote2md.py ~ def forget\(（撤去） |
