# 「表示」の面を、極める

amb**ə**r が Inkdrop に無いものを一つだけ持っているとすれば、これ ──
**組んだ姿のまま、直に書ける**。Inkdrop は編集面と preview 面が別で、
書くのは記号の側だけ（`docs.inkdrop.app/reference/write-notes`）。

だからここは、**違和感なく・バグなく・美しく**でなければいけない。
そしていまのところ、**いちばん壊れやすいところでもある**。

この文書は、その仕事の設計図。**途中で人が替わっても、ここから始められる
ように**書いてある。

---

## 一. なぜ危ないのか

面と字のあいだを行き来する道が二本ある。

```
    字 ──────── armPaper ────────▶ 面      （組む）
   .md                          contenteditable
    字 ◀─────── paperToMd ──────── 面      （戻す）
```

**戻す側（`paperToMd`）が心臓。** ここが一文字でも狂うと、**書いたものが
消える**。しかも消えたことに気づくのは、たいてい何回か保存したあと。

2026-09-08 の一日だけで二件出ている。

- **本文が別のノートに書き込まれた**（依頼 360）── 面が「どのノートの字か」
  を持っていなかった。二本のノートが、前書きだけ自分のまま中身が入れ替わった
- **見ただけのノートが保存されていた**（依頼 361）── 末尾の空行の数が
  一つずれるだけで「変わった」と判定していた

どちらも「面 → 字」のところ。**偶然ではなく、ここは構造的に危ない。**

### 誰が使っているか

`paperToMd` は窓だけのものではない。**電話も同じ一本を読んでいる**
（`ios/Cian/Paper.swift` が `renderer.js` の切り出しをそのまま持っていく）。
だから、

- ここを直せば**両方**直る
- ここを壊せば**両方**壊れる
- 窓でだけ試して通しても、**電話で同じことが起きるとは限らない**（面の
  組み方が `WKWebView` なので、`contenteditable` の癖が違う）

切り出しの範囲は `richBlock` から `landAt` まで。終わりの目印は
`/// この窓の「表示」の面を、上の切り出しに繋ぐ薄い包み。`

---

## 二. やること

### 1. 総当たりの試験を当てる（いちばん大事）

いまの `scripts/paper-test.js`（268 行）は**要点だけ**を見ている ── 升、
注記と表、前書き、引用から降りる、図の元の字。**一つずつの形**は見て
いるが、**組み合わせ**を見ていない。

作るのは「**字 → 面 → 字 が、元に戻るか**」を機械で回すもの。

```
    もとの .md ──▶ armPaper ──▶ 面 ──▶ paperToMd ──▶ 戻った .md
                                              ↑
                              ここが もとの .md と同じか
```

**回す形の一覧**（一つずつと、二つ以上の入れ子）:

| | 見るところ |
|---|---|
| 見出し `#`〜`######` | 段の深さ。`#` の後ろの空白 |
| 段落 | 空行の数。行の中の改行（`  ` 二つ／`\`） |
| 箇条書き `-` `*` `+` | **記号がそのまま戻るか**（`*` が `-` にならないか）。入れ子の段 |
| 番号リスト `1.` `1)` | 番号を振り直さないか（`1. 1. 1.` と書いた人の字） |
| 升 `- [ ]` `- [x]` | 大文字 `[X]`。升のある行の入れ子 |
| 引用 `>` | 二重 `>>`。引用の中の箇条書き・升・コード |
| 注記 `> [!NOTE]` 五種 | 種類の札（`.alert-h`）を字に戻さないこと |
| 表 | 揃え方の行（`:---` `---:` `:---:`）。空のセル。行末の `\|` の有無 |
| コード枠 ` ``` ` | 中の字を一切触らないこと。言語札。`~~~` |
| 図 mermaid | `data-md` をそのまま返すこと（読み解かない） |
| 水平線 `---` `***` `___` | 前書きの `---` と取り違えないこと |
| 強調 `**` `*` `~~` `` ` `` | 入れ子。日本語の直後（`あ**い**う`） |
| リンク・画像 | 題に `]` が入る場合。空の題 |
| 前書き | 有り／無し。`---` だけの行が本文にある場合 |
| 末尾 | 空行 0・1・2 個。改行で終わらないファイル |

**組み合わせは、掛け算で当てる。** 「引用の中の升」「箇条書きの中の表」
「注記の中のコード枠」── 一つずつは通っても、入れ子で落ちる。

#### 「同じ」の測り方

**そのままの一致では厳しすぎる。** 面は末尾の空行を自分で決めるし
（`tailStop`）、`- ` と `* ` を同じものとして組む。だから二段構えにする。

1. **強い一致** ── 字がそのまま戻る。ここを目指す
2. **弱い一致** ── 末尾の空行の数だけ違う。**許す**（`sameNote` と同じ考え）
3. **それ以外の違いは、ぜんぶ落第。** 「たぶん同じ意味」で通さない ──
   通した日に、人のノートで一文字消える

そして **`null` を返したものは落第にしない**。`paperToMd` は「戻せない
なら書かない」が正しい（空を返すとかたまりが黙って消える）。ただし
**どの形で `null` になったかは全部数えて出す** ── 「戻せない形」が増えて
いないかは、人が見て決めること。

#### 二周させる

```
    字 ──▶ 面 ──▶ 字' ──▶ 面 ──▶ 字''
```

**`字'` と `字''` は必ず同じでなければならない**（一周目で形が変わるのは
許すことがあっても、二周目で変わり続けるなら、それは保存のたびにノートが
書き換わり続けるということ ── 同期先で毎回差分になる）。

### 2. 打つほうも回す

戻すだけでなく、**打った跡が字に落ちるか**。

- 升の行で `Enter` → 次の升（依頼 351 で一度落ちている）
- 箇条書きの終わりで `Enter` 二回 → 外へ出る
- 引用・注記の中から、空の行で降りる
- 表の外側に caret を置けるか（`tailStop` の理由）
- 図の**上**に caret が落ちるか（`goToLine`）
- `Ctrl+Z` が、変わった行に戻るか（`landBack`）

これらは `paper-test.js` に既にあるものと、無いものが混ざっている。
**無いものを足す。**

### 3. 見つかった食い違いを直す

**直す前に、必ず試験を先に書く。** 「直した」と「直ったように見える」は
画面の上で同じ顔をしている（`scripts/requests.py` の頭に書いてあるのと
同じ理由）。

### 4. 電話でも同じことを確かめる

窓で通った試験が、電話でも通ることを見る。`WKWebView` の
`contenteditable` は Chromium と癖が違う ── 特に、

- 日本語の入力中（`composition`）の割り込み
- `execCommand` の効き方（`bold` / `insertText`）
- 空の節に caret を置いたときの `<br>` の入り方

---

## 三. やり方の決めごと

- **試験は `scripts/` に置く。** 依存を足さない（`node scripts/…` で走る）。
  いまの `paper-test.js` と同じく、`renderer.js` から**切り出して**動かす
  ── 写すと、写した側だけが直る
- **`jsdom` を足さない。** いまの `paper-test.js` は自前の小さな DOM で
  動いている。総当たりに耐えないと分かったら、そのとき相談する（依存を
  一つ足すのは、配る側にも効く決めごと）
- **見つけた食い違いは、一件ずつ台帳（`REQUESTS.ja.md`）に足す。**
  「なぜそう直したか」が残っていないと、次の設計で戻される
- **検査は必ず変異テストする**（`scripts/mutate.sh`）。壊した版で鳴るのを
  見てから戻す
- **本人のノート（`~/Documents/cian`）では試さない。** 一度やって、実物の
  ノートに空の升を二行足してしまっている

---

## 四. 見込み

一日仕事。内訳は、

| | |
|---|---|
| 総当たりの土台を作る | 半日 |
| 表を埋めて回す・落ちたところを数える | 数時間 |
| 直す（何件出るか次第） | 残り |
| 電話で確かめる | 数時間 |

**落ちる件数は読めない。** 読めないから測る、が今日の午前と同じ話で、
測ってから直す順番を決める。

---

## 五. いまの持ちもの

| | |
|---|---|
| `gui/renderer.js` | `richBlock`（1524）〜 `landAt`（1558）が切り出し。`armPaper`（1547）・`paperToMd`（1586）・`blockToMd`（1609） |
| `scripts/paper-test.js` | いまの試験（268 行）。自前の小さな DOM |
| `scripts/switch-test.js` | ノートを替えたときの試験（`data-of` の札） |
| `ios/Cian/Paper.swift` | 電話側の包み。同じ切り出しを読む |
| `scripts/mutate.sh` | 検査を壊して、鳴るのを見る |
| 台帳 360・361 | 今日出た二件。**まず、この二つを試験で踏み直せるか**から始めるとよい |
