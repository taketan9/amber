# 「表示」の面を、極める

amb**ə**r が Inkdrop に無いものを一つだけ持っているとすれば、これ ──
**組んだ姿のまま、直に書ける**。Inkdrop は編集面と preview 面が別で、
書くのは記号の側だけ（`docs.inkdrop.app/reference/write-notes`）。

だからここは、**違和感なく・バグなく・美しく**でなければいけない。
そしていまのところ、**いちばん壊れやすいところでもある**。

この文書は、その仕事の設計図。**途中で人が替わっても、ここから始められる
ように**書いてある。

---

## 一. なぜ危ないのか

面と字のあいだを行き来する道が二本ある。

```
    字 ──────── armPaper ────────▶ 面      （組む）
   .md                          contenteditable
    字 ◀─────── paperToMd ──────── 面      （戻す）
```

**戻す側（`paperToMd`）が心臓。** ここが一文字でも狂うと、**書いたものが
消える**。しかも消えたことに気づくのは、たいてい何回か保存したあと。

2026-09-08 の一日だけで二件出ている。

- **本文が別のノートに書き込まれた**（依頼 360）── 面が「どのノートの字か」
  を持っていなかった。二本のノートが、前書きだけ自分のまま中身が入れ替わった
- **見ただけのノートが保存されていた**（依頼 361）── 末尾の空行の数が
  一つずれるだけで「変わった」と判定していた

どちらも「面 → 字」のところ。**偶然ではなく、ここは構造的に危ない。**

### 誰が使っているか

`paperToMd` は窓だけのものではない。**電話も同じ一本を読んでいる**
（`ios/Cian/Paper.swift` が `renderer.js` の切り出しをそのまま持っていく）。
だから、

- ここを直せば**両方**直る
- ここを壊せば**両方**壊れる
- 窓でだけ試して通しても、**電話で同じことが起きるとは限らない**（面の
  組み方が `WKWebView` なので、`contenteditable` の癖が違う）

切り出しの範囲は `richBlock` から `landAt` まで。終わりの目印は
`/// この窓の「表示」の面を、上の切り出しに繋ぐ薄い包み。`

---

## 二. やること

### 1. 総当たりの試験を当てる（いちばん大事）

いまの `scripts/paper-test.js`（268 行）は**要点だけ**を見ている ── 升、
注記と表、前書き、引用から降りる、図の元の字。**一つずつの形**は見て
いるが、**組み合わせ**を見ていない。

作るのは「**字 → 面 → 字 が、元に戻るか**」を機械で回すもの。

```
    もとの .md ──▶ armPaper ──▶ 面 ──▶ paperToMd ──▶ 戻った .md
                                              ↑
                              ここが もとの .md と同じか
```

**回す形の一覧**（一つずつと、二つ以上の入れ子）:

| | 見るところ |
|---|---|
| 見出し `#`〜`######` | 段の深さ。`#` の後ろの空白 |
| 段落 | 空行の数。行の中の改行（`  ` 二つ／`\`） |
| 箇条書き `-` `*` `+` | **記号がそのまま戻るか**（`*` が `-` にならないか）。入れ子の段 |
| 番号リスト `1.` `1)` | 番号を振り直さないか（`1. 1. 1.` と書いた人の字） |
| 升 `- [ ]` `- [x]` | 大文字 `[X]`。升のある行の入れ子 |
| 引用 `>` | 二重 `>>`。引用の中の箇条書き・升・コード |
| 注記 `> [!NOTE]` 五種 | 種類の札（`.alert-h`）を字に戻さないこと |
| 表 | 揃え方の行（`:---` `---:` `:---:`）。空のセル。行末の `\|` の有無 |
| コード枠 ` ``` ` | 中の字を一切触らないこと。言語札。`~~~` |
| 図 mermaid | `data-md` をそのまま返すこと（読み解かない） |
| 水平線 `---` `***` `___` | 前書きの `---` と取り違えないこと |
| 強調 `**` `*` `~~` `` ` `` | 入れ子。日本語の直後（`あ**い**う`） |
| リンク・画像 | 題に `]` が入る場合。空の題 |
| 前書き | 有り／無し。`---` だけの行が本文にある場合 |
| 末尾 | 空行 0・1・2 個。改行で終わらないファイル |

**組み合わせは、掛け算で当てる。** 「引用の中の升」「箇条書きの中の表」
「注記の中のコード枠」── 一つずつは通っても、入れ子で落ちる。

#### 「同じ」の測り方

**そのままの一致では厳しすぎる。** 面は末尾の空行を自分で決めるし
（`tailStop`）、`- ` と `* ` を同じものとして組む。だから二段構えにする。

1. **強い一致** ── 字がそのまま戻る。ここを目指す
2. **弱い一致** ── 末尾の空行の数だけ違う。**許す**（`sameNote` と同じ考え）
3. **それ以外の違いは、ぜんぶ落第。** 「たぶん同じ意味」で通さない ──
   通した日に、人のノートで一文字消える

そして **`null` を返したものは落第にしない**。`paperToMd` は「戻せない
なら書かない」が正しい（空を返すとかたまりが黙って消える）。ただし
**どの形で `null` になったかは全部数えて出す** ── 「戻せない形」が増えて
いないかは、人が見て決めること。

#### 二周させる

```
    字 ──▶ 面 ──▶ 字' ──▶ 面 ──▶ 字''
```

**`字'` と `字''` は必ず同じでなければならない**（一周目で形が変わるのは
許すことがあっても、二周目で変わり続けるなら、それは保存のたびにノートが
書き換わり続けるということ ── 同期先で毎回差分になる）。

### 2. 打つほうも回す

戻すだけでなく、**打った跡が字に落ちるか**。

- 升の行で `Enter` → 次の升（依頼 351 で一度落ちている）
- 箇条書きの終わりで `Enter` 二回 → 外へ出る
- 引用・注記の中から、空の行で降りる
- 表の外側に caret を置けるか（`tailStop` の理由）
- 図の**上**に caret が落ちるか（`goToLine`）
- `Ctrl+Z` が、変わった行に戻るか（`landBack`）

これらは `paper-test.js` に既にあるものと、無いものが混ざっている。
**無いものを足す。**

### 3. 見つかった食い違いを直す

**直す前に、必ず試験を先に書く。** 「直した」と「直ったように見える」は
画面の上で同じ顔をしている（`scripts/requests.py` の頭に書いてあるのと
同じ理由）。

### 4. 電話でも同じことを確かめる

窓で通った試験が、電話でも通ることを見る。`WKWebView` の
`contenteditable` は Chromium と癖が違う ── 特に、

- 日本語の入力中（`composition`）の割り込み
- `execCommand` の効き方（`bold` / `insertText`）
- 空の節に caret を置いたときの `<br>` の入り方

---

## 三. やり方の決めごと

- **試験は `scripts/` に置く。** 依存を足さない（`node scripts/…` で走る）。
  いまの `paper-test.js` と同じく、`renderer.js` から**切り出して**動かす
  ── 写すと、写した側だけが直る
- **`jsdom` を足さない。** いまの `paper-test.js` は自前の小さな DOM で
  動いている。総当たりに耐えないと分かったら、そのとき相談する（依存を
  一つ足すのは、配る側にも効く決めごと）
- **見つけた食い違いは、一件ずつ台帳（`REQUESTS.ja.md`）に足す。**
  「なぜそう直したか」が残っていないと、次の設計で戻される
- **検査は必ず変異テストする**（`scripts/mutate.sh`）。壊した版で鳴るのを
  見てから戻す
- **本人のノート（`~/Documents/cian`）では試さない。** 一度やって、実物の
  ノートに空の升を二行足してしまっている

---

## 四. 見込み

一日仕事。内訳は、

| | |
|---|---|
| 総当たりの土台を作る | 半日 |
| 表を埋めて回す・落ちたところを数える | 数時間 |
| 直す（何件出るか次第） | 残り |
| 電話で確かめる | 数時間 |

**落ちる件数は読めない。** 読めないから測る、が今日の午前と同じ話で、
測ってから直す順番を決める。

---

## 五. いまの持ちもの

| | |
|---|---|
| `gui/renderer.js` | `richBlock`（1524）〜 `landAt`（1558）が切り出し。`armPaper`（1547）・`paperToMd`（1586）・`blockToMd`（1609） |
| `scripts/paper-test.js` | いまの試験（268 行）。自前の小さな DOM |
| `scripts/switch-test.js` | ノートを替えたときの試験（`data-of` の札） |
| `ios/Cian/Paper.swift` | 電話側の包み。同じ切り出しを読む |
| `scripts/mutate.sh` | 検査を壊して、鳴るのを見る |
| 台帳 360・361 | 今日出た二件。**まず、この二つを試験で踏み直せるか**から始めるとよい |

---

## 六. 振る舞いの決めごと

「表示」の面で、**鍵を押したら・選んで飾ったら・変換中に・戻せなかったら**、
何が起きるべきかを決めた。実装する側はここだけ読めばよい。

**読んだものと、確かめていないもの。** ここに「いま」とあるのは
`gui/renderer.js`（`richBlock` 〜 `landAt`・`syncRead`・鍵の受け口）と
`ios/Cian/Paper.swift`・`scripts/paper-test.js`・台帳 200〜370 を読んで
分かったことで、**画面で押して確かめてはいない**。「既定」とあるのは
`contenteditable` の素の振る舞いのことで、Chromium と `WKWebView` で
違いうる ── 四章の電話の確かめは、ここでも要る。

**誰のための面か。** Markdown を知らない人（iPhone で書くような人）が、
初期設定のまま快適に打てる面。**迷ったときの決め手はこれ**（2026-09-08、
Taketan）。Word や Notion で手が動く人が驚かないことを先に取り、vim の手は
「コード」の面が受ける。

**決めごとの芯は三つ。** 下の表はぜんぶ、この三つから引いてある。

1. **面の上の一打は、字の上の一つの記号に対応する。** 行頭で Backspace を
   押せば記号が一つ外れ、Tab を押せば一段深くなる（段落なら一字下がる）。「何も起きない・
   焦点が飛ぶ」は無い（それは既定の事故で、押し心地ではない）
2. **選びは意思、一打は事故。** 選んで消したものは、触れないかたまりでも
   消える（選んだのだから）。選ばずに押した一打で、図や枠は消えない
3. **触れないものには、caret を置かない・入れない・そこで組み直さない。**
   図・枠・絵は「元の字」をそのまま返すのが仕事で、その中で何かが
   起きた瞬間に「元の字」が嘘になる

### 甲. どこまで直に書けるか

| かたまり | 直に | なぜ |
|---|---|---|
| 段落・見出し・箇条書き・番号・升・引用・注記・表・強調・リンク | **触れる**（いまのまま） | 字に戻せる形をしている。戻せるものを触らせない理由は無い（依頼 301） |
| コード枠 ` ``` `・`~~~`・字下げの枠 | **触れない**（いまのまま） | 中の字は**一文字も変えてはいけない**。`contenteditable` は空白と改行を自分の都合で組み替え（`<br>`・`&nbsp;`・末尾の空白）、電話はさらに**自動修正と引用符の置き換え**（`"` → `“`）を掛ける ── 「打てるが、一度でも打つと壊れる」枠は、打てない枠より悪い。直すなら「コード」の面（窓は押すと並べて表示のその行へ・いまのまま） |
| 図（mermaid） | 触れない（いまのまま） | 見えているのは SVG で、字ではない。押すと工房（窓・電話とも、いまのまま） |
| 絵（`<figure>`） | 触れない（いまのまま） | 同上。消す道は吹き出し（下の表）。題（`alt`）を直す道は ── **迷った**: 直したい日は来るが、いまは「コード」で |
| **知らない札のかたまり**（生の HTML・`<details>`・`<dl>` など、`blockToMd` が知らないもの） | **触れないかたまりに数える** ── `data-md` をそのまま返す。**いまは違う**: `blockToMd` の `default` が中の字だけ取り、札を黙って落とす | 「黙って落ちる」は五章で決めた通り、いちばん悪い形。貼り付けは字だけ入れるので（`paste` の口）、知らない札は**ノートの字そのものから来ている** ── 人が書いたものを、保存のたびに削っている。**迷った**: 行の中の知らない札（`<sup>`・`<kbd>`・`<mark>`）は、そのかたまりごと触れなくするか、いまのまま字だけ取るか。かたまりごと、を勧める（生の HTML を書く人は「コード」を使える人） |

**触れないかたまりを押したとき、何が起きるべきか** ── いまは窓が
「押すと書く面のその行へ」の `title` を出し、押すと並べて表示に替わる。
電話は長押しで工房、**一叩きでは何も起きない**。

| どこで | 何が起きる | なぜ |
|---|---|---|
| 窓でも電話でも、触れないかたまりを押す（一叩き） | **小さな吹き出し**が出る。中身はかたまりごと ── 図: 「直す（工房）」「消す」／コード枠: 「コードで直す」「消す」／絵: 「消す」。**いまは違う**: 窓は枠を押すと並べて表示のその行へ、図と絵は工房へ。電話は一叩きでは何も起きず、長押しで工房 | 触れないものは全部**「押すと吹き出し」の一つの型**に揃える ── 記号を知らない人が覚えることを一つにする。リンクも同じ型（己）。「消す」を置くのは、図や枠を消すのに「コード」へ行かせないため（Taketan: 表示のまま消せること・2026-09-08）。消えたものは一つ戻すで戻る |
| 窓の右押し・電話の長押し | 同じ献立を、もう一つの入口として残す | 知っている人の手を奪わない。無くしても残しても害は無い |
| 指の形・枠の光り（`#read pre:hover`） | いまのまま | 「押せる」の合図 |
| 枠の**中**に caret を置こうとする（押す・矢印） | 置けない ── 矢印は跨ぐ（乙の矢印）、押せば上の表の通り | 芯の 3 |

### 乙. 鍵

| 鍵 | どこで | 何が起きる | なぜ |
|---|---|---|---|
| Enter | 空でない升の中 | 次の升を作る。真ん中なら後ろの字を次の升へ（いまのまま `checkEnter`） | 買い物リストは続けて打つもの（依頼 285） |
| Enter | 空の升 | 升を外して素の行に降りる。下の行は残す（いまのまま） | 点・番号の既定と同じ押し心地（依頼 285・300） |
| Enter | 点・番号 | 既定に任せる（次も同じ、空なら降りる） | 既定が正しい |
| Enter | 見出しの**末尾** | 次は**段落** | 見出しは一行のもの。見出しが二つ続くことはまず無く、Word も Docs も Notion もそうしている。既定もそう（`defaultParagraphSeparator` が `p`） |
| Enter | 見出しの**途中** | 前は見出しのまま、後ろは**段落**になる（Taketan が決めた・2026-09-08「大きいままじゃない」） | 既定（と Word）は見出しを二つに割る。「見出しの途中で Enter」は、たいてい「見出しを打ち終えて本文に降りたい」のに caret が末尾に無かっただけ ── 見出しが二つになるより、後ろが本文になるほうが直しが少ない |
| Enter | 空でない見出し・段落の**先頭** | 上に空の段落が一つ入る（既定） | 既定が正しい |
| Enter | 引用・注記の字のある行 | 引用が続く（既定・いまのまま） | 引用の中で改行するのは、引用の続きを書くとき |
| Enter | 引用・注記の空の行 | そこで箱を割って外へ降りる。下の行は残す（いまのまま `quitEnter`） | 出る道が要る（依頼 300） |
| Enter | 注記の種類の札 | 何も起きない（いまのまま） | 札は中身ではない |
| Enter | 表のセル | **下のセルへ**（同じ列）。最後の行なら行を一つ足す。**いまは違う**: 既定に任せている | セルの中に改行は書けない（Markdown の表は一行一行）── `inlineToMd` は `<br>` を `\n` にし、`<p>` は札を落として字だけ繋ぐので、**セルの中の Enter は表を壊すか、改行を黙って落とすかのどちらか**（既定がどちらを入れるかは確かめていない）。表計算はどれも Enter で下へ行く |
| Shift+Enter | 段落・見出し・箇条書きの中 | **Enter と同じ**（新しい段落・項目）。**いまは違う**: 既定が `<br>` を入れる | `<br>` は `\n` として保存され、core は段落の行を空白で繋ぐ（`markdown.rs` の `para.join(" ")`）── 次に組み直した瞬間、**打った改行が空白に化ける**。打ったのに姿が変わるのは、打てないより悪い。**迷った**: core に行の中の改行（行末 `\`）を足す日が来れば、そのとき Shift+Enter をそれに当てる。**それは core の仕事で、この面の仕事ではない** |
| Shift+Enter | 表のセル | 何も起きない | 同上。セルに改行は無い |
| Backspace | 箇条書き・番号・升の**行頭** | **記号が外れて段落になる**。一覧はそこで割れ、下の項目は残る。**いまは既定に任せている** ── Chromium の既定は、最初の項目なら記号を外し、途中の項目なら前の項目と**繋ぐ**はず（確かめていない） | 芯の 1。途中の項目が前と繋がると、**升が一つ黙って消える**（二つの「やること」が一行になる）。行頭の Backspace は「この行の記号を外す」で、どの位置の項目でも同じでなければ、押すたびに結果を見る癖がつく |
| Backspace | 入れ子の項目の行頭 | **一段浅くなる**（Shift+Tab と同じ）。いちばん浅い段で、上の行の通り記号が外れる | Tab と対にする。Notion も同じ |
| Backspace | 段落になった行の行頭（記号がもう無い） | 前の行と繋がる（既定） | 記号が無ければ、Backspace は字を消す鍵に戻る |
| Backspace | 見出しの行頭 | **見出しが段落になる**（`#` が外れる）。**いまは**: 既定が前の段落と繋ぐ | 芯の 1。見出しを前の段落の字に繋ぎたい人はまず居ない |
| Backspace | 引用・注記の**最初の行**の行頭 | その行が引用から出る（`>` が外れる）。箱に何も残らなければ箱ごと消える（注記なら札も） | 芯の 1。`quitEnter` と同じ割り方 |
| Backspace | 引用・注記の**途中の行**の行頭 | **前の行と繋がる**（既定のまま）。引用から出るのは最初の行の行頭だけ（Taketan が決めた・2026-09-08） | 引用の中の行は自分の記号を持っていない（`>` は箱の記号で、行の記号ではない）── 二行目に外すものは見えていないので、繋がるのが自然。途中で割れた引用はたいてい望んだ形ではない。まとめて外す道は別に置く（丙） |
| Backspace | 表のセルの中で、セルが空 | **何も起きない**。**いまは既定に任せている**（Chromium はセルや行を消しうる ── 確かめていない） | 行や列を消すのは表の道具（`tableDo`）の仕事。一打でセルの数が変わると、揃わない表が保存される（`blockToMd` は行ごとにセルを数えて書くので、列の数が行ごとに違う表が GFM で崩れる） |
| Backspace / Delete | 触れないかたまりの**すぐ下の行頭／すぐ上の行末**（選びなし） | **何も起きない** ── 図・枠・絵は消えない | 芯の 2。caret の無いものが一打で消えるのは事故。消す道は**押したときの吹き出しの「消す」**（甲）── 選んで消す道もある |
| Backspace | ノートの先頭の行頭 | 何も起きない | 消すものが無い |
| Tab / Shift+Tab | 表のセル | 次／前のセルへ。最後のセルで Tab なら行を足す。最初のセルで Shift+Tab は何もしない（いまのまま） | 依頼 284 |
| Tab | 箇条書き・番号・升の中（行のどこでも） | **一段深くなる**。升は升のまま。**上に項目が無ければ深くならない**（何も起きない）。**いまは**: 焦点が面から飛ぶ | 芯の 1。Taketan の手も「一覧の中で Tab なら子リスト」。Inkdrop も同じ（v6.1.0 から Tab が親の記号に合わせて深さを決め、Shift+Tab で戻り、番号は出入りのたびに振り直す）。Markdown で親の無い入れ子は書けない（`  - あ` だけの一覧は段落になる）ので、最初の項目は深くしない |
| Shift+Tab | 入れ子の項目の中 | 一段浅くなる。いちばん浅い段なら何もしない | Tab と対 |
| Tab | 段落（引用・注記の中の段落も） | **一字下がる** ── 段落の頭に全角空白 `　`（U+3000）が一つ入る。押すたびに一つ増える。**いまは**: 焦点が面から飛ぶ | Taketan: 字下げは欲しい（2026-09-08）。半角の空白と Tab の字は、四つ揃うと Markdown がコード枠にするので使えない。`　` は**ただの字**で、何段でもコード枠にならず、GitHub でも Obsidian でも iPhone でも同じ幅の空きとして出る ── ファイルに記号を足していない。日本語の作文の字下げそのもの。表の空セルを `　` で埋めている今の作りと同じ発想。Inkdrop の一覧の外の Tab も行の字下げ（`editor:indent`）で向きは同じ ── あちらは記号の面なので半角で書けるだけ |
| Shift+Tab／行頭の Backspace | 字下げた段落 | `　` が一つ外れる | Taketan: Shift+Tab でも Backspace でも外せること。Backspace は芯の 1 がそのまま効く（`　` は字なので、一打で一つ消える） |
| Enter | 字下げた段落の末尾 | 次の段落も同じ字下げ | Word と同じ。作文は段落ごとに下げる |
| Enter | 字下げだけで字の無い段落 | 字下げが外れて素の行になる | 升・引用と同じ「空でもう一度押したら降りる」 |
| Tab / Shift+Tab | 見出しの中 | **何も起きない。焦点も動かさない** | 見出しに字下げは無い。焦点が飛ぶのは事故 |
| 矢印 ↑↓ | 触れないかたまりの上下 | **跨ぐ** ── 図の上の行で ↓ を押すと図の下の行へ、下の行で ↑ を押すと上の行へ。二つ続いていれば二つとも跨ぐ | 芯の 3。図の中に caret が入ると「押せるものが打てるものに見える」（`goToLine` の理由と同じ） |
| 矢印 ↑ | ノートの**先頭**が触れないかたまりで、その下の行 | **先頭にも降りられる一行を置く**（`tailStop` と対の `headStop`）。空のままなら字に戻すとき落ちるので、ファイルは増えも減りもしない | 依頼 203 の裏返し ── 図で始まるノートの上に一行足す道が無い。**迷った**: 常に置くか、caret が近づいたときだけ出すか。常に置くほうが試験しやすい（増えたり消えたりするものは、往復の試験で数が合わなくなる） |
| 矢印 ↓ | 表の最後の行 | 表の下の行へ（既定）。表でノートが終わっていれば `tailStop` の段落へ | 依頼 203。**`tailStop` は最善ではないが、最も安い** ── 「caret が近づいたときだけ現れる行」は見た目が同じで、試験が一つ増えるだけ。末尾の空行一つの代金は、ファイルには出ない（`sameNote`） |
| 矢印 ←→ | 触れないかたまりの境 | 跨ぐ（↑↓ と同じ） | 同上 |
| Esc | 面で打っている途中 | 書き戻して焦点を外す（いまのまま） | 手を離す鍵 |
| ⌘Z / ⌘⇧Z | 面の中 | ノートの姿を一つ戻す・やり直す。**変わった行に caret を置く**（いまのまま `landBack`） | 依頼 254・345 |

**字下げは、いまの作りだと二か所で消える**（読んで分かったこと）。
`blockToMd` は段落を `.trim()` して書き、JavaScript の `trim()` は `　` も
削る。core の `to_html` も段落の行を `p.trim()` してから繋ぎ（`markdown.rs`
の段落の輪）、Rust の `trim()` も同じく削る。**どちらも、削るのを半角の
空白と改行だけにする。** `note::blocks` の書き出し（一覧の二行目）は
削ってよい ── あれは字ではなく要約。

折り返した二行目は下がらない（段落ごと右へ寄せる Word の字下げでは
ない）。それが欲しくなった日は、core が「空白四つは字下げ」と読む話に
なり、ノートが amber の外（GitHub・Obsidian）でコード枠になる ── それは
「ただの Markdown」を捨てる判断なので、この仕事では取らない。

**電話には Tab が無い**（外付けの鍵盤を除く）。下の帯に**「一段深く」
「一段浅く」の釦を足す**（Taketan が決めた・2026-09-08）。同じ釦が、
段落の中では字下げ／字下げ外しになる ── Tab と同じ対応（これは訊いて
いない。Tab を写しただけ）。

### 丙. 選んで飾ったとき

| どこで | 何が起きる | なぜ |
|---|---|---|
| 複数の段落を選んで太字 | **段落ごとに** `**…**` が閉じる。跨がない | Markdown の `**` は空行を跨げない。`inlineToMd` は札ごとに閉じるので、いまもそうなっている ── 決めごととして書いておく |
| 見出しを含む範囲を選んで太字 | **見出しは飛ばす**（段落だけ太字になる） | 見出しは既に太い。見た目が変わらないのに `## **見出し**` と記号だけ増えるのは、次の組み直しで「何も変わっていない」ように見えて、同期先では差分になる |
| 選んで太字、全部がもう太字 | 外れる（既定の反転） | 既定が正しい |
| 選ばずに太字 | ここから太字（いまのまま） | 依頼 202 |
| 飾りの終わりで打つ | 飾りの外に出る（いまのまま `outOfDress`） | 依頼 299 |
| 見出しの行を箇条書き・番号・升にする | **見出しは消える**（段落になってから点が付く）。逆に、箇条書きの行を見出しにすると**点が外れる** | 一行は見出しか項目か、どちらか一つ。`- ## 見出し` は書けはするが、読む人にも書く人にも意味が無い。**いまは既定に任せていて**、`<li><h2>` になるか `<h2><ul><li>` になるかは確かめていない ── どちらにせよ `blockToMd` は一覧の中の見出しも見出しの中の一覧も知らず（`inlineToMd` は `UL` を飛ばし、`default` は札を落とす）、**見た目は見出しのまま、保存すると何かが黙って落ちる**。落とすなら**押した瞬間に見えて落ちる**こと。Taketan: 「自分で選んだ操作だから驚かない」（2026-09-08） |
| 引用・注記の中の行を見出し・箇条書き・升にする | できる（いまのまま） | `> ## 見出し` も `> - [ ]` も Markdown が持っている形で、`blockLines` が戻せる |
| 引用の中で「引用」の釦を押す／範囲を選んで押す | **外れる**（付けると外すが同じ釦）。範囲を選んでいれば選んだ行ぜんぶ ── 途中の行だけ選べば、そこで引用が割れる。**見出し・箇条書き・升も同じ形**（中で押せば外れる・選べばまとめて）。**いまは違う**: `formatBlock` は付けるだけ | Taketan: 引用をまとめて外す道が要る（2026-09-08）。行頭の Backspace で出るのは一行ずつで、長い引用では手が疲れる。「同じ釦で付けて外す」は Word の太字と同じ手触り |
| 表のセルをまたいで選んで太字 | **セルごとに**閉じる | セルを跨ぐ `**` は書けない |
| 表のセルをまたいで選んで Backspace / Delete / 打つ | **選んだセルの中身を空にする。セルは残す。打った字は最初のセルに入る**。**いまは既定に任せている**（Chromium はセルや行を消したり繋いだりしうる ── 確かめていない） | 表計算の Delete と同じ。セルの数が変わる操作は表の道具だけ（乙の Backspace と同じ理由） |
| 表の外から中へ跨ぐ選びで消す | **消さない**（何も起きない） | 表の途中までを消す、に正しい答えが無い |
| 触れないかたまりを含む範囲を選んで消す（⌘A → Backspace を含む） | **一緒に消える** | 芯の 2。選んだのだから。一つ戻すで戻せる |
| 触れないかたまりを含む範囲を選んで太字 | 触れないかたまりの中は変わらない | `contenteditable=false` の中は既定でも触られない。触られていたとしても `data-md` を返すので字には出ない |

### 丁. 日本語入力（IME）

いま守っているのは、Enter と Tab の受け口（`isComposing` と `keyCode 229`
の両方を見る・依頼 210）と、飾りの外へ出るのを組み始めにする
（`compositionstart`）ところまで。**書き戻しと組み直しには、決めごとが無い。**

| いつ | 何が起きる | なぜ |
|---|---|---|
| 変換中（`compositionstart` 〜 `compositionend`） | **書き戻さない・保存しない。** `input` は変換の一字ごとに来るので `readChanged` の 700ms（電話は 500ms）が変換中に切れうる ── 切れても待ち、確定してから数え直す | 未確定の字を保存すると、同期先に「あいう」が届き、次に「愛」が届く ── 履歴が確定前の姿を積み、混ぜる側（依頼 336）には「向こうが二度書いた」に見える。そして `syncRead` は箱そのものに `contentEditable` を掛け直す（`armPaper`）── `WKWebView` で変換中の箱の属性を触ると変換が落ちる**おそれ**がある（確かめていない。四章の電話の確かめに入れる） |
| 変換の確定直後 | **組み直さない。** いつもの間合い（700ms）で書き戻すだけ | 確定した瞬間に組み直すと caret が飛ぶ。「打っている間は組み直さない」の内側 |
| 変換中に、組み直しの用事が来た（向こうからの更新の混ぜ込み・テーマ替え・升の押し下げ） | **待つ** ── `compositionend` のあと、次の書き戻しが通ってから組み直す。混ぜ込みは、書き戻した字を土台に混ぜる | 組み直すと未確定の字が消える。混ぜ込みは待てる（ファイルは既に向こうの字になっていて、こちらが書き戻す時に混ざる） |
| 変換中の Enter | 変換の確定であって、改行でも升の続きでもない（いまのまま） | 依頼 210 |
| 変換中の Tab / Backspace / 矢印 | **IME に渡す**（乙の表は効かない） | 変換候補の選び・文節の区切りが、それらの鍵で動く |
| 変換の組み始め | 飾りの外へ出る（いまのまま） | 組んでいる最中に選び目を動かすと変換が壊れる（依頼 299） |
| 焦点が外れた（`blur`）とき変換中だった | OS が先に確定するので、確定後の字を書き戻す（いまのまま `blur` → `syncRead`） | 電話で鍵盤を閉じたときがこれ |

**迷った**: 窓の 700ms と電話の 500ms は、揃える理由も分ける理由も
書き残っていない。「変換中は走らない」の規則が同じなら、数は違っていて
よい。揃えたければ 700 に。

### 戊. 「戻せない」の扱い

`paperToMd` が `null` を返すのは、触れないかたまりが `data-md` を持って
いないときだけ。`armPaper` は組み直すたびに全部へ持たせ、貼り付けは字
だけ入れるので、**まともな道では起きない** ── 起きたら不具合。だから
「よくあること」の顔で出してはいけない。

| いつ | 何が起きる | なぜ |
|---|---|---|
| `null` が返った | **書かない**（いまのまま） | 空を書くとかたまりが黙って消える。五章の通り |
| 人に何と言うか | 一度だけ言う（700ms ごとに言わない）: **「保存できません。いま打った字は画面の上にしかありません ── 選んで写してから、「コード」の面で直してください」**。帯の札は「保存できません」のまま、次に保存が通るまで消さない | いまの言葉（「図かコード枠の元の字が取れません」）は原因を言っていて、**打った字がどうなるかを言っていない**。人が知りたいのはそちら。「コードで直せ」だけでは、面を替えた瞬間に打った字が消える（下の行） |
| `null` のまま面を替える・ノートを替える・窓を閉じる | もう一度書き戻しを試し、駄目なら**訊く**: 「替えると、打った字が消えます。写しましたか」 | 面を替えると `applyView` がエディタの字から組み直すので、面の上にしか無い字は失われる。黙って失わない |
| `null` が出たとき、裏で | `console.warn` に、どのかたまりか（札の名前・`class`・`data-line`）を出す。**台帳に一件足す** | 「戻せない形が増えた」に気づく道は、数えることしか無い。二章の総当たりは `null` の数を出す（もう決まっている）── 走っている amber でも同じものを数える |
| 知らない札のかたまり | 甲の通り、触れないかたまりに数えて `data-md` を返す ── `null` にはしない | `null` は「持っていない」であって「知らない」ではない。知らないものは持ったまま返せる |

### 己. 美しさ

「美しい」を、要件として言える形にする。**四つ。**

1. **字が動かない。** 打つ前と打った後で、行間・幅・柱の位置が同じ。組み直しても
   同じ場所に同じ姿で戻る（依頼 338 の柱一本・34rem・見出しは上厚く下薄く）
2. **記号を一度も見せない。** `**` も `>` も `- [ ]` も、この面には出ない。出た
   瞬間にこの面の約束が破れる（Obsidian の Live Preview との違いはここ）
3. **caret が飛ばない。** 打っている間は組み直さない。組み直すときは、
   居た行に戻す（`whereAmI` → `goToLine`・依頼 346）
4. **面を替えても柱が動かない。** 「表示」「コード」「並べて」で字の左端が
   同じ（依頼 338）

| 問い | 決めごと | なぜ |
|---|---|---|
| 行間・余白・記号の見せ方はいまでよいか | **いまのまま。** 変えるなら三案を実寸で並べて Taketan が選ぶ（依頼 225・237 の作法） | 338 で一度決めている。美意識で上書きしない |
| 打った字が「表示」の形になるのはいつか | **打った瞬間に、組み直さずに。** 太字・見出し・箇条書き・升は、釦と鍵で押した瞬間に DOM の上で形になる（いまそうなっている）。**記号で打ったもの**（`**あ**` と打つ・`- ` と打つ）は形にならず、**次に組み直したとき**に形になる。これでよい | この面は記号を見せない面で、記号を打つ人は「コード」を持っている。「`- ` と打ったら点になる」（Notion・Typora の型）は便利だが、`*` が斜体になるのを嫌う人（`2*3` と打つ人）の逃げ道が要り、それは別の設計。**迷った**: 次の設計で、`- [ ] ` と `# ` だけ受けるのはあり得る |
| 組み直しのちらつき | **組み直しは、人の操作のあとにしか起きない**（升を押す・工房・一つ戻す・テーマ替え・向こうから来た字を混ぜる）。自動保存では組み直さない（いまのまま） | 手を止めたら組み直す、は caret が飛ぶ日を作る（依頼 346 の前がそれ） |
| 升の手触り | **押した瞬間に裏返る**（見た目は DOM で先に）。字は core の `check` を通していつもの保存（衝突の検査のため・いまのまま）。**組み直さない**。**いまは違う**: 保存のあと `drawRead` で組み直している | 押したものが 100ms 後に変わるのは「効いたか分からない」の入口。升の状態は DOM が既に持っている（`aria-pressed`）ので、組み直す理由が無い。**迷った**: 核の `check` の答えと DOM が食い違う日（同時に向こうが押した）は、混ぜ込みの組み直しに任せる |
| リンクの押し心地 | **押すと小さな吹き出し**（「開く」「直す」）。指を乗せると下線が濃くなる（いまのまま）。**いまは違う**: 押すと開く | Taketan が選んだ（2026-09-08）。打てる面で押した瞬間に飛ぶと、字を直したい人に道が無い（右押しは知らない人が多い）。Notion・Google Docs の型。触れないかたまりと同じ「押すと吹き出し」（甲）── 覚えることが一つ |
| 触れないかたまりの見た目 | 指を乗せたときだけ枠が光る（いまのまま）。**caret が跨いだときは光らない** | 光るのは「押せる」の合図で、通り過ぎるものに出すと目が引かれる |
| `tailStop`・`headStop` の一行 | **見えない**（空の段落の高さだけ）。印を付けない | 「ここに書けます」の印は Markdown のノートに無いものを描くことになる。無いところに caret を置けるのは、`contenteditable` の当たり前 |

### 壁打ちで決まったこと（2026-09-08）

上の表に散らばっているものを、Taketan が自分の手で決めた順に。
**これは訊き直さない。**

1. 物差しは「Markdown を知らない人が快適に」
2. 行頭の Backspace は記号（升・点・`#`）を外す。もう一度押すと上の行に繋がる
3. 引用は最初の行の行頭だけ出る。途中の行は前の行と繋がる。まとめて外すのは釦（範囲を選んで）
4. 見出しの途中で Enter を押したら、後ろは本文
5. リンクは押すと吹き出し（開く／直す）
6. 図・コード枠・絵は表示のまま消せる。押すと吹き出し（直す／消す）。電話の長押し・窓の右押しも同じ献立
7. 一覧の中の Tab は子リスト（Inkdrop と同じ）。一覧の外の Tab は字下げ（全角空白・案一）。Shift+Tab でも Backspace でも外れる
8. 電話の下の帯に「一段深く」「一段浅く」
9. 見出しが点付きになっても、点付きが見出しになっても驚かない（自分で選んだ操作だから）

### 迷ったところの一覧

実装する側が Taketan に訊けるように、上から集めた。**訊く前に手を
動かさない**のはここだけで、ほかは決めた通りに作ってよい。

| どこ | 何を迷ったか | 勧め |
|---|---|---|
| 甲 | 絵の題（`alt`）を面から直す道 | いまは「コード」で |
| 甲 | 行の中の知らない札（`<sup>` など）を、かたまりごと触れなくするか | かたまりごと |
| 乙 Shift+Enter | core に行の中の改行（行末 `\`）を足すか | この仕事では足さない |
| 乙 矢印 | `headStop` を常に置くか、近づいたときだけか | 常に |
| 乙 | 電話の帯の「一段深く／浅く」を、段落で字下げに使うか | 使う（Tab と同じ対応） |
| 丁 | 700ms と 500ms を揃えるか | 揃えなくてよい |
| 己 | `- [ ] ` と `# ` だけ、打った瞬間に形にするか | 次の設計 |
| 己 | 升を押した後の組み直しをやめるか | やめる |
