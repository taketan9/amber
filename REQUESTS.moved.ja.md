# cian の窓から運んできた依頼

**2026-09-06、cian から amber モードを外した日に届いた 19 件。** どれも
Taketan が名指しで頼んだことで、当時は cian の窓（Electron）の amber モードに
入っていた。モードごと cian から消えたので、約束の行き先はこちら。

**ここは検査しない。** `scripts/requests.py` が読むのは `REQUESTS.ja.md` の
表だけで、この表には入っていない。理由は嘘をつかないため ── amber の窓は
書き直しなので、これらの多くは「まだ果たしていない」。当てになる検査を書けない
約束を検査ありの表に置くと、**通っているのは検査であって約束ではない**という
一番悪い形になる。

**果たしたら `REQUESTS.ja.md` へ移すこと**（番号と、頼まれた言葉と、なぜの文は
そのまま。検査の欄だけ、実際にそれを担っている場所に書き換える）。「いまどこが
引き受けているか」は現時点の下見で、検査ではない。

元の表は `~/workspace/cian/gui/REQUESTS.ja.md`（2026-09-06 以前の履歴）。

| # | 日付 | 頼まれたこと | なぜ | いまどこが引き受けているか |
|---|------|------------|------|--------------------------|
| 47 | 2026-09-02 | cian モードでスクリーンショットや画像を貼り付けたい | 貼られた画像はノートの隣の `attachments/` に置き、本文には Markdown のリンクだけが入る。**受け口は `document` の capture** ── Monaco が `#v-body` に先に capture で付いていて `stopImmediatePropagation` するので、同じ節に後から付けた受け口には永久に届かない（配線したつもりで一度も呼ばれず、貼り付けは黙って消えていた）。登録は読み込み時に一度だけ ── エディタは作り直されるが容れ物は残るので、作るたびに付けると画像が二枚入る | `crates/amber-core/src/note.rs` の `attach`（置き場所と名前）。窓に貼り付けの受け口はまだ無い |
| 48 | 2026-09-02 | ノートの置き場所は複数指定できるといい（自分専用と、みんなで書くところ） | `cian.notes{ { name = …, path = … }, … }`。**並びは書いた順** ── Lua は文字列キーの順を保たないので、表にすると起動のたびにピッカーが入れ替わり、同じ位置に違うフォルダが来る。展開（`~`・SharePoint の URL → UNC）は**エンジン側** ── それが意味を持つのは実際にファイルがある機械だけで、窓はその機械ではない | 無し ── amber に Lua は無く、置き場所は1つをピッカーで選ぶ |
| 114 | 2026-09-05 | 窓版でもお気に入りの棚を | `:star`。棚は**いまある棚**（空のものも `.cian/settings.json` から）＋「新しい棚…」。書くのは電話と同じ `star:` の行なので、こちらで付けた星はあちらでも星。`show()` は**行ごとに答える**（`pick` コールバック）ので、返り値を待つ書き方だと Enter で何も起きない ── 最初そう書いて、開くだけのピッカーになった | `gui/renderer.js` の `starred` と左列の「お気に入り」、`api.rs` の `star` / `shelf` |
| 115 | 2026-09-05 | 窓版の通知はアプリ起動中だけでいい。常駐させるし、起動していない時は通知なしで | 常駐も自動起動も作らない ── 予約ごとに `setTimeout` を置くだけで、窓が閉じれば消える。**次にいつ鳴るかは `note::next_ring`**（「毎週水曜の9時」が何を意味するかは判断であって、front end がする算術ではない）。`setTimeout` はミリ秒が 32bit なので、**24.8日より先を渡すと即座に鳴る** ── 20日で切って、鳴ったら仕掛け直す。閉じている間に過ぎた定型は、フォルダを開いた時に作る（`note::catch_up` が**作った印を書く**。最初これを書き落として、開くたびに「1 件作りました」と言い続けた） | `crates/amber-core/src/note.rs` の `next_ring` / `catch_up`、`api.rs` の `remind` / `carryout` |
| 116 | 2026-09-05 | 窓版にもバックアップの範囲を | 範囲は電話と同じ4つ（すべて／フォルダ／タグ／ノート1つ）で、**どこに置くかは反対のペイン** ── 2画面ファイラの「向こう側が行き先」がそのまま答えなので、保存ダイアログを新しく作る必要がない。作ったら反対のペインを読み直す（見えない場所に置かれた zip は、無かったのと同じ） | `crates/amber-core/src/api.rs` の `backup`（4つの範囲）と `zipbox` |
| 119 | 2026-09-05 | cian モードは 左にノート一覧・右にノート（Inkdrop の構成）。下のシェルパネルも閉じる | ほかの見せ方では、エディタは一覧を**置き換える面**で、それはファイラとして正しい（見に行ったのだから）。ノートは別の形 ── 一覧が居場所で、ノートが読んでいるもので、行き来しながらどちらも離れない。**カーソルを追って開くが、キーは奪わない**（奪うと2回目の `j` がノートに打ち込まれる）。**ノートだけ**追う（`notes.by` にある行だけ）── そうしないと、通りすがりに `c.rs` を読み上げるファイラになる。CSS は `body` に付ける ── `#view` は `#work` の**外**に書かれているので、`#work[data-cian]` では1つも当たらず、しかも何も起きないだけで誰も気づかない | `gui/renderer.js` の `drawRail` / `drawList` ── 窓そのものが三列 |
| 120 | 2026-09-05 | Mac / Windows でも iPhone と同じく初回のノートフォルダを勝手に作る。設定から Google Drive や Dropbox へ変更、インポート・エクスポートも | **作る、要求しない。** 電話は自分のフォルダで始まる ── ノートを1本置く前に Lua を1行書けと言う窓は、誰もメモを取らない窓。既定は `~/Documents/cian`（人が書類を探しに行く場所で、iCloud Drive が入っていれば電話と同じフォルダへの最短路）。**フォルダ選びは OS に描かせる** ── 自分の Google Drive のパスを打てと言うのは変更手段ではない（`cian-pickdir`）。インポートは**コピーで、上書きしない**（同名は番号。持ってきた元にも残る）── 電話と同じ約束、同じ言葉 | `gui/main.js` の `firstRoot`（`~/Documents/cian` があればそれ、無ければ `~/Documents/amber` を作る） |
| 122 | 2026-09-05 | cian モードは枠の強調表示は不要。iPhone みたいにかっちょよく | 枠は「2つのうちどちらにキーがあるか」を言うもので、ここには一覧とノートしかない ── 唯一のものを囲む箱は何も言わない。見た目は**この画面だけ**変える: 行に余白、タグはチップ（`#仕事 #定型` は1つの文字列に読めるが、チップ2つは押せる2つに読める）、★ は暖色（「留めてある」は「触れる」ではないので、アクセントにしない）、列見出しは消す（2行の行に見出す列は無い）、フォルダ名を大きく＋「N のノート」 | `gui/index.html` の行とチップ ── 窓が最初からノートの見た目 |
| 137 | 2026-09-05 | cian モードから戻るとシェルパネルが消えたまま。元のパネル構成を維持して | 閉じる前に開いていたかを憶えて、戻るときに開き直す ── 黙って閉じて開き直さないモードは、そのうち入らなくなるモード | 無し ── amber の窓にモードもシェルパネルも無い。**この行はモードごと消えた** |
| 138 | 2026-09-05 | バックアップから戻すのに反対のペインの zip を指すのは現実的でない。自分で zip を選ばせて | 復元は**バックアップを探しに行くところから**始まる。カーソルの位置は、まだ探していないファイルの場所を知っている前提だった。OS のファイル選択に描かせる | `crates/amber-core/src/notebook.rs` の `restore`。窓のピッカーはまだ無い |
| 139 | 2026-09-05 | バックアップは すべて／タグ／フォルダ／選択中のファイル から選べるように（iPhone も同じ） | 電話と同じ4つ。**フォルダは全部並べる** ── 「フォルダ単位で」はフォルダを選ぶことで、そのフォルダに立ってから押すことではない。置き場所も保存ダイアログにした（反対のペインは賢いが間違い。バックアップは意図して置く場所がある） | `crates/amber-core/src/api.rs` の `backup` ── 範囲は all / book / tag / note の4つ |
| 140 | 2026-09-05 | インポートは md か zip か明示して | 「.md を取り込む…」と「バックアップ（zip）から戻す…」。同じ「取り込み」でも、片方は足す、片方は戻す | 無し ── 窓に取り込みの入口がまだ無い（iOS は `Where.swift` で分けてある） |
| 141 | 2026-09-05 | cian モードは iPhone に合わせて cian のロゴを左に。`[左]` もパスも不要 | ファイラの頭（どちら側か・どこにいるか・履歴の矢印）は**ファイルを操るための道具**で、ノートを読むためのものではない。ノートの側は名前と本数だけ。印はアイコンと同じ2つの括弧を**描く**（ドックの中と一覧の頭が同じものに見えるように）。**向きを ltr に戻す** ── パンくずは長いパスの頭を切るために右から左の箱で、その中に置くと並びが反転する（実際、印が右端に飛び、「7 のノート」が「のノート … 7」になった） | `gui/renderer.js` の `<div id="brand">` ── 左列の頭が印と名前 |
| 142 | 2026-09-05 | cian モードでは必ず設定しているフォルダへ。未設定なら既定を作る。「ノートの保存場所へ」の行は不要 | どこか別のディレクトリで cian の一覧を出しても、ノートの無い notes app でしかない。**ペインができてから**呼ぶ ── 起動時に憶えていたモードを当てる経路では `state.left` がまだ無く、そこへ飛ばすと描画ごと落ちて**窓が一枚も開かない**（実際そうなった） 。**出るときは元の場所へ戻す**（シェルパネルと同じ話。モードを一巡しただけで居場所を失わない）。歩いて開いただけのノートも一緒に閉じる ── 残るとエディタが窓を覆ったまま次のキーを全部飲む | `gui/main.js` の `firstRoot` と `gui/renderer.js` の起動時 `reload` |
| 143 | 2026-09-05 | cian モードの右クリックは cian に特化。ノートの内容をトップレイヤに | ノートを読んでいる人に出す最初の一画面が「zip に追加」や「ブランチ」で埋まっているのは、ファイラのメニューを流用しているだけで答えになっていない。棚・タグ・読む・新しいノートを上に、ファイルとしての操作は下に | 無し ── amber の窓に右クリックメニューがまだ無い |
| 144 | 2026-09-05 | cian モードのキーヘルプも cian に絞って | ブランチも差分も grep も、ノートを書いている人が押すものではない ── 並んでいるだけで「これは何のアプリだったか」を一行ぶん薄くする。**`viewer.on` だけで見ない** ── 右にノートが出た瞬間からエディタのキー表が出続け、一覧を歩いているのに「i 編集」と書いてある帯になっていた | 無し ── amber の窓にキーヘルプの帯がまだ無い。**cian の窓の家具についての行** |
| 145 | 2026-09-05 | cian（→ amber）モードを Inkdrop の三列に。左＝行き先、真ん中＝ノート、右＝中身 | **足りなかったのは装飾ではなく列**。ファイラの一覧はディレクトリを一階層ずつ見せるもので、それはファイルを操るための形。**フォルダもタグも★も同じ種類の答え**（この条件のノートを見せて）なので、ひとつの列に並ぶ。真ん中は `panelize` でノートだけの平坦一覧に差し替える ── 自前で描き直すと、マークも Enter も F3 もドラッグも全部作り直しになる。**索引は持たない**（毎回 `notes` から組み立てる。ノートはただのファイルで、フォルダと front matter がそのまま索引） | `gui/renderer.js` の `drawRail` ── 左＝行き先、真ん中＝ノート、右＝中身 |
| 146 | 2026-09-05 | amber のアイコンは案 S4「生成りの葉・尻尾つき」。窓の左列の印も同じ葉に | 多羅葉 ── **葉に字を書いたのが「葉書」の語源**で、メモの道具としてこれ以上の出自がない。**葉脈は描かない** ── 二行のあいだの隙間が勝手に葉脈に見えるので、描くと一本多い。葉は純白ではなく生成り `#fff4de`（純白は琥珀の上でわずかに青く見える）。前の印は `--accent` を拾っていたので配色を変えると別の色の印になっていた ── **ロゴは色替えに追従させない** | `packaging/amber.svg` と `gui/renderer.js` の `mark()` |
| 147 | 2026-09-05 | 窓の見える文字列を amber に（モード名・左列の名前・メニュー・`:view` の綴り・通知） | 名前を決めたのに画面が古い名前を言っていると、**どちらが本当か使う人には分からない**。`cian` はモードの内部の名前として残す ── 中まで変えると `body[data-cian]`・`.rows.cian`・`#cianrail`・drive.js の台本が一度に動くので、それはプロジェクトを分けるときに。`:view cian` も打てば通る（**指が憶えた綴りを、名前を変えたからといって止める理由は無い**） | `gui/renderer.js` の `<div class="name">amber</div>` ── 窓は最初から amber としか言わない |
