# 依頼の台帳

Taketan が名指しで頼んだこと。**これは装飾ではなく契約です。**

## なぜこれがあるか

2026-08-30、「きちんとアクティブパネルのみ枠線を太くしてほしい」と言われ、
`0eb71b5` で直しました。翌 8/31、見た目を作り直す `951bcd1` で**その行を自分で
削除しました** ── 「枠はカーソルが既に答えている問いへの一番うるさい答え方だ」
というコメントまで添えて。8/31 の夕方、同じ指摘をもう一度受けました。

見落としではありません。**上書きです。**設計パスに入った瞬間、その CSS が
「自分で決め直してよい既存コード」に見えた。そう見えたのは、コードのどこにも
**その行がどこから来たのか書いていなかった**からです。私が自分の判断で書いた行と
区別が付きませんでした。

同じ commit は枠を4つ消していて、うち3つ（シェルにキーがある・同期中・記録中）は
別の signal に載せ替えてありました。**置き換えずに消したのは、頼まれた1つだけ**です。

だからこの表があります。行ごとに「これが今も真であること」を機械で確かめます。
消すなら検査が落ちる。落ちたら、直す前に**訊く**。

## 使い方

```bash
python3 scripts/requests.py          # 全部通るか
python3 scripts/requests.py --list   # 何を見ているか
```

`検査` 欄は `ファイル ~ 正規表現`（マッチすること）か `ファイル !~ 正規表現`
（どこにも出てこないこと）です。

**識別子には終端を書くこと**（`\(` や `\b`）。`~ pub fn foo` は `pub fn foo2` に
そのまま当たるので、名前を変える変異をすり抜けます。2026-09-02 に3回やりました
── 検査を足したら壊して鳴らす、をやっていなければ3回とも気づいていません。

否定は専用の書き方にしてあります。最初は否定先読み `^(?!.*やめる).*$` で書いて
いて、これは「その語を含まない行が1行でもあれば成功」なので**常に通ります**。
3行が何も検査せずに「守られています」と言っていました。足したら必ず壊して、
鳴るのを見てから戻すこと。

## 表

| # | 日付 | 依頼（原文） | 満たすもの | 検査 |
|---|---|---|---|---|
| 1 | 2026-08-30 | きちんとアクティブパネルのみ枠線を太くしてほしい | キーのあるペインに枠。`data-focus` で条件付け。**枠は中身より上に描く**（inset box-shadow だと Windows の実スクロールバーが右辺を覆って消える）。色は `--mode-accent` ＝ 既定はアクセント、`/` `//` は緑・`:` は紫・`v` は橙・シェルは金（端末版 `focus_badge_color`。2026-09-01「対象のファイラパネルを緑っぽい枠にしてほしい」）。**太さは 2026-09-05 に 2px → 3px**（104 番台の依頼103。控えめすぎて目立たない、と） | `gui/index.html ~ (?s)\A(?=.*#work \{ --mode-accent: var\(--accent\); \})(?=.*data-focus="files"\]\s*\.pane\.active::after\s*\{[^}]*border: 3px solid var\(--mode-accent\))` |
| 2 | 2026-08-31 | フォーカス以外はちょっと控えめの配色に | 現在でないペインの一覧・パンくず・見出しを減光 | `gui/index.html ~ \.pane:not\(\.active\)[^{]*\{[^}]*opacity` |
| 3 | 2026-08-30 | 「やめる（Esc）」は日本語が変。キャンセル（Esc）がよい | 確認シートのボタンは「キャンセル」。足のヒントは端末版と同じ「取消」 | `gui/index.html ~ キャンセル` |
| 4 | 2026-08-30 | （同上）足のヒントに「やめる」を残さない | 端末版は取消を43回使い、やめるは1回 | `gui/renderer.js !~ Esc\s*やめる` |
| 5 | 2026-08-30 | 「/」「//」「:」で全部下部にコマンド入力できるように | 3つとも下端の1行に入力する。全画面のモーダルにしない | `gui/renderer.js ~ const PROMPT_SIGN = \{` |
| 6 | 2026-08-30 | 検索系だと緑枠、コマンド系だと紫っぽい枠に | 端末版 `focus_badge_color` の値をそのまま | `gui/index.html ~ --m-command:\s*rgb\(200,\s*100,\s*200\)` |
| 7 | 2026-08-30 | リモートペインの赤枠 | 端末版と同じ carmine | `gui/index.html ~ --m-remote:\s*rgb\(214,\s*45,\s*70\)` |
| 8 | 2026-08-30 | フォントは同梱する | HackGen Console NF を `vendor/fonts/cian.ttf` から窓が自分で読む | `gui/index.html ~ @font-face` |
| 9 | 2026-08-30 | 一覧は端末版の列構成に揃える | 4桁年の日時列 | `gui/renderer.js ~ \$\{d\.getFullYear\(\)\}-\$\{md\}` |
| 10 | 2026-08-30 | shell1 を名称変更できるようにしたい | `:shellname` / `:tabname`。端末版にも入れた | `gui/renderer.js ~ async function cmdShellName` |
| 11 | 2026-08-30 | シェルの初期表示を25%に | 起動時の分割比 | `gui/renderer.js ~ const layout = \{ main: 75` |
| 12 | 2026-08-30 | ゴーストの入力案内は不要なので消して欲しい | プロンプト行に placeholder を置かない | `gui/index.html !~ id="f-input"[^>]*placeholder` |
| 13 | 2026-08-31 | .md でプレビューと普通のテキストを行き来するショートカット | `Ctrl+E`。Monaco が同じ鍵を持つので捕捉フェーズで先に取る | `gui/renderer.js ~ async function togglePreview2` |
| 14 | 2026-08-31 | （vim 流で）Ctrl+C/V/X が効かない | 端末版 `viewer_vim_key` は Ctrl/Alt 付きを文法に渡さない。同じにする | `gui/renderer.js ~ cxvCXV` |
| 15 | 2026-08-31 | ブックマークのフォルダが最初から開いて見える | 端末版 `sc_level` と同じく1階層ずつ | `gui/renderer.js ~ let scPath` |
| 16 | 2026-08-31 | F11 でウィンドウが最大化しない | F11 で全画面。他のキーと同じ場所で読む | `gui/renderer.js ~ async function cmdFullscreen` |
| 17 | 2026-08-31 | :version でビルドの時間は見れないよ | ビルド日時と commit と書体を出す | `crates/cian-server/build.rs ~ CIAN_BUILT_AT` |
| 18 | 2026-08-30 | 会社ではGitコマンドは使えない前提で | Electron の場所は `electron.txt` に1行。zip に入れない（次の版で消えるので） | `gui/run.bat ~ for /f "usebackq delims=" %%L in \("%HERE%\\electron\.txt"\)` |
| 19 | 2026-08-31 | run.bat の文字化けエラー | `chcp` を実行しない。純 ASCII | `gui/run.bat !~ ^\s*chcp` |
| 20 | 2026-08-31 | cian-tui / cian-gui で表記の揺れ・動きの揺れがあれば揃えたい | 端末版の語を窓版のメニュー・スイッチ・並び替えに写し、**機械で確かめる**（`scripts/parity.py`）。CI にも入れた | `.github/workflows/ci.yml ~ python3 scripts/parity\.py` |
| 21 | 2026-08-31 | （同上）機能・挙動の違いをなくしていきたい | ビューアの文法の既定を端末版と同じ vim に | `gui/renderer.js ~ ^let style = 1;$` |
| 22 | 2026-09-02 | コピー・リネーム・移動のあとの Meta+z も戻したい | `Ctrl+z` / `⌘Z` を一覧の取り消しに（`Ctrl+Shift+z` でやり直し）。**コピーも取り消せるようにした** ── そのコピーが新しく作ったものだけをゴミ箱へ | `gui/renderer.js ~ if \(e\.shiftKey\) redo\(\); else undo\(\);` |
| 23 | 2026-09-02 | コピーの取り消しは、そのコピーが作ったものだけ | 先に存在した名前は上書きでも飛ばしでも対象外。判断は1か所（`copy_creates`）で、両前端が同じものを使う | `crates/cian-core/src/ops.rs ~ pub fn copy_creates` |
| 24 | 2026-09-02 | jj 系は端末版にも入れて欲しい | `jj` / `ｊｊ` / `っｊ` で挿入モードを抜ける。`ZZ` / `ZQ` も | `crates/cian-tui/src/viewer.rs ~ pub\(crate\) const JJ_ESCAPES` |
| 25 | 2026-09-02 | アイコンを exe に焼き込むのをやってほしい | `winresource` で `cian-tui.exe` / `cian.exe` / `cian-server.exe` に `cian.ico` を。**無ければビルドを止める**（黙って軽い zip を出した font の再発を防ぐ） | `crates/cian-bin/build.rs ~ res\.set_icon\(icon\)` |
| 26 | 2026-09-02 | AIの会話枠はいらない。コマンド提案・ディレクトリ検索・ゴミ検索の品質と精度を1段2段あげたい | モデルに渡す事実を増やした ── **種類・サイズ（ディレクトリは配下の合計）・最終更新からの日数・パス**の4列。ゴミ検索は木を4階層下まで見る（前は1階層） | `crates/cian-core/src/survey.rs ~ pub fn survey` |
| 27 | 2026-09-02 | （同上）コマンド提案の精度 | **OS ではなく「そのシェルが今どこにいるか」**を渡す ── タイトルの `user@host`／画面に残る `ssh` 行／プロンプトの形、そして `init.lua` の `notes` から**接続先のOS**まで。端末版が持っていたものを窓版にも。一覧とマークも渡し、1行で書けないときは `# ` で断らせる | `crates/cian-server/src/main.rs ~ cian_core::shellwhere::describe\(` |
| 28 | 2026-09-02 | （同上）見た範囲を偽らない | 打ち切ったら**モデルにも人にも**言う。「N件入らなかった」ではなく「何階層目までは全部見た」 | `crates/cian-core/src/survey.rs ~ pub fn whole_to` |
| 29 | 2026-09-02 | 端末版にあって窓版に無いポップアップを埋めたい | 開いている zip へのコピー（端末版 `ConfirmZipAdd`）。**それまでは zip の隣に落として「コピーしました」と言っていた** ── アーカイブ表示は作り物で、ペインは入る前の `cwd` を覚えたままだった | `crates/cian-server/src/main.rs ~ "zipadd" =>` |
| 30 | 2026-09-02 | リモートの深さを実装してほしい | ローカル↔サーバの `c`/`m`（**それまでは黙って手元にコピーしていた**）・サーバ内の移動・chmod・サーバ間の中継。同一サーバの移動は rename で済ませる | `crates/cian-server/src/jobs.rs ~ pub fn start_remote\(` |
| 31 | 2026-09-02 | （同上）実際に通したい | **127.0.0.1 に自分の sshd を立てて通す**（`scripts/remote.py`）。管理者権限は要らない。これが無かったのでリモートは一度も検証されていなかった | `scripts/remote.py ~ def free_port\(` |
| 32 | 2026-09-02 | （同上）鍵で入れるようにしたい | `cian.ssh{ users = { { key = "~/.ssh/id_ed25519" } } }`。**設定例は前から鍵認証を勧めていたのに、機能が無かった** | `crates/cian-scp/src/lib.rs ~ \.authenticate_publickey\(` |
| 33 | 2026-09-02 | （同上）ディレクトリごと送れるようにしたい | `cian_scp::plan_upload` / `plan_download` が木を歩き、ジョブが親から順に mkdir する。**端末版にも無かった**（両方ファイルだけ送っていた）。リンクは辿らない | `crates/cian-scp/src/lib.rs ~ pub fn plan_upload\(` |
| 34 | 2026-09-02 | Shift+f / Ctrl+f のポップアップの表題が `:find` `:grep` で何をするか分かりづらい | 表題を**何をするか**の文（`about`）に。欄の中に何を打つか（`arg`）、コマンド名は下に小さく残す ── 名前は覚える価値があるので消しはしない | `gui/renderer.js ~ a = await askFor\(cmd\.about,` |
| 35 | 2026-09-02 | 「z」の宛先入力窓の横幅が小さく、入力した文字列が見切れる | パスを訊く窓は広く（`min(60rem, 84vw)`）。380px は**ファイル名の幅**であってパスの幅ではなかった | `gui/index.html ~ #ask \.sheet\.wide \{ min-width` |
| 36 | 2026-09-02 | ビューアに対ディスク差分ガター | 編集中の行のうち**ディスクと違う行**を行番号の脇に出す。判定はエンジンの `cian_core::diff`（窓で二つ目の差分器を書かない） | `crates/cian-server/src/main.rs ~ "diskdiff" =>` |
| 37 | 2026-09-02 | AVD の Program Files に置いて共通デスクトップの run.bat で全員に使わせたい。初回の人には僕の init.lua や ssh 設定を配りたい | `run.bat` が `default-config\` から各自の `~/.config/cian` へ配る。**初回だけ配る**（以後その人のもの。ssh.lua も含めて上書きしない ── 使っている最中に設定を書き換えられる道具は信用できない）。**exe の隣に置く配り方は罠** ── 書き込み先まで Program Files になり、全員のしおりが保存できなくなる | `gui/run.bat ~ default-config` |
| 38 | 2026-09-02 | tar への書き戻し | `tar_modify` ── zip と同じ4つの操作（削除・改名・追加）。tar は差分編集できないので**丸ごと書き直す**が、**新しいものが完成するまで元を消さない**（誰かの唯一のコピーであることがある） | `crates/cian-core/src/archive.rs ~ pub fn tar_modify\(` |
| 39 | 2026-09-02 | ディレクトリ転送に確認シート | サーバへフォルダを送るとき、**中に何ファイル・何バイトあるか**を確認前に出す。数は転送が使うのと**同じ計画器**（`plan_upload`）から取る ── シートと実行が別々に数えると、約束と結果がずれる | `crates/cian-server/src/main.rs ~ "transferplan" =>` |
| 40 | 2026-09-02 | スクリプトマクロを窓版でも | `macro_script::run` をエンジンから呼ぶ。**それまでは「まだ動かせません」と断っていた** ── `macro.lua` は両方の種類を1ファイルに持つので、半分だけ動く状態だった | `crates/cian-server/src/main.rs ~ macro_script::run\(` |
| 41 | 2026-09-02 | （同上）`macro/` ディレクトリも読む | 探索の規則（`macro.lua` と `macro/*.lua`、`.en.lua` は飛ばす）を `cian_lua::macros::load_all` に。**窓版は前者しか読まず、同梱の例のように1ファイル1マクロにしている人にはランチャーが空だった** | `crates/cian-lua/src/macros.rs ~ pub fn load_all\(` |
| 42 | 2026-09-02 | ダークテーマのときもタイトルバーが明るいまま | タイトルバーは OS が描くので CSS では届かない。`nativeTheme.themeSource` を**実際に描かれている `--bg` の輝度**から決めて渡す（配色の名前ではなく）。起動時の地の色もエンジンに訊く ── `main.js` は18ある配色のうち3つしか表を持っていなかった | `gui/renderer.js ~ function tellFrame\(` |
| 43 | 2026-09-02 | 共有フォルダで複数人が同じものを書きたい（Inkdrop 相談の前提） | **保存前に、開いたときと同じファイルかを見る。**違えば書かずに訊く（差分を見る／別名／それでも上書き）。端末版は `:w` が断り `:w!` で通す。判定は `cian_core::stamp` の1か所 | `crates/cian-core/src/stamp.rs ~ pub fn changed\(` |
| 44 | 2026-09-02 | ビューは一覧・クラシック・ノートの三つでよい（アイコンは消す） | **入口だけ閉じた** ── 切替バー・メニュー・`:view icons` から外す。描く側は残す: アイコンの当たり判定は詳細ビューのアドレスバー・パンくず・タブ・サイドバーと同じ関数の中にいて、外そうとして三度とも詳細ビューを壊しかけた。**分解は別の日に** | `gui/renderer.js ~ const VIEWS = \[(?![^\]]*icons)` |
| 45 | 2026-09-02 | SharePoint の場所を指定できるようにしたい（crmaine と同じやり方で） | **認証しない。**URL を WebDAV の UNC パスに直すだけ（`\\host@SSL\DavWWWRoot\…`）。crmaine の `sharepoint_to_unc` を写した ── 貼られる4つの形と、**場所を持たない共有リンク**の見分けは高くついた知識なので思い出しで書かない。開けないときは crmaine と同じ手順を出す | `crates/cian-core/src/sharepoint.rs ~ pub fn to_unc\(` |
| 46 | 2026-09-02 | 指定したディレクトリでマークダウンのノートを管理したい（Inkdrop 相談。名前は **cian モード**） | **DB は作らない。**題は front matter の `title`、無ければ最初の見出し、無ければファイル名 ── その判断は `cian_core::note` の1か所にあり、窓版もいずれの iOS も同じ答えを見る（Electron は iPhone で動かないので、判断を窓に置くと二度書くことになる）。二行目に本文の頭。**ノート以外のファイルも並ぶ** ── ペインの一覧だから、リンク先の画像を隠すのは嘘になる | `crates/cian-core/src/note.rs ~ pub fn read\(` |
| 47 | 2026-09-02 | cian モードでスクリーンショットや画像を貼り付けたい | 貼られた画像はノートの隣の `attachments/` に置き、本文には Markdown のリンクだけが入る。**受け口は `document` の capture** ── Monaco が `#v-body` に先に capture で付いていて `stopImmediatePropagation` するので、同じ節に後から付けた受け口には永久に届かない（配線したつもりで一度も呼ばれず、貼り付けは黙って消えていた）。登録は読み込み時に一度だけ ── エディタは作り直されるが容れ物は残るので、作るたびに付けると画像が二枚入る | `gui/renderer.js ~ document.addEventListener\('paste', onEditorPaste, true\)` |
| 48 | 2026-09-02 | ノートの置き場所は複数指定できるといい（自分専用と、みんなで書くところ） | `cian.notes{ { name = …, path = … }, … }`。**並びは書いた順** ── Lua は文字列キーの順を保たないので、表にすると起動のたびにピッカーが入れ替わり、同じ位置に違うフォルダが来る。展開（`~`・SharePoint の URL → UNC）は**エンジン側** ── それが意味を持つのは実際にファイルがある機械だけで、窓はその機械ではない | `crates/cian-lua/src/lib.rs ~ pub struct NoteRoot \{` |
| 49 | 2026-09-02 | 自分のノートを書き足せるようにしたい | `:newnote` ── 題を訊いて、前置き（`title`／`created`／`tags`）だけのノートを作って開く。名前と体裁の判断は `cian_core::note`（iPhone へ運べる側）。ファイル名は**スラグにしない** ── 題は日本語のことが多く、ASCII に削ると `.md` だけが残る。消すのは OS が拒む文字だけ。`CON` は拡張子を付けても `CON`、末尾の点と空白は Explorer が黙って食う | `crates/cian-core/src/note.rs ~ pub fn new_note\(` |
| 50 | 2026-09-02 | タグでノートを絞れるようにしたい | 一覧は名前で絞る ── それはファイルには正しく、ノートには間違い。**タグはファイル名のどこにも無い**し、取り込んだページは `page-0012.md` の中に題を持っている。ペインが項目ごとに「探せる一行」を持ち（`Pane::search_text`）、その中身は `note::haystack`（題＋`#タグ`＋本文の頭）。**iPhone 側も同じ判断を使う**ので窓には置かない。到着時に marks・filter と一緒に捨てる ── 前のフォルダの言葉で次のフォルダを絞ることになる | `crates/cian-core/src/note.rs ~ pub fn haystack\(` |
| 51 | 2026-09-03 | iPhone でも使えるようにしたい（Mac と iPhone の連携が本命） | ノートの半分を C ABI の向こうへ。**扉は1つ**（`cian_call(method, params)` で JSON を渡して JSON が返る ── 窓がエンジンと交わしているのと同じ会話）。関数を増やすと Xcode の橋渡しヘッダを手で合わせ直すことになり、そこは何も検査できない。**判断は置かない** ── 題も抜粋もタグも命名も `cian_core::note` のまま。`trash` は任意機能にした（iPhone にゴミ箱の API は無い）。stamp は**秒に丸めない** ── 丸めた最初の版は保存のたびに「誰でもない相手」と競合していた | `crates/cian-ffi/src/lib.rs ~ pub unsafe extern "C" fn cian_call\(` |
| 52 | 2026-09-03 | iPhone のアプリを作り始めたい（Mac と同じノートを両方から） | SwiftUI ＋ `CianFFI.xcframework`。**Swift には何も判断を書かない** ── 題も抜粋もタグも `cian_call("notes")` の答えをそのまま使い、絞り込みも `search`（`note::haystack`）で当てるので、窓と同じ `#タグ` が同じノートを引く。置き場所は `UIDocumentPicker` で選んで security-scoped bookmark で覚える ── **iCloud も Google Drive も Dropbox も iOS では「ファイル」のプロバイダ**なので、これ1つで三者に届き、同期のコードを1行も書かない。`startAccessingSecurityScopedResource()` を忘れると、選べるのに読めない | `ios/Cian/Bridge.swift ~ static func call\(` |
| 53 | 2026-09-04 | iPhone でも写真をノートに入れたい（Mac の貼り付けと同じこと） | 置き場所と名前は `cian_core::note::attach` ── **窓が貼るスクリーンショットと電話が付ける写真が、同じ `attachments/` に同じ形の名前で落ちる。**別々に書けば、両方から書いたノートフォルダは「たまたま重なった2つのフォルダ」になる。拡張子は**バイトから見る**（撮影は PNG、写真は大抵 HEIC。取り違えると何でも開けないファイルが残る）。画像は**先にディスクへ、あとから本文へ** ── 逆順だと、届かないかもしれないファイルへのリンクが本文に残り、あとから「無い」のか「消された」のか区別できない | `crates/cian-core/src/note.rs ~ pub fn attach\(` |
| 54 | 2026-09-04 | （同上）ノートを消せるようにしたい | スワイプして「削除」の二段。**電話にゴミ箱は無い**ので `cian_core::DESKTOP` は false、`DeleteMode::Trash` は断る。消すのはノートだけで、`attachments/` の画像は残す ── 別のノートが同じ画像を指していることがある | `crates/cian-ffi/src/lib.rs ~ "delete" =>` |
| 55 | 2026-09-04 | Inkdrop のようにノートが「読める形」で出てほしい | 見出し・強調・箇条書き・引用・コード・区切り線・**画像**を組んで出す。**何が見出しかは `cian_core::note::blocks`、どう見えるかは前端** ── 逆に切ると Markdown の解釈が電話の上に載り、テストが一行も届かなくなる。`#仕事` は見出しではない（空白の有無で決まる。誤ると タグ付きのノートが全部 32pt で開く）。閉じていない ``` は残り全部を飲む。前置きは出さない（題もタグも既に画面にある） | `crates/cian-core/src/note.rs ~ pub fn blocks\(` |
| 56 | 2026-09-04 | （Inkdrop 化）ノートの本文まで探せるようにしたい | 一覧が持っている一行（題＋タグ＋冒頭100字）は打つそばから絞れるが、**思い出すのはたいてい奥の一文**。`cian_core::search`（窓の `:grep` と同じ）で歩き、行そのものを返して抜粋の代わりに見せる。打鍵ごとに歩かない ── 250ms 待って、前の探索は取り消す（5文字で5回歩けば、捨てる4回のぶん電池が減る）。1ノート1行（同じノートが20行出るのは答えとして悪い） | `crates/cian-ffi/src/lib.rs ~ "find" =>` |
| 57 | 2026-09-04 | ノートブックで絞りたい／並び順を変えたい（更新順・題順） | 絞りと並びは1つのメニュー（増えた一覧に対して訊くことは「どこの」と「どの順で」の2つだけ。ボタンを2つ置くと、ノートを作る側の場所が無くなる）。**絞っている間は題名がノートブック名**になる ── 絞ったのにフォルダ名のままだと「ノートが消えた一覧」に見える。題順は `localizedStandardCompare`（素の比較だと「あ」「い」も note-2/note-10 も並びを間違える） | `ios/Cian/NotesStore.swift ~ enum Order` |
| 58 | 2026-09-04 | タグを付けられるようにしたい | 前置きの `tags:` を書き換えて**本文として返す** ── 保存は普通の保存なので、**タグ付けも競合チェックを通る**。自分でファイルを書く実装にすると「ノートを書く道」が2本になり、2本目が誰かの段落を消す。フォルダにあるタグを候補に出す（「仕事」と「 仕事」は別のタグに見えないのに別物）。`#` は剥がす（人は見た目どおり `#` を打つ）。リスト形式の `tags:` は `- ` の行ごと差し替える | `crates/cian-core/src/note.rs ~ pub fn set_tags\(` |
| 59 | 2026-09-04 | ノートをピン留めしたい | 前置きの `pinned: true`。**外すときは行ごと消す** ── `pinned:` と値なしで残すと、次に読むものが「留まっている」と解釈する。`true`/`yes`/`1` の三通りを受ける（どれだったか誰も覚えていない）。並びは**安定** ── 留めたものの中では選んだ並びが保たれる（`pinned` だけで並べ替えると中でばらける） | `crates/cian-core/src/note.rs ~ pub fn set_field\(` |
| 60 | 2026-09-04 | ノートを別のノートブックへ移したい | **画像も連れて行く。**リンクは相対（`attachments/…`）なので、ノートだけ移すと全部切れる ── しかも切れたことは後で開くまで分からず、開いても「消したのか届かなかったのか」が区別できない。どれが「そのノートの」画像かは命名（`<題>-<時刻>.ext`）で分かる。**同名があれば移さない**（上書きしない）。画像を先に、ノートを最後に動かす ── 途中で失敗しても、ノートは元の場所で元の画像を指したまま | `crates/cian-core/src/note.rs ~ pub fn move_to\(` |
| 61 | 2026-09-04 | 読む／書くの切替は同じ絵柄で（開いた目・閉じた目） | 目とえんぴつは**2つの別のボタン**に見える。開閉する1つの目なら、それが1つのスイッチだと分かる | `ios/Cian/Desk.swift ~ "eye.slash" : "eye"` |
| 62 | 2026-09-04 | 新規作成は 題 → タグ → 作成、そのあと書き込みモードで開く（ボタンは「作成」） | 題を打った瞬間に出来てしまうと、**何のノートか言う前に世に出る**。あとからタグを付けるには、出たばかりのノートに戻ることになる。シートにしたのは、アラートの入力欄は問いを1つしか持てないから。`navigationDestination` は **`NavigationStack` の中**に置く（外側に付けると、ノートは出来るのに何も開かない） | `ios/Cian/Making.swift ~ struct Making` |
| 63 | 2026-09-04 | 電話から打つとき、マークダウンの定型を選びたい | 入力欄の上の帯。**書式ツールバーではない** ── ファイルはマークダウンのままで、押すと「自分で打ったはずの文字」が入る。行頭の印（`#` `- ` `- [ ] ` `> `）は**押し直すと外れる**（間違えて押した瞬間に欲しくなる）。素の `TextEditor` はカーソル位置を渡さないので、対象は**最終行** ── 正しく直すには UIKit が要り、この帯に見合わない | `ios/Cian/Writing.swift ~ private var marks` |
| 64 | 2026-09-04 | フォルダのボタンが何をするか分からない。保存場所の選択・変更と、取り込み・書き出しに使いたい | いきなりファイル選択が開くのは**訊かれていない問いに答えている** ── 「ファイルを探す」に見えて、実は「ノートはここに住んでいる」の話。まず今どこかを示し、選び直す・iPhone の中に戻す・取り込む・書き出すを並べる。取り込みは**コピー**（元は残る。まだ cian に住まわせると決めていない人が最初に知りたいこと）。同名は連番にして**上書きしない** | `ios/Cian/Where.swift ~ struct Where` |
| 65 | 2026-09-04 | フォルダを作れるように。平らに溜めるのも、PC のように整理するのも、どちらも使い方 | 一覧の上にフォルダ、下にノート（ファイルマネージャが最初からやっている並び）。入る・上へ戻る。**両方の使い方を残す** ── メニューの「全部まとめて見る」で平らに戻る。フォルダの一覧は**エンジンの歩いたディレクトリから**取る、ノートから逆算しない ── 作ったばかりのフォルダは空で、ノートから作った一覧には出ず、**それは「作れていない」のと見分けがつかない**。新しいノートは**いま開いているフォルダの中**にできる（そうでないと整理は毎回あとからの作業になる）。名前はシートで訊く ── テキスト欄のあるアラートは自動検査から操作できず、**見たことのない画面を出すことになる** | `ios/Cian/Booking.swift ~ struct Booking` |
| 66 | 2026-09-04 | フォルダ作成が並べ替えメニューの中で見つけられない | 上のバーへ。**作ることは、並べ替えの設定ではない** | `ios/Cian/ContentView.swift ~ "folder.badge.plus"` |
| 67 | 2026-09-04 | 置き場所の画面に、どこにあるか（パス）を出してほしい | パンくずで出す。**「cian」だけでは「何という名前か」の答えで、訊かれているのは「どこにあるか」** ── 電話では同じ名前のフォルダが3つのクラウドに在りうる | `ios/Cian/NotesStore.swift ~ var trail` |
| 68 | 2026-09-04 | 「保存場所を選ぶ」を押しても何も起きない | 原因は2つ。**同じ View に `.fileImporter` が2つ**あった（片方しか効かず、負けた方のボタンは無反応）。そして**シートの中からファイル選択は開けない** ── 提示の上に提示を重ねることになり、黙って何も起きない。シートを先に閉じ、下の画面から開く | `ios/Cian/Where.swift ~ let choose:` |
| 69 | 2026-09-04 | 「出し入れ」ではなく「バックアップ」「インポート」に | 名前を変え、いまあるノートを上書きしないことを画面に書いた（同名は番号を付ける） | `ios/Cian/Where.swift ~ バックアップとインポート` |
| 70 | 2026-09-04 | ピン留めすると何が起こるのか分からない | **「上に固定」という見出しの節に入れる。**黙って上へ跳ぶノートは、理由の見えない移動でしかない。**最上階では、どのフォルダに入っていても固定分を出す** ── ピンの意味は「探しに行かなくても手が届く」なので | `ios/Cian/NotesStore.swift ~ func pinnedHere\(` |
| 71 | 2026-09-04 | ノートを複数同時に開きたい。タブで分かる・切り替える・タブごとに閉じる・左右スワイプ | **本文は「机」が持つ、画面が持たない。**`TabView` はページを作っては捨てるので、ページに本文を置くと**保存前の段落がスワイプで消える**。開いているタブは `Desk` の状態で、エディタはそこへの窓。選択は**索引ではなくパス** ── 閉じると後ろの索引がずれ、選択が隣のノートを指し始める。閉じたら**左隣**へ（来た方向。遠くへ飛ぶと居場所を失う）。未保存は点で出す（閉じる判断をする場所に出す） | `ios/Cian/Desk.swift ~ final class Desk` |
| 72 | 2026-09-04 | ノートをドラッグ＆ドロップでフォルダへ。**中から外へ出す**のもやりたい | フォルダの行が受け口。外へ出す口は **`..` の行** ── cian のペインには最初から `..` があり、ずっと両方の意味だった（上へ行く／これを上へ置く）。掴んで運ばれるのは**パス**（どの操作もパスを取るし、ノートは2つのフォルダの中間には居られない）。落とした先の処理は長押しメニューと**同じ `store.move`** ＝ `note::move_to`（画像も連れて行く／同名は移さない）。指の部分だけは自動検査で駆動できていない | `ios/Cian/ContentView.swift ~ private func drop\(` |
| 73 | 2026-09-04 | 通知がほしい。`mm/dd hh:dd` で一度きり。毎週の定型 TODO（水曜 9 時に「週報」など）も | **判断は `cian-core` に、鳴らすのは OS に。**書式は本文の front matter（`remind:` と `repeat:`）＝**ノートがただのファイルであることを壊さない**ので、Mac の窓版も同じ文字列を読める。繰り返しは `due_since` が**留守の間に過ぎた分を数える**（月末 31 日は、その月の最終日へ寄せる。上限 32 個）。作られるのは**テンプレートの複製**で、テンプレート自身は残る（`carry_out`）。同じ日の分が既にあれば**作らずに既存を返す** ── 2 台が同時に追いついても 2 つにならない。iPhone は勝手にアプリを動かさないので、**時刻に届くのは通知だけ、その日のノートは次に開いたとき**。そう書いてある | `crates/cian-core/src/note.rs ~ pub fn due_since\(` |
| 74 | 2026-09-04 | バックアップとインポート。範囲を選べるように（すべて／フォルダ／タグ／ノート1つ） | 範囲が要るのは、バックアップが**何かの前**にやることだから ── 入れ直す前・人に渡す前・片付ける前で、要る量が違う。zip を作るのは `cian-ffi` の1つの口（`backup`）で、**窓版もいずれ同じ口を使える**。名前は `<対象>-<日付>.zip`。空のタグは**エラーにする**（黙って全部入りの zip を渡さない）。共有シートは `ShareLink` ではなく `UIActivityViewController` ── **zip は押した瞬間に作る**ので、画面を組み立てる時点では渡すものがまだ無い | `crates/cian-ffi/src/lib.rs ~ "backup" =>` |
| 75 | 2026-09-04 | cian だからシアンっぽい色を基調にした見た目に | **色は1つ、置き場所も1つ**（`Assets.xcassets/AccentColor`）。画面ごとに違う色を出すと、触れるものの見分けが画面ごとに変わる。明暗それぞれに別の値を持つ（暗い地の上では、同じシアンは沈む）。アプリのアイコンから採った色で、**アプリの外（ホーム画面）と中で同じ色** | `ios/Cian/CianApp.swift ~ \.tint\(Color\("AccentColor"\)\)` |
| 76 | 2026-09-05 | 読む画面で TODO のチェックを押したり外したりしたい | **行番号で裏返す**（何番目のチェックか、ではない）。画面は一瞬前の `blocks()` から描かれていて、上にタスクが1つ増えていたら数え方はずれる。行が違っていた・そこが箇条書きだった、のときは**何もしない** ── 画面とファイルが食い違っているのに書き込むのが一番悪い。押した瞬間にディスクへ（チェックを押してから保存ボタンを探す人はいない）。`- [ ]` は箇条書きでもあるが、**押せる方を返す** | `crates/cian-core/src/note.rs ~ pub fn set_check\(` |
| 77 | 2026-09-05 | 通知を押したら、そのノートを開いてほしい | 通知に**ノートのパスを積む**（`userInfo`）。受け口は `Ring` という小さな置き場 ── ロック画面から押されたときは、**渡す相手の画面がまだ無い**うちに答えが来るので、誰かが取りに来るまで持っておく係が要る。`Ring.listen()` は `init()` で（あとから delegate を差しても、その通知はもう誰にも届かない）。ノートが読み込まれる前に押された場合に備えて、**ring と notes の両方を見て、後に来た方が開く** | `ios/Cian/Bell.swift ~ final class Ring: NSObject` |
| 78 | 2026-09-05 | 一覧を日付でまとめたい。作成日と更新日の2種類 | 見出しは**並び順に従う** ── タイトル順に並べながら日付で区切ると、3月のノートの上に「今日」が乗る。だからタイトル順のときは**頭文字**が見出しになる。作成日は front matter の `created:`、無ければファイルの誕生時刻、それも無ければ更新時刻（日付の無いノートは日付の見出しから落ちて、消えたように見える） | `ios/Cian/NotesStore.swift ~ func bands\(` |
| 79 | 2026-09-05 | 「題で並べる」ではなく「タイトルで並べる」 | 語を1つに揃えた。**窓版の「ノートの題」も同時に直した** ── 片方だけ直すと、同じものを2つの名前で呼ぶことになる（それを機械で見ているのが `parity.py`） | `ios/Cian/NotesStore.swift ~ タイトル順` |
| 80 | 2026-09-05 | タグで絞るのに検索窓へ打ちたくない。既存のタグを押して選びたい | 帯に出るのは**いま見えているノートが実際に持っているタグ**（多い順）。フォルダ全部のタグを出すと誰も読まない帯になる。複数選ぶと **かつ**（両方持つノート）── **または**にすると、押すほど一覧が増えることになり、絞る操作としては逆。タグは検索と同じで**どこにあるノートでも探す**（フォルダの中にいても、そのフォルダの分だけ出すのは誰も訊いていない答え） | `ios/Cian/NotesStore.swift ~ var tagsHere: \[String\]` |
| 81 | 2026-09-05 | 書く道具をキーボードの上に。よく使うものは今ので良い。見出しは複数回押すと `#` `##` `###`。使わないものは畳んでおく（表・リンク・ソースコード・取消線・水平線…） | 上のバーは **戻る・タイトル・保存・読む書く・⋯** の5つだけにして、道具は**キーボードが出ているときだけ**下に出す（キーボードが引っ込んでいるときの帯は「キーボードの上」ではなく、ただの画面の底）。見出しは**押すたびに深くなる** ── ボタンを3つ置くと、1つの考えに3つの名前が付く。タグ・通知・写真は上の ⋯ に残す（読んでいる間はキーボードが無く、通知を仕掛けるのは読み終えた直後だから）。**文字色は入れていない** ── Markdown に文字色は無く、書き方を決めると両前端がそれを覚えることになるので、先に相談したい | `ios/Cian/Editor.swift ~ static func deepen\(` |
| 82 | 2026-09-05 | ノートを複数開いてスワイプすると、上のアイコンが一瞬増えて消える。ちらつきを消したい | `TabView` は**隣のページも生かしておく**ので、SwiftUI に2枚分のツールバーが渡り、入りきらない分が 「⋯」 に押し出される ── 増えて減っていたのは、その ⋯。**手前のページだけがツールバーを出す**ようにした。**これでは足りず（89）**、ツールバーごと机に上げた ── ページが作るものは、ページが作り直されるたびに作り直される | `ios/Cian/Desk.swift ~ private var chrome: some ToolbarContent` |
| 83 | 2026-09-05 | ノートの行は、題の字を押さないと反応しない。囲みのどこでも反応してほしい | ラベルは**いちばん長い行の幅しか無い**ので、題が短いノートは行の右半分が指に対して死んでいた。`frame(maxWidth:)` と `contentShape` で行全体を的にした。同じ行が場所によって答えたり答えなかったりするのは、バグとして読まれる | `ios/Cian/ContentView.swift ~ .contentShape\(Rectangle\(\)\)` |
| 84 | 2026-09-05 | ダークモードとライトモードの2つを選べるようにしたい | 選択肢は**3つ**（iPhone に合わせる／ライト／ダーク）── 2つしか無い切り替えは、一度倒すと元に戻せない切り替えになる。既定は「合わせる」。色は `AccentColor` が明暗それぞれの値を持っているので、切り替えても**シアンが沈まない** | `ios/Cian/CianApp.swift ~ enum Look: String` |
| 85 | 2026-09-05 | ピン留めではなく**お気に入り**にしたい。本当のフォルダにも居るし、お気に入りからも見える。お気に入り自身にも階層を作りたい。⭐️ で普通のフォルダと違うと分かるように | お気に入りは**2つ目の居場所**であって移動ではない ── ノートは書いた場所に残り、front matter の `star:` が「どの棚にも並ぶ」と言う。だから**ノートに書く**（移動・改名・同期を越えて付いていく。一覧をどこかに持つと、その一覧だけが古くなる）。棚は⭐️の四角で描く ── フォルダの絵にすると「動かせばファイルも動く」と約束することになる。**空の棚だけ**は書ける場所が無いので `.cian/settings.json` が憶える（その1行を失っても、失うのは空の棚だけ。ノートの居場所は失われない）。古い `pinned: true` も読む。外すと `pinned` も一緒に消す ── 残しておくと、外したのにお気に入りのままになる | `crates/cian-core/src/note.rs ~ pub star: Option<String>` |
| 86 | 2026-09-05 | フォルダの色は自分で決めたい。シアン系にこだわらない | 色でフォルダを見分けるのが目的なので、**全部を同じ色相に収める配色は目的に反する**。11色（シアンからはいいろまで）と「色をなくす」。書き場所は `.cian/settings.json` ── フォルダには色を書き込むノートが無い。壊れた設定ファイルは既定に戻すだけで、**ノートを見せない理由にはしない** | `crates/cian-core/src/notebook.rs ~ pub fn set_color\(` |
| 87 | 2026-09-05 | 書く道具がカーソル位置に効いてほしい | `TextEditor` はカーソルを渡さないので、道具は**最後の行**に効いていた（見出しを押すと、いちばん下に見出しができる）。`UITextView` に置き換えて選択範囲を報告させた。**IME が字を組んでいる間（`markedTextRange`）は、どちらの向きにも触らない** ── 確定前の字に書き込むと、組みかけの語が飛ぶ。編集は `Marks` に (text, 選択) → (text, 選択) の関数として置いた（画面の外に出したので、そのまま試験にかけられる形になる）。写真も、末尾ではなくカーソルの段落に入る | `ios/Cian/Editor.swift ~ enum Marks` |
| 88 | 2026-09-05 | 文字色（Markdown で使えるものが欲しい） | 書き方は `<span style="color:#rrggbb">`。**GitHub は `style` を落とす**ので色は付かないが、**字は読める** ── `$\color{red}{…}$` の抜け道は GitHub でだけ描かれ、他所では読めない記号の塊になる。ノートは**文へ壊れる**べきで、記法へ壊れてはいけない。読み取りは `note::spans` の1か所で、**3つの前端すべてが同じ答えを見る**（iPhone は run ごとに塗り、窓は `markdown::Inline::Colored`、端末は 24bit 色）。窓の HTML は「ファイルから来たものは全部エスケープする」という約束があるので、**通すのは検査済みの6桁だけ** ── `onclick` も `class` も、これまで通り字になる。色数はフォルダと同じ11色に固定（色でしか区別できないノートは、grep に映らず、読み上げにも伝わらない）。一覧の二行目と検索は `note::plain` で色の記法を外す | `crates/cian-core/src/note.rs ~ pub fn spans\(` |
| 89 | 2026-09-05 | 目のボタンで読む／書くを切り替えるときも、上のアイコンがちらつく（⋯ が一瞬増えて消える） | 前回の「手前のページだけが出す」では足りなかった ── **ページが作るツールバーは、ページが作り直されるたびに作り直される**。`TabView` は隣のページを生かしたまま、状態が変わるたびに作り直すので、その一瞬だけ入りきらず ⋯ に押し出される。ツールバーを**机（`DeskView`）に上げた** ── 1つしか無く、ページが何をしても作り直されない。読み書き・保存・写真・タグ・通知も机の持ち物にした | `ios/Cian/Desk.swift ~ private var chrome: some ToolbarContent` |
| 90 | 2026-09-05 | ノートの長押しメニューは お気に入りに登録する → ノートブックへ移す → 書き出す の順に | 手が伸びる順。お気に入りは片手間、移すのは整理、書き出すのは **cian の外へ出る**もので、外へ出るものは最後 | `ios/Cian/ContentView.swift ~ お気に入りに登録する` |
| 91 | 2026-09-05 | メモしたら自動保存してほしい。わざわざ保存しない | 打つのをやめて 0.9 秒で書く（一打ごとに書くと、一文で40回ファイルを書き直すことになり、同期フォルダでは向こうの端末に40回の変化として届く）。**画面を離れるときと、アプリが前面でなくなるときにも書く** ── 電話は断りなくアプリを止めるので、時計だけに任せると最後の一文が消える。保存ボタンは**状態表示に変えた**（●＝書いている／✓＝書いた）── 書かれたかどうか何も言わないノートは、置いて立ち去れない | `ios/Cian/Desk.swift ~ private func later\(` |
| 92 | 2026-09-05 | ミスったときのために Undo / Redo が欲しい | **電話自身の undo を使う**（振って戻す・3本指スワイプと同じ山）── Swift で書き直せば、電話と食い違う2つ目の undo ができる。そのために道具は**文字列を丸ごと入れ替えるのをやめ**、`replace(_:withText:)` で1回の編集として入れるようにした（丸ごと入れ替えは undo の山に載らないので、見出しを押した分だけ戻せなかった） | `ios/Cian/Editor.swift ~ final class Pen` |
| 93 | 2026-09-05 | iPhone のキーボードに矢印が無いのが本気で使いにくい。左下上右（vim の順）で道具に入れて | 長い文をスマホで書くのが辛い理由のほとんどがこれ（虫めがねを引きずってカーソルを置く）。上下は `.layout` 方向 ── 折り返された行に**描かれている通り**に動く。並びは h j k l | `ios/Cian/Editor.swift ~ func step\(` |
| 94 | 2026-09-05 | 表が 2×2 固定で使いにくい。もっといい UI/UX を | **骨組みを置くのが間違いだった** ── 打ち込む表は縦棒を数え続ける表で、揃え方の行（`:---` `:---:` `---:`）は誰も形を覚えていない。列の数・名前・揃えを選ぶ画面にして、縦棒は cian の仕事にした。**入るのはただの Markdown**（cian の表ではなく、どの道具でも読める表）。空欄は全角空白 ── 中身が空の升は描く側によっては消えてしまい、消えた表は「作れなかった」に見える | `ios/Cian/Tabling.swift ~ struct Tabling` |
| 95 | 2026-09-05 | 最初に出てくる「cian」のところをもう少しクールにしたい | ナビゲーションバーの大見出しは**他人の組版**。cian には名前が最初に目に入る画面が1つあるので、そこだけはアイコンと同じ組み方にした ── 描いた印（ホーム画面のアイコンと同じ2つの括弧）、丸ゴシックの太い字、そして中身を一行。**フォルダの中に降りたらバーに戻す** ── そこでの問いは「いまどこ」で、ロゴはそれに答えない。検索欄は `navigationBarDrawer(.always)` で出しっぱなしにする（バーを `.inline` にすると、既定では引き下げないと出てこない） | `ios/Cian/Writing.swift ~ struct Wordmark` |
| 96 | 2026-09-05 | フォルダを移動したときにアニメーションが欲しい。パッと切り替わるので押せたか分からない | **一瞬で変わる一覧は、変わらなかった一覧に見える** ── 指が着いた時にはもう中身が違っていて、目が追うものが無いので、もう一度押して確かめることになる。一覧ごと差し替えて、来た方向から滑らせる。**どちらへ行ったかで滑る向きを変える**（両方向で同じに滑るなら、滑らない方がまし）。復元や戻りで場所が変わったときは滑らせない ── 滑りは「あなたがした移動」のためのもの | `ios/Cian/ContentView.swift ~ private func go\(` |
| 97 | 2026-09-05 | 左→右のスワイプで上の階層へ、右→左でそれを戻す（cian → hoge → cian → hoge） | 上がった先を憶えておく（`forward`）── **戻せない操作は、そのうち誰も使わなくなる**。フォルダを手で選んだときは、その先は無かったことにする（新しい方向へ進んだので）。**画面端からだけ**受ける ── 画面いっぱいの横スワイプは行そのものの操作（お気に入り・削除）で、出口は電話が既に置いている場所に置く。`simultaneousGesture` なので**縦スクロールが勝つ**し、指が離れるまで何も決めない | `ios/Cian/NotesStore.swift ~ func back\(` |
| 98 | 2026-09-05 | フォルダ移動のスワイプ、指の移動が長すぎる。もっと短くても反応してほしい | 距離だけで見ないで、**離した瞬間まだ動いている指**を数に入れる（`predictedEndTranslation`）── 指を弾いた人は「もっと先まで行く」つもりで離している。48pt の意図的なドラッグを求めていたのは、スワイプではなくドラッグ | `ios/Cian/ContentView.swift ~ predictedEndTranslation` |
| 99 | 2026-09-05 | 表の画面、列と行の増やす・減らすの見た目を揃えて。文言は「増やす」「減らす」 | 同じことを2つの数にする道具が2つの別の考えに見えないように、1つの形にして両方で使う | `ios/Cian/Tabling.swift ~ private func more\(` |
| 100 | 2026-09-05 | 書く帯は、1段目＝見出し・箇条書き・チェック・太字・画像・⋯、2段目＝undo/redo …（空けて）… 矢印 で右詰め | 1段目は**ノートが何でできているか**、2段目は**動き回るための道具**。分けたのは、片方のつもりの指がもう片方を押さないようにするため。右詰めなのは親指の側だから。写真は上の ⋯ から降ろした（書いている最中に使うものなので） | `ios/Cian/Writing.swift ~ mark\("画像", "photo"` |
| 101 | 2026-09-05 | 新規作成すると title / created / tags が見えるノートができる。プログラムが見ているだけなら書く画面に出さないでほしい | 前書きは**ノートが自分を説明する言葉**で、ノートが言うことではない ── 自分で打っていないものを越えて自分の1行目に辿り着くのは変。書く画面は本文だけを持ち、保存のときに繋ぎ直す。どこで前書きが終わるかは `note::front` の1つの答え（`split`）なので、電話・窓・端末が同じ場所でノートを始める。**チェックは丸ごとに対して**行う ── 行番号はファイルの行番号だから | `crates/cian-ffi/src/lib.rs ~ "split" =>` |
| 102 | 2026-09-05 | 自動保存を既定に。設定で切って手動保存も選べるように | 切ると上のバーに「保存」が出る。**切っていても画面を離れるときに一度だけ訊く** ── 自分で決めたい人でも、消えていいとは言っていない | `ios/Cian/Desk.swift ~ private var autosave = true` |
| 103 | 2026-09-05 | 窓版のアクティブなパネルの枠、もう一段太く。おしゃれだが控えめすぎる | 2px → **3px**。枠の仕事は「探さなくても見えること」で、明るい配色では地の色が数%しか違わないので、枠だけがどちら側にキーがあるかを言っている | `gui/index.html ~ border: 3px solid var\(--mode-accent\)` |
| 104 | 2026-09-05 | 題を入れずに作ったときの既定のタイトルに、時分秒まで入れてほしい | 日付だけだと午後に3本作ると「-2」「-3」が付くだけで、**どれがどれか分からない**。`created:` は日付のまま ── あれは機械が読む欄で、タイトルは人が読むもの。ファイル名からは `:` が落ちる（Windows で作れない） | `crates/cian-core/src/note.rs ~ pub fn now_stamp\(` |
| 105 | 2026-09-05 | 設定の「保存場所を選ぶ…」を押しても何も起きない | **同じ画面に `fileImporter` が2つあると1つしか効かない**（SwiftUI は後勝ち、負けた方のボタンは何もしない）。2026-09-04 に一度直したときに書いた注意書きの2行下で、同じことを2度目にやっていた ── インポートに負けて、押しても配線されていないボタンに見えていた。**1つにして、何を選びに行くかで受け取るものを変える** | `ios/Cian/ContentView.swift ~ enum Fetching` |
| 106 | 2026-09-05 | フォルダを長押しで、色だけでなく名前の変更と削除もしたい。削除は「中のノートごと消していい？」と訊いてから | 削除は**何本が道連れになるかを数で言ってから**（「中のノート 3 本も一緒に消えます」）。**電話にゴミ箱は無い**ので、これは本物の削除で、取り消せない。`..` を渡してノートの外を消せないよう、パスを絶対化してから根の下にあることを確かめる。名前を変えたフォルダの中に立っていたら、上へ退く ── 消えた場所を見せ続けない | `crates/cian-ffi/src/lib.rs ~ ノートの外は触れません` |
| 107 | 2026-09-05 | フォルダのツリー構造が分かる表示がほしい | 一覧は**一階層ずつ**で、それはフォルダを**使う**ための正しい形。ツリーは別の問い ──「ここに何があって、どこまで深いのか」。字下げは `allBooks` のパスの深さそのもので、組み立てるものは何も無い。数字は下の階層も数える。いま居る場所には印を出す | `ios/Cian/Writing.swift ~ struct Tree` |
| 108 | 2026-09-05 | フォルダに入るとフォルダ名だけになる。パンくずリストでどこにいるか分かるように | **フォルダ自身の名前は「どこ」の答えではない** ── 別々の場所にある「2026」は一覧の上では見分けが付かない。各段は、その階層への戻り道。いま居る段だけは字の色にして押せなくする（同じ色にすると、押しても何も起きないものが押せるものに見える） | `ios/Cian/Writing.swift ~ struct Crumbs` |
| 109 | 2026-09-05 | 「（いちばん上）」という言い方をやめて、素人にも分かる言葉に（デフォルトなど） | 彼の案どおり「デフォルト」に。ノートを移す先と、お気に入りの棚の両方 | `ios/Cian/ContentView.swift ~ Button\("デフォルト"\)` |
| 110 | 2026-09-05 | お気に入りに階層を作りたい（棚が「いちばん上」しか無い） | **棚は作れていたが、作りに行く道が隠れていた** ── お気に入り画面からしか作れず、その画面は「お気に入りが1つ以上ある」ときにしか出てこない。だから最初の棚を、要るその場で作れなかった。棚を選ぶ画面に「新しい棚…」を置いた（作って、そのまま入れる。`棚/中の棚` と書けば階層） | `ios/Cian/Stars.swift ~ 新しい棚…` |
| 111 | 2026-09-05 | 通知は ⋯ の中ではなく最上段にベルで分かるように | **鳴るかどうかは状態**で、状態はメニューの奥にあると誰も知らない。仕掛かっているときは橙の塗りつぶし | `ios/Cian/Desk.swift ~ ToolbarItem\(id: "bell"` |
| 112 | 2026-09-05 | Mac でいよいよ使いたい。win-gui / mac-gui に cian モードを | **`cian-gui-macos.zip` を作った。** これまで窓版を Mac に載せる道は「リポジトリを clone して `npm start`」だけで、それは書いた本人には十分でも、人に渡せる形ではない。中身は Windows 版と同じ（front end・vendor 一式・エンジン）で、エンジンは **Intel と Apple Silicon の両方入り** ── 片方だけのバイナリは、もう片方の Mac では「動かない」ではなく**開かない**（何の説明も出ない）。重い方（winit 版・端末版・`.app`）は `everything` の裏のまま。**フォントが無ければビルドを止める** ── Windows 側で一度、黙って 6MB 軽い zip が出ている | `.github/workflows/release.yml ~ name: cian-gui-macos` |
| 113 | 2026-09-05 | 窓版でも cian モードを（チェックを押せるように） | HTML の升に**元の行番号を積む**（`data-line`）── `note::set_check` は行番号を取るので、窓に要るのはそれだけ。何番目の升かを数えると、前書きのあるノートでずれる。押すとエディタに戻して**ふつうに保存する**ので、打ち込みと同じ衝突検査を通る | `crates/cian-core/src/markdown.rs ~ data-line=` |
| 114 | 2026-09-05 | 窓版でもお気に入りの棚を | `:star`。棚は**いまある棚**（空のものも `.cian/settings.json` から）＋「新しい棚…」。書くのは電話と同じ `star:` の行なので、こちらで付けた星はあちらでも星。`show()` は**行ごとに答える**（`pick` コールバック）ので、返り値を待つ書き方だと Enter で何も起きない ── 最初そう書いて、開くだけのピッカーになった | `gui/renderer.js ~ async function cmdStar` |

## 増やすとき

依頼を受けて何かを入れたら、**その場でここに1行足す**。あとでまとめてやると、
まとめる前に自分で消します（それが起きたのがこの表の1行目です）。

検査は「その依頼が満たされていること」を見るもので、実装の形を凍結するもの
ではありません。**作り直して構いません。ただし検査を通したまま**、あるいは
検査ごと変えるなら**先に訊いてから**。
